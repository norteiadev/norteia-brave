---
phase: 17.1-painel-brave-remaining-pages-real-backend-slice-2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - brave/api/routers/cms.py
  - brave/api/routers/atrativos.py
  - tests/unit/test_transitions.py
autonomous: true
requirements: [UI-PAINEL-2]

must_haves:
  truths:
    - "PATCH /api/v1/destinos/{rio_id}/transition performs only edges in the server allow-list; any other edge returns 409"
    - "PATCH /api/v1/atrativos/{rio_id}/transition performs only allow-listed edges; mar → * stays 409-blocked"
    - "Every legal transition writes an audit row"
    - "No unmapped (expected,to) edge ever mutates a record"
  artifacts:
    - path: "brave/api/routers/cms.py"
      provides: "transition_destino + _ALLOWED_EDGES server-side allow-list"
      contains: "_ALLOWED_EDGES"
    - path: "brave/api/routers/atrativos.py"
      provides: "transition_atrativo (whatsapp delegates to gate approve; dlq→rio reprocess)"
      contains: "transition"
    - path: "tests/unit/test_transitions.py"
      provides: "Exhaustive offline edge-table test (no unmapped edge mutates)"
  key_links:
    - from: "brave/api/routers/cms.py"
      to: "brave.observability.audit.write_audit"
      via: "audit on every transition"
      pattern: "write_audit"
---

<objective>
Add ONE generic, audited, steward-guarded stage-transition endpoint per entity (destino, atrativo)
with a SERVER-SIDE edge allow-list — the server twin of the client mapDrop. This is the backend that
makes the full-pipeline Kanban drag real (including backward/force moves) without a per-pair PATCH
matrix.

Purpose: the Kanban is the sync surface; a drop must fire a real audited transition or be rejected.
The allow-list is the security boundary — illegal edges (notably mar → *) return 409, never mutate.
Output: transition_destino in cms.py + transition_atrativo in atrativos.py + the _ALLOWED_EDGES table
+ an exhaustive offline edge-table unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-CONTEXT.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-RESEARCH.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _ALLOWED_EDGES + TransitionBody + transition_destino (cms.py)</name>
  <files>brave/api/routers/cms.py, tests/unit/test_transitions.py</files>
  <read_first>
    - brave/api/routers/cms.py:43-47 (AdvanceBody style), :330-378 (descarte_destino audit+commit), :347-362 (mar descarte 409 block — keep), :607-646 (advance_atrativo_state expected_state 409 idea), :254 (promote), :381 (reprocess)
    - brave/observability/audit.py:22-74 (write_audit)
    - 17.1-RESEARCH.md Finding 4 (transition matrix; NEW edges; mar→* stays 409)
    - 17.1-PATTERNS.md "Generic transition endpoint" section
  </read_first>
  <behavior>
    - Allowed destino edges (expected→to): rio→mar (promote), rio→descarte (descarte), rio→dlq (reprocess/send-to-review), dlq→rio (reopen/reprocess), dlq→mar (promote), dlq→descarte (descarte).
    - mar→* → 409 "transição não suportada" (no depublish — cms.py:347-362 stays). Any edge whose key is absent from _ALLOWED_EDGES → 409.
    - expected mismatch with current column → 409 (optimistic concurrency).
    - Every performed edge writes an audit row, then db.commit().
  </behavior>
  <action>
    Define a module-level _ALLOWED_EDGES dict keyed by (expected_column, to_column) → handler tag for
    destinos, encoding exactly the edges in the behavior list above (the server twin of the client
    mapDrop; this is the documented paired contract). Add a TransitionBody Pydantic model
    (to: Literal["nascente","rio","whatsapp","mar","dlq","descarte"], expected: str; extra="forbid"),
    copying the AdvanceBody style (cms.py:43-47).
    Add PATCH /api/v1/destinos/{rio_id}/transition with dependencies=[Depends(require_steward_or_bearer)]:
    load rio via db.get(RioRecord, rio_id) (404 if None); look up edge = _ALLOWED_EDGES.get((body.expected,
    body.to)); if edge is None raise HTTPException(409, "transição não suportada"); perform the edge by
    REUSING the existing helpers (promote path for →mar, descarte path for →descarte, reprocess path for
    dlq↔rio — do NOT reimplement them); guard mar→* by leaving the existing cms.py:347-362 block intact
    (no new depublish). Call write_audit(session=db, action=f"transition_{body.to}", entity_type=
    rio.entity_type, record_id=rio.id, before_state={"column": body.expected}, after_state={"column":
    body.to}, actor="steward") then db.commit(); return {"status":"ok","to": body.to}.
    Write tests/unit/test_transitions.py: assert the destino edge table EXHAUSTIVELY — every
    (expected,to) pair NOT in _ALLOWED_EDGES resolves to None (→ would 409), every mar→* pair is absent,
    and no disallowed edge ever calls a mutation helper. Pure dict assertions — fully offline.
  </action>
  <acceptance_criteria>
    - `grep -c "_ALLOWED_EDGES" brave/api/routers/cms.py` returns ≥2
    - `grep -c "transição não suportada" brave/api/routers/cms.py` returns ≥1
    - test asserts every `("mar", X)` key is absent from the destino allow-list
    - `.venv/bin/python -m pytest tests/unit/test_transitions.py -x` passes
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_transitions.py -x</automated>
  </verify>
  <done>transition_destino performs only allow-listed edges, 409s everything else (incl. mar→*), audits each move; exhaustive offline edge test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: transition_atrativo (atrativos.py) — whatsapp delegate + dlq↔rio</name>
  <files>brave/api/routers/atrativos.py, tests/unit/test_transitions.py</files>
  <read_first>
    - brave/api/routers/atrativos.py (existing promote-override router — auth + structure to extend)
    - brave/api/routers/atrativos_gate.py:318 (gate approve — the ONLY into-whatsapp path; delegate, do not duplicate outreach)
    - brave/api/routers/cms.py:607-646 (advance_atrativo_state forward-only + expected_state 409), :649 (atrativo descarte → dlq)
    - brave/api/routers/cms.py (Task 1 output: _ALLOWED_EDGES + TransitionBody — reuse the model)
    - 17.1-RESEARCH.md Finding 4 (atrativo NEW edges: dlq→rio reprocess; force in_progress→dlq; whatsapp only from aguardando_consulta_whatsapp)
  </read_first>
  <behavior>
    - Allowed atrativo edges: rio→dlq (force send-to-review), dlq→rio (reprocess/reopen — currently missing), rio→mar (mar-ready promote-override), rio→descarte (→dlq descarte).
    - Into-whatsapp valid ONLY from sub_state aguardando_consulta_whatsapp → delegate to the existing gate approve; never duplicate its outreach dispatch.
    - mar→* → 409. Any edge absent from the atrativo allow-list → 409.
  </behavior>
  <action>
    Add a separate atrativo edge allow-list (module-level dict in atrativos.py, mirroring the destino
    one) encoding the behavior edges above. Add PATCH /api/v1/atrativos/{rio_id}/transition with
    dependencies=[Depends(require_steward_or_bearer)], importing TransitionBody from cms (or re-declaring
    identically). Resolve edge from the atrativo allow-list; None → 409 "transição não suportada".
    For →mar reuse the existing mar-ready promote-override (promote/service path already wired in
    atrativos.py); for →dlq reuse the atrativo descarte→dlq path (cms.py:649); for dlq→rio implement the
    NEW reprocess/reopen (set routing/sub_state back to the re-entry state — reuse advance/routing
    helpers, do NOT invent pipeline machinery); for any into-whatsapp edge DELEGATE to the gate approve
    (atrativos_gate.py:318) and require current sub_state == "aguardando_consulta_whatsapp" else 409.
    write_audit + db.commit() on every performed edge (same posture as Task 1). Keep mar→* 409-blocked.
    Extend tests/unit/test_transitions.py with the atrativo edge table: exhaustive absence of mar→* and
    of any unmapped edge; assert the into-whatsapp edge delegates to gate approve (mock) only from
    aguardando_consulta_whatsapp.
  </action>
  <acceptance_criteria>
    - `grep -c "transition" brave/api/routers/atrativos.py` returns ≥1
    - test asserts atrativo dlq→rio is allowed and mar→descarte is 409
    - `.venv/bin/python -m pytest tests/unit/test_transitions.py -x` passes (both entity tables)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_transitions.py -x</automated>
  </verify>
  <done>transition_atrativo supports the new backward/force edges, delegates into-whatsapp to the audited gate approve, 409s mar→* and any unmapped edge; offline edge test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard→BFF→collector | operator drag fires a server-authoritative stage transition here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-03-01 | Tampering | client invents an unmapped transition | mitigate | server-side _ALLOWED_EDGES allow-list; 409 on any edge not explicitly permitted; client mapDrop mirrors it (plan 06) |
| T-17.1-03-02 | Elevation | unauthenticated transition | mitigate | require_steward_or_bearer on both transition endpoints |
| T-17.1-03-03 | Integrity | depublish a live Mar record | mitigate | mar→* stays 409 (cms.py:347-362); no new depublish path |
| T-17.1-03-04 | Repudiation | stage moves | mitigate | write_audit on every performed edge |
| T-17.1-03-SC | Tampering | package installs | accept | no installs this plan |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_transitions.py -x` green; edge tables exhaustive for both entities.
- mar→* absent from both allow-lists (asserted in test).
</verification>

<success_criteria>
One generic audited transition endpoint per entity exists, gated by a server-side allow-list that mirrors the client mapDrop and keeps mar→* blocked; exhaustively offline-tested.
</success_criteria>

<output>
Create `.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-03-SUMMARY.md` when done.
</output>
