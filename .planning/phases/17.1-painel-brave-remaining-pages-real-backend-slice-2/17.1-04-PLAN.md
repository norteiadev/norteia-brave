---
phase: 17.1-painel-brave-remaining-pages-real-backend-slice-2
plan: 04
type: execute
wave: 2
depends_on: ["17.1-01"]
files_modified:
  - dashboard/lib/dedup-api.ts
  - dashboard/mocks/handlers/dedup.ts
  - dashboard/components/painel/PainelDuplicados.tsx
  - dashboard/components/painel/__tests__/PainelDuplicados.test.tsx
autonomous: true
requirements: [UI-PAINEL-2]

must_haves:
  truths:
    - "Duplicados view renders candidate↔Mar pair cards with matched/diverged chips and a labeled similarity"
    - "merge / keep / discard buttons fire the real resolve PATCH and invalidate the dedup query"
    - "The MSW handler mirrors the backend DedupPairsResponse shape field-for-field"
  artifacts:
    - path: "dashboard/lib/dedup-api.ts"
      provides: "typed dedup client (fetchDedupPairs, resolveDedupPair, keys)"
      contains: "fetchDedupPairs"
    - path: "dashboard/mocks/handlers/dedup.ts"
      provides: "MSW handlers at the double-prefixed /api/api/v1/dedup/... path"
      contains: "/api/api/v1/dedup"
    - path: "dashboard/components/painel/PainelDuplicados.tsx"
      provides: "Duplicados pair-resolution view"
      contains: "PainelDuplicados"
  key_links:
    - from: "dashboard/components/painel/PainelDuplicados.tsx"
      to: "dashboard/lib/dedup-api.ts"
      via: "useQuery(fetchDedupPairs) + useMutation(resolveDedupPair)"
      pattern: "fetchDedupPairs"
---

<objective>
Build the Duplicados frontend vertical slice: a typed dedup client, its MSW handler mirroring the
backend response shape (the A5 typed-contract discipline), and the PainelDuplicados view that lists
pair cards and resolves each via merge / keep / discard.

Purpose: surfaces the new dedup backend (plan 01) in the painel shell. The client + MSW handler
together ARE the consumer contract (no pact-js — A5).
Output: dashboard/lib/dedup-api.ts, dashboard/mocks/handlers/dedup.ts, PainelDuplicados.tsx + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-CONTEXT.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-RESEARCH.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-PATTERNS.md
@dashboard/lib/painel-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: dedup-api.ts client + dedup.ts MSW handler</name>
  <files>dashboard/lib/dedup-api.ts, dashboard/mocks/handlers/dedup.ts</files>
  <read_first>
    - dashboard/lib/destinos-api.ts (full file — header docblock, typed interfaces, qs() helper :69-76, keys factory :62-67, apiFetch fn per endpoint :93-103)
    - dashboard/lib/api-client.ts:34-38 (bff() rewrite), :40-48 (ApiError.status), :55-75 (apiFetch)
    - dashboard/mocks/handlers/engine.ts (full file — double-prefix rule :15, factory per scenario, typed payloads :3-8)
    - brave/api/routers/dedup.py (plan 01 output — the exact DedupPairItem/DedupPairsResponse + ResolveBody field names to mirror)
  </read_first>
  <action>
    Create dashboard/lib/dedup-api.ts mirroring destinos-api.ts: a header docblock listing the backing
    endpoints; TS interfaces DedupPairItem + DedupPairsResponse mirroring the plan-01 Pydantic models
    field-for-field (candidate_id, mar_id, candidate_rio_id, mar_rio_id, uf, municipio, entity_type,
    similarity, similarity_source, matched_fields, diverged_fields, + items/total/offset/limit); a
    dedupKeys factory sharing one "dedup" prefix; fetchDedupPairs(uf?) → apiFetch("api/v1/dedup/pairs"
    + qs); resolveDedupPair(candidateRioId, { action, mar_id }) → apiFetch PATCH
    "api/v1/dedup/pairs/{id}/resolve". Use the relative api/v1/... form (bff rewrites to /api/api/v1/...).
    Create dashboard/mocks/handlers/dedup.ts mirroring engine.ts: register at the double-prefixed
    http://localhost:3000/api/api/v1/dedup/... (Pitfall 6); export factories dedupPairsSuccess(),
    dedupPairsEmpty(), and a resolve handler; type the mock payloads against the lib interfaces so the
    handler IS the field-for-field contract mirror.
  </action>
  <acceptance_criteria>
    - `grep -c "fetchDedupPairs\|resolveDedupPair" dashboard/lib/dedup-api.ts` returns ≥2
    - `grep -c "/api/api/v1/dedup" dashboard/mocks/handlers/dedup.ts` returns ≥1 (double-prefix)
    - dedup-api interfaces include every DedupPairItem field from plan 01
    - `cd dashboard && bun run build` (typecheck) succeeds for the new client
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bun run test dedup-api 2>/dev/null || cd dashboard && bunx tsc --noEmit</automated>
  </verify>
  <done>Typed dedup client + MSW handler exist, mirror the backend shape field-for-field, and register at the double-prefixed path.</done>
</task>

<task type="auto">
  <name>Task 2: PainelDuplicados.tsx view + test</name>
  <files>dashboard/components/painel/PainelDuplicados.tsx, dashboard/components/painel/__tests__/PainelDuplicados.test.tsx</files>
  <read_first>
    - dashboard/components/painel/PainelCusto.tsx (full file — "use client", useQuery :51-54, Seg :201-228, SummaryCard :230-245, data-testid discipline, --painel-* token styling)
    - dashboard/components/painel/PainelTopbar.tsx:84-99 (useMutation + toast + invalidateQueries scaffold)
    - dashboard/lib/dedup-api.ts (Task 1 output)
    - dashboard/components/painel/__tests__/PainelCusto.test.tsx:11-22,64-70 (renderWithClient, server.use, findAllByTestId, request assertion)
    - 17.1-CONTEXT.md "Specific Ideas" (design pair cards: matched/diverge chips + merge/keep/discard)
  </read_first>
  <action>
    Create dashboard/components/painel/PainelDuplicados.tsx ("use client") mirroring PainelCusto's
    structure: useQuery({ queryKey: dedupKeys..., queryFn: () => fetchDedupPairs(uf) }) to load pairs;
    render one pair card per item showing candidate vs Mar, a "coincide" chip per matched_field and a
    "diverge" chip per diverged_field (with candidate/mar values), and the labeled similarity (render
    similarity_source so the operator knows what the number means — embeddings are a stub, A1). Each card
    has three buttons — Mesclar no existente (merge), Manter ambos (keep), Descartar candidato (discard)
    — wired via a useMutation over resolveDedupPair with toast + invalidateQueries(["dedup"]) (copy the
    PainelTopbar :84-99 mutation scaffold). Pure --painel-* token styling; data-testid on every asserted
    node (cards, chips, buttons).
    Write __tests__/PainelDuplicados.test.tsx using renderWithClient + server.use(dedupPairsSuccess()):
    assert pair cards render with matched/diverged chips; assert clicking Descartar fires the real
    resolve PATCH (action="discard") via server.events.on("request:start", ...); assert the empty
    handler renders an empty state.
  </action>
  <acceptance_criteria>
    - `grep -c "fetchDedupPairs\|resolveDedupPair" dashboard/components/painel/PainelDuplicados.tsx` returns ≥2
    - `grep -c "data-testid" dashboard/components/painel/PainelDuplicados.tsx` returns ≥3
    - `cd dashboard && bun run test PainelDuplicados` passes (renders pairs + fires resolve PATCH)
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bun run test PainelDuplicados</automated>
  </verify>
  <done>PainelDuplicados renders real pair cards with matched/diverged chips + labeled similarity and resolves merge/keep/discard against the real endpoint; component test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→BFF | operator resolves a dedup pair here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-04-01 | Info disclosure | LGPD phone in pair cards | mitigate | view reads only dedup-api fields (canonical/normalized labels) — no phone field exists in the response |
| T-17.1-04-02 | Tampering | invented resolve action | mitigate | resolveDedupPair only sends the typed action enum; backend validates (plan 01) |
| T-17.1-04-SC | Tampering | npm installs | accept | no installs (reuse TanStack/MSW) |
</threat_model>

<verification>
- `cd dashboard && bun run test PainelDuplicados dedup-api` green.
- Handler registered at /api/api/v1/dedup/... (double-prefix).
</verification>

<success_criteria>
Duplicados is a real view: pairs load from the backend, matched/diverged chips + labeled similarity render, and merge/keep/discard hit the real resolve endpoint; client+handler mirror the contract.
</success_criteria>

<output>
Create `.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-04-SUMMARY.md` when done.
</output>
