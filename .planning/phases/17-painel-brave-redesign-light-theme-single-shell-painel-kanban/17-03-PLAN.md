---
phase: 17-painel-brave-redesign-light-theme-single-shell-painel-kanban
plan: 03
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - dashboard/components/painel/PainelMetrics.tsx
  - dashboard/components/painel/PainelFilters.tsx
  - dashboard/components/painel/__tests__/PainelMetrics.test.tsx
  - dashboard/components/painel/__tests__/PainelFilters.test.tsx
autonomous: true
requirements: [UI-PAINEL-1]

must_haves:
  truths:
    - "Two metric cards (Destinos / Atrativos) show total-no-escopo, sincronizados, falhas and a progresso% bar derived from buildMetrics"
    - "The type segmented control (Tudo / Destinos / Atrativos) reports the selected filter to its parent"
    - "The UF-scope dropdown multi-selects from the 27 UFs and reports the selection; 'Todas' clears the scope"
  artifacts:
    - path: "dashboard/components/painel/PainelMetrics.tsx"
      provides: "Destinos + Atrativos metric cards (presentational, props-driven)"
      min_lines: 40
    - path: "dashboard/components/painel/PainelFilters.tsx"
      provides: "Type segmented control + UF-scope multi-select dropdown (presentational, props-driven)"
      min_lines: 50
  key_links:
    - from: "dashboard/components/painel/PainelMetrics.tsx"
      to: "lib/painel-data buildMetrics"
      via: "import + render"
      pattern: "buildMetrics"
    - from: "dashboard/components/painel/PainelFilters.tsx"
      to: "lib/painel-data BR_UFS + TypeFilter"
      via: "import"
      pattern: "BR_UFS"
---

<objective>
Build the Painel view's header region: the two metric cards (Destinos / Atrativos) and the filter controls (type segmented control + UF-scope multi-select dropdown). Both are presentational, props-driven components — state lives in the PainelView container (plan 17-05). Metric values come from `buildMetrics` over the board cards.

Purpose: Faithful, tested top-of-view chrome that the container wires to real data.
Output: `PainelMetrics.tsx` + `PainelFilters.tsx` + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-painel-brave-redesign-light-theme-single-shell-painel-kanban/17-CONTEXT.md
@.planning/phases/17-painel-brave-redesign-light-theme-single-shell-painel-kanban/design/Painel-Brave.dc.html
@dashboard/lib/painel-data.ts
@dashboard/components/engine/EngineControl.tsx

<interfaces>
From dashboard/lib/painel-data.ts (built in 17-02):
- type TypeFilter = "all" | "destino" | "atrativo";
- type PainelEntityType = "destino" | "atrativo";
- interface PainelCard { ...; type; uf; column; ... }
- function buildMetrics(cards: PainelCard[], type: PainelEntityType): { total; mar; falha; pct };
- function filterCards(cards, { type, ufs }): PainelCard[];
- const BR_UFS: string[];  // 27 UF codes

Design references (Painel-Brave.dc.html):
- Metric card markup: lines 102-123 (dot + "Destinos"/"Atrativos" title, mono total + "no escopo", Sincronizados in green --status-mar, Falhas in red --status-descarte, Progresso label + pct + filled bar).
- Type segmented control: lines 126-134 (inline-flex pill bg --painel-chip, 3 buttons with count badges, active state).
- UF-scope dropdown: lines 135-146 (button "Escopo UF" + label + ▾; popover with "Todas" clear + wrap of UF chips + helper text).

Tokens: use the scoped painel CSS vars only (--card, --status-mar, --status-descarte, --painel-chip, --painel-navy, --painel-muted, --painel-muted-2). NO hardcoded hex.
Test helper: renderWithClient from components/cms/__tests__/test-utils.tsx (these are pure components — a plain render is fine too).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PainelMetrics — Destinos + Atrativos cards</name>
  <read_first>.planning/.../design/Painel-Brave.dc.html lines 101-124; dashboard/lib/painel-data.ts (buildMetrics signature)</read_first>
  <files>dashboard/components/painel/PainelMetrics.tsx, dashboard/components/painel/__tests__/PainelMetrics.test.tsx</files>
  <behavior>
    - Given a cards array, renders two cards: one for type "destino", one for "atrativo".
    - Each card shows total (data-testid="metric-{type}-total"), sincronizados (green), falhas (red), and a progress bar whose width = pct% with a "{pct}%" label.
    - Values computed via buildMetrics(cards, type) — the component calls it internally; props = `{ cards: PainelCard[] }`.
  </behavior>
  <action>Create `PainelMetrics.tsx` ("use client"). Prop: `{ cards: PainelCard[] }`. Render a flex row of two cards reusing the design layout (lines 102-123) with painel tokens. A private `MetricCard({ label, color, m })` subcomponent renders the dot, label, mono total + "no escopo", a row with "Sincronizados" (mono, `text-[var(--status-mar)]`), "Falhas" (mono, `text-[var(--status-descarte)]`), and a progress block (label "Progresso" + "{pct}%" + a track `bg-[var(--painel-chip)]` with an inner fill `width:{pct}%` `bg-[var(--status-mar)]`). data-testids: metric-destino-total/-mar/-falha/-pct and metric-atrativo-*. Compute `const destinoM = buildMetrics(cards, "destino"); const atrativoM = buildMetrics(cards, "atrativo")`. Write `PainelMetrics.test.tsx`: pass a hand-built cards array (use the PainelCard shape directly — 2 destinos 1 mar, 2 atrativos 1 descarte) and assert metric-destino-total="2", metric-destino-mar shows the mar count, the destino pct, and metric-atrativo-falha reflects the descarte count.</action>
  <verify>
    <automated>cd dashboard && bun run test components/painel/__tests__/PainelMetrics.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Both cards render with totals/sincronizados/falhas/progress derived from buildMetrics.
    - Progress bar width tracks pct; green/red use --status-mar/--status-descarte vars.
    - No hardcoded hex.
  </acceptance_criteria>
  <done>Metric cards built + tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PainelFilters — type segmented control + UF-scope dropdown</name>
  <read_first>.planning/.../design/Painel-Brave.dc.html lines 126-146; dashboard/components/engine/EngineControl.tsx (UF chip multi-select toggle pattern, lines 226-258); dashboard/lib/painel-data.ts (BR_UFS, TypeFilter)</read_first>
  <files>dashboard/components/painel/PainelFilters.tsx, dashboard/components/painel/__tests__/PainelFilters.test.tsx</files>
  <behavior>
    - Type segmented control: 3 buttons (Tudo="all", Destinos="destino", Atrativos="atrativo") with optional count badges; clicking calls onTypeChange(key); active button is marked.
    - UF dropdown: a trigger button "Escopo UF {label}" toggles a popover of 27 UF chips; clicking a chip toggles it in the selection and calls onUfsChange(nextUfs); a "Todas" button clears to []; empty selection label = "Todas".
  </behavior>
  <action>Create `PainelFilters.tsx` ("use client"). Props: `{ type: TypeFilter; onTypeChange: (t: TypeFilter)=>void; ufs: string[]; onUfsChange: (ufs: string[])=>void; counts?: { all: number; destino: number; atrativo: number } }`. Render the segmented pill (design lines 129-133) with the 3 buttons (data-testid="filter-type-{key}"), active = `data-active="true"` + token-based active styling; show counts[key] in a badge when provided. Render the UF dropdown (design lines 135-146): trigger button (data-testid="filter-uf-trigger") showing "Escopo UF" + (ufs.length===0 ? "Todas" : `${ufs.length} UF`); local `useState(open)` toggles the popover; map BR_UFS to chip buttons (data-testid="filter-uf-{uf}") whose selected state = ufs.includes(uf), toggling via the EngineControl pattern (selected ? filter out : append) then onUfsChange; a "Todas" button (data-testid="filter-uf-clear") calls onUfsChange([]). Popover uses painel tokens (bg --card, border --painel-border-outer). Add the helper text from the design. Write `PainelFilters.test.tsx`: (a) clicking filter-type-destino calls onTypeChange("destino"); (b) opening the dropdown then clicking filter-uf-BA with ufs=[] calls onUfsChange(["BA"]); (c) clicking filter-uf-BA with ufs=["BA","SP"] calls onUfsChange(["SP"]); (d) filter-uf-clear calls onUfsChange([]); (e) trigger label shows "Todas" when ufs empty.</action>
  <verify>
    <automated>cd dashboard && bun run test components/painel/__tests__/PainelFilters.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Type control reports the selected filter and marks the active button.
    - UF dropdown multi-selects from the 27 UFs (toggle add/remove), "Todas" clears, label reflects count.
    - BR_UFS imported from lib/painel-data (not re-declared); no hardcoded hex.
  </acceptance_criteria>
  <done>Filter controls built + tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| painel light subtree → dark routes | These components render only inside .painel-light; must not pull dark-only assumptions |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Tampering | theme tokens | mitigate | Reference painel CSS vars only; no hardcoded hex; no edits to globals.css |
| T-17-03-02 | Information Disclosure | metric/filter render | accept | Components receive only PainelCard (already PII-stripped in 17-02) |
| T-17-03-SC | Tampering | bun installs | mitigate | No new packages |
</threat_model>

<verification>
- `cd dashboard && bun run test components/painel/__tests__/PainelMetrics.test.tsx components/painel/__tests__/PainelFilters.test.tsx` passes.
- Full suite green.
</verification>

<success_criteria>
- Metric cards + filter controls match the design, are props-driven, and are unit-tested.
</success_criteria>

<output>
Create `.planning/phases/17-painel-brave-redesign-light-theme-single-shell-painel-kanban/17-03-SUMMARY.md` when done.
</output>
