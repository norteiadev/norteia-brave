---
phase: 07-real-places-hardening-targeted-atrativos-discovery-mtur-refr
plan: "07"
type: execute
wave: 4
depends_on: ["07-05"]
gap_closure: true
files_modified:
  - brave/core/rio/routing.py
  - scripts/loadtest_destinos_atrativos.py
  - tests/unit/lanes/test_discovery_agent.py
autonomous: true
requirements: [PLACE-03]

must_haves:
  truths:
    - "produce_for_destino-created atrativo rio records have parent_mar_id accessible in rio.normalized"
    - "Harness Step-4 summary groups atrativos by parent_mar_id without referencing the non-existent RioRecord.parent_mar_id column"
    - "New offline test confirms the atrativo parent link is queryable from a produced attraction rio record"
    - "Full offline test suite still passes (401+ tests)"
  artifacts:
    - path: "brave/core/rio/routing.py"
      provides: "process_nascente_record copies parent_mar_id from nascente payload to rio.normalized"
      contains: "parent_mar_id"
    - path: "scripts/loadtest_destinos_atrativos.py"
      provides: "Step-4 summary groups by rio.normalized parent_mar_id without RioRecord.parent_mar_id column"
      contains: "normalized"
    - path: "tests/unit/lanes/test_discovery_agent.py"
      provides: "test_produce_for_destino_parent_link_in_normalized asserting parent_mar_id lands in rio.normalized"
  key_links:
    - from: "discovery_agent.py::produce_for_destino"
      to: "nascente.payload parent_mar_id"
      via: "store_raw payload includes parent_mar_id; process_nascente_record copies to normalized"
      pattern: "parent_mar_id"
    - from: "brave/core/rio/routing.py::process_nascente_record"
      to: "rio.normalized parent_mar_id"
      via: "attraction payload copy block (mirrors place_id_cache pattern lines 155-156)"
      pattern: "parent_mar_id"
    - from: "scripts/loadtest_destinos_atrativos.py::Step 4"
      to: "rio.normalized"
      via: "load atrativos then group by normalized.get('parent_mar_id') in Python"
      pattern: "normalized.get"
---

<objective>
Close G2: the harness crashes at Step 4 with AttributeError because
scripts/loadtest_destinos_atrativos.py line 204 calls select(RioRecord.parent_mar_id, ...) but
parent_mar_id is not a column on RioRecord — it lives only in the nascente payload dict and does
not flow to rio.normalized. The fix has two parts:

(1) In process_nascente_record (brave/core/rio/routing.py), copy parent_mar_id from the nascente
payload into rio.normalized, exactly mirroring the place_id_cache copy pattern at lines 155-156
of the same file. This makes the atrativo-to-parent link first-class queryable from RioRecord
without a nascente JOIN.

(2) In scripts/loadtest_destinos_atrativos.py Step 4, replace the invalid
select(RioRecord.parent_mar_id, ...) query with a Python-side approach: load all atrativo
RioRecords, then group by (r.normalized or {}).get("parent_mar_id") in a plain dict comprehension.
This avoids dialect-specific JSON SQL and works with the existing SQLAlchemy JSON type.

Purpose: Step 4 summary and the ACCEPTANCE gate are unreachable while the harness crashes. This fix
makes the summary print correctly and enables ACCEPTANCE: PASS on a real run.

Output: Updated routing.py (4-line addition); updated harness Step 4; new offline test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-CONTEXT.md
@.planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-05-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From brave/core/models.py — RioRecord columns (confirmed: NO parent_mar_id column):
  class RioRecord(Base):
      id: UUID primary key
      nascente_id: UUID FK
      entity_type: String(64)
      normalized: JSON nullable      -- parent_mar_id must live here after the fix
      routing: String(32)
      ...
  IMPORTANT: MarRecord also has a parent_mar_id column (UUID FK to mar_records.id) used for
  supersession. Do NOT confuse with the atrativo-to-destino link. This plan does NOT touch
  MarRecord or add a column to RioRecord.

From brave/core/rio/routing.py process_nascente_record (lines 138-160) — the pattern to mirror:
  normalized: dict = {
      "name": name,
      "address": address,
      "lat": lat,
      "lon": lon,
      "origem_value": float(payload.get("origem_value", 0.0)),
      ...
  }
  # Attraction-specific: preserve place_id_cache (lines 155-156)
  if nascente.entity_type == "attraction" and "place_id_cache" in payload:
      normalized["place_id_cache"] = payload["place_id_cache"]
  ADD IMMEDIATELY AFTER:
  if nascente.entity_type == "attraction" and "parent_mar_id" in payload:
      normalized["parent_mar_id"] = payload["parent_mar_id"]
  Then: normalized = label_entity(nascente.entity_type, normalized)

From brave/lanes/atrativos/discovery_agent.py produce_for_destino (lines 468-490):
  payload: dict = {
      ...
      "parent_mar_id": str(parent_mar.id),   -- in nascente.payload, NOT yet in rio.normalized
      "place_id_cache": place_id,
      ...
  }

From scripts/loadtest_destinos_atrativos.py Step 4 crash (lines 203-208):
  atr_rows = session.execute(
      select(RioRecord.parent_mar_id, func.count(RioRecord.id))   -- CRASHES
      .where(RioRecord.entity_type == "attraction")
      .group_by(RioRecord.parent_mar_id)
      .order_by(RioRecord.parent_mar_id)
  ).all()
  Replace with Python-side grouping (see action section).
  Note: func is still used in the mar_count query above (line 195) and the routing summary
  (line 223) -- do NOT remove it from the sqlalchemy import.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Copy parent_mar_id to rio.normalized in process_nascente_record + offline test</name>
  <files>brave/core/rio/routing.py, tests/unit/lanes/test_discovery_agent.py</files>
  <read_first>
    - brave/core/rio/routing.py — read the full file; locate the normalized dict building block (lines 138-160); find the place_id_cache copy at lines 155-156 to understand the exact pattern; confirm label_entity is called at line 159 so the parent_mar_id copy must go between line 156 and 159
    - brave/core/models.py lines 108-161 — confirm RioRecord has no parent_mar_id column and normalized is JSON type
    - tests/unit/lanes/test_discovery_agent.py — read the last test test_produce_for_destino_returns_zero_on_missing_municipio to understand the mock style; the new test appends after it
  </read_first>
  <behavior>
    - test_produce_for_destino_parent_link_in_normalized: builds a minimal mock NascenteRecord with
      entity_type="attraction" and payload containing "parent_mar_id": "uuid-test-parent",
      "place_id_cache": "ChIJtest", "origem_value": 60.0, "completude_value": 75.0,
      "corroboracao_value": 0.0, "atualidade_value": 0.0, "validacao_humana_value": 0.0,
      "name": "Test Atrativo". Patches find_duplicate to return None, compute_embedding to return
      [0.0]*1536, label_entity to return normalized as-is, session.scalar to return None
      (idempotency check), session.add as no-op, session.flush as no-op.
      Calls process_nascente_record(session_mock, nascente_mock, ScoreConfig()).
      Asserts: the RioRecord captured by session.add has
      normalized["parent_mar_id"] == "uuid-test-parent".
  </behavior>
  <action>
    ROUTING.PY UPDATE — brave/core/rio/routing.py:

    In process_nascente_record, after the place_id_cache copy block (lines 155-156), add:

      # Attraction-specific: preserve parent_mar_id from payload so harness and downstream
      # queries can group atrativos by parent destino without a nascente JOIN.
      # Mirrors the place_id_cache copy above (D-03 / G2 gap fix).
      if nascente.entity_type == "attraction" and "parent_mar_id" in payload:
          normalized["parent_mar_id"] = payload["parent_mar_id"]

    This is a 4-line addition. The normalized dict is a fresh Python dict (not yet persisted),
    so direct assignment is correct — no flag_modified needed.
    Do NOT add a RioRecord.parent_mar_id column. Do NOT modify alembic migrations (the
    normalized column is already JSON; adding a key to it is not a schema change).

    TEST UPDATE — tests/unit/lanes/test_discovery_agent.py:

    Append test_produce_for_destino_parent_link_in_normalized after the last test. Use the
    behavior spec above. Key implementation notes:
    - Import process_nascente_record from brave.core.rio.routing at the top of the test function
    - Import ScoreConfig from brave.config.settings
    - Patch "brave.core.rio.routing.find_duplicate" to return None
    - Patch "brave.core.rio.routing.compute_embedding" to return [0.0]*1536
    - Patch "brave.core.rio.routing.label_entity" with side_effect=lambda etype, norm: norm
    - Mock session: session.scalar.return_value = None; session.add = MagicMock(); session.flush = MagicMock()
    - Build nascente_mock with spec=NascenteRecord or as a plain MagicMock with the attributes
      set: source_ref="places:BA:ChIJtest", entity_type="attraction", uf="BA",
      content_hash="abc123", payload={...as above...}
    - Call process_nascente_record and capture the first positional arg of session.add (the RioRecord)
    - Assert rio.normalized["parent_mar_id"] == "uuid-test-parent"
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/lanes/test_discovery_agent.py::test_produce_for_destino_parent_link_in_normalized -v</automated>
  </verify>
  <done>
    - brave/core/rio/routing.py has the 4-line parent_mar_id copy block after the place_id_cache block
    - test_produce_for_destino_parent_link_in_normalized passes
    - grep -n "parent_mar_id" brave/core/rio/routing.py returns at least one match in process_nascente_record
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix harness Step-4 to group by rio.normalized parent_mar_id in Python</name>
  <files>scripts/loadtest_destinos_atrativos.py</files>
  <read_first>
    - scripts/loadtest_destinos_atrativos.py — read the full file; focus on Step 4 (lines 193-237):
      the crash is at line 204 (select(RioRecord.parent_mar_id, ...)); the atr_rows loop is at
      lines 211-213; the parents_ok count is at line 215; the mar_count query at line 195 still uses
      func.count so func must remain in the import
  </read_first>
  <action>
    Replace ONLY the Step-4 atrativo query block (lines 203-208 in the original). All other lines
    in Steps 1, 2, 3, the mar_count query, the routing summary, and the ACCEPTANCE gate are
    unchanged.

    Replace lines 203-208 with the following Python-side grouping:

      # G2 fix: RioRecord has no parent_mar_id column — the link lives in rio.normalized.
      # Load all atrativos and group by normalized.get("parent_mar_id") in Python.
      # process_nascente_record now copies parent_mar_id from payload to normalized
      # (brave/core/rio/routing.py), so all new atrativo rios carry it.
      atr_records = list(session.scalars(
          select(RioRecord).where(RioRecord.entity_type == "attraction")
      ).all())
      atr_counts: dict[str, int] = {}
      for r in atr_records:
          parent_id = (r.normalized or {}).get("parent_mar_id") or "unknown"
          atr_counts[parent_id] = atr_counts.get(parent_id, 0) + 1
      atr_rows = sorted(atr_counts.items())

    The downstream loop at lines 211-213 iterates (parent_id, cnt) tuples — same shape as
    Row objects from the old query, so no changes needed there.
    The parents_ok count at line 215 iterates (_, cnt) tuples — no change needed.
    The func import must remain (still used by mar_count query and routing summary).

    MUST NOT add TRUNCATE, DROP, or DELETE statements.
    MUST NOT change Steps 1-3, the ACCEPTANCE gate, or the mar_count query.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast; ast.parse(open('scripts/loadtest_destinos_atrativos.py').read()); print('SYNTAX OK')" && .venv/bin/python -m pytest tests/ --ignore=tests/integration/test_real_llm_smoke.py -q</automated>
  </verify>
  <done>
    - scripts/loadtest_destinos_atrativos.py has no reference to RioRecord.parent_mar_id as a column (grep -n "RioRecord\.parent_mar_id" returns 0 matches)
    - File parses cleanly (ast.parse)
    - File contains "normalized.get" in Step 4
    - Full offline suite passes (>=401 tests, 0 failures)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nascente.payload -> rio.normalized | parent_mar_id value is a UUID string produced by discovery_agent; no untrusted user input |
| harness -> DB | Step 4 query loads all attraction RioRecords; no mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-G2-01 | Information Disclosure | rio.normalized parent_mar_id | accept | parent_mar_id is a Brave-internal UUID linking two internal records; no PII; no external exposure |
| T-07-G2-02 | Tampering | RioRecord schema | mitigate | Fix adds a key to the normalized JSON dict only — no new column, no alembic migration, no schema change. Existing tests guard against accidental column additions |
| T-07-G2-03 | Denial of Service | Step-4 Python-side grouping memory | accept | All-attraction RioRecord load is bounded by the harness acceptance bar (<=100 records in a 10x10 test); not a production query path |
| T-07-G2-04 | Denial of Service | DB auto-truncate | accept | Plan action explicitly forbids TRUNCATE/DROP/DELETE; no destructive SQL added |
| T-07-G2-SC | Tampering | npm/pip installs | accept | No new packages; all imports already present in project dependencies |
</threat_model>

<verification>
1. Run grep -n "parent_mar_id" brave/core/rio/routing.py — must match in the process_nascente_record body.
2. Run grep -n "RioRecord\.parent_mar_id" scripts/loadtest_destinos_atrativos.py — must return 0 matches.
3. Run grep -n "normalized.get" scripts/loadtest_destinos_atrativos.py — must match in Step 4.
4. Run .venv/bin/python -m pytest tests/unit/lanes/test_discovery_agent.py::test_produce_for_destino_parent_link_in_normalized -v — must pass.
5. Run .venv/bin/python -m pytest tests/ --ignore=tests/integration/test_real_llm_smoke.py -q — must be >=401 passing, 0 failures.
6. Run .venv/bin/python -c "import ast; ast.parse(open('scripts/loadtest_destinos_atrativos.py').read()); print('OK')" — must print OK.
</verification>

<success_criteria>
- process_nascente_record copies parent_mar_id from attraction nascente payload to rio.normalized (4-line addition mirroring place_id_cache pattern)
- Harness Step-4 summary loads atrativos and groups by normalized.get("parent_mar_id") in Python — no column reference
- Offline test test_produce_for_destino_parent_link_in_normalized passes
- grep -n "RioRecord.parent_mar_id" scripts/loadtest_destinos_atrativos.py returns 0 matches
- Full offline suite passes
</success_criteria>

<output>
Create `.planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-07-SUMMARY.md` when done.
</output>
