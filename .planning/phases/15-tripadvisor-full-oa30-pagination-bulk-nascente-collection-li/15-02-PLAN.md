---
phase: 15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - brave/clients/base.py
  - brave/clients/null_tripadvisor.py
  - tests/fakes/fake_tripadvisor.py
  - brave/clients/null_nominatim.py
  - tests/fakes/fake_nominatim.py
autonomous: true
requirements: [TA-12]
must_haves:
  truths:
    - "Every TripAdvisor client (protocol, null, fake) advertises fetch_attractions_paginated"
    - "Every geocoder client (protocol, null, fake) advertises geocode_national"
    - "Protocol-compliance checks pass for all four implementers of each protocol"
  artifacts:
    - path: "brave/clients/base.py"
      provides: "fetch_attractions_paginated + geocode_national protocol stubs (the contracts)"
      contains: "fetch_attractions_paginated"
    - path: "tests/fakes/fake_tripadvisor.py"
      provides: "FakeTripAdvisorClient.fetch_attractions_paginated (call-recording, yields fixture_pages)"
      contains: "paginated_calls"
  key_links:
    - from: "brave/clients/base.py"
      to: "brave/lanes/tripadvisor/atrativos.py + brave/lanes/tripadvisor/client.py"
      via: "structural typing — implementers must satisfy these signatures"
      pattern: "def fetch_attractions_paginated"
---

<objective>
Widen BOTH client protocols and their offline/fake implementers with the two new methods this
phase needs, BEFORE any consumer code is written (interface-first ordering). This prevents the
Pitfall-6 break: adding a method to only the real client leaves the protocol, null, and fake
non-compliant and crashes the sweep under `run_real_externals=False`.

Two contracts:
1. `fetch_attractions_paginated` — async iterator of `(offset, cards)` over the HTML SSR transport.
2. `geocode_national` — national forward-geocode (no UF input) for the bulk all-Brazil lane.

Purpose: Lock the contracts so plans 15-04 (real client), 15-05 (real geocoder), and 15-06
(bulk ingest) implement against fixed signatures with no codebase scavenger hunt.
Output: Protocol stubs + null stubs + fake call-recorders for both methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-PATTERNS.md

<interfaces>
<!-- Existing contracts the new stubs mirror. Do NOT re-explore the codebase. -->

brave/clients/base.py — existing TripAdvisorClientProtocol (lines ~239-294):
  async def fetch_attractions(self, geo_id: int, max_pages: int = 1) -> list[dict[str, Any]]
  (single-page; protocol stub at ~260-278)

brave/clients/base.py — existing GeocoderClientProtocol (lines ~297-311):
  async def geocode(self, location_id: str, name: str, uf: str) -> dict[str, Any] | None
  returns {"lat": float, "lon": float, "osm_id": int|None, "municipio_name": str|None} or None

brave/clients/null_tripadvisor.py — fetch_attractions returns [] (lines 39-51), _check_protocol_compliance at :66
tests/fakes/fake_tripadvisor.py — fetch_attractions records calls + returns fixture (lines 69-82), _check at :98
brave/clients/null_nominatim.py — NullGeocoderClient.geocode returns None, _check_protocol_compliance present
tests/fakes/fake_nominatim.py — FakeGeocoderClient (call-recording geocode)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fetch_attractions_paginated to TripAdvisor protocol, null, and fake</name>
  <read_first>
    - brave/clients/base.py (TripAdvisorClientProtocol, the fetch_attractions stub lines ~260-278)
    - brave/clients/null_tripadvisor.py (fetch_attractions returns [], _check_protocol_compliance :66)
    - tests/fakes/fake_tripadvisor.py (fetch_attractions records calls + returns fixture, __init__ fixture_attractions, _check :98)
    - .planning/phases/15-...-li/15-PATTERNS.md (sections for null_tripadvisor + fake_tripadvisor — exact excerpts)
  </read_first>
  <action>
    Add `fetch_attractions_paginated(self, geo_id: int, start_page: int = 1, max_pages: int = 334)`
    returning an `AsyncIterator[tuple[int, list[dict[str, Any]]]]` (yields `(offset, cards)` per page)
    to: (a) `TripAdvisorClientProtocol` in `brave/clients/base.py` as a sibling stub of
    `fetch_attractions`, with a docstring stating it yields one `(offset, parsed_cards)` tuple per
    HTML SSR page and reuses `_parse_attractions_page`; (b) `NullTripAdvisorClient` as an async
    generator that yields nothing (`return` then a `yield` made unreachable, so it is an async
    generator — keep the "no Playwright / no network" posture, T-11-01-03); (c) `FakeTripAdvisorClient`
    — add `paginated_calls: list[dict]` and a `fixture_pages` constructor arg (mirror `fixture_attractions`),
    record each call as `{"geo_id", "start_page", "max_pages"}`, then `yield offset, cards` for each
    entry in `fixture_pages.get(geo_id, [])`. Update each module's `_check_protocol_compliance()` so the
    new method is covered. Do NOT touch `fetch_attractions` anywhere.
  </action>
  <acceptance_criteria>
    - `grep -c 'def fetch_attractions_paginated' brave/clients/base.py brave/clients/null_tripadvisor.py tests/fakes/fake_tripadvisor.py` shows one match per file.
    - `NullTripAdvisorClient().fetch_attractions_paginated(...)` is an async generator that yields zero items (asserted in a test or via `inspect.isasyncgen`).
    - Existing `test_*_protocol_compliance` for the TripAdvisor clients still pass.
    - `fetch_attractions` signatures are byte-for-byte unchanged (the WR-02 single-page contract is untouched).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_client.py tests/unit -k "protocol or compliance or fake_tripadvisor" -q</automated>
  </verify>
  <done>All three TripAdvisor implementers advertise the new async-iterator method; compliance green; single-page contract intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add geocode_national to Geocoder protocol, null, and fake</name>
  <read_first>
    - brave/clients/base.py (GeocoderClientProtocol, the geocode stub lines ~297-311)
    - brave/clients/null_nominatim.py (NullGeocoderClient.geocode returns None, _check_protocol_compliance)
    - tests/fakes/fake_nominatim.py (FakeGeocoderClient — geocode call-recording + fixture return)
  </read_first>
  <action>
    Add `geocode_national(self, location_id: str, name: str) -> dict[str, Any] | None` (NO `uf` arg —
    the all-Brazil bulk lane has no per-UF context) to: (a) `GeocoderClientProtocol` in `base.py` as a
    sibling stub of `geocode`, documented to return the SAME 4-key LGPD-safe shape
    `{"lat": float, "lon": float, "osm_id": int|None, "municipio_name": str|None}` or `None`, derived
    from a national `"{name}, Brazil"` query; (b) `NullGeocoderClient.geocode_national` returning `None`
    (no network, T-14-05); (c) `FakeGeocoderClient.geocode_national` — record the call and return a
    configurable fixture result (mirror the existing fake `geocode` recorder). Update each
    `_check_protocol_compliance()`. The LGPD note stays: only the 4 keys, never display_name/street.
  </action>
  <acceptance_criteria>
    - `grep -c 'def geocode_national' brave/clients/base.py brave/clients/null_nominatim.py tests/fakes/fake_nominatim.py` shows one match per file.
    - `NullGeocoderClient` geocode_national returns None with no network call (asserted offline).
    - Geocoder `_check_protocol_compliance` for null + fake still pass.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit -k "geocod or nominatim or compliance" -q</automated>
  </verify>
  <done>All three geocoder implementers advertise geocode_national; compliance green; LGPD 4-key shape preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client protocol → consumers | Mis-typed/missing methods crash the offline (null) path used in CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Denial of Service | NullTripAdvisorClient / NullGeocoderClient | mitigate | Null stubs perform zero network I/O (yield nothing / return None), keeping CI keyless and offline (T-11-01-03 / T-14-05). |
| T-15-02-02 | Information Disclosure | geocode_national return shape | mitigate | Protocol docstring fixes the 4-key LGPD-safe shape; no display_name/street/address can enter the contract. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
Run both `<verify>` commands with `RUN_REAL_EXTERNALS` UNSET. All protocol-compliance and fake tests green.
</verification>

<success_criteria>
Both new contracts exist on all four implementers each; consumers in 15-04/15-05/15-06 can type against them
with no further protocol changes.
</success_criteria>

<output>
Create `.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-02-SUMMARY.md` when done
</output>
