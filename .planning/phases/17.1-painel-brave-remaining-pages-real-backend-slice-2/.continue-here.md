---
context: phase
phase: 17.1-painel-brave-remaining-pages-real-backend-slice-2
task: 6
total_tasks: 8
status: in_progress
last_updated: 2026-06-27T22:46:03.356Z
---

# Resume: Phase 17.1 — Painel Brave slice 2 (planning, paused before PLAN.md written)

Paused mid-planning. All planning INPUTS exist + committed; **0 PLAN.md files written**. Next step is
to (re-)run the planner so it writes the plans, then plan-check, then execute.

<current_state>
GSD plan-phase for 17.1 reached the planner spawn. The planner was running in the background and was
**stopped by the pause** before writing any PLAN.md. Working tree is clean; last commit `874dc4f`
(validation + pattern map). Phase 17 (slice 1) is shipped + CLOSED — do NOT touch it.
</current_state>

<completed_work>

Completed:
- Design `Painel Brave.dc.html` imported (DesignSync) + fully reviewed (markup + support.js model).
- Backend reality verified (Explore): sync backbone exists; Duplicados + Varreduras need new backend;
  **compute_embedding is a ZERO-STUB** so dedup cosine is degenerate.
- Phase 17.1 added to ROADMAP (continuation of closed Phase 17).
- 17.1-CONTEXT.md (incl. locked "Research resolutions"), 17.1-RESEARCH.md, 17.1-PATTERNS.md (18/18
  analogs), 17.1-VALIDATION.md — all written + committed.
</completed_work>

<remaining_work>

- Task 6 (in progress): planner writes the PLAN.md files. Intended shape (~6 plans / 3 waves):
  - **Wave 1 (backend, parallel — exclusive files):**
    - `17.1-01` Duplicados: NEW `brave/api/routers/dedup.py` (list pairs compute-on-read field
      similarity + resolve merge/keep/discard) + Pydantic models + offline tests.
    - `17.1-02` Varreduras: `RunHistory` model in `brave/core/models.py` +
      `alembic/versions/0007_add_runs_history.py` + write points in `engine.py`/`sweep.py` +
      NEW `brave/api/routers/runs.py` (list + reprocess-failures) + skip-safe integration test.
    - `17.1-03` Transition: ONE generic audited per-entity `transition` endpoint (`cms.py`/
      `atrativos.py`) + server-side `_ALLOWED_EDGES` allow-list (mar→* stays 409) + tests.
  - **Wave 2 (frontend data layer):** `lib/painel-data.ts` 5→6 columns (whatsapp/falha sourcing);
    NEW `lib/dedup-api.ts`, `lib/runs-api.ts`; extend `lib/engine-api.ts` (`injectTASession`,
    `transition`); extend `lib/painel-actions.ts` mapDrop for full-pipeline; NEW MSW handlers
    `mocks/handlers/dedup.ts`,`runs.ts` (register in `index.ts`).
  - **Wave 3 (frontend views + wiring):** NEW `components/painel/PainelDuplicados.tsx`,
    `PainelVarreduras.tsx`, `PainelOrigem.tsx` (modal + TA cURL paste) + Motor toggle; cross-wire
    Mapeamento/Conversas/Custo/Drawer + two-group nav (Processamento / Operação); Vitest + MSW tests.
- Task 7: gsd-plan-checker (iterate ≤3).
- Task 8: gsd-execute-phase wave-by-wave; keep both test suites green (backend RUN_REAL_EXTERNALS unset).
</remaining_work>

<decisions_made>

- New phase 17.1 (not `--force` over closed phase 17) — closeout was valid, scope is new.
- Full backend for Duplicados + Varreduras; full-pipeline drag (operator-chosen max scope).
- Duplicados similarity = compute-on-read field/token (Python), embeddings-real deferred (stub).
- Merge = union candidate source_refs into existing Mar + discard candidate Rio, audited, **no 409**.
- Contract = Pydantic + mirrored MSW (no pact-js).
- 6-col board: whatsapp = atrativos sub_state `aguardando_consulta_whatsapp`; falha = `GET /api/v1/failures`.
- ONE generic audited per-entity `transition` endpoint, server-side allow-list mirroring `mapDrop`.
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/17.1-.../17.1-CONTEXT.md` — locked scope + "Research resolutions" (OVERRIDE research where they differ)
2. `.planning/phases/17.1-.../17.1-RESEARCH.md` — endpoint shapes, runs schema, transition matrix, offline boundaries
3. `.planning/phases/17.1-.../17.1-PATTERNS.md` — file:line analogs for every new/modified file
4. `.planning/phases/17.1-.../17.1-VALIDATION.md` — Nyquist Dimension-8 strategy
5. `.planning/phases/17-.../17-04-SUMMARY.md` + `17-05-SUMMARY.md` — what slice 1 shipped (board + mapDrop)

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN] Using `ls` to check files under this repo's rtk shell proxy → returns EMPTY intermittently and falsely reads as "files missing" (it stalled the planner once). → Use the Read tool with explicit absolute paths; use `find` (not `ls`) if you must list.
- [ANTI-PATTERN] Inventing a Duplicados similarity from pgvector cosine → embeddings are a zero-stub, cosine is degenerate. → Compute field/token similarity in Python.
- [ANTI-PATTERN] Bypassing `lib/painel-actions.ts` mapDrop for new transitions → it is the client security boundary. → EXTEND mapDrop + mirror a server-side allow-list.

## Infrastructure State
- Working tree clean at commit `874dc4f`. No running servers/watchers from this session.
- Background planner agent `a6b1e611b492f1fa4` was STOPPED (killed). Do not assume its partial reasoning; re-spawn fresh.

<context>
Driving the build through GSD per operator request. We are at the planner step. Everything the planner
needs is on disk and committed; it simply needs to emit the 6 PLAN.md files in the wave structure above,
honoring the locked CONTEXT resolutions. Then plan-check, then execute-phase.
</context>

<next_action>
Start with: re-run `/gsd:plan-phase 17.1` (standard mode). It will detect existing CONTEXT/RESEARCH/
PATTERNS/VALIDATION and proceed straight to spawning the planner. Ensure the planner WRITES PLAN.md
files (the prior run was killed before writing). Then continue to plan-check + execute.
</next_action>
