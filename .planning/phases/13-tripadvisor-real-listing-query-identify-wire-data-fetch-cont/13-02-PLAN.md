---
phase: "13"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - brave/api/routers/tripadvisor_session.py
  - brave/lanes/tripadvisor/atrativos.py
  - tests/unit/api/test_tripadvisor_session.py
  - tests/unit/lanes/tripadvisor/test_atrativos.py
autonomous: true
requirements:
  - TA-12

must_haves:
  truths:
    - "_run_canary probes the real AttractionsFusion listing query (qid a5cb7fa004b5e4b5) not fetch_destinations; empty sections list triggers invalid_session"
    - "atrativos._ingest_one reads name from cardTitle.text, locationId from detailId, rating/review_count from bubbleRating, sets most_recent_review_at=None"
    - "Offline tests for canary + atrativos lane pass without hitting TripAdvisor"
  artifacts:
    - path: "brave/api/routers/tripadvisor_session.py"
      provides: "_run_canary probes fetch_attractions with real qid + single page"
      contains: "fetch_attractions"
    - path: "brave/lanes/tripadvisor/atrativos.py"
      provides: "Card-field mapping aligned to AttractionsFusion response shape"
      contains: "cardTitle"
    - path: "tests/unit/api/test_tripadvisor_session.py"
      provides: "Canary probes fetch_attractions test"
    - path: "tests/unit/lanes/tripadvisor/test_atrativos.py"
      provides: "Card-field mapping tests"
  key_links:
    - from: "_run_canary"
      to: "TripAdvisorClient.fetch_attractions"
      via: "asyncio.wait_for with max_pages=1"
      pattern: "fetch_attractions"
    - from: "atrativos._ingest_one"
      to: "TripAdvisorReviewSignals"
      via: "entity.get('rating'), entity.get('review_count'), most_recent_review_at=None"
      pattern: "most_recent_review_at"
---

<objective>
Fix _run_canary to probe fetch_attractions (the real listing query, qid a5cb7fa004b5e4b5) instead of fetch_destinations. Fix atrativos._ingest_one to consume the new normalized card dict shape (cardTitle.text → name, detailId → locationId, bubbleRating fields) with most_recent_review_at=None at Nascente.

Purpose: The canary currently probes fetch_destinations which uses a wrong contract — canary empty-result detection is meaningless until it probes the actual query being validated. The atrativos lane _ingest_one reads old field names (entity.get("name"), entity.get("reviewCount")) that do not match the new normalized card dict.
Output: Updated _run_canary, updated _ingest_one field reads, new offline tests.
</objective>

<execution_context>
@/Users/leandro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leandro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont/GAP-12-A-FINDINGS.md
@.planning/phases/13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont/13-CONTEXT.md
@brave/api/routers/tripadvisor_session.py
@brave/lanes/tripadvisor/atrativos.py
@tests/unit/api/test_tripadvisor_session.py

<interfaces>
<!-- Key contracts the executor needs. -->

From brave/api/routers/tripadvisor_session.py:
- _run_canary(session, ta_config, redis) currently calls:
    client.fetch_destinations("RJ", max_pages=1)
  — must change to:
    client.fetch_attractions(geo_id=303380, max_pages=1)  # 303380 = Brazil national geoId
  — rationale: fetch_attractions uses qid a5cb7fa004b5e4b5; validating it confirms the real listing works.
  — All other canary semantics preserved: SessionExpiredError/TimeoutError → delete key → 422;
    Exception (infra) → 503 canary_unverified (do NOT delete key); empty result → 422.

From brave/lanes/tripadvisor/atrativos.py:
- _ingest_one(self, uf, entity, *, run_rio) currently reads:
    location_id = str(entity.get("locationId", ""))    ← matches new dict key "locationId" (int from detailId)
    name = str(entity.get("name", ""))                 ← matches new dict key "name"
    review_count = int(entity.get("reviewCount", 0))   ← OLD key; new dict key is "review_count"
    rating = float(entity.get("rating", 0.0))          ← matches new dict key "rating"
    most_recent_str = entity.get("mostRecentReviewDate")  ← NOT in listing card; must be None
  — Changes needed:
    a. review_count = int(entity.get("review_count", 0))   (underscore, not camel)
    b. Remove most_recent_str parsing block entirely; set most_recent_dt = None unconditionally.
       Decision per CONTEXT.md: most_recent_review_at=None at Nascente (listing card lacks it).
       Add a comment: "# most_recent_review_at: not in AttractionsFusion listing card — None at Nascente (Phase 13 decision)"
    c. category = str(entity.get("category", ""))  — new field from card; add to payload dict as "category": category.
       (No schema change needed — TripAdvisorAtrativoPayload does not have a category field; carry it only in the raw payload dict passed to store_raw, not in the Pydantic model.)
  — No changes to: completude_from_fields, corroboracao_from_reviews, store_raw, process_nascente_record, quarantine paths.

From tests/unit/api/test_tripadvisor_session.py:
- Existing canary tests mock _run_canary at module level — they do NOT call through the real _run_canary with TripAdvisorClient; only the two WR-02 async tests do.
- The two WR-02 tests monkeypatch TripAdvisorClient.fetch_destinations — must be updated to monkeypatch fetch_attractions.

Normalized card dict (output of _parse_attractions_page in plan 13-01):
  {"name": str, "locationId": int, "rating": float, "review_count": int, "category": str}
  — location_id in _ingest_one comes from str(entity.get("locationId", ""))  [already correct]
  — lat/lng: entity.get("lat") / entity.get("lng") — not present in listing card, will be None [already handled]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix _run_canary to probe fetch_attractions + update WR-02 canary tests</name>
  <files>brave/api/routers/tripadvisor_session.py, tests/unit/api/test_tripadvisor_session.py</files>
  <read_first>
    - brave/api/routers/tripadvisor_session.py (full file — _run_canary, inject_session)
    - tests/unit/api/test_tripadvisor_session.py (full file — especially the WR-02 async tests at the bottom)
    - .planning/phases/13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont/13-CONTEXT.md (Canary decision)
  </read_first>
  <action>
    In brave/api/routers/tripadvisor_session.py:

    1. In _run_canary, replace:
         results = await asyncio.wait_for(
             client.fetch_destinations("RJ", max_pages=1),
             timeout=_CANARY_TIMEOUT_SECONDS,
         )
       with:
         results = await asyncio.wait_for(
             client.fetch_attractions(geo_id=303380, max_pages=1),
             timeout=_CANARY_TIMEOUT_SECONDS,
         )
       Use geo_id=303380 (Brazil national geoId from uf_geoids.json; "RJ" would require a geo resolve; hardcoding the national geoId avoids that dependency in the canary path).

    2. Update the SessionExpiredError except clause: the exception class import is unchanged (SessionExpiredError from client.py) — no import change needed.

    3. Update the docstring comment inside _run_canary from "fetch_destinations" to "fetch_attractions" (WR-06 comment about single-page bound).

    In tests/unit/api/test_tripadvisor_session.py:

    4. In test_canary_infra_error_returns_503_and_keeps_key: change:
         monkeypatch.setattr(TripAdvisorClient, "fetch_destinations", _raise_infra)
       to:
         monkeypatch.setattr(TripAdvisorClient, "fetch_attractions", _raise_infra)
       Update the coroutine signature to match fetch_attractions:
         async def _raise_infra(self, geo_id, max_pages=None):
             raise ValueError("unknown geoId / infra fault")

    5. In test_canary_session_expired_returns_422_and_deletes_key: change:
         monkeypatch.setattr(TripAdvisorClient, "fetch_destinations", _raise_expired)
       to:
         monkeypatch.setattr(TripAdvisorClient, "fetch_attractions", _raise_expired)
       Update coroutine signature:
         async def _raise_expired(self, geo_id, max_pages=None):
             raise SessionExpiredError("403 DataDome block")

    6. Add one new test to validate the canary probes the correct method. Add test_canary_probes_fetch_attractions (non-async, using monkeypatch): monkeypatch TripAdvisorClient.fetch_attractions to record that it was called and return a non-empty list; monkeypatch fetch_destinations to raise AssertionError("canary must not call fetch_destinations"). Call _run_canary with a stub session + fake TripAdvisorConfig + fakeredis. Assert the canary completed without error (did not raise), proving it called fetch_attractions not fetch_destinations.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/api/test_tripadvisor_session.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - _run_canary calls client.fetch_attractions(geo_id=303380, max_pages=1) (not fetch_destinations)
    - All 13+ existing test_tripadvisor_session.py tests pass
    - test_canary_probes_fetch_attractions passes
    - WR-02 tests updated to monkeypatch fetch_attractions
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix atrativos._ingest_one card-field mapping + add offline lane tests</name>
  <files>brave/lanes/tripadvisor/atrativos.py, tests/unit/lanes/tripadvisor/test_atrativos.py</files>
  <read_first>
    - brave/lanes/tripadvisor/atrativos.py (full file — _ingest_one field reads)
    - tests/unit/lanes/tripadvisor/test_atrativos.py (existing tests — pattern for FakeTripAdvisorClient usage)
    - brave/lanes/tripadvisor/schemas.py (TripAdvisorAtrativoPayload, TripAdvisorReviewSignals)
  </read_first>
  <action>
    In brave/lanes/tripadvisor/atrativos.py:

    1. In _ingest_one, change:
         review_count = int(entity.get("reviewCount", 0))
       to:
         review_count = int(entity.get("review_count", 0))
       (The new normalized card dict from _parse_attractions_page uses underscore.)

    2. Replace the most_recent_str block (lines that read entity.get("mostRecentReviewDate") + the datetime parse try/except) with:
         most_recent_dt: datetime | None = None
         # most_recent_review_at: not in AttractionsFusion listing card — None at Nascente (Phase 13 decision)
       Remove the datetime import line for timezone if it is no longer used anywhere in the file. Check if datetime is still needed for completude_from_fields or other calls; if only most_recent_dt used timezone, it can be removed; if datetime is still needed for type annotations, keep the import but remove the timezone usage.

    3. After name = str(entity.get("name", "")), add:
         category = str(entity.get("category", ""))
       In the payload dict passed to store_raw, add:
         "category": category,
       between the review_signals / §7.6 value fields. (TripAdvisorAtrativoPayload does not declare category — this goes only in the raw payload dict, not the Pydantic model.)

    4. No changes to: ibge resolve, destino_rio_map parent resolution, store_raw call, process_nascente_record call, completude_from_fields, corroboracao_from_reviews, or the atualidade_from_recency call (atualidade_from_recency(None) should return a valid float — verify this is handled in scoring.py; if not, add a None guard in this file only: atualidade_value = atualidade_from_recency(most_recent_dt) if most_recent_dt is not None else 0.0).

    In tests/unit/lanes/tripadvisor/test_atrativos.py (if file exists — check first; if not, create it):

    5. Add class TestAtrativosIngestCardFields:
       - test_ingest_one_maps_review_count_underscore: build a card entity dict with review_count=500 (underscore key) and reviewCount=999 (old camelCase key — should be ignored). Inject into _ingest_one via FakeTripAdvisorClient. Capture the nascente payload via a SQLAlchemy in-memory stub or by monkeypatching store_raw. Assert stored payload review_count == 500 (not 999).
       - test_ingest_one_sets_most_recent_review_at_none: build a card entity with no mostRecentReviewDate and no most_recent_review_at. Assert review_signals.most_recent_review_at is None in the store_raw payload.
       - test_ingest_one_stores_category: build a card entity with category="Waterfalls". Assert payload["category"] == "Waterfalls" in the store_raw call.

    Note on test scaffolding: use the existing test pattern in the file (FakeTripAdvisorClient + SQLAlchemy mock session via MagicMock or the existing testing.conftest Session fixture). Keep tests strictly offline — no DB connection needed if store_raw is monkeypatched.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/lanes/tripadvisor/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - atrativos._ingest_one reads entity["review_count"] (not "reviewCount")
    - most_recent_review_at is unconditionally None (no mostRecentReviewDate parsing)
    - category is stored in raw payload
    - test_ingest_one_maps_review_count_underscore passes
    - test_ingest_one_sets_most_recent_review_at_none passes
    - test_ingest_one_stores_category passes
    - Full offline suite passes: .venv/bin/python -m pytest tests/ -q exits 0 (or with known pre-existing skips only)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| _run_canary → fetch_attractions | live TripAdvisor call with injected session; probing real listing qid now makes empty-result detection meaningful |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Information Disclosure | canary geo_id 303380 hardcoded | accept | national geoId is a public TripAdvisor location identifier, not a secret |
| T-13-02-02 | Information Disclosure | most_recent_review_at=None at Nascente | accept | decision per CONTEXT.md; no PII risk from null field |
| T-13-02-SC | Tampering | npm/pip/cargo installs | accept | no new package dependencies in this plan |
</threat_model>

<verification>
Full offline suite:
  .venv/bin/python -m pytest tests/unit/lanes/tripadvisor/ tests/unit/api/test_tripadvisor_session.py -q

Spot-check:
  grep -n "fetch_attractions" brave/api/routers/tripadvisor_session.py | grep "303380"  # must exist
  grep -n "review_count" brave/lanes/tripadvisor/atrativos.py | grep -v "reviewCount"  # underscore only
  grep -n "mostRecentReviewDate" brave/lanes/tripadvisor/atrativos.py  # must return 0 matches
</verification>

<success_criteria>
- _run_canary calls fetch_attractions(geo_id=303380, max_pages=1) — never fetch_destinations
- atrativos._ingest_one maps entity["review_count"] correctly; most_recent_review_at is always None
- category is carried into the raw Nascente payload
- 3 new atrativos field-mapping tests pass
- 1 new canary probe test passes
- All existing tests still pass
</success_criteria>

<output>
Create .planning/phases/13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont/13-02-SUMMARY.md when done
</output>
