---
phase: 04-dashboard-territorial-cms
plan: 06
type: execute
wave: 4
depends_on: ["04-02", "04-04"]
files_modified:
  - dashboard/app/gate/page.tsx
  - dashboard/components/gate/GateQueue.tsx
  - dashboard/components/gate/GateReviewPanel.tsx
  - dashboard/components/gate/RampContext.tsx
  - dashboard/components/gate/gate-actions.ts
  - dashboard/lib/gate-api.ts
  - dashboard/mocks/handlers/gate.ts
  - dashboard/components/gate/__tests__/GateQueue.test.tsx
  - dashboard/components/gate/__tests__/gate-actions.test.tsx
autonomous: true
requirements: [DASH-03]
must_haves:
  truths:
    - "The WhatsApp gate UI works the aguardando_consulta_whatsapp queue with approve/reject"
    - "Approve enqueues outreach; reject routes to dlq/descarte — via the existing atrativos_gate endpoints"
    - "The gate shows ramp + quality-rating context so the operator sees the volume cap and quality state"
    - "Approving never reaches the browser with raw phone PII (masked)"
  artifacts:
    - path: "dashboard/components/gate/GateQueue.tsx"
      provides: "The gate master list over /atrativos/gate, reusing the DLQ master-detail scaffold"
    - path: "dashboard/components/gate/RampContext.tsx"
      provides: "Ramp + quality-rating context panel"
  key_links:
    - from: "dashboard/components/gate/gate-actions.ts"
      to: "existing PATCH /api/v1/atrativos/gate/{id}/approve|reject"
      via: "useMutation through the BFF, invalidateQueries(['gate'])"
      pattern: "invalidateQueries"
    - from: "dashboard/components/gate/GateReviewPanel.tsx"
      to: "ReviewPanel (reused from DLQ slice)"
      via: "shared master-detail scaffold (D-06)"
      pattern: "ReviewPanel"
---

<objective>
WhatsApp gate slice (DASH-03). Build the gate queue UI over the **existing** `atrativos_gate.py` endpoints (GET `/atrativos/gate`, PATCH approve/reject) — reusing the DLQ master-detail scaffold (`ReviewPanel`/`QueueList`, D-06) since they share shape (list → detail → approve/reject) — plus a ramp + quality-rating context panel so the operator sees the enforced volume ramp and the WhatsApp quality state before approving outreach.

Purpose: Delivers DASH-03 end-to-end. No new mutation endpoints (reuse Phase 3's gate router); the slice is mostly UI + reuse. Enforces LGPD: the gate never shows raw `phone_e164` (masked).
Output: The `/gate` view + gate components + offline Vitest/MSW tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md
@.planning/phases/04-dashboard-territorial-cms/04-RESEARCH.md
@.planning/phases/03-atrativos-lane-whatsapp-compliance/03-CONTEXT.md
@.planning/phases/04-dashboard-territorial-cms/04-04-SUMMARY.md
@.planning/phases/04-dashboard-territorial-cms/04-02-SUMMARY.md

<interfaces>
<!-- Existing endpoints (reuse via BFF) — from atrativos_gate.py. No new endpoints. -->
GET /api/v1/atrativos/gate?uf&limit → rows where sub_state='aguardando_consulta_whatsapp': {rio_id,uf,sub_state,routing,dlq_reason,score,score_version,canonical_key,normalized}
PATCH /api/v1/atrativos/gate/{id}/approve → flips to whatsapp_in_progress, enqueues outreach (now either-or Bearer/steward auth per plan 01)
PATCH /api/v1/atrativos/gate/{id}/reject → routes to dlq/descarte
quality-rating flag (Redis, RED → auto-pause context) + ConsentLog (opt-out / ramp context) — Phase 3
ReviewPanel from plan 04 — reuse for the detail pane (D-06)
UI-SPEC copy: "Aprovar contato", gate reject AlertDialog "Rejeitar atrativo?", empty "Fila de gate vazia", masked phone "telefone (minimizado)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GateQueue + GateReviewPanel (reuse DLQ scaffold) + RampContext</name>
  <files>dashboard/components/gate/GateQueue.tsx, dashboard/components/gate/GateReviewPanel.tsx, dashboard/components/gate/RampContext.tsx, dashboard/lib/gate-api.ts, dashboard/mocks/handlers/gate.ts, dashboard/components/gate/__tests__/GateQueue.test.tsx</files>
  <read_first>
    - dashboard/components/dlq/QueueList.tsx + ReviewPanel.tsx (the reusable master-detail scaffold from plan 04)
    - .planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md (gate copy, masked-phone LGPD rule, ramp/quality context, StatusBadge)
    - .planning/phases/03-atrativos-lane-whatsapp-compliance/03-CONTEXT.md (gate endpoint shape, ramp/quality-rating, ConsentLog opt-out)
  </read_first>
  <action>Build `dashboard/components/gate/GateQueue.tsx` reusing the DLQ `QueueList` scaffold (D-06) but bound to `GET /api/v1/atrativos/gate?uf&limit` and filtered to `sub_state='aguardando_consulta_whatsapp'`; UF filter keeps the BA/RJ/SP/SC/CE/PE-first order. Build `dashboard/components/gate/GateReviewPanel.tsx` composing the reused `ReviewPanel` for the detail pane (Nascente/Rio/ScoreBreakdownPanel/signals), with gate-specific action bar. Build `dashboard/components/gate/RampContext.tsx` — a context panel showing the enforced volume ramp (remaining cap) and the WhatsApp quality-rating state (GREEN/AMBER/RED, destructive styling on RED per UI-SPEC). Enforce LGPD: any phone shown is masked ("+55 11 9••••-••42", labeled "telefone (minimizado)") — never render raw `phone_e164` (the backend masks it; the UI must not reconstruct it). Add `dashboard/lib/gate-api.ts` (query keys + fetchers, key `['gate']`). Tests: GateQueue renders the queue + empty state "Fila de gate vazia"; masked-phone assertion (no raw e164 in the DOM).</action>
  <verify>
    <automated>cd dashboard && bunx vitest run components/gate/__tests__/GateQueue.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/components/gate/GateReviewPanel.tsx` imports/reuses the DLQ `ReviewPanel`
    - `grep -rn "minimizado" dashboard/components/gate` returns a match (masked-phone label)
    - GateQueue test asserts the empty-state copy + that no raw `+55 11 9XXXXXXXX`-style full number renders
    - `cd dashboard && bunx vitest run components/gate/__tests__/GateQueue.test.tsx` exits 0
  </acceptance_criteria>
  <done>The gate queue + detail pane reuse the DLQ scaffold, show ramp/quality context, and mask phone PII.</done>
</task>

<task type="auto">
  <name>Task 2: gate-actions (approve/reject via existing endpoints) + /gate page</name>
  <files>dashboard/components/gate/gate-actions.ts, dashboard/app/gate/page.tsx, dashboard/components/gate/__tests__/gate-actions.test.tsx</files>
  <read_first>
    - dashboard/components/dlq/dlq-actions.ts (the mutation pattern to mirror)
    - .planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md ("Aprovar contato", "Rejeitar atrativo?" AlertDialog, toasts)
    - dashboard/components/gate/GateQueue.tsx (from Task 1)
  </read_first>
  <action>Build `dashboard/components/gate/gate-actions.ts` with TanStack `useMutation` hooks calling the existing endpoints through the BFF: approve ("Aprovar contato" → PATCH `/atrativos/gate/{id}/approve`, onSettled `invalidateQueries(['gate'])`, toast "Contato aprovado — saída enfileirada") and reject (PATCH `/atrativos/gate/{id}/reject`, behind a shadcn `AlertDialog` "Rejeitar atrativo?" with red confirm, toast state-explicit). No new mutation endpoints — reuse Phase 3's. Wire `dashboard/app/gate/page.tsx` as the master-detail layout (GateQueue ↔ GateReviewPanel + RampContext). Tests: approve invalidates+refetches `['gate']`; reject requires the AlertDialog confirm; all MSW states (success/empty/error/401).</action>
  <verify>
    <automated>cd dashboard && bunx vitest run components/gate && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "invalidateQueries" dashboard/components/gate/gate-actions.ts` returns a match
    - approve calls PATCH `/atrativos/gate/{id}/approve` through the BFF (relative `/api/...`)
    - reject uses a shadcn `AlertDialog`
    - `cd dashboard && bunx vitest run components/gate` exits 0 (all four MSW states)
    - `cd dashboard && bunx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The gate approve/reject work via existing endpoints with ramp/quality context, refetch on mutate, all offline-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → gate approve | approve enqueues real WhatsApp outreach (compliance-gated); browser→BFF→FastAPI Bearer |
| FastAPI → browser | phone PII must be minimized before crossing to the browser (LGPD) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-18 | Information Disclosure | phone_e164 PII | mitigate | gate/conversation reads mask phone server-side; UI labels "telefone (minimizado)" and never reconstructs raw e164 (LGPD minimization, RESEARCH §3 R3) |
| T-04-19 | Elevation of Privilege | gate approve via BFF | mitigate | approve forwarded only with a valid browser Bearer → either-or gate (plan 01); Phase 3 send-path compliance gate still enforced server-side (human gate + ramp + opt-out unchanged) |
| T-04-20 | Tampering | ramp bypass | accept | the volume ramp is enforced in the Phase 3 send path, not the UI; the UI only displays context — no UI bypass possible |
| T-04-SC | Tampering | npm deps (none added) | accept | no new package installs (reuses plan 02/04 deps) |
</threat_model>

<verification>
- `cd dashboard && bunx vitest run components/gate` exits 0 (queue/actions, success/empty/error/401, masked phone).
- `cd dashboard && bunx tsc --noEmit` exits 0.
</verification>

<success_criteria>
DASH-03 complete: the WhatsApp gate UI works the aguardando_consulta_whatsapp queue (approve/reject) with ramp/quality context via existing endpoints, masks phone PII, all offline-tested.
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-territorial-cms/04-06-SUMMARY.md` when done.
</output>


## CONTEXT Decision Traceability

Implements CONTEXT.md decisions: D-04 (TanStack Query), D-06 (shared master-detail ReviewPanel reuse for the WhatsApp gate)
