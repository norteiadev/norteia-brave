---
phase: 15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li
plan: 04
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - brave/lanes/tripadvisor/client.py
  - brave/config/settings.py
  - tests/unit/lanes/tripadvisor/test_pagination.py
autonomous: true
requirements: [TA-12]
must_haves:
  truths:
    - "The embedded-JSON extractor recovers the same sections[] shape _parse_attractions_page consumes from a real captured page"
    - "fetch_attractions_paginated GETs the -oa{N}- HTML page per page, throttles, and yields (offset, cards)"
    - "A 403/429 from the HTML page raises SessionExpiredError (no retry)"
    - "The single-page fetch_attractions WR-02 contract still raises NotImplementedError on max_pages>1"
    - "No cookie/session/UA/proxy value is ever logged"
  artifacts:
    - path: "brave/lanes/tripadvisor/client.py"
      provides: "_extract_sections_from_html + fetch_attractions_paginated (HTML SSR transport)"
      contains: "fetch_attractions_paginated"
    - path: "brave/config/settings.py"
      provides: "TripAdvisorConfig.page_throttle_seconds (BRAVE_TA_PAGE_THROTTLE_SECONDS)"
      contains: "page_throttle_seconds"
  key_links:
    - from: "brave/lanes/tripadvisor/client.py::fetch_attractions_paginated"
      to: "brave/lanes/tripadvisor/client.py::_parse_attractions_page"
      via: "extractor output fed to existing parser unchanged"
      pattern: "_parse_attractions_page"
    - from: "fetch_attractions_paginated"
      to: "tests/fixtures/tripadvisor/attractions_oa30.html"
      via: "respx-mocked HTML body + offline extractor test"
      pattern: "attractions_oa30"
---

<objective>
Implement the real HTML SSR transport: a static `_extract_sections_from_html` that recovers the
embedded FlexCard `sections[]` JSON island from a captured page, and `fetch_attractions_paginated`,
an async generator that GETs each `-oa{N}-` page, extracts, parses (reusing `_parse_attractions_page`
UNCHANGED), throttles, and fails fast on 403/429. Add the configurable page throttle. The single-page
GraphQL `fetch_attractions` (WR-02 `NotImplementedError` on `max_pages>1`) stays byte-for-byte intact —
this is a SEPARATE transport (HTML GET, not GraphQL POST).

Purpose: Deliver the locked HTML-SSR pagination transport, test-first against the Wave-0 fixture.
Output: extractor + paginated fetch + throttle config + offline tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-PATTERNS.md
@.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-RESEARCH.md
@tests/fixtures/tripadvisor/attractions_oa30.html

<interfaces>
<!-- Copy-from analogs in the same file. Do not re-explore. -->
brave/lanes/tripadvisor/client.py:
  _TA_GRAPHQL_URL constant (:50) — add _TA_HTML_URL beside it:
    "https://www.tripadvisor.com/Attractions-g{geo_id}-Activities-a_allAttractions.true-oa{offset}-Brazil.html"
  _parse_attractions_page (:126-179): @staticmethod, never-raises, skip-with-debug parser. REUSE unchanged.
  fetch_attractions (:266-387): cookie/proxy/UA/httpx wiring to copy (session=self._get_session();
    cookies=session.get("cookies",{}); user_agent=session.get("user_agent",""); proxy=self._config.proxy_url or None;
    async with httpx.AsyncClient(cookies=cookies, follow_redirects=True, proxy=proxy) as hc: ...).
    403/429 raise pattern (:366-370) raises SessionExpiredError. WR-02 NotImplementedError at :304-318 — DO NOT TOUCH.
  Offset formula (CONTEXT): offset = (page-1)*30; page 1 == oa0 (valid).
brave/config/settings.py: TripAdvisorConfig.session_ttl Field (:256-262), env_prefix="BRAVE_TA_" (:286). CR-02: NO Field(alias=...).
tests/fakes/fake_tripadvisor.py: fetch_attractions_paginated (added in 15-02) for non-network tests.
tests/unit/lanes/tripadvisor/test_client.py: existing respx mock pattern + the test asserting fetch_attractions raises on max_pages>1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _extract_sections_from_html (test-first against the fixture)</name>
  <read_first>
    - tests/fixtures/tripadvisor/attractions_oa30.html (the real captured page — inspect the actual <script> marker + JSON nesting)
    - brave/lanes/tripadvisor/client.py (_parse_attractions_page :126-179 — the consumer + never-raises posture; the safe-extract at :376-380)
    - .planning/phases/15-...-li/15-PATTERNS.md (_extract_sections_from_html section + RESEARCH Pattern 2)
  </read_first>
  <behavior>
    - Given the real fixture HTML, the extractor returns a non-empty list whose items carry
      __typename == "WebPresentation_SingleFlexCardSection" (the shape _parse_attractions_page expects).
    - Feeding the extractor output to _parse_attractions_page yields ~30 normalized cards each with
      name, locationId, rating, review_count, category (LGPD aggregate only — no author/text).
    - Given malformed/empty HTML (no script island), the extractor returns [] and never raises.
  </behavior>
  <action>
    Add a `@staticmethod _extract_sections_from_html(html: str) -> list` to `TripAdvisorClient` using
    ONLY stdlib `re` + `json` (NO lxml/beautifulsoup4/selectolax/playwright — RESEARCH Don't-Hand-Roll;
    test_client.py asserts no scraper dep). Strategy: iterate `<script>` blobs, find the one containing
    "WebPresentation_SingleFlexCardSection", `json.loads` the JSON island (strip any leading assignment +
    trailing `;`), then recursively walk dict/list to locate the list whose elements are dicts carrying
    that `__typename` (a small `_find_flexcard_sections` recursive helper). Do NOT hardcode the GraphQL
    envelope path `data[0]["data"]["Result"][0]["sections"]` — the SSR embedding path may differ. Return
    `[]` on any miss; never raise. Pin the exact `<script>` marker against the fixture you read.
  </action>
  <acceptance_criteria>
    - test_pagination.py loads tests/fixtures/tripadvisor/attractions_oa30.html, calls the extractor, and asserts len(sections) >= 25.
    - The extractor output passed to _parse_attractions_page produces >= 25 cards each with non-empty name + locationId + numeric rating/review_count.
    - Empty/garbage HTML input returns [] (no exception).
    - `grep -Eiq 'lxml|beautifulsoup|selectolax|playwright' brave/lanes/tripadvisor/client.py` returns NO match.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_pagination.py -k extract -x -q</automated>
  </verify>
  <done>Extractor recovers the real FlexCard sections offline and feeds the existing parser; never raises; no DOM parser added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fetch_attractions_paginated async generator + page throttle config</name>
  <read_first>
    - brave/lanes/tripadvisor/client.py (fetch_attractions :266-387 cookie/proxy/UA/httpx wiring + 403/429 raise; WR-02 :304-318 — keep intact)
    - brave/config/settings.py (TripAdvisorConfig.session_ttl Field :256-262; env_prefix :286)
    - tests/unit/lanes/tripadvisor/test_client.py (respx mock pattern; the max_pages>1 NotImplementedError test that must stay green)
  </read_first>
  <behavior>
    - For start_page..start_page+max_pages, GETs _TA_HTML_URL.format(geo_id, offset=(page-1)*30) with the
      session cookie jar + UA + proxy (copied from fetch_attractions), extracts + parses, yields (offset, cards).
    - Sleeps page_throttle_seconds between pages (asyncio.sleep), not after the last page.
    - A 403 or 429 response raises SessionExpiredError immediately (no retry, no further pages).
    - Logs per page carry ONLY offset/page/card-count/error-class — never cookies, user_agent, session_id, proxy_url.
  </behavior>
  <action>
    Add `page_throttle_seconds: float = Field(default=2.0, description="...BRAVE_TA_PAGE_THROTTLE_SECONDS...")`
    to `TripAdvisorConfig` (CR-02: NO alias; update the class docstring env-var list). Add module constant
    `_TA_HTML_URL` beside `_TA_GRAPHQL_URL`. Implement `fetch_attractions_paginated(self, geo_id, start_page=1,
    max_pages=334)` as an async generator: read the session via `self._get_session()`, build cookies/UA/proxy
    exactly as `fetch_attractions` does, loop pages computing `offset=(page-1)*30`, GET the HTML URL inside
    `async with httpx.AsyncClient(cookies=..., follow_redirects=True, proxy=...)`, raise `SessionExpiredError`
    on status 403/429 then `resp.raise_for_status()`, call `self._extract_sections_from_html(resp.text)` →
    `self._parse_attractions_page(sections)`, `yield offset, cards`, then `await asyncio.sleep(throttle)` when
    throttle>0 and not the last page. The URL is built from a fixed template + int geo_id + computed int offset
    (no user-controlled host — SSRF-safe). Do NOT modify `fetch_attractions`.
  </action>
  <acceptance_criteria>
    - `grep -q 'page_throttle_seconds' brave/config/settings.py` and `! grep -q 'alias=' ` for that field (CR-02).
    - respx test: a 2-page run yields offsets [0, 30] (or [30, 60] for start_page=2) with ~30 cards each, GETting the -oa{N}- HTML URLs.
    - respx test: a 403 (or 429) response raises SessionExpiredError and stops (no further GET).
    - Existing test_client.py test asserting fetch_attractions(max_pages=2) raises NotImplementedError STILL passes.
    - No structlog call in the new method references cookies/user_agent/session_id/proxy (source grep).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_pagination.py tests/unit/lanes/tripadvisor/test_client.py -k "paginated or max_pages or throttle or contract" -x -q</automated>
  </verify>
  <done>Paginated HTML transport yields (offset, cards) with throttle + 403/429 fail-fast; single-page contract intact; no secret logging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collector → TripAdvisor (HTML) | Outbound scrape with the operator cookie jar; anti-bot may 403; secrets must not leak to logs. |
| page URL construction | Offset/geoId feed a URL template — must not be injectable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Information Disclosure | per-page structlog calls | mitigate | Log only offset/page/count/`type(exc).__name__`; never cookies/user_agent/session_id/proxy_url (T-11-01-01/02, T-13-01-01) — asserted by source grep. |
| T-15-04-02 | Tampering / SSRF | _TA_HTML_URL builder | mitigate | URL = fixed template + int geo_id + computed int offset = (page-1)*30; no user-controlled host or path injection. |
| T-15-04-03 | Information Disclosure | LGPD review fields | mitigate | Extractor feeds the UNCHANGED _parse_attractions_page, which reads only aggregate bubbleRating/reviewCount; no author/text recovered. |
| T-15-04-04 | Denial of Service | DataDome over sequential GETs | mitigate | Configurable BRAVE_TA_PAGE_THROTTLE_SECONDS throttle + 403/429 fail-fast (no retry storm). |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH: no new deps; scraper/playwright must stay absent). |
</threat_model>

<verification>
Run the `<verify>` commands with `RUN_REAL_EXTERNALS` UNSET. Extractor + paginated-fetch + single-page-contract tests green.
</verification>

<success_criteria>
Real HTML-SSR pagination transport works offline against the fixture and respx mocks; slice and full run share this
one parameterized code path; single-page contract and security/LGPD posture preserved.
</success_criteria>

<output>
Create `.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-04-SUMMARY.md` when done
</output>
