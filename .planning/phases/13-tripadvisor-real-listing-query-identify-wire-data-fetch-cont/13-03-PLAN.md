---
phase: "13"
plan: "03"
type: execute
wave: 3
depends_on:
  - "13-01"
  - "13-02"
files_modified:
  - data/tripadvisor/README
  - data/tripadvisor/RUNBOOK-NIVEL3.md
autonomous: false
requirements:
  - TA-12

must_haves:
  truths:
    - "RUNBOOK-NIVEL3.md instructs operators to capture on the Attractions listing page and pick the POST whose Response has WebPresentation_SingleFlexCardSection"
    - "README OPERATOR GATE section reflects correct capture URL pattern, qid a5cb7fa004b5e4b5 context, and session_id / TASID guidance"
    - "Level 3 acceptance criteria (Nascente records > 0 after real sweep) is defined and runnable by an operator"
  artifacts:
    - path: "data/tripadvisor/RUNBOOK-NIVEL3.md"
      provides: "Updated Level 3 capture + inject + verify steps"
      contains: "AttractionsFusion"
    - path: "data/tripadvisor/README"
      provides: "Updated OPERATOR GATE section with Attractions page URL and TASID note"
      contains: "TASID"
  key_links:
    - from: "RUNBOOK-NIVEL3.md Passo 1"
      to: "https://www.tripadvisor.com/Attractions-g<geoId>-Activities-..."
      via: "operator navigates attractions listing, not tourism/state page"
      pattern: "Attractions-g"
---

<objective>
Update the TA-09 operator runbook and README to reflect the Phase 13 wiring: capture on the Attractions listing page, filter for the POST whose Response contains WebPresentation_SingleFlexCardSection, and note the TASID / session_id requirement. Add Level 3 acceptance criteria (Nascente > 0) as the final verify step.

Purpose: Operators following the Phase 12 runbook will navigate to a state tourism page — that captures telemetry/ad qids, not the listing query. They must now navigate to the Attractions listing page.
Output: Updated README OPERATOR GATE + updated RUNBOOK-NIVEL3.md Passos 1–5 + Level 3 acceptance block.
</objective>

<execution_context>
@/Users/leandro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leandro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont/GAP-12-A-FINDINGS.md
@.planning/phases/13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont/13-CONTEXT.md
@data/tripadvisor/README
@data/tripadvisor/RUNBOOK-NIVEL3.md

<interfaces>
<!-- Key facts for runbook updates. -->

Phase 13 capture URL pattern (from GAP-12-A-FINDINGS.md):
  https://www.tripadvisor.com/Attractions-g<geoId>-Activities-...
  Example (Brazil national geoId 294280):
  https://www.tripadvisor.com/Attractions-g294280-Activities-Brazil.html
  Example (Minas Gerais geoId 303380):
  https://www.tripadvisor.com/Attractions-g303380-Activities-Minas_Gerais_State_Brazil.html

How to identify the listing POST:
  - In Network tab, filter "graphql/ids"
  - Right-click a POST request → Preview/Response tab
  - The CORRECT request's response contains "WebPresentation_SingleFlexCardSection"
  - Telemetry/ad/trips requests return user_navigated, page_viewed, GetPageSlotSettings, Trips_ReferenceInput — skip these

TASID session_id note:
  - ta_bootstrap automatically extracts session_id from the TASID cookie
  - If TASID is absent from the captured cookies, ta_bootstrap prints "NOT FOUND" warning
  - Operator must be logged in for TASID to be present

Level 3 acceptance (RUNBOOK Passo 5 / CRITÉRIOS DE ACEITE):
  Nascente records > 0 after a real sweep with the corrected client:
    docker compose exec -T postgres psql -U brave -d norteia_brave -c \
      "select source, entity_type, count(*) from nascente where source='tripadvisor' group by source, entity_type;"
  Expected: at least one row with entity_type='attraction' and count > 0.

Current Passo 1 tells operator to navigate to:
  https://www.tripadvisor.com/Tourism-g303380-Minas_Gerais_State.html  ← WRONG for attractions listing
  Must change to the Attractions-g... URL pattern.

Current CRITÉRIOS DE ACEITE item 3: "sweep 1 UF ingere Nascente > 0" — keep but add entity_type='attraction' specificity.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update RUNBOOK-NIVEL3.md — correct Passo 1 URL and add Phase 13 guidance</name>
  <files>data/tripadvisor/RUNBOOK-NIVEL3.md</files>
  <read_first>
    - data/tripadvisor/RUNBOOK-NIVEL3.md (full file)
    - .planning/phases/13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont/GAP-12-A-FINDINGS.md (live-validated URL pattern + SingleFlexCardSection indicator)
  </read_first>
  <action>
    Rewrite RUNBOOK-NIVEL3.md with these specific changes (keep all other content intact):

    1. Update the header comment to reference Phase 13 (data-fetch contract) in addition to Phase 12 (session-injection seam). Keep the "NÃO roda em CI" note.

    2. Passo 1 — change the capture URL from the Tourism page to the Attractions listing page:
       - Remove: "https://www.tripadvisor.com/Tourism-g303380-Minas_Gerais_State.html"
       - Replace with two concrete examples:
           https://www.tripadvisor.com/Attractions-g294280-Activities-Brazil.html        (geoId nacional 294280)
           https://www.tripadvisor.com/Attractions-g303380-Activities-Minas_Gerais_State_Brazil.html  (MG 303380)
       - Add identification instructions: "No Network filter, procure os POSTs para graphql/ids. O POST CORRETO tem no Response 'WebPresentation_SingleFlexCardSection'. Ignore requests com user_navigated, GetPageSlotSettings, Trips_ReferenceInput — são telemetria/anúncios."
       - Add TASID note: "Certifique-se de estar logado no TripAdvisor para que o cookie TASID apareça. O ta_bootstrap extrai o TASID automaticamente; se não encontrar, avisa 'NOT FOUND'."

    3. Passo 2 — update the "Esperado" output to include session_id:
       - Add: "session_id: found  ← TASID capturado"
       - If session_id shows "NOT FOUND", instruct: "→ recapture estando logado no TripAdvisor"

    4. CRITÉRIOS DE ACEITE item 3: refine from "sweep 1 UF ingere Nascente > 0" to:
       "sweep 1 UF ingere Nascente > 0 com entity_type='attraction' — verificar com:
         docker compose exec -T postgres psql -U brave -d norteia_brave -c \
           \"select source, entity_type, count(*) from nascente where source='tripadvisor' group by source, entity_type;\""

    5. TROUBLESHOOTING — add two new entries:
       - "ta_bootstrap avisa 'qid is a telemetry/ad/trips query' → capturou o POST errado; use a página Attractions-g<geoId> e filtre por SingleFlexCardSection"
       - "ta_bootstrap session_id: NOT FOUND → cookie TASID ausente; fazer login no TripAdvisor antes de capturar"
  </action>
  <verify>
    <automated>grep -c "SingleFlexCardSection" data/tripadvisor/RUNBOOK-NIVEL3.md</automated>
  </verify>
  <done>
    - RUNBOOK-NIVEL3.md contains "Attractions-g" URL pattern (not Tourism page only)
    - RUNBOOK-NIVEL3.md contains "SingleFlexCardSection" identification instruction
    - RUNBOOK-NIVEL3.md contains "TASID" note in Passo 1
    - RUNBOOK-NIVEL3.md CRITÉRIOS item 3 includes entity_type='attraction' check
    - TROUBLESHOOTING covers wrong-qid and missing-TASID cases
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README OPERATOR GATE section with Phase 13 corrections</name>
  <files>data/tripadvisor/README</files>
  <read_first>
    - data/tripadvisor/README (full file — OPERATOR GATE section)
    - .planning/phases/13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont/13-CONTEXT.md (Runbook decision)
  </read_first>
  <action>
    Update data/tripadvisor/README OPERATOR GATE section with these specific changes:

    1. Step 4 — change the capture navigation URL:
       - Remove: "https://www.tripadvisor.com/Tourism-g303380-Minas_Gerais_State.html"
       - Replace with: "Navigate to the Attractions listing page for a Brazilian state, for example:
           https://www.tripadvisor.com/Attractions-g303380-Activities-Minas_Gerais_State_Brazil.html
           Use the geoId from data/tripadvisor/uf_geoids.json for the target UF."

    2. Step 5 — add POST identification guidance:
       - After "In the Network tab, filter for 'graphql/ids'."
       - Add: "Right-click a POST request → Preview or Response tab. The correct listing request's response contains 'WebPresentation_SingleFlexCardSection' and 'listType': 'POI'. Telemetry/ad/trips requests contain user_navigated, GetPageSlotSettings, or Trips_ReferenceInput — skip these. Copy as cURL (bash) from the listing POST only."
       - Add: "Ensure you are logged in to TripAdvisor before capturing — the TASID session cookie is required by the listing query variables. ta_bootstrap will warn 'session_id: NOT FOUND' if TASID is absent."

    3. In step 8 (ta_bootstrap output), add expected session_id line:
       "session_id: found"
       And note: "If session_id shows NOT FOUND: re-capture while logged in to TripAdvisor."

    4. In the CURRENT FILES section, update the description to mention AttractionsFusion:
       Under uf_geoids.json description, add: "Also used as routeParameters.geoId in the AttractionsFusion listing query (qid a5cb7fa004b5e4b5, Phase 13)."

    Keep all other README sections (SOURCE, LEGAL RISK, MITIGATIONS, LGPD BASIS, SESSION LIFETIME) unchanged.
  </action>
  <verify>
    <automated>grep -c "Attractions-g" data/tripadvisor/README</automated>
  </verify>
  <done>
    - README OPERATOR GATE step 4 references Attractions-g URL pattern
    - README step 5 includes SingleFlexCardSection identification guidance
    - README step 8 includes session_id expected output
    - All LEGAL RISK / LGPD sections unchanged
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 13-01 and 13-02 rewired fetch_attractions to qid a5cb7fa004b5e4b5 with the real AttractionsFusion variables shape. Plan 13-03 updated the runbook. This checkpoint runs a live Level 3 sweep to confirm Nascente records > 0 — the actual acceptance criterion for this phase.
  </what-built>
  <how-to-verify>
    Run the Level 3 sweep following RUNBOOK-NIVEL3.md:

    1. Capture a session from the Attractions listing page (logged in, Attractions-g URL, pick the POST with SingleFlexCardSection in Response).

    2. Inject:
         python scripts/ta_bootstrap.py --curl /tmp/ta.curl --endpoint http://localhost:8000
       Verify output includes: "session_id: found" and "canary result: ready"

    3. Run a 1-UF sweep:
         curl -s -X POST http://localhost:8000/api/v1/engine/start \
           -H "X-Steward-Secret: dev-steward-secret" -H "Content-Type: application/json" \
           -d '{"source":"tripadvisor","depth":"nascente_rio","ufs":["MG"],"lane":"atrativos"}' | jq

    4. Check Nascente:
         docker compose exec -T postgres psql -U brave -d norteia_brave -c \
           "select source, entity_type, count(*) from nascente where source='tripadvisor' group by source, entity_type;"
       Expected: at least one row with entity_type='attraction' and count > 0.

    If count = 0: check worker logs for errors. Common causes:
      - Worker missing RUN_REAL_EXTERNALS=1 (uses NullClient)
      - canary 422 mid-run (session expired) — re-inject and retry
      - All cards quarantined (IBGE / parent_destino_absent) — check quarantine table for reason
  </how-to-verify>
  <resume-signal>Type "approved" with the Nascente count observed, or describe the failure mode.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → capture | Attractions listing page contains live DataDome session cookies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Information Disclosure | TASID in runbook example output | mitigate | runbook never shows real cookie values; examples use placeholder "found" |
| T-13-03-SC | Tampering | npm/pip/cargo installs | accept | no new package dependencies in this plan |
</threat_model>

<verification>
File content checks:
  grep -c "SingleFlexCardSection" data/tripadvisor/RUNBOOK-NIVEL3.md   # >= 1
  grep -c "Attractions-g" data/tripadvisor/RUNBOOK-NIVEL3.md   # >= 1
  grep -c "TASID" data/tripadvisor/RUNBOOK-NIVEL3.md   # >= 1
  grep -c "Attractions-g" data/tripadvisor/README   # >= 1
  grep -c "session_id" data/tripadvisor/README   # >= 1

Level 3 acceptance (human-gate — see checkpoint task):
  Nascente count with entity_type='attraction' > 0 after a real 1-UF sweep.
</verification>

<success_criteria>
- README and RUNBOOK-NIVEL3.md both reference the Attractions-g URL pattern for capture
- SingleFlexCardSection identification guidance is present in Passo 1 (RUNBOOK) and Step 5 (README)
- TASID / session_id requirement is documented with missing-TASID troubleshooting
- Level 3 human checkpoint: Nascente records > 0 with entity_type='attraction' confirmed by operator
</success_criteria>

<output>
Create .planning/phases/13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont/13-03-SUMMARY.md when done
</output>
