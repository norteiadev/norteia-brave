---
phase: 02-destinos-lane
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - brave/lanes/destinos/__init__.py
  - brave/lanes/destinos/schemas.py
  - brave/clients/mtur.py
  - brave/clients/null_mtur.py
  - brave/clients/notebooklm.py
  - brave/clients/null_notebooklm.py
  - tests/fakes/fake_mtur.py
  - tests/fakes/fake_notebooklm.py
  - data/mtur/municipios_mtur_2024.csv
autonomous: true
requirements: [DEST-01, DEST-02, DEST-03]
must_haves:
  truths:
    - "Implements CONTEXT.md decisions D-01: (Mtur bundled static seed dataset) and D-02: (NotebookLM ingest-all, dedup-driven overlap) — see 02-CONTEXT.md"
    - "MturClient.fetch_municipalities('BA') returns a list of dicts with ibge_code, name, categoria (Oferta Principal / Complementar / Apoio), uf"
    - "NotebookLMClient.fetch_report handles missing reports gracefully (returns {})"
    - "FakeMturClient and FakeNotebookLMClient satisfy their Protocol types at static-type check time"
    - "DesmembramentoResult and DestinoItem validate correctly with Pydantic v2"
  artifacts:
    - path: "brave/lanes/destinos/schemas.py"
      provides: "DesmembramentoResult + DestinoItem Pydantic v2 models"
      contains: "DesmembramentoResult"
    - path: "brave/clients/mtur.py"
      provides: "MturClient reading bundled CSV"
      contains: "MturClient"
    - path: "brave/clients/notebooklm.py"
      provides: "NotebookLMClient reading local JSON reports"
      contains: "NotebookLMClient"
    - path: "tests/fakes/fake_mtur.py"
      provides: "FakeMturClient with call recording"
      contains: "FakeMturClient"
    - path: "tests/fakes/fake_notebooklm.py"
      provides: "FakeNotebookLMClient keyed by municipio"
      contains: "FakeNotebookLMClient"
    - path: "data/mtur/municipios_mtur_2024.csv"
      provides: "Bundled Mtur seed dataset with ≥1 BA municipality"
  key_links:
    - from: "brave/clients/mtur.py"
      to: "data/mtur/municipios_mtur_2024.csv"
      via: "_load_csv() reading DATA_PATH glob"
      pattern: "_load_csv|DATA_PATH"
    - from: "brave/clients/mtur.py"
      to: "brave/clients/base.py"
      via: "_check_protocol_compliance MturClientProtocol"
      pattern: "_check_protocol_compliance"
---

<objective>
Build the data contracts (DesmembramentoResult schema), real client implementations (MturClient, NotebookLMClient), null/offline stubs, fake test doubles, and the bundled Mtur seed dataset. This is the interface-first wave — all producer code in Plans 05, 07, 08 consumes these seams.

Purpose: The D-18 boundary requires producers (brave/lanes/) to consume clients via Protocol seams. All three producers depend on these contracts. Building them in Wave 1 before the lanes ensures executors receive concrete contracts, not codebase exploration tasks.

Output: DesmembramentoResult schema; MturClient + NullMturClient; NotebookLMClient + NullNotebookLMClient; FakeMturClient + FakeNotebookLMClient; bundled Mtur seed CSV; lane package marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-destinos-lane/02-CONTEXT.md
@.planning/phases/02-destinos-lane/02-RESEARCH.md
@.planning/phases/02-destinos-lane/02-PATTERNS.md

<interfaces>
<!-- MturClientProtocol (brave/clients/base.py) -->
```python
class MturClientProtocol(Protocol):
    async def fetch_municipalities(self, uf: str) -> list[dict[str, Any]]: ...
```

<!-- NotebookLMClientProtocol (brave/clients/base.py) -->
```python
class NotebookLMClientProtocol(Protocol):
    async def fetch_report(self, municipio: str) -> dict[str, Any]: ...
```

<!-- NullNorteiaApiClient pattern (brave/clients/null_norteia_api.py) -->
```python
class NullNorteiaApiClient:
    """No-op client — used when run_real_externals=False."""
    async def push_destination(self, payload: dict) -> dict:
        return {"id": "null", "source_ref": payload.get("source_ref", "")}
```

<!-- FakePlacesClient pattern (tests/fakes/fake_places.py) — exact structure to mirror -->
```python
class FakePlacesClient:
    def __init__(self, fixtures: list[dict] | None = None) -> None:
        self._fixtures = fixtures or [...]
        self.calls: list[dict] = []
    async def text_search(self, ...) -> list[dict]:
        self.calls.append(...)
        return [...]
def _check_protocol_compliance() -> None:
    _client: PlacesClientProtocol = FakePlacesClient()  # noqa: F841
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DesmembramentoResult schema + Mtur seed CSV + lane package marker</name>
  <files>
    brave/lanes/destinos/__init__.py,
    brave/lanes/destinos/schemas.py,
    data/mtur/municipios_mtur_2024.csv
  </files>

  <read_first>
    - brave/core/score/schemas.py — Pydantic v2 BaseModel with Field pattern to mirror
    - .planning/phases/02-destinos-lane/02-RESEARCH.md §"DesmembramentoAgent Design" §"DesmembramentoResult Schema" — exact field set and validators
    - .planning/phases/02-destinos-lane/02-RESEARCH.md §"Mtur Dataset" — expected CSV columns, 2025 nomenclature change
    - brave/lanes/__init__.py — empty package marker pattern
    - brave/lanes/base.py — LaneProtocol.produce(uf) signature
  </read_first>

  <behavior>
    - DestinoItem validates: nome min_length=2, tipo in Literal set, posicionamento min_length=5
    - DesmembramentoResult validates: municipio_ibge matches r"^\d{7}$", destinos is a list
    - DesmembramentoResult(municipio_ibge="2927408", municipio_nome="Porto Seguro", destinos=[DestinoItem(nome="Trancoso", tipo="vila", posicionamento="Vila historica")]) succeeds
    - DesmembramentoResult(municipio_ibge="WRONG", ...) raises ValidationError
    - data/mtur/municipios_mtur_2024.csv contains at least 5 BA municipalities including Porto Seguro (IBGE 2927408) with categoria values from both old (A/B/C/D/E) and new nomenclature
  </behavior>

  <action>
    Create brave/lanes/destinos/__init__.py as an empty package marker (no imports).

    Create brave/lanes/destinos/schemas.py with:
    - DestinoItem(BaseModel): nome str Field(min_length=2), tipo Literal["distrito","praia","vila","localidade","ilha","balneario","outros"], posicionamento str Field(min_length=5)
    - DesmembramentoResult(BaseModel): municipio_ibge str Field(pattern=r"^\d{7}$"), municipio_nome str, destinos list[DestinoItem] Field(default_factory=list)
    - Module docstring citing §7.4 of PLANO-BRAVE.md and D-03

    Create data/mtur/municipios_mtur_2024.csv as a synthetic fixture seed file that satisfies the parser. Use realistic column names from RESEARCH.md (co_municipio, no_municipio, sg_uf, categoria, no_regiao_turistica). Include at least 10 rows spanning BA, RJ, SP with both old category names (A, B, C, D, E) and new names ("Municípios turísticos", "com oferta turística complementar", "de apoio ao turismo") to exercise _map_categoria both paths. Include Porto Seguro (2927408, BA, A), Trancoso is inside Porto Seguro (sub-destino, not a separate municipality row).

    Do NOT put real PII or private data in the CSV — all data is public Brazilian government open data (Mtur is published under open data license). Add a SHA-256 placeholder file data/mtur/municipios_mtur_2024.csv.sha256 with the text "PLACEHOLDER — replace with real SHA-256 after downloading official Mtur dataset".
  </action>

  <verify>
    <automated>cd /Users/leandro/Projects/norteia/norteia-brave && python -c "from brave.lanes.destinos.schemas import DesmembramentoResult, DestinoItem; r=DesmembramentoResult(municipio_ibge='2927408',municipio_nome='Porto Seguro',destinos=[DestinoItem(nome='Trancoso',tipo='vila',posicionamento='Vila historica')]); print('schema OK:', r.municipio_ibge)"</automated>
  </verify>

  <done>
    - brave/lanes/destinos/__init__.py exists (empty)
    - DesmembramentoResult and DestinoItem import and validate correctly
    - data/mtur/municipios_mtur_2024.csv has header + >=10 data rows covering BA/RJ/SP with both old (A-E) and new category names
    - data/mtur/municipios_mtur_2024.csv.sha256 placeholder exists
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MturClient + NullMturClient + NotebookLMClient + NullNotebookLMClient + fakes</name>
  <files>
    brave/clients/mtur.py,
    brave/clients/null_mtur.py,
    brave/clients/notebooklm.py,
    brave/clients/null_notebooklm.py,
    tests/fakes/fake_mtur.py,
    tests/fakes/fake_notebooklm.py
  </files>

  <read_first>
    - brave/clients/base.py — MturClientProtocol and NotebookLMClientProtocol exact signatures
    - brave/clients/norteia_api.py — real client module pattern (docstring, header, _check_protocol_compliance)
    - brave/clients/null_norteia_api.py — NullNorteiaApiClient no-op pattern (lines 20-27)
    - tests/fakes/fake_places.py — FakePlacesClient exact module structure to mirror
    - tests/fakes/fake_norteia_api.py lines 80-82 — _check_protocol_compliance structural typing pattern
    - .planning/phases/02-destinos-lane/02-PATTERNS.md §"brave/clients/mtur.py" — full MturClient implementation with _load_csv + _map_categoria
    - .planning/phases/02-destinos-lane/02-PATTERNS.md §"brave/clients/notebooklm.py" — NotebookLMClient local-file pattern
    - .planning/phases/02-destinos-lane/02-PATTERNS.md §"tests/fakes/fake_mtur.py" — FakeMturClient exact code
    - .planning/phases/02-destinos-lane/02-PATTERNS.md §"tests/fakes/fake_notebooklm.py" — FakeNotebookLMClient exact code
  </read_first>

  <behavior>
    - MturClient().fetch_municipalities("BA") with the seed CSV returns at least 1 record with ibge_code, name, categoria in {"Oferta Principal","Complementar","Apoio"}, uf="BA"
    - MturClient handles BOTH old category names (A, B, C, D, E) AND new nomenclature ("turísticos", "complementar", "apoio") via _map_categoria
    - MturClient raises FileNotFoundError with clear message when no CSV exists under data/mtur/
    - NullMturClient.fetch_municipalities(uf) returns [] always (offline stub)
    - NotebookLMClient.fetch_report("Porto Seguro:BA:2927408") returns {} when no report file exists
    - NullNotebookLMClient.fetch_report returns {} always
    - FakeMturClient.calls records each uf string passed to fetch_municipalities
    - FakeNotebookLMClient.calls records each municipio string passed to fetch_report
    - All four client classes satisfy their Protocol via _check_protocol_compliance static type annotation
  </behavior>

  <action>
    Create brave/clients/mtur.py: module docstring citing MturClientProtocol and D-01. Implement DATA_PATH = pathlib.Path(__file__).parent.parent.parent / "data" / "mtur". Implement _load_csv() that globs DATA_PATH for "municipios_mtur_*.csv", picks the latest (sorted descending), reads with csv.DictReader(encoding="utf-8-sig"). Implement _map_categoria(raw: str) -> str with old (A/B/C/D/E) and new nomenclature mappings per PATTERNS.md. Implement MturClient with async fetch_municipalities(uf). Add _check_protocol_compliance() at module bottom.

    Create brave/clients/null_mtur.py: NullMturClient with fetch_municipalities returning [] and _check_protocol_compliance.

    Create brave/clients/notebooklm.py: module docstring. Implement REPORTS_PATH = pathlib.Path(__file__).parent.parent.parent / "data" / "notebooklm". fetch_report(municipio: str) parses municipio as "nome:uf:ibge" or plain name; tries to read data/notebooklm/{uf}/{ibge}.json if parseable; returns {} on any OSError. Add _check_protocol_compliance.

    Create brave/clients/null_notebooklm.py: NullNotebookLMClient with fetch_report returning {} and _check_protocol_compliance.

    Create tests/fakes/fake_mtur.py and tests/fakes/fake_notebooklm.py exactly as shown in PATTERNS.md §fakes sections. Include calls list, configurable fixtures/reports, and _check_protocol_compliance.
  </action>

  <verify>
    <automated>cd /Users/leandro/Projects/norteia/norteia-brave && python -c "
import asyncio
from brave.clients.mtur import MturClient
from tests.fakes.fake_mtur import FakeMturClient
from tests.fakes.fake_notebooklm import FakeNotebookLMClient
client = MturClient()
results = asyncio.run(client.fetch_municipalities('BA'))
assert len(results) >= 1, f'Expected >=1 BA municipalities, got {len(results)}'
assert all(r['categoria'] in ('Oferta Principal','Complementar','Apoio') for r in results)
fake = FakeMturClient()
r2 = asyncio.run(fake.fetch_municipalities('BA'))
assert fake.calls == ['BA']
print('All client checks OK, BA records:', len(results))
"</automated>
  </verify>

  <done>
    - MturClient reads the seed CSV and returns >=1 BA record with correct categoria mapping
    - _check_protocol_compliance passes static typing at module import (no TypeError)
    - NullMturClient.fetch_municipalities returns []
    - NotebookLMClient.fetch_report returns {} for missing reports
    - FakeMturClient and FakeNotebookLMClient record calls correctly
    - All six client files exist with _check_protocol_compliance
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MturClient → data/mtur/ CSV | File I/O from repo data dir; no network, no user input |
| NotebookLMClient → data/notebooklm/ JSON | File I/O from repo data dir; no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | data/mtur CSV seed file | accept | File is bundled in repo, content-version tracked by filename year. In production, replace with real download + SHA-256 verification. Seed CSV contains only public Mtur data (no PII). |
| T-02-03-02 | Information Disclosure | data/notebooklm JSON reports | accept | Reports are structured NotebookLM outputs derived from public municipal tourism data. No PII. Files are local to the repo. |
| T-02-03-03 | Denial of Service | MturClient FileNotFoundError on missing CSV | mitigate | _load_csv raises FileNotFoundError with clear message; producer catches and logs; does not crash the worker. |
| T-02-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages. slopcheck not required. |
</threat_model>

<verification>
```bash
cd /Users/leandro/Projects/norteia/norteia-brave
python -c "
import asyncio
from brave.clients.mtur import MturClient
results = asyncio.run(MturClient().fetch_municipalities('BA'))
print(f'BA municipalities: {len(results)}')
for r in results[:3]:
    print(r)
"
python -m pytest tests/fakes/ -v 2>/dev/null || python -c "print('no fakes tests yet -- OK')"
python -c "from brave.clients.mtur import _check_protocol_compliance; _check_protocol_compliance(); print('Mtur protocol OK')"
python -c "from brave.clients.notebooklm import _check_protocol_compliance; _check_protocol_compliance(); print('NotebookLM protocol OK')"
```
</verification>

<success_criteria>
- DesmembramentoResult schema validates correctly with all valid/invalid inputs
- MturClient reads seed CSV, maps categories correctly (A/B → Oferta Principal, new names → same)
- All six client files have _check_protocol_compliance structural type guards
- FakeMturClient and FakeNotebookLMClient record calls and return configurable fixtures
- data/mtur/municipios_mtur_2024.csv has realistic test data for BA/RJ/SP
- No real network calls; no new pip packages required
</success_criteria>

<output>
Create `.planning/phases/02-destinos-lane/02-03-SUMMARY.md` when done
</output>
