---
phase: 17-painel-brave-redesign-light-theme-single-shell-painel-kanban
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/app/globals.css
  - dashboard/app/painel/page.tsx
  - dashboard/components/painel/nav.ts
  - dashboard/components/painel/PainelShell.tsx
  - dashboard/components/painel/PainelSidebar.tsx
  - dashboard/components/painel/PainelTopbar.tsx
  - dashboard/components/painel/PainelView.tsx
  - dashboard/components/painel/__tests__/PainelShell.test.tsx
  - dashboard/components/painel/__tests__/PainelTopbar.test.tsx
autonomous: true
requirements: [UI-PAINEL-1]
# NOTE: REQUIREMENTS.md has no entry for this UI redesign slice. Per the phase
# planning brief, the synthetic id "UI-PAINEL-1" tracks the whole slice-1 scope.

must_haves:
  truths:
    - "Visiting /painel renders a light-theme shell (232px white sidebar + 58px topbar) with the painel view active"
    - "The 5 non-painel nav items render a centered 'Em breve' placeholder when selected"
    - "The topbar motor switch reflects fetchEngineStatus().state and toggling it calls startEngine()/stopEngine()"
    - "The topbar shows the TripAdvisor session pill from fetchTASessionStatus() and the current 'Origem {source}' read-only"
    - "The existing 10 dark routes and their Vitest suites are unchanged (globals.css :root/.dark untouched)"
  artifacts:
    - path: "dashboard/app/painel/page.tsx"
      provides: "Client SPA shell + local view-switcher at the new /painel route"
      contains: "painel-light"
    - path: "dashboard/components/painel/PainelTopbar.tsx"
      provides: "Topbar with TA pill, source label, motor on/off switch wired to engine-api"
      min_lines: 40
    - path: "dashboard/app/globals.css"
      provides: "Scoped .painel-light token block (append-only)"
      contains: ".painel-light"
  key_links:
    - from: "dashboard/components/painel/PainelTopbar.tsx"
      to: "lib/engine-api startEngine/stopEngine/fetchEngineStatus/fetchTASessionStatus"
      via: "TanStack useQuery + useMutation"
      pattern: "fetchEngineStatus|startEngine|stopEngine"
    - from: "dashboard/app/painel/page.tsx"
      to: "components/painel/PainelShell + PainelView"
      via: "import + render under .painel-light wrapper"
      pattern: "painel-light"
---

<objective>
Build the light-theme single-shell at the NEW route `/painel`, alongside the existing 10 dark routes (non-breaking): a 232px white sidebar (logo + 6 nav items in groups Processamento/Operação + operator footer), a 58px topbar (page title/subtitle + TripAdvisor session pill + read-only "Origem {source}" + motor on/off switch), and a local-state view-switcher. Only the `painel` view is wired (it renders a `PainelView` stub this plan; later plans fill it); the other five views render a centered "Em breve" placeholder.

Purpose: Establish the scoped light surface + shell chrome + real topbar wiring that every Painel view sits inside, without touching the global dark theme.
Output: `/painel` route, shell components, and a scoped `.painel-light` CSS token block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-painel-brave-redesign-light-theme-single-shell-painel-kanban/17-CONTEXT.md
@.planning/phases/17-painel-brave-redesign-light-theme-single-shell-painel-kanban/design/Painel-Brave.dc.html
@dashboard/components/engine/EngineControl.tsx
@dashboard/lib/engine-api.ts
@dashboard/app/globals.css
@dashboard/app/layout.tsx
@dashboard/mocks/handlers/engine.ts

<interfaces>
<!-- Reuse these directly — no codebase exploration needed. -->

From dashboard/lib/engine-api.ts:
- type EngineState = "idle" | "running" | "stopping";
- type EngineSource = "default" | "tripadvisor";
- const SOURCE_LABELS: Record<EngineSource, string>;  // default→"Padrão", tripadvisor→"TripAdvisor"
- const ENGINE_REFETCH_INTERVAL_MS = 10_000;
- const engineKeys = { status: ["engine","status"] };
- function fetchEngineStatus(): Promise<EngineStatus>;   // EngineStatus.state, .source, .counts
- function startEngine(body?): Promise<EngineActionResult>;
- function stopEngine(): Promise<EngineActionResult>;
- const taSessionKeys = { status: ["ta","session","status"] };
- function fetchTASessionStatus(): Promise<TASessionStatus>;  // {present, expires_in?, reason}

The layout sets `<html className="dark">` (next-themes defaultTheme="dark", enableSystem=false).
The painel subtree must re-declare its own light tokens via a `.painel-light` wrapper class.

MSW double-prefix rule (jsdom): browser → http://localhost:3000/api/api/v1/... . Reuse
mocks/handlers/engine.ts exports: engineStatus(overrides), engineStartSuccess(), engineStopSuccess(), taSessionStatus(overrides).

Test helper: renderWithClient(ui) from dashboard/components/cms/__tests__/test-utils.tsx
(sets operator token + fresh QueryClient with retries off).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the scoped .painel-light token block to globals.css</name>
  <read_first>dashboard/app/globals.css (read the full file; note the :root and .dark blocks and the @theme inline var mappings — these MUST NOT be edited)</read_first>
  <files>dashboard/app/globals.css</files>
  <action>APPEND-ONLY: add a new `.painel-light { ... }` rule at the END of globals.css. Do NOT modify the existing `:root`, `.dark`, `@theme inline`, or `@layer base` blocks — they back the 10 dark routes and their snapshots. The new class re-declares the shadcn CSS vars that reused components (StageBadge, Badge) consume, plus painel-only literal tokens, using the EXACT values from 17-CONTEXT.md decisions. Set: --background: oklch(0.98 0.01 90); --foreground: #18181b; --card: #fff; --card-foreground: #18181b; --popover: #fff; --popover-foreground: #18181b; --border: #e6e4e0; --input: #e6e4e0; --muted: #f0eee9; --muted-foreground: #6b7280; --primary: #15315e; --primary-foreground: #fff; --ring: #15315e; --status-mar: oklch(0.55 0.15 150); --status-dlq: oklch(0.72 0.15 75); --status-descarte: oklch(0.55 0.20 27). Also add painel-only literals: --painel-navy: #15315e; --painel-cream: oklch(0.98 0.01 90); --painel-border-outer: #e6e4e0; --painel-border-inner: #f0eee9; --painel-chip: #f0eee9; --painel-text: #18181b; --painel-muted: #6b7280; --painel-muted-2: #9ca3af; --painel-hint: #94a3b8. Finish the rule with `color: var(--painel-text); background: var(--painel-cream);`. Add a header comment explaining this is scoped to /painel and must never flip the global dark theme. Because `.painel-light` is a class on a descendant of `<html class="dark">`, its declarations win for that subtree only, so StageBadge's `bg-[var(--status-mar)]` etc. resolve to the light values automatically. Use CSS-var references (no hardcoded hex) inside the painel components — they reference these tokens.</action>
  <verify>
    <automated>cd dashboard && grep -q ".painel-light" app/globals.css && grep -cv '^\s*--' app/globals.css >/dev/null && bun run test components/dlq components/cms 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains a `.painel-light` rule with all tokens above; the original `:root` and `.dark` blocks are byte-for-byte unchanged.
    - A representative existing dark-route suite (e.g. components/dlq, components/cms) still passes — proving no token regression.
  </acceptance_criteria>
  <done>Scoped light tokens exist; existing dark suites green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sidebar + nav config + shell scaffold</name>
  <read_first>.planning/.../design/Painel-Brave.dc.html (lines 26-95: aside sidebar, nav groups, operator footer, main+topbar frame); dashboard/components/engine/EngineControl.tsx (state/label patterns)</read_first>
  <files>dashboard/components/painel/nav.ts, dashboard/components/painel/PainelSidebar.tsx, dashboard/components/painel/PainelShell.tsx, dashboard/components/painel/PainelView.tsx, dashboard/components/painel/__tests__/PainelShell.test.tsx</files>
  <behavior>
    - PainelShell renders the sidebar (6 nav buttons, 2 group headers, operator footer) + a topbar slot + a content slot.
    - Clicking a nav item invokes the onSelect callback with that view key; the active item is visually marked (aria-current or data-active).
    - PainelView (stub) renders a container with data-testid="painel-view" (later plans fill it).
  </behavior>
  <action>Create `nav.ts` exporting `type PainelViewKey = "painel"|"duplicados"|"mapeamento"|"varreduras"|"conversas"|"custo";` and a `NAV_GROUPS` constant: group "Processamento" → [painel (label "Painel (Kanban)"), duplicados (label "Duplicados"), mapeamento, varreduras]; group "Operação" → [conversas (label "Conversas WhatsApp"), custo (label "Custo & LLM")]. Each item: { key, label, group }. Create `PainelSidebar.tsx` ("use client") rendering a 232px (`w-58` ≈ use `w-[232px]`) white (`bg-[var(--card)]`) column: logo header (`<img src="/logo-norteia-brave.svg" alt="Norteia Brave" />` from public/), nav with uppercase group headers and a `<button>` per item (icon optional — reuse the inline SVGs from the design or a simple dot), an operator footer (round navy "OP" avatar using `bg-[var(--painel-navy)]`, "Operador Brave", "CMS Territorial"). Props: `{ active: PainelViewKey; onSelect: (k: PainelViewKey) => void }`. Active item gets `data-active="true"` + `aria-current="page"`. pt-BR copy exactly per the mockup. Create `PainelView.tsx` ("use client") as a STUB: `export function PainelView() { return <div data-testid="painel-view" className="grid place-items-center h-full text-[var(--painel-muted)]">Painel — carregando…</div>; }` (plan 17-05 replaces the body). Create `PainelShell.tsx` ("use client") composing `<aside><PainelSidebar/></aside>` + a `<main>` with a topbar slot prop and a children content slot; use design layout (flex, h-screen). All colors via the painel CSS vars — NO hardcoded hex in component className. Write `PainelShell.test.tsx`: render PainelShell with a stub topbar + content; assert the 6 nav labels render, the two group headers render, the operator footer "Operador Brave" renders, and clicking "Duplicados" calls onSelect("duplicados").</action>
  <verify>
    <automated>cd dashboard && bun run test components/painel/__tests__/PainelShell.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - PainelShell renders all 6 nav items + 2 group headers + operator footer.
    - onSelect fires with the correct view key on nav click; active item is marked.
    - PainelView stub renders data-testid="painel-view".
    - No hardcoded hex in painel component classNames (tokens only).
  </acceptance_criteria>
  <done>Sidebar + shell + nav config + view stub built and unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Topbar (TA pill + source label + motor switch) + /painel page with view-switcher</name>
  <read_first>dashboard/components/engine/EngineControl.tsx (the EXACT start/stop useMutation pattern, sessionLabel/sessionColor, ENGINE_REFETCH_INTERVAL_MS polling, explainError); dashboard/lib/engine-api.ts; dashboard/mocks/handlers/engine.ts; .planning/.../design/Painel-Brave.dc.html (lines 71-95: topbar order TA pill · Origem trigger · divider · motor label+switch)</read_first>
  <files>dashboard/components/painel/PainelTopbar.tsx, dashboard/app/painel/page.tsx, dashboard/components/painel/__tests__/PainelTopbar.test.tsx</files>
  <behavior>
    - Topbar polls fetchEngineStatus (10s) and renders the motor switch reflecting state: idle→off, running/stopping→on.
    - Toggling the switch when idle calls startEngine (confirm-before-start acceptable via window.confirm); when running/stopping calls stopEngine. Reuse EngineControl's mutation/onError(toast)/onSettled(invalidate engineKeys.status) pattern verbatim.
    - The TA session pill renders from fetchTASessionStatus using the sessionLabel/sessionColor logic ported from EngineControl; its click is a no-op placeholder this slice.
    - "Origem {source}" trigger renders the current source (SOURCE_LABELS[status.source ?? "default"]) READ-ONLY (no modal this slice — deferred per CONTEXT to keep slice tight).
    - Page title/subtitle are static per active view: painel → title "Painel", subtitle "Quadro de processamento"; others → title = nav label, subtitle "Em breve".
  </behavior>
  <action>Create `PainelTopbar.tsx` ("use client"). Props: `{ title: string; subtitle: string }`. Internally: `useQuery({ queryKey: engineKeys.status, queryFn: fetchEngineStatus, refetchInterval: ENGINE_REFETCH_INTERVAL_MS, refetchOnWindowFocus: false })`; `useQuery` for taSessionKeys.status/fetchTASessionStatus; two `useMutation`s (start, stop) copying EngineControl's pattern (onError → toast.error(explainError), onSettled → qc.invalidateQueries({queryKey: engineKeys.status})). Port `explainError`, `sessionLabel`, `sessionColor` (or import if exported — they are NOT exported, so re-declare them locally). Render right-aligned: TA pill (button, data-testid="painel-ta-pill", dot + label, color class), "Origem {SOURCE_LABELS[source]}" button (data-testid="painel-source", read-only), a vertical divider, motor label ("Motor" + state text) + a switch `<button role="switch" aria-checked={state!=="idle"} data-testid="painel-motor-switch" onClick={onToggleMotor}>`. `onToggleMotor`: if state==="idle" → `if (window.confirm("Ligar o motor de coleta?")) start.mutate()`; else stop.mutate(). All colors via painel CSS vars. Create `dashboard/app/painel/page.tsx` ("use client"): a default-export component holding `const [view, setView] = useState<PainelViewKey>("painel")`, deriving {title,subtitle} from view, and rendering `<div className="painel-light h-screen">` wrapping `<PainelShell active={view} onSelect={setView} topbar={<PainelTopbar title=.. subtitle=.. />}>{ view === "painel" ? <PainelView/> : <EmBreve label={navLabel}/> }</PainelShell>`. Add a small inline `EmBreve` placeholder (centered card "Em breve" + the view label). The wrapper `.painel-light` class is what scopes the light theme. Write `PainelTopbar.test.tsx` using renderWithClient + server.use(engineStatus(), taSessionStatus(), engineStartSuccess(), engineStopSuccess()): (a) idle status → switch aria-checked=false; (b) running status → aria-checked=true and toggling calls stop (mock window.confirm not needed for stop) — assert no throw + the stop handler reachable; (c) idle + window.confirm stubbed true + click switch → POST start fires (assert toast or invalidation via a follow-up status); (d) TA pill renders "Pronta" for present:true and "Precisa bootstrap" for {present:false, reason:"needs_bootstrap"}; (e) source label renders "Padrão" by default.</action>
  <verify>
    <automated>cd dashboard && bun run test components/painel/__tests__/PainelTopbar.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Motor switch reflects engine state and toggling fires start (idle, confirmed) / stop (running) through the BFF MSW handlers.
    - TA pill renders correct label/color for present and needs_bootstrap states.
    - Source label renders read-only from engine status.
    - /painel page mounts the shell under .painel-light and switches views locally; non-painel views show "Em breve".
  </acceptance_criteria>
  <done>Topbar wired to real engine-api; /painel route live with working view-switcher.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → BFF (/api/...) | Operator Bearer crosses here; service token injected server-side only |
| painel subtree → global dark theme | CSS scope boundary; light tokens must not leak to dark routes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Information Disclosure | PainelTopbar engine calls | mitigate | Use lib/engine-api (apiFetch/bff) only; never call FastAPI directly; service token never in client bundle (BFF injects it) |
| T-17-01-02 | Tampering | globals.css edit | mitigate | Append-only `.painel-light`; never touch `:root`/`.dark`; regression-test an existing dark suite |
| T-17-01-03 | Denial of Service | engine status poll | accept | 10s refetch matches existing EngineControl cadence; no new load |
| T-17-01-SC | Tampering | npm/bun installs | mitigate | This plan adds NO new packages (native React only) — no install gate triggered |
</threat_model>

<verification>
- `cd dashboard && bun run test components/painel` passes.
- `cd dashboard && bun run test` (full suite) stays green — existing 25 suites / 164 tests untouched.
- Manual: `/painel` renders light; the 10 dark routes still render dark.
</verification>

<success_criteria>
- New `/painel` route renders the light shell + working topbar + view-switcher.
- No edits to globals.css `:root`/`.dark`; existing suites green.
- Topbar motor switch + TA pill + source label wired to real engine-api via the BFF.
</success_criteria>

<output>
Create `.planning/phases/17-painel-brave-redesign-light-theme-single-shell-painel-kanban/17-01-SUMMARY.md` when done.
</output>
