---
phase: 10-engine-stage-depth-selector-cost-gated-collection
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/lib/engine-api.ts
  - dashboard/components/engine/EngineControl.tsx
  - dashboard/mocks/handlers/engine.ts
  - dashboard/components/engine/__tests__/EngineControl.test.tsx
autonomous: true
requirements: [ENG-01, ENG-02, ENG-07]
must_haves:
  truths:
    - "The /processo engine control shows a 3-option depth selector: Apenas nascente | Nascente → Rio | Nascente → Rio → Mar"
    - "The 'Ligar motor' button stays disabled until a depth is selected"
    - "Starting the engine sends the selected depth in the POST /start body"
    - "The status payload's depth is rendered back when a run is active"
  artifacts:
    - path: "dashboard/lib/engine-api.ts"
      provides: "EngineDepth type + depth on EngineStatus + depth in startEngine body"
      contains: "nascente_rio_mar"
    - path: "dashboard/components/engine/EngineControl.tsx"
      provides: "Depth selector (PT-BR labels) + disabled-until-chosen start button + depth in start mutation + active-depth read-back when running"
      contains: "depth"
  key_links:
    - from: "dashboard/components/engine/EngineControl.tsx"
      to: "startEngine({ depth })"
      via: "selected depth state"
      pattern: "depth"
    - from: "dashboard/lib/engine-api.ts:startEngine"
      to: "POST api/v1/engine/start body"
      via: "JSON body includes depth"
      pattern: "depth"
---

<objective>
Add the operator-facing **depth selector** to the engine control on `/processo`. Three PT-BR options map to the fixed depth enum; the "Ligar motor" button stays disabled until the operator picks one (no implicit default that silently spends); the chosen depth is sent in the start mutation; the active run's depth is read back from `/status` and rendered while running.

Purpose: Deliver the client half of the required-selection contract (ENG-01) and the depth round-trip (ENG-02) against the fixed enum, mirroring the existing offline Vitest + MSW harness.
Output: depth types/fetchers in `engine-api.ts`, the selector + disabled-start logic + running-state depth read-back in `EngineControl.tsx`, the `depth` field in the MSW engine handler, and Vitest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-engine-stage-depth-selector-cost-gated-collection/10-CONTEXT.md

<interfaces>
<!-- Fixed depth contract (identical to backend plan 10-01). PT-BR labels are UI-only. -->
EngineDepth = "nascente" | "nascente_rio" | "nascente_rio_mar"
PT-BR labels: nascente="Apenas nascente"; nascente_rio="Nascente → Rio"; nascente_rio_mar="Nascente → Rio → Mar"

From dashboard/lib/engine-api.ts (existing — extend):
  EngineState = "idle" | "running" | "stopping"
  interface EngineStatus { state; current_uf; ufs_done; ufs_total; counts }   // add depth
  startEngine(body?: { ufs?: string[]; lane?: "destinos"|"atrativos"|"both" })  // add depth to body type
  apiFetch wraps the BFF; double-prefix rule means MSW mocks at /api/api/v1/engine/...

From dashboard/components/engine/EngineControl.tsx (existing):
  useQuery(engineKeys.status, fetchEngineStatus) → data; start = useMutation(() => startEngine())
  Idle branch renders the <Button data-testid="engine-start">Ligar motor</Button>
  start.mutate() is called with no args today

From dashboard/mocks/handlers/engine.ts (existing):
  engineStatus(overrides) builds an EngineStatus; engineStartSuccess(); BASE = http://localhost:3000/api/api/v1/engine
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add depth to engine-api types + start body, and to the MSW handler</name>
  <read_first>
    - dashboard/lib/engine-api.ts (full)
    - dashboard/mocks/handlers/engine.ts (full — the engineStatus builder + engineStartSuccess)
  </read_first>
  <behavior>
    - engine-api.ts exports EngineDepth = "nascente" | "nascente_rio" | "nascente_rio_mar".
    - EngineStatus gains depth: EngineDepth | null.
    - startEngine's body type gains depth?: EngineDepth (sent in the JSON body when present).
    - engineStatus(overrides) default payload includes depth: null and remains overridable.
    - engineStartSuccess echoes a depth field so component tests can assert the round-trip.
  </behavior>
  <action>
    In dashboard/lib/engine-api.ts add `export type EngineDepth = "nascente" | "nascente_rio" | "nascente_rio_mar";`, add `depth: EngineDepth | null;` to EngineStatus, and extend the startEngine body param type with `depth?: EngineDepth` (the existing JSON.stringify(body) already forwards it). Add a DEPTH_LABELS constant mapping each depth to its PT-BR label for reuse by the component (selector options AND the running-state read-back). In dashboard/mocks/handlers/engine.ts add `depth: null` to the engineStatus default object and include `depth` in the engineStartSuccess response JSON.
  </action>
  <verify>
    <automated>cd dashboard && bun run test -- engine-api</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'nascente_rio_mar' dashboard/lib/engine-api.ts` returns at least 1.
    - EngineStatus has a `depth` field; startEngine body type accepts `depth`.
    - The MSW engineStatus default and engineStartSuccess both carry `depth`.
    - `cd dashboard && bun run test` type-checks/compiles these modules (no TS errors).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Depth selector + disabled-until-chosen start + running-state depth read-back in EngineControl + Vitest</name>
  <read_first>
    - dashboard/components/engine/EngineControl.tsx (full)
    - dashboard/components/engine/__tests__/EngineControl.test.tsx (full — mirror buildStatus + server.use + userEvent patterns)
    - dashboard/lib/engine-api.ts (EngineDepth + DEPTH_LABELS from Task 1)
  </read_first>
  <behavior>
    - In the idle branch, render a depth selector (radio group or select) with exactly three options labelled "Apenas nascente", "Nascente → Rio", "Nascente → Rio → Mar", each carrying its enum value.
    - Local selectedDepth state starts undefined; "Ligar motor" is disabled while selectedDepth is undefined (in addition to the existing `pending` disable).
    - On start, call startEngine({ depth: selectedDepth }) — i.e. start.mutate receives the chosen depth.
    - When state is "running"/"stopping" the selector is hidden (start is already gone in those branches today). The active depth from data.depth is rendered as read-back text (REQUIRED, not optional — this is the ENG-02 client read-back half): show "Profundidade: <PT-BR label>" using DEPTH_LABELS, on a stable testid (e.g. data-testid="engine-active-depth").
    - PT-BR labels per the response-language directive; testids stable for assertions (e.g. data-testid="engine-depth" on the group, value-carrying options).
  </behavior>
  <action>
    Add a useState<EngineDepth | undefined> for selectedDepth. In the idle branch render a depth selector above/next to the start Button using the three EngineDepth values and their PT-BR labels (reuse DEPTH_LABELS). Change start's mutationFn to accept the depth: `useMutation({ mutationFn: (depth: EngineDepth) => startEngine({ depth }), ... })` and the button onClick to `() => selectedDepth && start.mutate(selectedDepth)`. Extend the start Button's disabled prop to `pending || !selectedDepth`. Add data-testid hooks: "engine-depth" on the selector container and a stable testid/role per option. In the running/stopping branch, render the active depth read-back from data.depth as "Profundidade: <DEPTH_LABELS[data.depth]>" on data-testid="engine-active-depth" (guard for null but render whenever data.depth is set). Keep all existing state/progress/counts markup intact.
  </action>
  <verify>
    <automated>cd dashboard && bun run test -- EngineControl</automated>
  </verify>
  <acceptance_criteria>
    - New Vitest cases (MSW-backed, mirroring the existing file) assert: on idle load the "engine-start" button is disabled; after selecting a depth option the button becomes enabled; clicking start issues POST /start whose body contains the selected depth (assert via an MSW handler that captures the request body, e.g. await request.json()); all three options are present with the PT-BR labels.
    - A running-state read-back case (REQUIRED): with the engine status mocked as state="running" and depth set (e.g. "nascente_rio"), the active depth renders — assert the "engine-active-depth" node shows the PT-BR label "Nascente → Rio". This proves the ENG-02 status→UI read-back.
    - The disabled-until-chosen assertion fails if the button starts enabled (genuine ENG-01 guard).
    - `cd dashboard && bun run test` passes for EngineControl (offline, MSW only — no network).
    - `grep -c 'depth' dashboard/components/engine/EngineControl.tsx` returns at least 3.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → BFF → POST /engine/start | depth is operator-chosen UI input; server-side validation (plan 10-01) is the authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering / cost | client depth selection | mitigate (defense-in-depth) | The disabled-until-chosen button prevents an accidental spend, but the authoritative guard is server-side 422 (plan 10-01). The client never assumes a default depth. |
| T-10-07 | Spoofing | engine BFF calls | accept | Operator Bearer is attached by apiFetch/the BFF as for every other engine call; unchanged by this plan. |
| T-10-SC | Tampering | npm installs | mitigate | No new npm packages; selector uses existing UI primitives (Button + native/shadcn controls already in the dashboard). If any install is proposed, stop and require the Package Legitimacy Gate. |
</threat_model>

<verification>
- `cd dashboard && bun run test` green for engine-api + EngineControl.
- Button disabled on load; enabled only after a depth pick; start body carries depth (asserted by capturing the MSW request body).
- Active depth read-back renders when state="running" (asserted on engine-active-depth).
- All three PT-BR labels render. No real network — MSW only.
</verification>

<success_criteria>
- Three-option depth selector with PT-BR labels on /processo's engine control.
- "Ligar motor" disabled until a depth is chosen (ENG-01 client half).
- Selected depth sent in the start body; active depth read back from status and rendered while running (ENG-02 client half — read-back is required and asserted).
- 100% offline via Vitest + MSW.
</success_criteria>

<output>
Create `.planning/phases/10-engine-stage-depth-selector-cost-gated-collection/10-03-SUMMARY.md` when done.
</output>
</content>
