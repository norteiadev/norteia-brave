---
phase: 02-destinos-lane
plan: "09"
type: execute
wave: 4
depends_on: ["02-06", "02-07", "02-08"]
files_modified:
  - tests/integration/test_destinos_lane.py
autonomous: true
requirements: [DEST-01, DEST-02, DEST-03, DEST-04, DEST-05, TEST-02]
must_haves:
  truths:
    - "A single Mtur municipality flows end-to-end: seed → Nascente → Rio → DLQ → steward validate → Mar → push to destinations (faked norteia-api)"
    - "NotebookLM corroboration boost enables the Mtur record to cross the Mar threshold after validation"
    - "DesmembramentoAgent quarantine path is covered by the integration test"
    - "PATCH /api/v1/dlq/{id}/validate and POST /api/v1/dlq/validate-batch both covered by integration tests"
    - "Full suite runs offline (no real Mtur/NotebookLM/OpenRouter/norteia-api calls)"
  artifacts:
    - path: "tests/integration/test_destinos_lane.py"
      provides: "End-to-end integration tests for the Destinos lane"
      contains: "test_mtur_lane_end_to_end"
  key_links:
    - from: "tests/integration/test_destinos_lane.py"
      to: "brave/api/routers/dlq.py"
      via: "TestClient PATCH /api/v1/dlq/{id}/validate"
      pattern: "validate.*dlq|dlq.*validate"
    - from: "tests/integration/test_destinos_lane.py"
      to: "brave/lanes/destinos/mtur.py"
      via: "MturSeedIngest.produce('BA') with FakeMturClient"
      pattern: "MturSeedIngest"
---

<objective>
Write the end-to-end integration test for the Destinos lane. This is the phase acceptance test: it exercises the full path from seed ingest through to the DLQ validate endpoint triggering Mar promotion and push.

Purpose: All prior plans in this phase ship building blocks. This plan assembles them into a single failing-then-passing end-to-end test. After this plan, the phase goal is verifiable: "a single Mtur municipality flows seed → Nascente → Rio → DLQ → validate → Mar → push (faked)". It is also the CI gate — the offline test command that must pass before the phase is declared complete.

Output: tests/integration/test_destinos_lane.py with four integration tests covering the full Destinos lane.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-destinos-lane/02-CONTEXT.md
@.planning/phases/02-destinos-lane/02-RESEARCH.md
@.planning/phases/02-destinos-lane/02-PATTERNS.md

<interfaces>
<!-- db_session fixture (tests/conftest.py) — real Postgres session via docker-compose -->
```python
@pytest.fixture
def db_session(): ...  # yields SQLAlchemy Session backed by test DB
```

<!-- TestClient (fastapi.testclient) pattern from tests/integration/test_fastapi_endpoints.py -->
```python
from fastapi.testclient import TestClient
from brave.api.main import app
client = TestClient(app)

def test_example(db_session):
    response = client.get("/api/v1/dlq")
    assert response.status_code == 200
```

<!-- MturSeedIngest call pattern -->
```python
import asyncio
from brave.lanes.destinos.mtur import MturSeedIngest
from tests.fakes.fake_mtur import FakeMturClient
from brave.config.settings import ScoreConfig

fake_mtur = FakeMturClient(fixtures=[{
    "ibge_code": "2927408",
    "name": "Porto Seguro",
    "categoria": "Oferta Principal",
    "uf": "BA",
}])
lane = MturSeedIngest(mtur_client=fake_mtur, session=db_session, config=ScoreConfig())
asyncio.run(lane.produce("BA"))
```

<!-- DLQ validate endpoint (PATCH) -->
# GET /api/v1/dlq?uf=BA&entity_type=destination → returns list with "id" field
# PATCH /api/v1/dlq/{id}/validate → 202, {"status":"accepted","rio_id":..., "routing":...}

<!-- Pact contract shape (updated in Plan 01) -->
# canonical must include ibge_code
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end Destinos lane integration tests</name>
  <files>tests/integration/test_destinos_lane.py</files>

  <read_first>
    - tests/integration/test_end_to_end_pipeline.py — full file; the integration test pattern this mirrors (db_session, asyncio.run for async producers, TestClient for API endpoints)
    - tests/integration/test_fastapi_endpoints.py — TestClient usage pattern for DLQ endpoints
    - tests/conftest.py — all available fixtures (db_session, client, score_config)
    - tests/fakes/fake_mtur.py — FakeMturClient fixtures (Porto Seguro BA, 2927408)
    - tests/fakes/fake_notebooklm.py — FakeNotebookLMClient
    - tests/fakes/fake_llm.py — FakeLLMClient
    - tests/fakes/fake_norteia_api.py — FakeNorteiaApiClient
    - .planning/phases/02-destinos-lane/02-PATTERNS.md §"tests/integration/test_destinos_lane.py" — all four test templates
    - brave/core/models.py — NascenteRecord, RioRecord, MarRecord, PoisonQuarantine fields
  </read_first>

  <behavior>
    Test 1 — test_mtur_lane_end_to_end:
    - MturSeedIngest.produce("BA") with FakeMturClient (Porto Seguro, Oferta Principal)
    - After produce: NascenteRecord with source="mtur" exists
    - RioRecord for Porto Seguro has routing in ("dlq", "descarte") — cold start, no human validation yet
    - With threshold_dlq=40 and completude=100 (FakeMturClient has all fields), score = 30+20+0+10.5+0 = 60.5 → dlq confirmed

    Test 2 — test_validate_endpoint_promotes_to_mar_with_corroboration:
    - Setup: create a RioRecord in DLQ with origin=100, completude=100, corroboracao=50, atualidade=70, validacao_humana=0 (score=78.5, routing="dlq")
    - PATCH /api/v1/dlq/{rio_id}/validate → 202
    - After validate: rio.routing == "mar" (score=78.5+15=93.5 with validacao_humana=100... actually: 30+20+10+10.5+15=85.5 → mar)
    - AuditRecord with action="dlq_validated" exists
    - Verify rio.normalized["validacao_humana_value"] == 100.0 (flag_modified test)

    Test 3 — test_validate_batch_endpoint:
    - Setup: create 3 RioRecords in DLQ with uf="BA", entity_type="destination", corroboracao=50
    - POST /api/v1/dlq/validate-batch?uf=BA&entity_type=destination → 202
    - Response body: {"status":"accepted","uf":"BA","validated":3}
    - After: all 3 records have routing=="mar"

    Test 4 — test_notebooklm_corroboration_boosts_mtur:
    - Setup: MturSeedIngest.produce("BA") → Mtur RioRecord in DLQ (corroboracao=0)
    - NotebookLMIngest.produce("BA") with FakeNotebookLMClient reporting Porto Seguro
    - After NotebookLM produce: RioRecord.normalized["corroboracao_value"] == 50.0
    - PATCH /api/v1/dlq/{rio_id}/validate → routing becomes "mar"

    All four tests use @pytest.mark.integration marker.
  </behavior>

  <action>
    Create tests/integration/test_destinos_lane.py.

    Module docstring: "End-to-end Destinos lane integration tests (DEST-01..05, TEST-02). All offline — fake clients only; CI keyless. Requires docker-compose Postgres+Redis (db_session fixture)."

    Four test functions per behavior spec. Each test is decorated @pytest.mark.integration.

    For Test 1: call asyncio.run(lane.produce("BA")) with FakeMturClient fixtures for Porto Seguro (Oferta Principal). Assert NascenteRecord exists. Get RioRecord, assert routing in ("dlq", "mar"). With threshold_dlq=40 and atualidade_value=MTUR_ATUALIDADE_DEFAULT=70, completude=100: score = 30+20+0+10.5+0 = 60.5 → "dlq". Assert routing == "dlq". Assert source_ref == "mtur:BA:2927408".

    For Test 2: create RioRecord manually in db_session with routing="dlq", normalized={"origem_value":100,"completude_value":100,"corroboracao_value":50,"atualidade_value":70,"validacao_humana_value":0}, score=78.5, entity_type="destination", uf="BA". Use TestClient to PATCH. Assert 202. Reload RioRecord from DB. Assert routing=="mar". Assert normalized["validacao_humana_value"]==100.0.

    For Test 3: create 3 minimal RioRecords in DLQ with corroboracao=50. PATCH validate-batch. Assert validated==3 in response. Assert all 3 routing=="mar".

    For Test 4: Full NotebookLMIngest test — produce Mtur first, then produce NotebookLM with FakeNotebookLMClient returning a report for Porto Seguro. mtur_municipalities must be passed to NotebookLMIngest.__init__. Assert corroboracao_value boosted on existing RioRecord. Then validate via PATCH and assert routing=="mar".

    Handle async produce calls via asyncio.run() (check if conftest uses anyio/asyncio markers). If the integration tests are sync, wrap all async lane calls in asyncio.run().
  </action>

  <verify>
    <automated>cd /Users/leandro/Projects/norteia/norteia-brave && python -m pytest tests/integration/test_destinos_lane.py -v -m integration 2>&1 | tail -30</automated>
  </verify>

  <done>
    - All four @pytest.mark.integration tests pass
    - test_mtur_lane_end_to_end: NascenteRecord with source="mtur" and RioRecord with routing="dlq" after produce
    - test_validate_endpoint_promotes_to_mar_with_corroboration: routing=="mar" after PATCH, normalized["validacao_humana_value"]==100.0
    - test_validate_batch_endpoint: 3 records validated, all routing=="mar"
    - test_notebooklm_corroboration_boosts_mtur: corroboracao_value==50.0 after NotebookLM produce
    - pytest tests/integration/test_destinos_lane.py exits 0 with docker-compose Postgres+Redis running
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration tests → Postgres | Uses docker-compose test DB; no production data |
| integration tests → FastAPI TestClient | In-process HTTP client; no real network sockets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09-01 | Tampering | Integration tests creating RioRecords directly | accept | Direct DB writes in tests are isolated to the test session (rollback fixture). Production pipeline cannot be affected. |
| T-02-09-02 | Denial of Service | Integration tests running without docker-compose | accept | Tests fail fast with a clear DB connection error; does not hang. CI must run with docker-compose. |
| T-02-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages. slopcheck not required. |
</threat_model>

<verification>
```bash
cd /Users/leandro/Projects/norteia/norteia-brave
# Run full offline suite (requires docker-compose up -d)
python -m pytest tests/integration/test_destinos_lane.py -v --tb=short 2>&1 | tail -30

# Confirm all Phase 1 tests still pass (no regressions)
python -m pytest tests/unit/ tests/contract/ -v 2>&1 | tail -20
```
</verification>

<success_criteria>
- All four integration tests pass with docker-compose Postgres+Redis
- Full offline suite (unit + integration + contract) passes without any real external calls
- flag_modified correctness is directly verified in test_validate_endpoint (re-reads normalized from DB)
- corroboration boost verified in test_notebooklm_corroboration_boosts_mtur (reads back corroboracao_value)
- No test hits real Mtur/NotebookLM/OpenRouter/norteia-api APIs (all fakes)
- pytest exits 0 with no warnings about Phase 1 test regressions
</success_criteria>

<output>
Create `.planning/phases/02-destinos-lane/02-09-SUMMARY.md` when done
</output>
