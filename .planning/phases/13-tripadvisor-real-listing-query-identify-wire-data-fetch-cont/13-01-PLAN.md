---
phase: "13"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - brave/lanes/tripadvisor/client.py
  - brave/api/routers/tripadvisor_session.py
  - scripts/ta_bootstrap.py
  - tests/unit/lanes/tripadvisor/test_client.py
autonomous: true
requirements:
  - TA-12

must_haves:
  truths:
    - "fetch_attractions POSTs preRegisteredQueryId a5cb7fa004b5e4b5 and the AttractionsFusion variables shape (request.routeParameters.contentType=attraction, sessionId threaded)"
    - "Parsed cards are dicts with keys name, locationId, rating, review_count, category sourced from the real response paths"
    - "ta_bootstrap extracts TASID from cookies and rejects telemetry/ad/trips qids with an operator warning"
    - "SessionInjectBody accepts optional session_id field; session dict stored in Redis includes session_id"
    - "All offline tests pass without hitting TripAdvisor"
  artifacts:
    - path: "brave/lanes/tripadvisor/client.py"
      provides: "Rewired fetch_attractions with AttractionsFusion variables + SingleFlexCardSection parser"
      contains: "a5cb7fa004b5e4b5"
    - path: "brave/api/routers/tripadvisor_session.py"
      provides: "SessionInjectBody with session_id field"
      contains: "session_id"
    - path: "scripts/ta_bootstrap.py"
      provides: "TASID extraction + listing-qid validation"
      contains: "a5cb7fa004b5e4b5"
    - path: "tests/unit/lanes/tripadvisor/test_client.py"
      provides: "Offline tests for new payload shape + card parsing + qid validation"
  key_links:
    - from: "client.py fetch_attractions"
      to: "https://www.tripadvisor.com/data/graphql/ids"
      via: "httpx POST with variables.sessionId = session['session_id']"
      pattern: "a5cb7fa004b5e4b5"
    - from: "client.py _parse_attractions_page"
      to: "data.Result[0].sections[]"
      via: "filter __typename == WebPresentation_SingleFlexCardSection"
      pattern: "SingleFlexCardSection"
---

<objective>
Rewire TripAdvisorClient.fetch_attractions to the live-validated AttractionsFusion listing query (qid a5cb7fa004b5e4b5), replacing the wrong Phase-11 {locationId, offset, limit} variables with the real request.routeParameters shape. Thread session_id (TASID) through the session model. Extend ta_bootstrap to extract TASID and reject non-listing qids.

Purpose: The client currently posts a variables shape that matches no real TripAdvisor query — fix the root cause so fetch_attractions returns actual FlexCard data.
Output: Updated client.py (new variables + response parser), updated SessionInjectBody + ta_bootstrap, new offline tests with Iguazu Falls fixture.
</objective>

<execution_context>
@/Users/leandro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leandro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont/GAP-12-A-FINDINGS.md
@.planning/phases/13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont/13-CONTEXT.md
@brave/lanes/tripadvisor/client.py
@brave/api/routers/tripadvisor_session.py
@scripts/ta_bootstrap.py
@tests/unit/lanes/tripadvisor/test_client.py

<interfaces>
<!-- Key contracts the executor needs. All extracted from current codebase. -->

From brave/lanes/tripadvisor/client.py:
- BRAVE_TA_SESSION_KEY = "brave:ta:session"
- _TA_GRAPHQL_URL = "https://www.tripadvisor.com/data/graphql/ids"
- _MAX_PAGES = 50
- Session dict shape (written by injection, read by _get_session):
  {"cookies": {name: value}, "query_ids": {"destinations": "<16-hex>", "attractions": "<16-hex>"},
   "user_agent": "...", "acquired_at": "..."}
  — Phase 13 adds: "session_id": "<TASID value>"
- fetch_attractions(self, geo_id: int, offset: int = 0) -> list[dict[str, Any]]
  Currently uses wrong payload: {"variables": {"locationId": geo_id, "offset": offset, "limit": 20}}
  Parses from data[0].data.attractions (path that does not exist in real response)

From GAP-12-A-FINDINGS.md (live-validated contract):
Real variables shape for fetch_attractions payload item:
  {
    "variables": {
      "request": {
        "tracking": {"screenName": "AttractionsFusion", "pageviewUid": "<uuid4>"},
        "routeParameters": {
          "geoId": <int>,
          "contentType": "attraction",
          "webVariant": "AttractionsFusion",
          "filters": [{"id": "allAttractions", "value": ["true"]}]
        },
        "updateToken": null
      },
      "commerce": {"attractionCommerce": {"pax": [{"ageBand": "ADULT", "count": 2}]}},
      "tracking": {"screenName": "AttractionsFusion", "pageviewUid": "<uuid4>"},
      "sessionId": "<TASID value from session['session_id']>",
      "unitLength": "MILES", "currency": "USD",
      "currentGeoPoint": null, "mapSurface": false, "debug": false, "polling": false
    },
    "extensions": {"preRegisteredQueryId": "a5cb7fa004b5e4b5"}
  }

Real response parse path:
  data = resp.json()  # list
  data[0]["data"]["Result"][0]["sections"]  # list of 41 items for Brazil page 1
  Keep only: section["__typename"] == "WebPresentation_SingleFlexCardSection"
  Per card: section["singleFlexCardContent"]  # WebPresentation_FlexCard
    name = card["cardTitle"]["text"]
    locationId = card["cardLink"]["webRoute"]["typedParams"]["detailId"]  (int)
    rating = card["bubbleRating"]["rating"]  (float)
    review_count = card["bubbleRating"]["reviewCount"]  (int)
    category = card["primaryInfo"]["text"]  (str)
  Page size = 30 FlexCard sections per response.
  Empty-page signal: zero SingleFlexCardSection items in sections list.

From brave/api/routers/tripadvisor_session.py:
- SessionInjectBody fields: cookies, query_ids, user_agent, acquired_at, client_hints (optional), locale (optional), acquisition_ip (optional)
- model_config = {"extra": "forbid"}
- session dict stored to Redis: {"cookies", "query_ids", "user_agent", "acquired_at"}
  — Phase 13 adds: "session_id" derived from cookies["TASID"] or body.session_id

From scripts/ta_bootstrap.py:
- parse_curl(curl_str: str) -> dict  (returns payload matching SessionInjectBody)
- Current qid extraction: heuristic (ATTRACTION in variables_str), no reject-list
- Phase 13: must extract TASID from cookies["TASID"] for session_id field
- Phase 13: must reject known non-listing qids with operator warning

Known non-listing (reject) qids:
  636d0b9184b2fc29 = telemetry (user_navigated/page_viewed)
  986742f2dd8b0ec8 = pixel metrics
  42bec0ee6ec0bfd1 = aux locationId-keyed query
  46dcf3e69ea8ba5a = ad_mission_control.GetPageSlotSettings
  25f9ddb1ce629144 = Trips_ReferenceInput

Real listing qid: a5cb7fa004b5e4b5 (AttractionsFusion)

Test fixture (Iguazu Falls — from live response):
  {
    "cardTitle": {"text": "Iguazu Falls"},
    "cardLink": {"webRoute": {"typedParams": {"detailId": 312332}}},
    "bubbleRating": {"rating": 4.9, "reviewCount": 45811},
    "primaryInfo": {"text": "Waterfalls"},
    "__typename": "WebPresentation_FlexCard"
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire fetch_attractions to AttractionsFusion query + add session_id to session model</name>
  <files>brave/lanes/tripadvisor/client.py, brave/api/routers/tripadvisor_session.py</files>
  <read_first>
    - brave/lanes/tripadvisor/client.py (full file — current fetch_attractions, _get_session, fetch_destinations)
    - brave/api/routers/tripadvisor_session.py (full file — SessionInjectBody, inject_session, _run_canary)
    - .planning/phases/13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont/GAP-12-A-FINDINGS.md (authoritative contract)
  </read_first>
  <action>
    In brave/lanes/tripadvisor/client.py:

    1. Import uuid from stdlib at the top of the module.

    2. Add a private helper _parse_attractions_page(raw_sections: list) -> list[dict]: iterate raw_sections, keep only items where item.get("__typename") == "WebPresentation_SingleFlexCardSection", extract singleFlexCardContent per card, return a list of normalized dicts with keys: name (card["cardTitle"]["text"]), locationId (card["cardLink"]["webRoute"]["typedParams"]["detailId"] as int), rating (card["bubbleRating"]["rating"] as float), review_count (card["bubbleRating"]["reviewCount"] as int), category (card["primaryInfo"]["text"]). Use .get() with safe defaults throughout; skip malformed cards (log a debug warning, do not raise).

    3. Rewrite fetch_attractions(self, geo_id: int, max_pages: int | None = None) -> list[dict[str, Any]]:
       - Remove the offset parameter (new pagination is page-count-based, not offset-based, per the real query contract).
       - Read session via _get_session(); extract session_id = session.get("session_id", "").
       - Use qid hardcoded to "a5cb7fa004b5e4b5" (NOT from session["query_ids"]["attractions"] — the real qid is fixed; do not read it from session to avoid injecting a stale/wrong qid).
       - page_limit = _MAX_PAGES if max_pages is None else min(max_pages, _MAX_PAGES).
       - Loop over page_num in range(page_limit): build pageview_uid = str(uuid.uuid4()) per page; build the payload list with one item — the real AttractionsFusion variables shape (routeParameters.geoId=geo_id, contentType="attraction", webVariant="AttractionsFusion", filters=[{id:"allAttractions", value:["true"]}], updateToken=null, sessionId=session_id, unitLength="MILES", currency="USD", currentGeoPoint=null, mapSurface=false, debug=false, polling=false) and extensions.preRegisteredQueryId="a5cb7fa004b5e4b5".
       - Parse with _parse_attractions_page: extract sections from resp.json()[0]["data"]["Result"][0]["sections"] (use safe .get() fallback to []). If sections list is empty → break. Cards = _parse_attractions_page(sections). If len(cards) == 0 → break (end of pages). Append cards to results. If len(cards) < 30 → break (partial/last page).
       - Preserve 403/429 → SessionExpiredError, and resp.raise_for_status() guards unchanged.
       - Keep proxy threading (self._config.proxy_url or None) unchanged.

    4. Keep fetch_destinations unchanged for this plan (it is rewired in plan 13-02 after destinos contentType is confirmed).

    In brave/api/routers/tripadvisor_session.py:

    5. Add session_id: str | None = Field(default=None, description="TASID cookie value threaded into listing query variables.sessionId. Derived from cookies['TASID'] if absent.") to SessionInjectBody. extra="forbid" is already set — no change needed there.

    6. In inject_session, after building the session dict, set session["session_id"] = body.session_id or body.cookies.get("TASID") or "". This ensures the session stored in Redis always carries session_id (from explicit field or auto-derived from TASID cookie).

    Security notes (preserve all existing T-12-02-* rules):
    - session_id value is a cookie value — NEVER log it. The existing audit-log pattern (cookie_count + query_ids keys only) is unchanged.
    - No change to the 64 KB body check or auth guards.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_client.py tests/unit/api/test_tripadvisor_session.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - fetch_attractions payload item contains extensions.preRegisteredQueryId == "a5cb7fa004b5e4b5"
    - fetch_attractions payload item variables.request.routeParameters.contentType == "attraction"
    - fetch_attractions payload item variables.sessionId is set (from session["session_id"])
    - SessionInjectBody accepts session_id as optional field
    - All existing offline tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend ta_bootstrap with TASID extraction and listing-qid reject list; add offline tests</name>
  <files>scripts/ta_bootstrap.py, tests/unit/lanes/tripadvisor/test_client.py</files>
  <read_first>
    - scripts/ta_bootstrap.py (full file — parse_curl, inject_session, main)
    - tests/unit/lanes/tripadvisor/test_client.py (existing test patterns — respx + fakeredis, test class layout)
    - .planning/phases/13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont/GAP-12-A-FINDINGS.md (reject-list qids + listing qid)
  </read_first>
  <action>
    In scripts/ta_bootstrap.py:

    1. Add a KNOWN_NON_LISTING_QIDS frozenset at module level:
       {"636d0b9184b2fc29", "986742f2dd8b0ec8", "42bec0ee6ec0bfd1", "46dcf3e69ea8ba5a", "25f9ddb1ce629144"}

    2. Add LISTING_QID = "a5cb7fa004b5e4b5" constant.

    3. In parse_curl, after extracting query_ids from the batch JSON, add two passes:
       a. Reject-list check: for each qid found, if qid in KNOWN_NON_LISTING_QIDS, print a warning to stderr: "Warning: captured qid {qid} is a telemetry/ad/trips query, not a listing — skip this request and capture a POST from the Attractions listing page." Do NOT include the rejected qid in query_ids.
       b. TASID extraction: after cookie parsing is complete, extract session_id = cookies.get("TASID", ""). Add it to the returned dict as "session_id": session_id.

    4. Update the return dict to include "session_id": session_id alongside the existing cookies/query_ids/user_agent/acquired_at keys.

    5. In main, after printing the query_ids line, also print: "session_id: {'found' if payload.get('session_id') else 'NOT FOUND — TASID cookie missing from cURL; ensure you captured while logged in'}".

    In tests/unit/lanes/tripadvisor/test_client.py:

    6. Add class TestTripAdvisorAttractionsFusionContract with the following tests:
       - test_fetch_attractions_uses_attractions_fusion_qid: seed fakeredis with a session that has session_id="TASID_VALUE". Using respx.mock, capture the POST body. Assert captured_body[0]["extensions"]["preRegisteredQueryId"] == "a5cb7fa004b5e4b5". Assert "locationId" NOT in captured_body[0]["variables"] (old shape gone). Assert captured_body[0]["variables"]["request"]["routeParameters"]["contentType"] == "attraction". Assert captured_body[0]["variables"]["sessionId"] == "TASID_VALUE".
       - test_fetch_attractions_parses_single_flex_card_sections: build a mock response with one SingleFlexCardSection (Iguazu Falls fixture: cardTitle.text="Iguazu Falls", detailId=312332, rating=4.9, reviewCount=45811, primaryInfo.text="Waterfalls") and two non-matching sections (AdPlaceholder, PaginationLinksList). Assert result has exactly 1 card. Assert result[0]["name"] == "Iguazu Falls". Assert result[0]["locationId"] == 312332. Assert result[0]["rating"] == 4.9. Assert result[0]["review_count"] == 45811. Assert result[0]["category"] == "Waterfalls".
       - test_fetch_attractions_empty_sections_stops_pagination: mock returns sections=[] on first page. Assert result == [] and exactly 1 HTTP call made.
       - test_fetch_attractions_partial_page_stops_pagination: mock returns 15 FlexCard sections (< 30). Assert exactly 1 HTTP call and 15 cards returned.

    7. Add class TestBootstrapQueryIdRejectList:
       - test_parse_curl_rejects_known_non_listing_qids: build a minimal cURL string whose --data-raw contains qid 46dcf3e69ea8ba5a (the ad qid). Assert parse_curl returns query_ids == {} (rejected). Assert the warning message is printed to stderr (use capsys or monkeypatch print to capture stderr).
       - test_parse_curl_extracts_tasid_as_session_id: build a cURL string with Cookie: TASID=E75FBE95 (among others). Assert parse_curl(...)["session_id"] == "E75FBE95".
       - test_parse_curl_session_id_empty_when_no_tasid: cURL with Cookie: datadome=abc (no TASID). Assert parse_curl(...)["session_id"] == "".

    Fixture helper (shared at module level or inside class):
    Build a full TA response envelope: [{"data": {"Result": [{"sections": sections_list}]}}]
    One SingleFlexCardSection item = {"__typename": "WebPresentation_SingleFlexCardSection", "singleFlexCardContent": {"cardTitle": {"text": "Iguazu Falls"}, "cardLink": {"webRoute": {"typedParams": {"detailId": 312332}}}, "bubbleRating": {"rating": 4.9, "reviewCount": 45811}, "primaryInfo": {"text": "Waterfalls"}}}
    One AdPlaceholder = {"__typename": "WebPresentation_AdPlaceholder"}
    One PaginationLinksList = {"__typename": "WebPresentation_PaginationLinksList"}
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_client.py -x -q -k "AttractionsFusion or Bootstrap" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - TestTripAdvisorAttractionsFusionContract: 4 tests pass
    - TestBootstrapQueryIdRejectList: 3 tests pass
    - ta_bootstrap parse_curl returns session_id == TASID cookie value
    - Known non-listing qids are rejected with stderr warning (not silently included in query_ids)
    - Full test suite passes: .venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_client.py tests/unit/api/test_tripadvisor_session.py -q exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → ta_bootstrap | cURL string contains live DataDome credential (TASID, datadome cookie) |
| ta_bootstrap → POST /tripadvisor/session | session_id field carries TASID value — treated as cookie |
| session Redis key → fetch_attractions | session_id threaded into variables.sessionId — never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Information Disclosure | session_id in session dict | mitigate | session_id is a cookie value — extend existing T-12-02-01 never-log rule to cover session_id; audit logs emit only cookie_count + query_ids keys, session_id presence as boolean only |
| T-13-01-02 | Spoofing | preRegisteredQueryId hardcoded | accept | qid is a public TripAdvisor endpoint identifier, not a secret; hardcoding prevents injection of an attacker-controlled qid via the session payload |
| T-13-01-SC | Tampering | npm/pip/cargo installs | accept | no new package dependencies in this plan |
</threat_model>

<verification>
Full offline suite must pass:
  .venv/bin/python -m pytest tests/unit/lanes/tripadvisor/ tests/unit/api/test_tripadvisor_session.py -q

Spot-check assertions:
  grep -c "a5cb7fa004b5e4b5" brave/lanes/tripadvisor/client.py   # must be >= 1
  grep -c "SingleFlexCardSection" brave/lanes/tripadvisor/client.py  # must be >= 1
  grep -c "session_id" brave/api/routers/tripadvisor_session.py   # must be >= 1
  grep -c "KNOWN_NON_LISTING_QIDS" scripts/ta_bootstrap.py   # must be >= 1
</verification>

<success_criteria>
- fetch_attractions sends preRegisteredQueryId == "a5cb7fa004b5e4b5"
- fetch_attractions variables contain request.routeParameters.contentType == "attraction" and sessionId from session["session_id"]
- _parse_attractions_page filters to SingleFlexCardSection only; Iguazu Falls test card parses to {name, locationId, rating, review_count, category}
- SessionInjectBody accepts session_id; inject_session derives it from TASID cookie if omitted
- ta_bootstrap rejects the 5 known non-listing qids with a clear operator warning
- ta_bootstrap extracts TASID into session_id field
- 0 regressions in existing tests
</success_criteria>

<output>
Create .planning/phases/13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont/13-01-SUMMARY.md when done
</output>
