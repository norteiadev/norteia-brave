# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.*
!.env.example
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Pact contract artifacts (generated at test runtime)
tests/contract/pacts/
!tests/contract/pacts/.gitkeep
*.pact.json

# Node (dashboard)
node_modules/
.next/
dist/

# Docker / data
*.log
.DS_Store
postgres-data/
redis-data/
.gstack/

# Agent tooling per-machine state (oh-my-claudecode)
.omc/

# Local stack runtime state (scripts/app.sh pids + logs)
.run/

# POC de imagens — índice do acervo MTur (7,2 MB, regenerável em ~2 min)
# .venv/bin/python -m scripts.poc_images.harvest_mtur
scripts/poc_images/out/mtur_index.json
