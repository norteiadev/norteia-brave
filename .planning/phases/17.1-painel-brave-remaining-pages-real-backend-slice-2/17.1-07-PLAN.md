---
phase: 17.1-painel-brave-remaining-pages-real-backend-slice-2
plan: 07
type: execute
wave: 4
depends_on: ["17.1-04", "17.1-05", "17.1-06"]
files_modified:
  - dashboard/components/painel/PainelOrigem.tsx
  - dashboard/components/painel/PainelTopbar.tsx
  - dashboard/components/painel/PainelSidebar.tsx
  - dashboard/app/painel/page.tsx
  - dashboard/components/painel/__tests__/PainelOrigem.test.tsx
autonomous: true
requirements: [UI-PAINEL-2]

must_haves:
  truths:
    - "The Origem modal lets the operator pick mtur / tripadvisor / google_places and (re)inject a TA session from a pasted cURL"
    - "A stale TA paste surfaces a distinct 422 toast; an infra fault surfaces a distinct 503 toast"
    - "The Motor toggle collects a depth then calls startEngine(depth)/stopEngine"
    - "The TA session pill + expiry toast are driven by the real expires_in (warn at 5 min), not a hardcoded clock"
    - "All 6 views are reachable in one shell via the two-group nav (Processamento / Operação)"
  artifacts:
    - path: "dashboard/components/painel/PainelOrigem.tsx"
      provides: "source-pick + TA cURL inject modal"
      contains: "injectTASession"
    - path: "dashboard/components/painel/PainelSidebar.tsx"
      provides: "two-group nav (Processamento / Operação)"
      contains: "Operação"
    - path: "dashboard/app/painel/page.tsx"
      provides: "view-switcher routing all 6 views"
      contains: "Duplicados"
  key_links:
    - from: "dashboard/components/painel/PainelOrigem.tsx"
      to: "dashboard/lib/engine-api.ts injectTASession"
      via: "useMutation on cURL paste"
      pattern: "injectTASession"
    - from: "dashboard/app/painel/page.tsx"
      to: "PainelDuplicados / PainelVarreduras"
      via: "view-switcher render"
      pattern: "PainelDuplicados"
---

<objective>
Finish the shell: the Origem source modal (with TA cURL (re)inject + 422/503 toasts + real TTL), the
Motor on/off toggle (depth-required start), the two-group nav, and the page view-switcher that makes
all six views reachable and the already-built Mapeamento/Conversas/Custo/Drawer consistent inside the
finished shell.

Purpose: this is the integration plan that wires plans 04/05/06 together with the existing views into
the design's nav. It is the single owner of page.tsx + the nav so no other plan conflicts on them.
Output: PainelOrigem.tsx, PainelTopbar.tsx (motor depth + Origem trigger + TA pill), PainelSidebar.tsx
(two-group nav), app/painel/page.tsx (view-switcher), + PainelOrigem test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-CONTEXT.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-RESEARCH.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PainelOrigem.tsx modal (source pick + TA cURL inject + 422/503) + test</name>
  <files>dashboard/components/painel/PainelOrigem.tsx, dashboard/components/painel/__tests__/PainelOrigem.test.tsx</files>
  <read_first>
    - dashboard/components/painel/PainelTopbar.tsx:84-99 (useMutation + toast + invalidate scaffold), :134-151 (TA pill)
    - dashboard/lib/engine-api.ts injectTASession + fetchTASessionStatus (plan 06 Task 1 + existing :112-125)
    - dashboard/lib/api-client.ts:40-48 (ApiError.status — distinguish 422 vs 503)
    - brave/api/routers/tripadvisor_session.py:51-101 (SessionInjectBody: cookies/query_ids/user_agent/acquired_at; 422 invalid_session / 503 canary_unverified)
    - 17.1-CONTEXT.md "Specific Ideas" (Origem modal radio rows + TA cURL textarea + TTL badge; warn at 5 min)
  </read_first>
  <action>
    Create dashboard/components/painel/PainelOrigem.tsx ("use client"): a modal with radio rows for
    source mtur / tripadvisor / google_places. When tripadvisor is selected, show a cURL textarea; parse
    the pasted cURL into the strict SessionInjectBody shape (cookies, query_ids, user_agent, acquired_at)
    via a small in-modal parser, then submit through a useMutation over injectTASession. On error,
    distinguish ApiError.status === 422 ("sessão inválida — cole um cURL atual") from 503
    ("verificação indisponível — tente novamente") with distinct toasts (RESEARCH §5 gotcha 1). Show a
    TTL badge from fetchTASessionStatus().expires_in. Pure --painel-* token styling; data-testid on the
    radios, textarea, submit, and the two error states.
    Write __tests__/PainelOrigem.test.tsx (renderWithClient + MSW): assert source radios render; assert a
    cURL paste submits the parsed body to injectTASession; assert a 422 handler surfaces the invalid-
    session toast and a 503 handler surfaces the infra toast (distinct copy).
  </action>
  <acceptance_criteria>
    - `grep -c "injectTASession" dashboard/components/painel/PainelOrigem.tsx` returns ≥1
    - `grep -c "422\|503" dashboard/components/painel/PainelOrigem.tsx` returns ≥2 (distinct handling)
    - `cd dashboard && bun run test PainelOrigem` passes (incl. 422 vs 503 toasts)
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bun run test PainelOrigem</automated>
  </verify>
  <done>The Origem modal picks a source, (re)injects a TA session from pasted cURL, and surfaces distinct 422/503 toasts + a real TTL badge; component test green.</done>
</task>

<task type="auto">
  <name>Task 2: PainelTopbar.tsx — Motor depth + Origem trigger + TA pill</name>
  <files>dashboard/components/painel/PainelTopbar.tsx</files>
  <read_first>
    - dashboard/components/painel/PainelTopbar.tsx (full file — TA pill :134-151, read-only "Origem {source}" button :154-166, motor switch useMutation :87-113)
    - dashboard/lib/engine-api.ts:84-97 (startEngine — requires depth, backend 422 without it engine.py:107-112), :99 (stopEngine), :28 (DEPTH_LABELS), :116 (expires_in)
    - dashboard/components/painel/PainelOrigem.tsx (Task 1 — modal to open)
  </read_first>
  <action>
    Extend dashboard/components/painel/PainelTopbar.tsx: make the read-only "Origem {source}" button
    (:154-166) clickable to open the PainelOrigem modal. Change the motor toggle so START collects a
    depth first (reuse DEPTH_LABELS, engine-api.ts:28) then calls startEngine(depth); STOP calls
    stopEngine() (start REQUIRES a depth — backend 422 without it). Drive the TA pill warn state from the
    real fetchTASessionStatus().expires_in (already polled at ENGINE_REFETCH_INTERVAL_MS): warn at 5 min
    from expires_in, NOT a hardcoded clock. Keep the existing mutation+toast+invalidate scaffold.
  </action>
  <acceptance_criteria>
    - `grep -c "PainelOrigem" dashboard/components/painel/PainelTopbar.tsx` returns ≥1 (modal wired)
    - `grep -c "startEngine\|DEPTH" dashboard/components/painel/PainelTopbar.tsx` returns ≥1 (depth collected before start)
    - `grep -c "expires_in" dashboard/components/painel/PainelTopbar.tsx` returns ≥1 (real TTL drives the pill)
    - `cd dashboard && bun run test PainelTopbar` passes
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bun run test PainelTopbar</automated>
  </verify>
  <done>The topbar opens the Origem modal, starts the motor only with a depth, and drives the TA pill warn from the real expires_in; existing topbar test green.</done>
</task>

<task type="auto">
  <name>Task 3: Two-group nav + page view-switcher (all 6 views reachable)</name>
  <files>dashboard/components/painel/PainelSidebar.tsx, dashboard/app/painel/page.tsx</files>
  <read_first>
    - dashboard/components/painel/PainelSidebar.tsx (full file — current nav item list to regroup)
    - dashboard/app/painel/page.tsx (full file — current single-shell view-switcher)
    - dashboard/components/painel/PainelDuplicados.tsx (plan 04), PainelVarreduras.tsx (plan 05) — the new views to route
    - dashboard/components/painel/{PainelMapeamento,PainelConversas,PainelCusto,PainelDrawer}.tsx (existing views to keep reachable)
    - 17.1-CONTEXT.md domain item 6 (nav two groups: Processamento = Painel/Duplicados/Mapeamento/Varreduras; Operação = Conversas/Custo)
  </read_first>
  <action>
    Regroup the nav in dashboard/components/painel/PainelSidebar.tsx into TWO labeled groups:
    Processamento (Painel, Duplicados, Mapeamento, Varreduras) and Operação (Conversas, Custo) — matching
    the design canvas. Extend dashboard/app/painel/page.tsx view-switcher to render PainelDuplicados and
    PainelVarreduras for their nav keys, and confirm the existing Mapeamento/Conversas/Custo views + the
    record-edit Drawer remain reachable/consistent inside the finished shell. Keep the .painel-light token
    scope; do not touch the 10 legacy dark routes. data-testid on each nav group + item.
  </action>
  <acceptance_criteria>
    - `grep -c "Processamento\|Operação" dashboard/components/painel/PainelSidebar.tsx` returns ≥2
    - `grep -c "PainelDuplicados\|PainelVarreduras" dashboard/app/painel/page.tsx` returns ≥2
    - `cd dashboard && bun run test PainelShell PainelSidebar` passes (all 6 views reachable; no regression)
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bun run test PainelShell PainelSidebar</automated>
  </verify>
  <done>The two-group nav lists all 6 views and the page view-switcher renders Duplicados + Varreduras while keeping Mapeamento/Conversas/Custo/Drawer reachable; shell tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→BFF→collector | operator (re)injects a TA session + starts/stops the motor here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-07-01 | Info disclosure | TA cookies/session leak via Origem inject | mitigate | values posted to the BFF (server Bearer); never logged client-side; backend canary + 64KB guard + audit-counts-only (tripadvisor_session.py) |
| T-17.1-07-02 | Spoofing | stale TA paste accepted | mitigate | backend synchronous canary returns 422 invalid_session; modal surfaces it distinctly (no silent accept) |
| T-17.1-07-03 | Elevation | motor start without auth/depth | mitigate | startEngine requires depth (422 without) + BFF Bearer; steward-guarded backend |
| T-17.1-07-SC | Tampering | npm installs | accept | no installs |
</threat_model>

<verification>
- `cd dashboard && bun run test PainelOrigem PainelTopbar PainelShell PainelSidebar` green.
- Full dashboard suite green: `cd dashboard && bun run test`.
</verification>

<success_criteria>
The shell is complete: Origem modal + TA inject (422/503 distinct), depth-required Motor toggle, real-TTL pill, two-group nav, and all 6 views reachable in one shell — with the legacy dark routes untouched.
</success_criteria>

<output>
Create `.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-07-SUMMARY.md` when done.
</output>
