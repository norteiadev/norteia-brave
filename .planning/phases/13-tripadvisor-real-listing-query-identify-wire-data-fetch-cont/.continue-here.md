---
context: phase
phase: 13-tripadvisor-real-listing-query-identify-wire-data-fetch-cont
task: null
total_tasks: null
status: planning-done-unverified
last_updated: 2026-06-25T00:19:31.061Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking-severity constraints. The key risk (wrong queryId) is fully documented and mitigated below — advisory._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| "HTTP 200 = real data" | The Phase-11 spike recorded queryIds it labeled "AttractionsFusion"/"location lookup" that were actually ad-config + trips queries — they return 200 but no listings. This sent Phase 11/12 down a wrong data-fetch contract; the lane collected 0 records. | advisory | When wiring/validating any TA query, assert the RESPONSE SHAPE (`data.Result[0].sections[]` → `WebPresentation_SingleFlexCardSection` with `cardTitle`/`bubbleRating`/`cardLink.webRoute.typedParams.detailId`), not just status 200. Offline test fixture must be a trimmed REAL response. |
| Reverting to `{locationId,offset,limit}` | The old client variable shape matches NO real TA query. | advisory | Use the request shape locked in 13-CONTEXT.md / GAP-12-A-FINDINGS.md: `request.routeParameters{geoId,contentType,webVariant,filters}` + `sessionId`. |
| Wave-1 file overlap | 13-01 and 13-02 both modify `brave/api/routers/tripadvisor_session.py`. | advisory | execute-phase's intra-wave files_modified check will auto-serialize them (correct). Optionally re-wave 13-02 to Wave 2, or accept serial. |

<current_state>
Phase 12 (TripAdvisor session-injection seam) is COMPLETE and verified (22/22 must-haves; marked complete in ROADMAP/STATE). Live Level-3 UAT then proved the lane collected ZERO real records and uncovered GAP-12-A. We identified + live-validated the REAL listing query and opened Phase 13 to wire it. gsd-planner wrote all 3 Phase-13 plans (13-01/02/03) to disk, but the API socket closed on its return BEFORE the gsd-plan-checker verification loop ran. Plans are spot-checked valid (frontmatter, must_haves, not truncated) but UNVERIFIED.
</current_state>

<completed_work>
- Phase 12 executed end-to-end: 4 plans (2 waves) merged, code-review (2 blockers + 4 warnings) fixed + proven live, verified 22/22, marked COMPLETE.
- Level-3 live UAT (user's real browser captures): cookie/DataDome portability CONFIRMED (httpx replay → 200); GAP-12-A found and root-caused.
- BREAKTHROUGH: identified the real AttractionsFusion listing query — qid `a5cb7fa004b5e4b5`, `request.routeParameters` shape, `data.Result[0].sections[]` parse. Live-validated: HTTP 200, 314KB, real attractions (Iguazu Falls, detailId 312332, rating 4.9, 45811 reviews).
- Phase 13 created in ROADMAP; `GAP-12-A-FINDINGS.md` (full contract + field map + wiring checklist) + `13-CONTEXT.md` (locked decisions) written.
- gsd-planner wrote `13-01-PLAN.md`, `13-02-PLAN.md`, `13-03-PLAN.md` (committed as WIP in this handoff).
- Memory `tripadvisor-graphql-real-shape` corrected (mislabeled-qids → real qid).
</completed_work>

<remaining_work>
- VERIFY the 3 Phase-13 plans with gsd-plan-checker (the planner died before this step).
- EXECUTE Phase 13: wire `client.py` to qid `a5cb7fa004b5e4b5` + new request/response shape; add `sessionId` to session payload + `ta_bootstrap` (reject telemetry/ad qids); fix `_run_canary` to probe the real query; update README/RUNBOOK; offline tests with a trimmed real-response fixture.
- Re-run Level 3 → confirm Nascente records > 0.
</remaining_work>

<decisions_made>
- Phase 12 = COMPLETE (seam scope done+verified); data-fetch = new Phase 13. Seam is correct — do NOT rebuild it.
- Real listing contract LOCKED (live-validated) in 13-CONTEXT.md + GAP-12-A-FINDINGS.md.
- Attractions wired first; destinos contentType = confirm-then-wire; most_recent_review_at = prefer null-at-Nascente (not in the listing card).
</decisions_made>

<blockers>
- gsd-planner API socket error after writing all 3 plans → plans valid but UNVERIFIED. Workaround: spot-checked OK; verify-then-execute.
</blockers>

## Required Reading (in order)
1. `.planning/phases/13-.../GAP-12-A-FINDINGS.md` — the live-validated request/response contract + field map + wiring checklist (authoritative).
2. `.planning/phases/13-.../13-CONTEXT.md` — locked decisions for Phase 13.
3. `.planning/phases/12-.../12-HUMAN-UAT.md` — GAP-12-A history (why the seam works but collection didn't).
4. `.planning/phases/13-.../13-01-PLAN.md`, `13-02-PLAN.md`, `13-03-PLAN.md` — the unverified plans.
5. Memory `tripadvisor-graphql-real-shape` — real qid + the spike-mislabel correction.

## Critical Anti-Patterns (do NOT repeat these)
- Trusting "200 = data" → assert response SHAPE (SingleFlexCardSection cards), not just status.
- Using `{locationId,offset,limit}` or telemetry/ad/trips qids → use qid `a5cb7fa004b5e4b5` + `request.routeParameters` shape.

## Infrastructure State
- Local stack (postgres/redis/uvicorn:8011/celery worker) was torn down at end of Level-3 testing.
- /tmp TA credential files (ta.curl, ta_cookie.txt, ta_replay.py) DELETED. A fresh real-browser session capture is needed for any new live test (~30min DataDome TTL).
- Branch: main. Phase 13 plan docs committed as WIP (this handoff). Pre-existing uncommitted: .gitignore (M), a few stray .gitkeep + v1.0-MILESTONE-AUDIT.md (untracked, not ours).

<context>
The headline insight of this session: the TripAdvisor lane never actually worked — the spike was optimistic ("200 = data"), and its queryIds were ads/trips/telemetry. We found and validated the real one. Phase 13 is now a bounded wiring job, not an open investigation. Everything the executor needs is in GAP-12-A-FINDINGS.md.
</context>

<next_action>
Start with: verify the 3 Phase-13 plans — spawn gsd-plan-checker (or re-run `/gsd:plan-phase 13`, which resumes at the verification loop since plans now exist). If clean, `/clear` then `/gsd:execute-phase 13`. Watch the Wave-1 overlap: 13-01 + 13-02 both touch `brave/api/routers/tripadvisor_session.py` (execute-phase will serialize them).
</next_action>
