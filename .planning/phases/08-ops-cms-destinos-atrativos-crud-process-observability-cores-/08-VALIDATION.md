---
phase: 08
slug: ops-cms-destinos-atrativos-crud-process-observability-cores
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-19
---

# Phase 08 — Validation Strategy

> Per-phase validation contract. Reconstructed from artifacts (State B) and audited by gsd-nyquist-auditor.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (backend)** | pytest 9.x |
| **Framework (dashboard)** | vitest 4.x + MSW 2.x |
| **Config files** | `pyproject.toml` (backend), `dashboard/vitest.config.ts` (dashboard) |
| **Quick run command** | `unset RUN_REAL_EXTERNALS && .venv/bin/python -m pytest -q` |
| **Full suite (backend)** | `unset RUN_REAL_EXTERNALS && .venv/bin/python -m pytest` |
| **Full suite (dashboard)** | `cd dashboard && bun run test` |
| **Estimated runtime** | ~22s backend · ~5s dashboard |

Backend: 438 passed / 1 skipped. Dashboard: 127 passed / 20 files (after Nyquist gap fill).

---

## Sampling Rate

- **After every task commit:** Run the suite for the changed side (backend pytest or dashboard vitest)
- **After every plan wave:** Run both full suites
- **Before `/gsd:verify-work`:** Both suites green
- **Max feedback latency:** ~30s
- **Offline mandate:** no test hits real externals; `RUN_REAL_EXTERNALS` must be unset

---

## Per-Task Verification Map

| Requirement | Plan(s) | Component | Test Type | Automated Command | Status |
|-------------|---------|-----------|-----------|-------------------|--------|
| D-01 token swap (navy `--primary` / terracota `--accent` / off-white `--background`; `@theme inline` bridge intact) | 08-03 | globals.css | unit (fs/oklch assertion) | `cd dashboard && bunx vitest run app/__tests__/globals-tokens.test.ts` | ✅ green |
| D-02 StageBadge (routing, FSM sub_states, score banding ≥85/40-84.9/<40, source chip, validation-pending, CSS-var-only) | 08-03 | StageBadge.tsx | unit (component) | `cd dashboard && bunx vitest run components/cms/__tests__/StageBadge.test.tsx` | ✅ green |
| D-03 destinos CRUD (auth 401, shapes, PII mask, promote/descarte/reprocess) | 08-01, 08-04 | cms.py + DestinoList.tsx | unit (backend + component) | `pytest tests/test_cms_endpoints.py` · `vitest run components/cms/__tests__/DestinoList.test.tsx` | ✅ green |
| D-04 atrativos CRUD (auth, advance 409, PII mask, descarte) | 08-01, 08-05 | cms.py + AtrativoList.tsx | unit (backend + component) | `pytest tests/test_cms_endpoints.py` · `vitest run components/cms/__tests__/AtrativoList.test.tsx` | ✅ green |
| D-05 process observability (workers auth, broker-down degrade, failures payload absent, /processo tiles+funnel) | 08-02, 08-06 | workers.py + processo/page.tsx | unit (backend + component) | `pytest tests/test_workers_endpoints.py` · `vitest run app/processo/__tests__/processo.test.tsx` | ✅ green |
| D-06 JourneyStepper (destino 4-step / atrativo 7-step, completed/current/pending, audit mapping, descarte terminal, compact) | 08-03, 08-06 | JourneyStepper.tsx | unit (component) | `cd dashboard && bunx vitest run components/cms/__tests__/JourneyStepper.test.tsx` | ✅ green |
| D-07 offline backend suite (Bearer 401, response shapes, PII masking, 409, broker-absent) | 08-07 | tests/test_cms_endpoints.py, tests/test_workers_endpoints.py | integration (offline) | `unset RUN_REAL_EXTERNALS && .venv/bin/python -m pytest tests/test_cms_endpoints.py tests/test_workers_endpoints.py` | ✅ green |

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No framework install needed — pytest + vitest/MSW already in place.

---

## Manual-Only Verifications

All phase behaviors have automated verification.

> Follow-up (not a phase-08 gap): the WR-05 descarte-of-promoted-Mar path now returns 409 (covered by `test_descarte_destino_blocked_when_mar_exists`); a true retract/depublish-to-norteia-api flow remains unbuilt and out of scope.

---

## Validation Audit 2026-06-19

| Metric | Count |
|--------|-------|
| Requirements audited | 7 (D-01…D-07) |
| Already covered | 4 (D-03, D-04, D-05, D-07) |
| Gaps found | 3 (D-01, D-02, D-06) |
| Resolved (tests generated) | 3 |
| Escalated | 0 |

Tests generated by gsd-nyquist-auditor (no implementation modified):
- `dashboard/app/__tests__/globals-tokens.test.ts` (D-01)
- `dashboard/components/cms/__tests__/StageBadge.test.tsx` (D-02)
- `dashboard/components/cms/__tests__/JourneyStepper.test.tsx` (D-06)

---

## Validation Sign-Off

- [x] All requirements have automated verification
- [x] Sampling continuity: no requirement without automated verify
- [x] Wave 0 covers all MISSING references (none needed)
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-06-19
