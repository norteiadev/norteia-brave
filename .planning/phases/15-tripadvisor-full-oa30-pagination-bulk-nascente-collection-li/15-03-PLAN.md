---
phase: 15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - brave/lanes/tripadvisor/sweep_progress.py
  - brave/api/routers/tripadvisor_session.py
  - tests/unit/lanes/tripadvisor/test_sweep_progress.py
  - tests/unit/api/test_tripadvisor_session.py
autonomous: true
requirements: [TA-12]
must_haves:
  truths:
    - "A pure Redis-state module records sweep progress (pages, attractions, offset, errors, state)"
    - "Resume offset is readable from the progress state for mid-run recovery"
    - "GET /api/v1/tripadvisor/sweep/progress returns the progress JSON under bearer/steward auth"
    - "An unauthenticated request to the progress endpoint is rejected"
  artifacts:
    - path: "brave/lanes/tripadvisor/sweep_progress.py"
      provides: "start/record_page/record_error/stop_needs_bootstrap/mark_done/get_progress/get_resume_offset over Redis"
      contains: "brave:ta:sweep:progress"
    - path: "brave/api/routers/tripadvisor_session.py"
      provides: "GET /sweep/progress + TASweepProgressResponse"
      contains: "/api/v1/tripadvisor/sweep/progress"
  key_links:
    - from: "brave/api/routers/tripadvisor_session.py"
      to: "brave/lanes/tripadvisor/sweep_progress.py::get_progress"
      via: "endpoint serializes the Redis snapshot"
      pattern: "get_progress"
    - from: "GET /sweep/progress"
      to: "require_steward_or_bearer"
      via: "FastAPI dependency"
      pattern: "require_steward_or_bearer"
---

<objective>
Build the live-progress BACKEND: a pure Redis-state module (writer = Celery worker, reader =
endpoint) mirroring `brave/core/engine.py`, plus a read-only FastAPI status endpoint mirroring
`session_status`. This is the data surface the dashboard panel (15-08) polls and the sweep
orchestration (15-07) writes to.

Purpose: One testable writer surface + one authenticated reader, both fakeredis-testable offline.
Output: `sweep_progress.py` (new) + `GET /api/v1/tripadvisor/sweep/progress` + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-PATTERNS.md

<interfaces>
<!-- Mirror targets. Read these analogs; do not re-explore. -->
brave/core/engine.py (lines 1-159): pure Redis-state module — _decode(value) helper, module-level
  key constants (brave:engine:*), get_status() snapshot. fakeredis-testable, no DB, no dispatch.
brave/api/routers/tripadvisor_session.py (lines 103-109): TASessionStatusResponse (Pydantic BaseModel,
  Literal enum field). (lines 333-369): session_status endpoint — @router.get, dependencies=
  [Depends(require_steward_or_bearer)], redis: Redis = Depends(get_redis). Router already mounted (main.py:67).
brave/api/deps.py (:85): require_steward_or_bearer (constant-time, fail-closed) — the TA-endpoint standard.
Redis key conventions in repo: brave:ta:session (client.py:47), brave:ta:geo: (geo.py), brave:ta:needs_bootstrap (pipeline.py:49).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sweep_progress.py pure Redis-state module</name>
  <read_first>
    - brave/core/engine.py (lines 1-159 — _decode, key constants, get_status snapshot to mirror)
    - .planning/phases/15-...-li/15-PATTERNS.md (sweep_progress.py section + RESEARCH Pattern 3)
  </read_first>
  <behavior>
    - start(redis, pages_total, resume_from_offset=0): HSET brave:ta:sweep:progress with state="running",
      pages_total, pages_done=0, attractions_ingested=0, error_count=0, current_offset/last_completed_offset
      seeded from resume_from_offset, started_at + updated_at timestamps.
    - record_page(redis, offset, ingested_delta): HINCRBY pages_done by 1 and attractions_ingested by
      ingested_delta; HSET current_offset=offset and last_completed_offset=offset; bump updated_at.
    - record_error(redis): HINCRBY error_count by 1.
    - stop_needs_bootstrap(redis): HSET state="stopped_needs_bootstrap".
    - mark_done(redis): HSET state="done".
    - get_progress(redis) -> dict: snapshot with EXACTLY the endpoint field set (state, pages_done,
      pages_total, attractions_ingested, current_offset, error_count, started_at); ints decoded via the
      _decode helper; returns state="idle" + zeros when the hash is absent.
    - get_resume_offset(redis) -> int: read last_completed_offset (0 when absent).
    - Resume arithmetic contract (pinned by test): after start(pages_total=334) then
      record_page(offset=30, ingested_delta=30), get_resume_offset()==30, and the consumer computing
      start_page must resume at page 3 / offset 60 (i.e. the page AFTER last_completed_offset).
  </behavior>
  <action>
    Create `brave/lanes/tripadvisor/sweep_progress.py` as pure functions over a sync Redis client —
    no DB, no dispatch — copying the structure of `brave/core/engine.py`: the `_decode(value)` helper
    verbatim, a module-level `_PROGRESS_KEY = "brave:ta:sweep:progress"` (a Redis HASH) following the
    `brave:ta:*` convention, and the functions in `<behavior>`. State ∈ {idle, running, done,
    stopped_needs_bootstrap}. NEVER store or read cookie/session values in this hash (offsets/counts only).
  </action>
  <acceptance_criteria>
    - `grep -q 'brave:ta:sweep:progress' brave/lanes/tripadvisor/sweep_progress.py`.
    - test_sweep_progress.py (fakeredis): start→record_page→get_progress returns state=running, pages_done=1, attractions_ingested=30, current_offset=30.
    - get_resume_offset returns 30 after the above; absent-hash get_progress returns state="idle".
    - record_error increments error_count; stop_needs_bootstrap sets state="stopped_needs_bootstrap"; mark_done sets state="done".
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_sweep_progress.py -x -q</automated>
  </verify>
  <done>Pure-state module + fakeredis tests green; snapshot shape matches the endpoint exactly; resume offset readable.</done>
</task>

<task type="auto">
  <name>Task 2: GET /api/v1/tripadvisor/sweep/progress endpoint</name>
  <read_first>
    - brave/api/routers/tripadvisor_session.py (TASessionStatusResponse :103-109; session_status :333-369)
    - brave/api/deps.py (:85 require_steward_or_bearer)
    - brave/lanes/tripadvisor/sweep_progress.py (get_progress — written in Task 1)
  </read_first>
  <action>
    Add `TASweepProgressResponse(BaseModel)` to `tripadvisor_session.py` mirroring `TASessionStatusResponse`:
    fields `state: Literal["running","done","stopped_needs_bootstrap","idle"]`, `pages_done: int`,
    `pages_total: int`, `attractions_ingested: int`, `current_offset: int`, `error_count: int`,
    `started_at: str | None = None` (use `extra="forbid"` discipline). Add a `@router.get(
    "/api/v1/tripadvisor/sweep/progress", dependencies=[Depends(require_steward_or_bearer)],
    response_model=TASweepProgressResponse)` handler that returns
    `TASweepProgressResponse(**sweep_progress.get_progress(redis))` with `redis: Redis = Depends(get_redis)`.
    Read-only — no write surface. NEVER include cookie/session values in the response or any log line.
  </action>
  <acceptance_criteria>
    - `grep -q '/api/v1/tripadvisor/sweep/progress' brave/api/routers/tripadvisor_session.py`.
    - Endpoint with a valid bearer/steward credential returns 200 + the progress JSON (fakeredis-seeded).
    - Endpoint with NO credential returns 401/403 (fail-closed, mirrors session_status auth).
    - Response carries no cookie/session/datadome field.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/api/test_tripadvisor_session.py -k "sweep_progress or progress or auth" -x -q</automated>
  </verify>
  <done>Authenticated read endpoint returns the Redis snapshot; unauthenticated request rejected; no secrets leak.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard/operator → API | Untrusted bearer token crosses here to read sweep progress. |
| Celery worker → Redis → endpoint | Progress hash is the only shared state; must carry no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Information Disclosure | GET /sweep/progress | mitigate | `require_steward_or_bearer` dependency (constant-time, fail-closed); unauthenticated request returns 401/403 (asserted). |
| T-15-03-02 | Information Disclosure | brave:ta:sweep:progress hash + endpoint logs | mitigate | Hash stores only offsets/counts/state/timestamps — never cookies/session_id/proxy (T-12-02-01); response model `extra="forbid"` prevents field drift. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
Run both `<verify>` commands with `RUN_REAL_EXTERNALS` UNSET. Module + endpoint tests green via fakeredis.
</verification>

<success_criteria>
Progress state module and authenticated read endpoint exist, are fakeredis-testable, and expose a snapshot
shape that 15-07 writes and 15-08 renders.
</success_criteria>

<output>
Create `.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-03-SUMMARY.md` when done
</output>
