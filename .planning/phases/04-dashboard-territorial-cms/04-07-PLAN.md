---
phase: 04-dashboard-territorial-cms
plan: 07
type: execute
wave: 5
depends_on: ["04-02", "04-05"]
files_modified:
  - brave/api/routers/dashboard.py
  - tests/integration/test_dashboard_endpoints.py
  - dashboard/app/cost/page.tsx
  - dashboard/components/cost/CostByLaneChart.tsx
  - dashboard/components/cost/CostByModelChart.tsx
  - dashboard/components/cost/CostSummary.tsx
  - dashboard/lib/cost-api.ts
  - dashboard/mocks/handlers/cost.ts
  - dashboard/components/cost/__tests__/CostByLaneChart.test.tsx
  - dashboard/components/cost/__tests__/CostSummary.test.tsx
autonomous: true
requirements: [DASH-04]
must_haves:
  truths:
    - "The Cost & LLM view shows spend per lane and per model from llm_generations"
    - "Cost can be grouped by lane or model over a time window"
    - "Empty period renders the empty-state copy, not a crash"
  artifacts:
    - path: "brave/api/routers/dashboard.py"
      provides: "GET /api/v1/cost?group_by=lane|model&since= aggregating llm_generations"
      contains: "def get_cost"
    - path: "dashboard/components/cost/CostByLaneChart.tsx"
      provides: "Recharts cost-by-lane chart"
  key_links:
    - from: "brave/api/routers/dashboard.py"
      to: "LLMGeneration.usd_cost"
      via: "func.sum group_by lane|model_slug"
      pattern: "func.sum"
    - from: "dashboard/components/cost/CostByLaneChart.tsx"
      to: "GET /api/v1/cost"
      via: "useQuery through the BFF"
      pattern: "useQuery"
---

<objective>
Cost slice (DASH-04). Add `GET /api/v1/cost?group_by=lane|model&since=` aggregating `llm_generations` (`func.sum(usd_cost)`, token sums, count grouped by `lane` or `model_slug`) — a straight GROUP BY — then build the Cost & LLM view: Recharts charts for spend per lane and per model with a summary.

Purpose: There is no cost-by-lane/model endpoint (RESEARCH §1); DASH-04 needs it. This slice fills the endpoint and renders it end-to-end.
Output: `get_cost` on `dashboard.py` + the cost view + offline pytest + Vitest/MSW tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md
@.planning/phases/04-dashboard-territorial-cms/04-PATTERNS.md
@.planning/phases/04-dashboard-territorial-cms/04-RESEARCH.md
@.planning/phases/04-dashboard-territorial-cms/04-05-SUMMARY.md
@.planning/phases/04-dashboard-territorial-cms/04-02-SUMMARY.md

<interfaces>
<!-- From models.py + the PATTERNS cost endpoint excerpt. -->
LLMGeneration(llm_generations): lane, model_slug, resolved_provider, prompt_tokens, completion_tokens, usd_cost(Numeric 10,6), created_at
Cost aggregation (PATTERNS dashboard.py): col = LLMGeneration.lane if group_by=="lane" else LLMGeneration.model_slug; select(col, func.sum(usd_cost), func.sum(prompt+completion tokens), func.count(id)).group_by(col); coerce float(cost or 0)
Guard with Depends(require_bearer). dashboard.py exists — ADD to it.
UI-SPEC: mono numerals for USD, charts via shadcn chart wrapper, empty "Sem dados no período".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/v1/cost group-by endpoint</name>
  <files>brave/api/routers/dashboard.py, tests/integration/test_dashboard_endpoints.py</files>
  <read_first>
    - .planning/phases/04-dashboard-territorial-cms/04-PATTERNS.md (the cost-by-lane/model aggregation excerpt — copy the func.sum + group_by shape)
    - brave/api/routers/metrics.py (group_by + pre-seed idioms)
    - brave/api/routers/dashboard.py (existing — add the route here)
    - brave/core/models.py (LLMGeneration)
  </read_first>
  <behavior>
    - Test: GET /api/v1/cost?group_by=lane with a valid Bearer returns 200 with rows [{key, usd_cost, tokens, count}]
    - Test: group_by=model groups by model_slug
    - Test: a `since` filter restricts to the window (created_at)
    - Test: empty llm_generations → rows == [] (or zeroed), 200
    - Test: missing Bearer → 401 before any DB work
  </behavior>
  <action>Add `@router.get("/api/v1/cost", dependencies=[Depends(require_bearer)])` `def get_cost(group_by: str = Query("lane"), since: ... = Query(None), db: Session = Depends(get_db)) -> dict` to the existing `brave/api/routers/dashboard.py`, copying the PATTERNS aggregation excerpt: `col = LLMGeneration.lane if group_by == "lane" else LLMGeneration.model_slug`; `select(col, func.sum(LLMGeneration.usd_cost), func.sum(LLMGeneration.prompt_tokens + LLMGeneration.completion_tokens), func.count(LLMGeneration.id)).group_by(col)` with an optional `created_at >= since` filter; return `{"group_by": group_by, "rows": [{"key": k, "usd_cost": float(cost or 0), "tokens": int(tok or 0), "count": int(n)}]}`. Read-only (D-01).</action>
  <verify>
    <automated>pytest tests/integration/test_dashboard_endpoints.py -k cost -x</automated>
  </verify>
  <acceptance_criteria>
    - `brave/api/routers/dashboard.py` contains `def get_cost(` decorated `dependencies=[Depends(require_bearer)]`
    - `grep -n "func.sum" brave/api/routers/dashboard.py` returns a match
    - cost test (group_by=lane rows shape, group_by=model, empty→[], 401 no-Bearer) passes
  </acceptance_criteria>
  <done>GET /api/v1/cost returns spend grouped by lane or model from llm_generations behind the Bearer gate, proven offline.</done>
</task>

<task type="auto">
  <name>Task 2: Cost & LLM view — charts + summary</name>
  <files>dashboard/components/cost/CostByLaneChart.tsx, dashboard/components/cost/CostByModelChart.tsx, dashboard/components/cost/CostSummary.tsx, dashboard/lib/cost-api.ts, dashboard/app/cost/page.tsx, dashboard/mocks/handlers/cost.ts, dashboard/components/cost/__tests__/CostByLaneChart.test.tsx, dashboard/components/cost/__tests__/CostSummary.test.tsx</files>
  <read_first>
    - .planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md (mono USD numerals, chart color rules, empty "Sem dados no período")
    - .planning/phases/04-dashboard-territorial-cms/04-RESEARCH.md (§5 Recharts 3 via shadcn chart, stacked bar/pie for cost-by-lane/model)
    - brave/api/routers/dashboard.py (the /cost shape from Task 1)
  </read_first>
  <action>Build `dashboard/components/cost/CostByLaneChart.tsx` and `CostByModelChart.tsx` (Recharts 3 via the shadcn `chart` wrapper — stacked bar / pie per lane/model, primary blue series), and `dashboard/components/cost/CostSummary.tsx` (total USD + tokens in Geist Mono tabular-nums). Each fetches `GET /api/v1/cost?group_by=lane|model&since=` through the BFF via `useQuery`. A group-by toggle (lane/model) and a window selector drive refetch. Wire `dashboard/app/cost/page.tsx`. Empty = "Sem dados no período" + "Ajuste a janela de tempo..."; loading = Skeleton; error = retry. Add `dashboard/lib/cost-api.ts`. Tests: CostByLaneChart renders the lane series; CostSummary renders totals; all MSW states (success/empty/error/401).</action>
  <verify>
    <automated>cd dashboard && bunx vitest run components/cost && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/components/cost/CostByLaneChart.tsx` uses `useQuery` against `/api/v1/cost`
    - `dashboard/components/cost/CostSummary.tsx` renders total USD in mono
    - `cd dashboard && bunx vitest run components/cost` exits 0 (all four MSW states incl. empty "Sem dados no período")
    - `cd dashboard && bunx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The Cost & LLM view shows spend per lane and per model with a summary, all offline-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BFF → FastAPI /cost | Bearer-gated aggregate read over llm_generations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Spoofing | get_cost | mitigate | `Depends(require_bearer)`; 401 before DB work |
| T-04-22 | Information Disclosure | cost aggregates | accept | returns aggregate USD/token sums grouped by lane/model; no per-record content, no PII, no secrets |
| T-04-23 | Tampering | read endpoint | accept | read-only select+group_by; no writes |
| T-04-SC | Tampering | python deps (none added) | accept | no new package installs |
</threat_model>

<verification>
- `pytest tests/integration/test_dashboard_endpoints.py -k cost -x` passes.
- `cd dashboard && bunx vitest run components/cost` exits 0.
</verification>

<success_criteria>
DASH-04 complete: the Cost & LLM view shows spend per lane/model from llm_generations, proven offline.
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-territorial-cms/04-07-SUMMARY.md` when done.
</output>


## CONTEXT Decision Traceability

Implements CONTEXT.md decisions: D-01 (read-only cost aggregation endpoint over llm_generations), D-04 (TanStack Query), D-05 (Recharts)
