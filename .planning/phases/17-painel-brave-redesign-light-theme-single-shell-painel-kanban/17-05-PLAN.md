---
phase: 17-painel-brave-redesign-light-theme-single-shell-painel-kanban
plan: 05
type: execute
wave: 3
depends_on: ["17-01", "17-02", "17-03", "17-04"]
files_modified:
  - dashboard/lib/painel-actions.ts
  - dashboard/components/painel/PainelView.tsx
  - dashboard/lib/__tests__/painel-actions.test.ts
  - dashboard/components/painel/__tests__/PainelView.test.tsx
autonomous: true
requirements: [UI-PAINEL-1]

must_haves:
  truths:
    - "PainelView composes metrics + filters + board over usePainelBoard + usePainelMetrics real data and owns the type + UF-scope filter state"
    - "The Destinos/Atrativos metric cards use usePainelMetrics (envelope totals); the Nascente column count uses usePainelMetrics().nascenteCount"
    - "Dropping a card on a column fires ONLY the real matching mutation: →Sincronizado=promote, →Descarte=descarte, →Revisão=reprocess (destinos)"
    - "A drop with no matching real action reverts the card and shows a sonner toast 'Ação não disponível neste estágio' — no invented endpoint/transition is called"
    - "The ↺ Reprocessar button on falha cards calls reprocess (destinos)"
    - "Mutations apply optimistically, invalidate destinos+atrativos+engine keys on settle, and revert on error with a toast"
  artifacts:
    - path: "dashboard/lib/painel-actions.ts"
      provides: "Pure drop/retry → real-mutation mapping + dispatch + the usePainelMutations hook"
      min_lines: 70
    - path: "dashboard/components/painel/PainelView.tsx"
      provides: "Wired Painel (Kanban) container (replaces the 17-01 stub)"
      min_lines: 60
  key_links:
    - from: "dashboard/lib/painel-actions.ts"
      to: "promoteDestino/descarteDestino/reprocessDestino + descartarAtrativo + promoteAtrativo(mar-ready)"
      via: "dispatch by entity+target"
      pattern: "promoteDestino|descarteDestino|reprocessDestino|descartarAtrativo|promoteAtrativo"
    - from: "dashboard/components/painel/PainelView.tsx"
      to: "PainelMetrics + PainelFilters + PainelBoard + usePainelBoard + usePainelMetrics"
      via: "compose + wire drop/retry"
      pattern: "PainelBoard|usePainelBoard|usePainelMetrics"
---

<objective>
Wire the Painel (Kanban) view end-to-end: replace the 17-01 `PainelView` stub with a container that loads real board data (`usePainelBoard`) + truthful metrics (`usePainelMetrics`), owns the type + UF-scope filter state, composes the metric cards / filters / board, and turns drag-drops and the ↺ Reprocessar button into the REAL matching mutations. Drops that map to no real action revert with a sonner toast — no invented endpoints or transitions. Mutations are optimistic, invalidate the destinos/atrativos/engine query keys, and roll back on error.

Purpose: This is the riskiest slice (CONTEXT flags drag-drop for its own plan) — isolating the mutation mapping keeps "never invent a transition" enforceable and tested.
Output: `lib/painel-actions.ts` + finished `PainelView.tsx` + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-painel-brave-redesign-light-theme-single-shell-painel-kanban/17-CONTEXT.md
@dashboard/lib/painel-data.ts
@dashboard/lib/destinos-api.ts
@dashboard/lib/atrativos-api.ts
@dashboard/lib/mar-ready-api.ts
@dashboard/components/mar-ready/MarReadyActions.tsx
@dashboard/components/painel/PainelMetrics.tsx
@dashboard/components/painel/PainelFilters.tsx
@dashboard/components/painel/PainelBoard.tsx
@dashboard/mocks/handlers/destinos.ts
@dashboard/mocks/handlers/atrativos.ts
@dashboard/mocks/handlers/mar-ready.ts

<interfaces>
From dashboard/lib/painel-data.ts (17-02):
- interface PainelCard { id; type:"destino"|"atrativo"; column: PainelColumnKey; ... }
- type PainelColumnKey = "nascente"|"in_progress"|"mar"|"dlq"|"descarte"
- type TypeFilter = "all"|"destino"|"atrativo"
- interface EntityMetric { total; mar; falha; pct }
- function usePainelBoard(): { cards; isPending; isError }
- function usePainelMetrics(): { destino: EntityMetric; atrativo: EntityMetric; nascenteCount: number; isPending }
- function filterCards(cards,{type,ufs}); buildColumns; BR_UFS

From 17-03/17-04 (presentational components):
- <PainelMetrics destino={EntityMetric} atrativo={EntityMetric} />
- <PainelFilters type onTypeChange ufs onUfsChange counts? />
- <PainelBoard cards nascenteCount? onDropToColumn onCardDragStart onCardRetry isPending? />  (renders RecordCard internally)

Mutation fns (REUSE — do not add endpoints):
- destinos-api: promoteDestino(id), descarteDestino(id), reprocessDestino(id) ; destinoKeys.all = ["destinos"]
- atrativos-api: descartarAtrativo(id) ; atrativoKeys.all = ["atrativos"]   (NOTE: atrativos-api has NO reprocess and NO promote)
- mar-ready-api: promoteAtrativo(rioId)  (the audited promote-override; returns 409 when not mar_ready — surfaces as a revert+toast, exactly the "no matching action" UX)
- engine-api: engineKeys.status = ["engine","status"]

Mutation pattern to mirror EXACTLY: dashboard/components/mar-ready/MarReadyActions.tsx (onMutate snapshot+optimistic, onError rollback+toast.error(explainError), onSuccess toast.success, onSettled invalidateQueries).

Drop→action mapping (the ONLY allowed real actions):
| target column | destino card | atrativo card |
| mar (Sincronizado) | promoteDestino | promoteAtrativo (mar-ready) |
| descarte (Descarte) | descarteDestino | descartarAtrativo |
| dlq (Revisão) | reprocessDestino | (none → revert+toast) |
| nascente / in_progress / same column | (none → revert+toast) | (none → revert+toast) |
Retry button (falha cards): destino → reprocessDestino ; atrativo → (none → revert+toast).
Unmapped toast copy (per CONTEXT): "Ação não disponível neste estágio".

MSW (reuse, no new handler file): destinosListSuccess(items?), destinoPromoteSuccess(), destinoDescarteSuccess(), destinoReprocessSuccess() (destinos.ts); atrativosListSuccess(), atrativoDescarteSuccess() (atrativos.ts); promoteSuccess/promoteFailure for atrativo promote (mar-ready.ts); engineStatus() (engine.ts) for usePainelMetrics' nascente count. Double-prefix base http://localhost:3000/api/api/v1/...
Test helper: renderWithClient from components/cms/__tests__/test-utils.tsx. jsdom native DnD: fireEvent.dragStart(cardEl) then fireEvent.drop(colEl).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: painel-actions — pure drop/retry mapping + dispatch + mutation hook</name>
  <read_first>dashboard/components/mar-ready/MarReadyActions.tsx (the optimistic/rollback/invalidate pattern + explainError); dashboard/lib/destinos-api.ts; dashboard/lib/atrativos-api.ts; dashboard/lib/mar-ready-api.ts</read_first>
  <files>dashboard/lib/painel-actions.ts, dashboard/lib/__tests__/painel-actions.test.ts</files>
  <behavior>
    Pure mapping (RED first):
    - `mapDrop(card, target): DropAction | null` per the table above. Returns null for: same column (card.column === target), nascente/in_progress targets, and atrativo→dlq.
    - `mapRetry(card): DropAction | null` — destino → {kind:"reprocess"}, atrativo → null.
    - DropAction = { kind:"promote"|"descarte"|"reprocess"; entity:"destino"|"atrativo"; id:string }.
    Dispatch:
    - `runAction(a: DropAction): Promise<unknown>` — promote+destino→promoteDestino(id); promote+atrativo→promoteAtrativo(id); descarte+destino→descarteDestino(id); descarte+atrativo→descartarAtrativo(id); reprocess+destino→reprocessDestino(id); reprocess+atrativo→throws (must never be constructed).
    Hook:
    - `usePainelMutations(options)` returns `{ drop(card, target): void; retry(card): void }`. Internally a single useMutation over runAction with onError (rollback callback + toast.error(explainError)), onSuccess (toast.success), onSettled (invalidate ["destinos"], ["atrativos"], ["engine","status"]). When mapDrop/mapRetry returns null, do NOT call the mutation — call toast.error("Ação não disponível neste estágio") and a revert callback instead.
  </behavior>
  <action>Create `dashboard/lib/painel-actions.ts` (a TS module importing @tanstack/react-query + sonner; the hook uses React). Export `type DropAction`, `mapDrop`, `mapRetry`, `runAction`, and `usePainelMutations`. Port `explainError` from MarReadyActions (401→"Sessão expirada ou token inválido.", 409→"Ação não disponível neste estágio.", else err.message). The hook accepts `options: { onOptimistic?: (card, target) => void; onRevert?: () => void }` so the container owns the optimistic board state (override map). Flow for `drop(card, target)`: `const a = mapDrop(card, target); if (!a) { toast.error("Ação não disponível neste estágio"); return; } onOptimistic?.(card, target); mutation.mutate(a);` with mutation onError → { onRevert?.(); toast.error(explainError(err)); }, onSuccess → toast.success("Registro atualizado"), onSettled → invalidate the 3 keys. `retry(card)`: `const a = mapRetry(card); if (!a) { toast.error("Ação não disponível neste estágio"); return; } mutation.mutate(a);`. Write `painel-actions.test.ts` (pure, no MSW): assert mapDrop for every row of the table (destino→mar=promote/destino, atrativo→mar=promote/atrativo, both →descarte, destino→dlq=reprocess, atrativo→dlq=null, →nascente=null, same-column=null) and mapRetry(destino)=reprocess, mapRetry(atrativo)=null. Watch RED, then implement.</action>
  <verify>
    <automated>cd dashboard && bun run test lib/__tests__/painel-actions.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - mapDrop/mapRetry return exactly the allowed real actions and null everywhere else (no invented transitions).
    - runAction dispatches to the correct existing API fn per entity+kind.
    - Unmapped drops/retries never call a mutation.
  </acceptance_criteria>
  <done>Mapping + dispatch + hook built and unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PainelView container — compose + wire drag-drop + retry over real data</name>
  <read_first>dashboard/components/painel/PainelView.tsx (the 17-01 stub being replaced); dashboard/components/painel/PainelMetrics.tsx; dashboard/components/painel/PainelFilters.tsx; dashboard/components/painel/PainelBoard.tsx; dashboard/lib/painel-actions.ts; dashboard/mocks/handlers/destinos.ts (sampleDestinos default routings are dlq+mar — NO descarte card by default)</read_first>
  <files>dashboard/components/painel/PainelView.tsx, dashboard/components/painel/__tests__/PainelView.test.tsx</files>
  <behavior>
    - Loads usePainelBoard() (cards) + usePainelMetrics() (destino/atrativo EntityMetric + nascenteCount); holds `type: TypeFilter` and `ufs: string[]` state; computes displayed cards = filterCards(boardCards, {type, ufs}) with an optimistic-override map applied (cardId → column).
    - Renders <PainelMetrics destino={metrics.destino} atrativo={metrics.atrativo} />, <PainelFilters type/ufs + onChange />, <PainelBoard cards={scoped} nascenteCount={metrics.nascenteCount} onDropToColumn onCardDragStart onCardRetry />.
    - Tracks the currently dragged card (onCardDragStart); on onDropToColumn(target) calls usePainelMutations().drop(draggedCard, target).
    - Optimistic: onOptimistic sets override[card.id]=target (card visually moves); onRevert clears the override; onSettled invalidation refetches and clears.
    - Metric cards reflect the whole base (usePainelMetrics is not UF-scoped this slice); the UF scope filters the board only.
  </behavior>
  <action>Replace `dashboard/components/painel/PainelView.tsx` ("use client") stub with the container. State: `useState<TypeFilter>("all")`, `useState<string[]>([])` for ufs, `useState<Record<string,PainelColumnKey>>({})` for overrides, `useRef<PainelCard|null>` for the dragged card. `const { cards, isPending } = usePainelBoard(); const metrics = usePainelMetrics();` apply overrides: `const effective = cards.map(c => overrides[c.id] ? { ...c, column: overrides[c.id] } : c);` then `const scoped = filterCards(effective, { type, ufs });`. `const actions = usePainelMutations({ onOptimistic: (card,target)=>setOverrides(o=>({...o,[card.id]:target})), onRevert: ()=>setOverrides({}) });`. Render: header region with `<PainelMetrics destino={metrics.destino} atrativo={metrics.atrativo} />` + `<PainelFilters type={type} onTypeChange={setType} ufs={ufs} onUfsChange={setUfs} />`, then `<PainelBoard cards={scoped} nascenteCount={metrics.nascenteCount} isPending={isPending} onCardDragStart={(c)=>{dragged.current=c;}} onDropToColumn={(target)=>{ if(dragged.current){ actions.drop(dragged.current, target); dragged.current=null; } }} onCardRetry={(c)=>actions.retry(c)} />`. Use painel tokens / layout from the design (lines 99-172). Write `PainelView.test.tsx` with renderWithClient + server.use(destinosListSuccess(), atrativosListSuccess(), engineStatus(), destinoDescarteSuccess(), destinoReprocessSuccess(), destinoPromoteSuccess(), atrativoDescarteSuccess()): (a) board renders sample cards after load; (b) drag a destino card (the mar one, id 2222...) and drop on painel-col-descarte → a destinos descarte PATCH is observed (assert via success toast OR by intercepting the request) and the card optimistically appears under Descarte; (c) drag an atrativo card and drop on painel-col-dlq (Revisão) → NO PATCH fires and toast "Ação não disponível neste estágio" appears (spy on sonner or assert the rendered toast text); (d) M-2: seed a descarte destino so a ↺ Reprocessar button exists — `server.use(destinosListSuccess([{ id:"33333333-3333-3333-3333-333333333333", entity_type:"destination", uf:"MG", routing:"descarte", score:21.0, name:"Falha X", canonical_key:"mg:x:falha", validation_pending:false, mar_id:null, published_at:null }]))` then click `record-card-retry` and assert the `/destinos/.../reprocess` PATCH fires (destinoReprocessSuccess in the handler set). Keep assertions resilient (waitFor).</action>
  <verify>
    <automated>cd dashboard && bun run test components/painel/__tests__/PainelView.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - PainelView renders real board data + metrics and applies type + UF-scope filters to the board.
    - Mapped drop fires exactly one real mutation and optimistically moves the card; unmapped drop fires no request and toasts the unavailable message.
    - ↺ Reprocessar on a SEEDED descarte destino calls reprocess (the default sample has no descarte card — test (d) seeds one); invalidation hits destinos+atrativos+engine keys.
    - Nascente column count comes from usePainelMetrics().nascenteCount.
    - Existing dark routes + their suites remain green (full suite run).
  </acceptance_criteria>
  <done>Painel (Kanban) view fully wired to real data + real mutations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| drag gesture → mutation | A drop must only ever trigger a real, mapped backend transition |
| browser → BFF | Operator Bearer; service token server-side only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05-01 | Elevation of Privilege | drop→mutation mapping | mitigate | mapDrop is a closed allow-list; unmapped drops revert+toast and never call any endpoint — unit-tested for every column/entity pair |
| T-17-05-02 | Tampering | optimistic state | mitigate | onError fully rolls back the override; onSettled invalidates to reconcile with server truth |
| T-17-05-03 | Information Disclosure | mutation calls | mitigate | All mutations via lib/*-api (apiFetch/bff Bearer); no direct FastAPI; no service token in client |
| T-17-05-04 | Spoofing | atrativo promote-override | mitigate | promoteAtrativo (mar-ready) is the audited override; non-mar_ready cards 409 → revert+toast (no silent bypass of the §7.6 gate) |
| T-17-05-SC | Tampering | bun installs | mitigate | No new packages — native HTML5 DnD + existing TanStack/sonner |
</threat_model>

<verification>
- `cd dashboard && bun run test lib/__tests__/painel-actions.test.ts components/painel/__tests__/PainelView.test.tsx` passes.
- `cd dashboard && bun run test` full suite green (existing 25 suites / 164 tests untouched).
- Manual: at /painel, dragging a card to Sincronizado/Descarte/Revisão fires the real mutation; an invalid drop reverts with a toast.
</verification>

<success_criteria>
- Painel (Kanban) renders real data + truthful metrics; drag-drop fires ONLY real, mapped mutations; invalid drops revert with a toast; optimistic + invalidate + rollback all hold.
</success_criteria>

<output>
Create `.planning/phases/17-painel-brave-redesign-light-theme-single-shell-painel-kanban/17-05-SUMMARY.md` when done.
</output>
