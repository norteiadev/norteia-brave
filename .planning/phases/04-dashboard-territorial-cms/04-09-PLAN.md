---
phase: 04-dashboard-territorial-cms
plan: 09
type: execute
wave: 7
depends_on: ["04-02", "04-08"]
files_modified:
  - dashboard/app/funnels/page.tsx
  - dashboard/app/conversations/page.tsx
  - dashboard/components/funnels/FunnelChart.tsx
  - dashboard/components/conversations/ConversationList.tsx
  - dashboard/components/conversations/TranscriptPanel.tsx
  - dashboard/lib/funnels-api.ts
  - dashboard/lib/conversations-api.ts
  - dashboard/mocks/handlers/conversations.ts
  - dashboard/components/funnels/__tests__/FunnelChart.test.tsx
  - dashboard/components/conversations/__tests__/TranscriptPanel.test.tsx
autonomous: true
requirements: [DASH-05]
must_haves:
  truths:
    - "Funnels show destinos & atrativos counts by UF and source across stages (ingested → in_progress → mar/dlq/descarte)"
    - "Conversations show a WhatsApp transcript per atrativo read from the append-only conversation_message log (R2 Option B)"
    - "Transcripts never expose raw phone PII — only the masked phone is rendered"
  artifacts:
    - path: "dashboard/components/funnels/FunnelChart.tsx"
      provides: "Stage bars by UF/source via shadcn chart, fetched with useQuery"
      contains: "useQuery"
    - path: "dashboard/components/conversations/TranscriptPanel.tsx"
      provides: "Transcript bubbles (inbound/outbound) with masked phone label"
    - path: "dashboard/app/conversations/page.tsx"
      provides: "Conversations master-detail view"
  key_links:
    - from: "dashboard/components/funnels/FunnelChart.tsx"
      to: "GET /api/v1/funnels"
      via: "useQuery through the BFF"
      pattern: "useQuery"
    - from: "dashboard/components/conversations/TranscriptPanel.tsx"
      to: "GET /api/v1/conversations/{rio_id}"
      via: "useQuery through the BFF, masked phone only"
      pattern: "conversations"
---

<objective>
Funnels + Conversations **frontend half** (DASH-05) — build the funnels + conversations views over the endpoints delivered in 04-08. Funnels render stage bars by UF/source; conversations render a master-detail transcript per atrativo from the append-only `conversation_message` log, showing only the masked phone (R3 LGPD). All offline-tested via MSW.

Purpose: 04-08 delivered `GET /api/v1/funnels` and `GET /api/v1/conversations[/{rio_id}]`; this slice renders both end-to-end so DASH-05 is shippable and verifiable in the browser.
Output: funnels + conversations views + their api libs + MSW handlers + Vitest tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md
@.planning/phases/04-dashboard-territorial-cms/04-PATTERNS.md
@.planning/phases/04-dashboard-territorial-cms/04-RESEARCH.md
@.planning/phases/04-dashboard-territorial-cms/04-08-SUMMARY.md
@.planning/phases/04-dashboard-territorial-cms/04-07-SUMMARY.md
@.planning/phases/04-dashboard-territorial-cms/04-02-SUMMARY.md

<interfaces>
<!-- Endpoint shapes come from 04-08 (get_funnels + get_conversations/{rio_id}). -->
GET /api/v1/funnels?entity_type&uf&source → stage counts (ingested → in_progress → mar/dlq/descarte) grouped by uf/source.
GET /api/v1/conversations → list (rio_id, masked phone, last message, counts).
GET /api/v1/conversations/{rio_id} → transcript: messages ordered by created_at (direction outbound/inbound, role, content, extracted), phone masked. 404 on unknown rio_id.
All fetched through the BFF (relative `/api/...`) via TanStack Query `useQuery` — never FastAPI directly (plan 02 BFF + api-client).
shadcn `chart` wrapper (Recharts 3) for funnels; primary blue series. Each later slice owns its own mocks/handlers/<slice>.ts and applies it per-suite via server.use(...) (plan 02 convention) — add mocks/handlers/conversations.ts here.
UI-SPEC: masked phone label "telefone (minimizado)"; conversations empty "Sem conversas ainda"; funnels empty "Sem dados no período"; Geist Sans body; Skeleton loading; retry on error; 401 → login redirect.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Funnels + Conversations views (master-detail, masked transcript)</name>
  <files>dashboard/components/funnels/FunnelChart.tsx, dashboard/components/conversations/ConversationList.tsx, dashboard/components/conversations/TranscriptPanel.tsx, dashboard/lib/funnels-api.ts, dashboard/lib/conversations-api.ts, dashboard/app/funnels/page.tsx, dashboard/app/conversations/page.tsx, dashboard/mocks/handlers/conversations.ts, dashboard/components/funnels/__tests__/FunnelChart.test.tsx, dashboard/components/conversations/__tests__/TranscriptPanel.test.tsx</files>
  <read_first>
    - .planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md (funnel/conversation copy, masked-phone label, transcript text body, empty "Sem conversas ainda")
    - .planning/phases/04-dashboard-territorial-cms/04-RESEARCH.md (§5 Recharts funnel, useQuery)
    - .planning/phases/04-dashboard-territorial-cms/04-08-SUMMARY.md (the /funnels + /conversations response shapes delivered by 04-08)
    - dashboard/lib/api-client.ts (plan 02 — BFF fetch wrappers + query-key convention)
  </read_first>
  <action>Build `dashboard/components/funnels/FunnelChart.tsx` (Recharts 3 via shadcn chart — stage bars by UF/source, primary blue series; filters for entity_type/uf/source) fetching `GET /api/v1/funnels` via `useQuery` through the BFF, and wire `dashboard/app/funnels/page.tsx`. Build `dashboard/components/conversations/ConversationList.tsx` (master list of conversations) + `dashboard/components/conversations/TranscriptPanel.tsx` (transcript bubbles, inbound/outbound, Geist Sans body, extraction snapshot; phone shown only as the masked value labeled "telefone (minimizado)") fetching `GET /api/v1/conversations[/{rio_id}]`, and wire `dashboard/app/conversations/page.tsx` as master-detail. Empty states: funnels "Sem dados no período", conversations "Sem conversas ainda". Loading = Skeleton; error = retry; 401 → login redirect. Add `dashboard/lib/funnels-api.ts` + `dashboard/lib/conversations-api.ts` (query keys + fetchers through the BFF) and `dashboard/mocks/handlers/conversations.ts` (its own per-slice handler module, applied via server.use). Tests: FunnelChart renders the stage series; TranscriptPanel renders bubbles + asserts only the masked phone appears in the DOM (no raw e164); cover all MSW states (success/empty/error/401).</action>
  <verify>
    <automated>cd dashboard && bunx vitest run components/funnels components/conversations && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/components/funnels/FunnelChart.tsx` uses `useQuery` against `/api/v1/funnels`
    - `dashboard/components/conversations/TranscriptPanel.tsx` renders the masked phone label "minimizado" and no raw e164
    - `cd dashboard && bunx vitest run components/funnels components/conversations` exits 0 (all four MSW states)
    - `cd dashboard && bunx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The funnels (by UF/source) and conversations (masked transcript) views render end-to-end, all offline-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BFF → browser (conversations view) | only the masked phone may render; no raw e164 in the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-28 | Information Disclosure | transcript view PII | mitigate | TranscriptPanel renders only the masked phone (label "telefone (minimizado)"); test asserts no raw e164 string in the DOM; endpoint already masks (04-08) |
| T-04-29 | Spoofing | funnels/conversations fetches | mitigate | all fetches go through the BFF (plan 02), which validates the browser Bearer and 401→login redirect; views never call FastAPI directly |
| T-04-SC | Tampering | npm deps (none added) | accept | no new package installs; reuse shadcn chart + TanStack Query from plan 02 scaffold |
</threat_model>

<verification>
- `cd dashboard && bunx vitest run components/funnels components/conversations` exits 0.
- TranscriptPanel test asserts only the masked phone appears in the DOM (no raw e164).
- `cd dashboard && bunx tsc --noEmit` exits 0.
</verification>

<success_criteria>
DASH-05 frontend complete: funnels show destinos & atrativos by UF/source, conversations show masked WhatsApp transcripts from the append-only log (R2 Option B, served by 04-08), all proven offline with no raw PII reaching the browser.
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-territorial-cms/04-09-SUMMARY.md` when done.
</output>


## CONTEXT Decision Traceability

Implements CONTEXT.md decisions: D-03 (App Router views), D-04 (TanStack Query), D-05 (Recharts/shadcn), D-07 (Vitest + MSW)
