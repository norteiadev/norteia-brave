---
phase: 15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/tripadvisor/attractions_oa30.html
  - data/tripadvisor/README.md
autonomous: false
requirements: [TA-12]
must_haves:
  truths:
    - "A real captured AttractionsFusion -oa30- HTML page exists offline, scrubbed of cookies/PII"
    - "The fixture embeds the same 30 FlexCard sections the extractor must recover"
    - "No datadome/session cookie value or Set-Cookie header survives in the committed fixture"
  artifacts:
    - path: "tests/fixtures/tripadvisor/attractions_oa30.html"
      provides: "Wave-0 offline fixture that unblocks the embedded-JSON extractor (15-04)"
      contains: "WebPresentation_SingleFlexCardSection"
  key_links:
    - from: "tests/fixtures/tripadvisor/attractions_oa30.html"
      to: "brave/lanes/tripadvisor/client.py::_extract_sections_from_html (15-04)"
      via: "offline respx/fixture load in test_pagination.py"
      pattern: "attractions_oa30\\.html"
---

<objective>
Capture and scrub ONE real TripAdvisor AttractionsFusion HTML page (the `-oa30-` second
page of the all-Brazil g294280 listing) as a permanent offline test fixture. This is the
Wave-0 blocker: the embedded-JSON extractor (`_extract_sections_from_html`, plan 15-04)
cannot be written or unit-tested correctly until a real page is in hand, because the exact
`<script>` marker and JSON nesting are unknown until captured (RESEARCH A2, MEDIUM confidence).

Purpose: De-risk the single highest-uncertainty piece of the phase before any extractor code.
Output: `tests/fixtures/tripadvisor/attractions_oa30.html` (scrubbed) + a capture note appended
to `data/tripadvisor/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-CONTEXT.md
@.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-RESEARCH.md
@data/tripadvisor/README.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator captures + scrubs the real -oa30- HTML fixture</name>
  <read_first>
    - data/tripadvisor/README.md (existing operator cURL / `/browse` capture runbook from Phase 12)
    - .planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-CONTEXT.md (page URL template + embedded card markers)
  </read_first>
  <what-built>
    Nothing automated — this is a genuine human-only step. Automated browsers are DataDome-walled
    from this IP (Phase 12 locked finding); only a real operator browser session can fetch the page
    at HTTP 200 with the full datadome+session cookie jar.
  </what-built>
  <how-to-verify>
    1. In a real browser already carrying a valid TripAdvisor datadome+session jar (same session used
       for `POST /api/v1/tripadvisor/session`), navigate to:
       `https://www.tripadvisor.com/Attractions-g294280-Activities-a_allAttractions.true-oa30-Brazil.html`
    2. Confirm the page renders ~30 attraction cards (page 2 of the all-Brazil listing). Save the full
       rendered HTML (View Source / Save Page As → HTML only, ~1.5 MB).
    3. SCRUB before saving into the repo: remove every `Set-Cookie` header artifact, any `datadome=`
       value, any `__Secure-`/`sessionid=`/auth token string, and any operator IP/email. The fixture
       must contain ONLY the public listing markup + embedded card JSON.
    4. Save the scrubbed file to `tests/fixtures/tripadvisor/attractions_oa30.html`.
    5. Append a short "Wave-0 fixture capture" note to `data/tripadvisor/README.md` recording that the
       capture MUST be from a real HTML attractions-page navigation (not only a GraphQL XHR) — RESEARCH
       Pitfall 2 / A3: the HTML DataDome surface can differ from the GraphQL one.
  </how-to-verify>
  <acceptance_criteria>
    - `tests/fixtures/tripadvisor/attractions_oa30.html` exists and is > 100 KB.
    - `grep -q 'WebPresentation_SingleFlexCardSection' tests/fixtures/tripadvisor/attractions_oa30.html` succeeds.
    - `grep -q 'cardTitle' tests/fixtures/tripadvisor/attractions_oa30.html` succeeds.
    - `grep -Eiq 'datadome=|set-cookie|__Secure-|sessionid=' tests/fixtures/tripadvisor/attractions_oa30.html` returns NO match (exit 1) — secrets scrubbed.
  </acceptance_criteria>
  <verify>
    <automated>test -s tests/fixtures/tripadvisor/attractions_oa30.html && grep -q 'WebPresentation_SingleFlexCardSection' tests/fixtures/tripadvisor/attractions_oa30.html && grep -q 'cardTitle' tests/fixtures/tripadvisor/attractions_oa30.html && ! grep -Eiq 'datadome=|set-cookie|__secure-|sessionid=' tests/fixtures/tripadvisor/attractions_oa30.html && echo FIXTURE_OK</automated>
  </verify>
  <resume-signal>Type "captured" once the scrubbed fixture is saved, or describe any blocker (e.g. HTML page 403s despite a green session — that is RESEARCH Pitfall 2 and must be recorded).</resume-signal>
  <done>Scrubbed fixture present with FlexCard markers and zero secret strings; README capture note added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → repo | A captured live page can carry secret cookies/PII into version control if not scrubbed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | committed HTML fixture | mitigate | Mandatory scrub of datadome/Set-Cookie/__Secure-/sessionid + the automated grep gate in `<verify>` blocks commit of any secret-bearing fixture. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
Run the `<verify>` grep gate. Fixture must exist, carry FlexCard markers, and carry no secret strings.
</verification>

<success_criteria>
Offline fixture committed, scrubbed, and structurally proven to embed the 30 FlexCard sections the
extractor will recover in 15-04.
</success_criteria>

<output>
Create `.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-01-SUMMARY.md` when done
</output>
