---
phase: 04-dashboard-territorial-cms
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - dashboard/app/dlq/page.tsx
  - dashboard/components/dlq/QueueList.tsx
  - dashboard/components/dlq/ReviewPanel.tsx
  - dashboard/components/dlq/ScoreBreakdownPanel.tsx
  - dashboard/components/dlq/StatusBadge.tsx
  - dashboard/components/dlq/dlq-actions.ts
  - dashboard/lib/dlq-api.ts
  - dashboard/mocks/handlers/dlq.ts
  - dashboard/components/dlq/__tests__/QueueList.test.tsx
  - dashboard/components/dlq/__tests__/ReviewPanel.test.tsx
  - dashboard/components/dlq/__tests__/ScoreBreakdownPanel.test.tsx
autonomous: true
requirements: [DASH-01]
must_haves:
  truths:
    - "Implements D-04 (TanStack Query mutations + optimistic re-score + queue invalidation) for the DLQ review surface"
    - "The DLQ review queue shows a state-filtered list (BA/RJ/SP/SC/CE/PE first) beside a detail panel"
    - "The detail panel renders Nascente payload + Rio normalized + §7.6 per-criterion breakdown + signals + WhatsApp log"
    - "The operator can approve (validar e publicar), reject (→descarte), edit→re-score (salvar e reprocessar), reprocess, and validate batch-by-state"
    - "Editing triggers a re-score and the queue refetches"
    - "Every view state (success/empty/error/401) is exercised offline with MSW"
  artifacts:
    - path: "dashboard/components/dlq/ScoreBreakdownPanel.tsx"
      provides: "The signature §7.6 per-criterion bars (origem/completude/corroboração/atualidade/validação-humana)"
      contains: "origem"
    - path: "dashboard/components/dlq/QueueList.tsx"
      provides: "State-filtered master list with batch-by-state selection"
    - path: "dashboard/components/dlq/ReviewPanel.tsx"
      provides: "Master-detail review panel reused by DLQ + gate"
  key_links:
    - from: "dashboard/components/dlq/dlq-actions.ts"
      to: "existing PATCH /api/v1/dlq/{id}/validate|descarte|reprocess + POST validate-batch"
      via: "useMutation through the BFF, onSettled invalidateQueries(['dlq'])"
      pattern: "invalidateQueries"
    - from: "dashboard/components/dlq/ReviewPanel.tsx"
      to: "GET /api/v1/dlq/{rio_id}"
      via: "useQuery through the BFF"
      pattern: "useQuery"
---

<objective>
DLQ slice — frontend half (DASH-01), the highest-value surface. Build the master-detail DLQ review queue per UI-SPEC D-06: a state-filtered `QueueList` (BA/RJ/SP/SC/CE/PE prioritized to mirror the Phase 2 steward order) beside a `ReviewPanel` showing Nascente payload, Rio normalized data, the signature `ScoreBreakdownPanel` (§7.6 per-criterion bars), signals, and the WhatsApp log. Actions: approve, reject, edit→re-score, reprocess, and batch-by-state — all via the existing `dlq.py` PATCH/POST mutation endpoints through the BFF (no new mutations).

Purpose: Delivers DASH-01 end-to-end — the operator works the DLQ batch-by-state with per-criterion explainability, editing triggers a re-score, queue refetches.
Output: The `/dlq` view + the reusable QueueList/ReviewPanel/ScoreBreakdownPanel/StatusBadge components + offline Vitest+MSW tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md
@.planning/phases/04-dashboard-territorial-cms/04-RESEARCH.md
@.planning/phases/04-dashboard-territorial-cms/04-02-SUMMARY.md
@.planning/phases/04-dashboard-territorial-cms/04-03-SUMMARY.md

<interfaces>
<!-- Backing endpoints. Mutations are existing (call via BFF); detail is from plan 03. -->
Existing (reuse via BFF): GET /api/v1/dlq?uf&entity_type&limit (list), PATCH /api/v1/dlq/{id}/validate (→re-score→push if Mar), PATCH /api/v1/dlq/{id}/descarte (reject), PATCH /api/v1/dlq/{id}/reprocess, POST /api/v1/dlq/validate-batch?uf&entity_type&limit (batch-by-state, limit≤1000)
New (plan 03): GET /api/v1/dlq/{rio_id} → {id, routing, sub_state, dlq_reason, score, score_version, score_breakdown(§7.6), normalized, nascente_payload, signals, whatsapp_log}
Batch-by-state = UF filter + multi-select → POST validate-batch?uf= (RESEARCH §5)
UI-SPEC copy: "Validar e publicar", "Validar lote por estado", "Salvar e reprocessar", "Reprocessar", reject AlertDialog "Rejeitar registro?", batch confirm "Validar {n} registros de {UF} em lote?", toasts state-transition-explicit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScoreBreakdownPanel + StatusBadge + ReviewPanel (detail render)</name>
  <files>dashboard/components/dlq/ScoreBreakdownPanel.tsx, dashboard/components/dlq/StatusBadge.tsx, dashboard/components/dlq/ReviewPanel.tsx, dashboard/mocks/handlers/dlq.ts, dashboard/components/dlq/__tests__/ScoreBreakdownPanel.test.tsx, dashboard/components/dlq/__tests__/ReviewPanel.test.tsx</files>
  <read_first>
    - .planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md (ScoreBreakdownPanel + ReviewPanel + StatusBadge bespoke component specs; typography/color/mono-data rules; PT-BR copy)
    - .planning/phases/04-dashboard-territorial-cms/04-03-SUMMARY.md (detail endpoint shape)
    - dashboard/lib/api-client.ts (BFF fetch wrappers from plan 02)
  </read_first>
  <action>Build `dashboard/components/dlq/ScoreBreakdownPanel.tsx` — the signature component (UI-SPEC): the five §7.6 criteria (origem 30% · completude 20% · corroboração 20% · atualidade 15% · validação-humana 15%) as labeled horizontal bars using shadcn `Progress` + `Label`, each showing weight, raw value, and weighted contribution, with the total score as the Display-size readout; threshold cap color per bar (green ≥85 / amber 51–84.9 / red ≤50) from the semantic status colors, mono font for numerals. Build `dashboard/components/dlq/StatusBadge.tsx` — routing/sub_state badge using the semantic status colors (Mar=green, DLQ=amber, descarte=red). Build `dashboard/components/dlq/ReviewPanel.tsx` — the master-detail right pane: fetches `GET /api/v1/dlq/{rio_id}` via TanStack Query through the BFF and renders Nascente payload (Geist Mono JSON), Rio normalized, the `ScoreBreakdownPanel`, signals, and the WhatsApp log; loading = shadcn `Skeleton` (UI-SPEC), error = "Não foi possível carregar" + "Tentar novamente". Keep `ReviewPanel` action-agnostic (actions injected) so plan 05's gate reuses it. Tests: render `ScoreBreakdownPanel` asserts five criteria labels + total; `ReviewPanel` asserts success/empty/error/401 states via MSW.</action>
  <verify>
    <automated>cd dashboard && bunx vitest run components/dlq/__tests__/ScoreBreakdownPanel.test.tsx components/dlq/__tests__/ReviewPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/components/dlq/ScoreBreakdownPanel.tsx` renders all five §7.6 criteria (grep finds origem, completude, corroboração, atualidade)
    - `dashboard/components/dlq/ReviewPanel.tsx` uses `useQuery` against the DLQ detail endpoint
    - ScoreBreakdownPanel test asserts the total score readout + five bars
    - ReviewPanel test covers success/empty/error/401 (MSW) and exits 0
  </acceptance_criteria>
  <done>The signature §7.6 panel and the reusable detail review panel render the full DLQ detail with all four MSW states covered.</done>
</task>

<task type="auto">
  <name>Task 2: QueueList (batch-by-state) + dlq-actions + /dlq page wiring</name>
  <files>dashboard/components/dlq/QueueList.tsx, dashboard/components/dlq/dlq-actions.ts, dashboard/lib/dlq-api.ts, dashboard/app/dlq/page.tsx, dashboard/components/dlq/__tests__/QueueList.test.tsx</files>
  <read_first>
    - .planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md (QueueList batch-by-state, copy table, AlertDialog destructive confirmations, toasts)
    - .planning/phases/04-dashboard-territorial-cms/04-RESEARCH.md (§5 TanStack Table v8 + useMutation onMutate optimistic + onSettled invalidateQueries(['dlq']))
    - dashboard/components/dlq/ReviewPanel.tsx (from Task 1)
    - dashboard/app/api/[...path]/route.ts (BFF — mutations go through here)
  </read_first>
  <action>Build `dashboard/components/dlq/QueueList.tsx` — the master list using TanStack Table v8 + shadcn `table`: a UF filter defaulting to the BA/RJ/SP/SC/CE/PE order (UI-SPEC D-06), row selection for batch, 36px dense rows, mono `canonical_key`/`score`, `StatusBadge` per row; selecting a row drives the `ReviewPanel`. Build `dashboard/components/dlq/dlq-actions.ts` with TanStack `useMutation` hooks calling the existing endpoints through the BFF: validate ("Validar e publicar" → onMutate optimistic, onSettled `invalidateQueries(['dlq'])`), descarte (reject, behind a shadcn `AlertDialog` "Rejeitar registro?" with red confirm), reprocess ("Reprocessar"), edit→re-score ("Salvar e reprocessar" — inline edit then validate, refetch), and validate-batch ("Validar lote por estado" with the selected UF+count, behind the "Validar {n} registros de {UF} em lote?" confirm). Emit the state-transition-explicit toasts (sonner) from UI-SPEC ("Registro validado → Mar", etc.). Build `dashboard/lib/dlq-api.ts` (query keys + typed fetchers). Wire `dashboard/app/dlq/page.tsx` as the master-detail layout (QueueList ↔ ReviewPanel, xl gap per UI-SPEC). Tests: QueueList renders the BA-first ordering, batch selection enables "Validar lote", and an approve mutation invalidates+refetches — all offline via MSW (success/empty/error/401).</action>
  <verify>
    <automated>cd dashboard && bunx vitest run components/dlq && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/components/dlq/QueueList.tsx` orders UF filter BA/RJ/SP/SC/CE/PE first
    - `grep -rn "invalidateQueries" dashboard/components/dlq/dlq-actions.ts` returns a match
    - reject uses a shadcn `AlertDialog`; batch uses the confirm copy
    - `cd dashboard && bunx vitest run components/dlq` exits 0 (all four MSW states)
    - `cd dashboard && bunx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The DLQ master-detail view works batch-by-state with approve/reject/edit→re-score/reprocess via existing endpoints, edits trigger re-score + refetch, all offline-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → DLQ mutations | approve/validate writes to production (sets validação-humana=100 → push); flows browser→BFF→FastAPI with Bearer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Elevation of Privilege | DLQ mutation via BFF | mitigate | mutations forwarded only with a valid browser Bearer (plan 02 BFF) → either-or Bearer/steward gate (plan 01); no client-side bypass |
| T-04-13 | Tampering | batch validate blast radius | mitigate | batch confirm AlertDialog ("Validar {n} registros de {UF}") before the high-impact POST; limit≤1000 enforced server-side |
| T-04-14 | Repudiation | who validated what | accept | per-actor audit deferred (single operator token this milestone, CONTEXT deferred); existing AuditLog records the action |
| T-04-SC | Tampering | npm deps (tanstack table, already pinned) | accept | versions pinned + bun.lock committed in plan 02; only UI-SPEC-allowlisted shadcn blocks |
</threat_model>

<verification>
- `cd dashboard && bunx vitest run components/dlq` exits 0 (success/empty/error/401 for queue + panel + score breakdown).
- `cd dashboard && bunx tsc --noEmit` exits 0.
</verification>

<success_criteria>
DASH-01 complete: the DLQ review queue shows Nascente payload + Rio data + §7.6 per-criterion score + signals + WhatsApp log, supports approve/reject/edit/reprocess with batch-by-state, edit triggers re-score, all components tested offline.
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-territorial-cms/04-04-SUMMARY.md` when done.
</output>


## CONTEXT Decision Traceability

Implements CONTEXT.md decisions: D-04 (TanStack Query mutations/optimistic re-score), D-05 (shadcn ScoreBreakdownPanel), D-06 (master-detail review UX, batch-by-state BA/RJ/SP/SC/CE/PE)
