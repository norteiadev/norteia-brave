---
phase: 17.1-painel-brave-remaining-pages-real-backend-slice-2
plan: 06
type: execute
wave: 2
depends_on: ["17.1-03"]
files_modified:
  - dashboard/lib/engine-api.ts
  - dashboard/lib/painel-data.ts
  - dashboard/components/painel/PainelBoard.tsx
  - dashboard/lib/painel-actions.ts
  - dashboard/lib/__tests__/painel-data.test.ts
  - dashboard/lib/__tests__/painel-actions.test.ts
autonomous: true
requirements: [UI-PAINEL-2]

must_haves:
  truths:
    - "The board renders 6 stage columns: nascente, rio, whatsapp, mar, dlq, falha"
    - "Atrativo cards in sub_state aguardando_consulta_whatsapp bucket into the whatsapp column"
    - "falha cards come from GET /api/v1/failures and are real (draggable)"
    - "mapDrop maps the new full-pipeline edges to the real transition endpoint and returns null (revert+toast) for any unmapped edge — never an invented call"
    - "mar → * and into-nascente drops always revert + toast (no endpoint call)"
  artifacts:
    - path: "dashboard/lib/painel-data.ts"
      provides: "6-column model + whatsapp/falha sourcing"
      contains: "whatsapp"
    - path: "dashboard/lib/painel-actions.ts"
      provides: "transition DropAction kind + extended mapDrop allow-list mirroring the server"
      contains: "transition"
    - path: "dashboard/lib/engine-api.ts"
      provides: "transition() + injectTASession() + fetchFailures() clients"
      contains: "injectTASession"
  key_links:
    - from: "dashboard/lib/painel-actions.ts"
      to: "dashboard/lib/engine-api.ts transition()"
      via: "runAction transition branch"
      pattern: "transition"
    - from: "dashboard/lib/painel-data.ts"
      to: "GET /api/v1/failures"
      via: "fetchFailures → falha cards"
      pattern: "failures"
---

<objective>
Upgrade the board data layer from 5 to 6 columns and extend the client security allow-list for the
full-pipeline drag: add the typed transition/injectTASession/fetchFailures clients, source the
whatsapp column from atrativo sub_state and the falha column from GET /api/v1/failures, and extend
mapDrop to emit the new transition edges that mirror the server allow-list (plan 03) — while keeping
every unmapped drop a revert+toast no-op.

Purpose: this is the core sync surface. The board IS the sync; mapDrop is the client security boundary
that must agree edge-for-edge with the server _ALLOWED_EDGES.
Output: engine-api.ts (3 clients), painel-data.ts (6 columns), PainelBoard.tsx (dots),
painel-actions.ts (transition edges) + extended unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-CONTEXT.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-RESEARCH.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-PATTERNS.md
@dashboard/lib/painel-data.ts
@dashboard/lib/painel-actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: engine-api.ts — transition() + injectTASession() + fetchFailures() clients</name>
  <files>dashboard/lib/engine-api.ts</files>
  <read_first>
    - dashboard/lib/engine-api.ts:84-97 (startEngine POST+JSON body), :99 (stopEngine), :112-125 (TASessionStatus + fetchTASessionStatus), :28 (DEPTH_LABELS), :47 (EnginePipelineCounts)
    - dashboard/lib/api-client.ts:34-48 (bff, ApiError.status)
    - brave/api/routers/cms.py + atrativos.py (plan 03 — transition endpoint paths + TransitionBody shape)
    - brave/api/routers/tripadvisor_session.py:51-101 (SessionInjectBody extra=forbid — cookies/query_ids/user_agent/acquired_at; 422 invalid_session / 503 canary_unverified)
    - brave/api/routers/workers.py (GET /api/v1/failures — PoisonQuarantine response shape to mirror for falha cards)
  </read_first>
  <action>
    Extend dashboard/lib/engine-api.ts mirroring startEngine (:84-97):
    - transition(entity: "destino"|"atrativo", rioId: string, body: { to: string; expected: string })
      → apiFetch PATCH "api/v1/{entity}s/{rioId}/transition" with JSON body (matches plan-03 TransitionBody).
    - injectTASession(body: { cookies; query_ids; user_agent; acquired_at }) → apiFetch POST
      "api/v1/tripadvisor/session" (strict SessionInjectBody shape). Surface ApiError.status so callers
      can distinguish 422 invalid_session vs 503 canary_unverified.
    - fetchFailures() → apiFetch GET "api/v1/failures"; declare a FailureItem interface mirroring the
      workers.py response (id, entity_type, uf, name, error/reason) for falha-card projection.
    Add any new query keys to the engineKeys factory. Relative api/v1/... form throughout.
  </action>
  <acceptance_criteria>
    - `grep -c "injectTASession\|fetchFailures\|export function transition" dashboard/lib/engine-api.ts` returns ≥3
    - transition() targets api/v1/destinos|atrativos/{id}/transition
    - `cd dashboard && bunx tsc --noEmit` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bunx tsc --noEmit</automated>
  </verify>
  <done>engine-api exposes typed transition, injectTASession (with 422/503 distinguishable), and fetchFailures clients.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: painel-data.ts — 6-column model + whatsapp/falha sourcing</name>
  <files>dashboard/lib/painel-data.ts, dashboard/components/painel/PainelBoard.tsx, dashboard/lib/__tests__/painel-data.test.ts</files>
  <read_first>
    - dashboard/lib/painel-data.ts (full file — PainelColumnKey :37-42, COLUMN_DEFS :76-82, KNOWN_COLUMNS/routingToColumn :91-105, toPainelCards :124-157, envelope-total metrics :283-294)
    - dashboard/components/painel/PainelBoard.tsx:34-40 (COLUMN_DOT map), :57-60 (nascente count-only)
    - dashboard/lib/atrativos-api.ts (AtrativoListItem — confirm sub_state field; the cms list filters by it, cms.py:486)
    - dashboard/lib/engine-api.ts (fetchFailures + FailureItem from Task 1)
    - 17.1-RESEARCH.md Finding 5 gotcha 2-4 (whatsapp from sub_state; falha sourcing; envelope total)
  </read_first>
  <behavior>
    - PainelColumnKey gains "rio", "whatsapp", "falha" (total 6). COLUMN_DEFS = 6 pt-BR labels in order: Nascente, Rio · validação, WhatsApp · contato, Mar · publicado, DLQ · revisão, Falha.
    - routingToColumn maps in_progress→rio (not "in_progress"); mar/dlq/descarte unchanged; unknown→nascente.
    - toPainelCards buckets an atrativo into whatsapp when sub_state === "aguardando_consulta_whatsapp" (read sub_state from AtrativoListItem), else by routing.
    - falha cards are projected from fetchFailures() FailureItem[] with column="falha" (real, draggable).
    - metrics still read the envelope total, never array length.
  </behavior>
  <action>
    Extend dashboard/lib/painel-data.ts to the 6-column model: add "rio"/"whatsapp"/"falha" to
    PainelColumnKey; rewrite COLUMN_DEFS to the 6 ordered pt-BR labels above; update KNOWN_COLUMNS +
    routingToColumn so in_progress maps to "rio". Add a toPainelCards signature/overload (or a new
    projector) that also accepts atrativo sub_state and failures: bucket atrativos in
    aguardando_consulta_whatsapp into "whatsapp"; project FailureItem[] (from fetchFailures) into
    PainelCards with column="falha", error set from the failure reason. Update usePainelBoard to also
    query fetchFailures and pass failures into the projector. Keep the LGPD allow-list (no phone fields)
    and the envelope-total metric reads intact. Add COLUMN_DOT entries for rio/whatsapp/falha in
    PainelBoard.tsx:34-40.
    Extend dashboard/lib/__tests__/painel-data.test.ts: assert 6 columns; assert in_progress→rio; assert
    an atrativo with sub_state aguardando_consulta_whatsapp buckets into whatsapp; assert a FailureItem
    projects into a falha card; assert no phone field leaks into a PainelCard.
  </action>
  <acceptance_criteria>
    - `grep -c '"rio"\|"whatsapp"\|"falha"' dashboard/lib/painel-data.ts` returns ≥3
    - COLUMN_DEFS has exactly 6 entries
    - `grep -c "aguardando_consulta_whatsapp" dashboard/lib/painel-data.ts` returns ≥1
    - `cd dashboard && bun run test painel-data` passes
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bun run test painel-data</automated>
  </verify>
  <done>The board model is 6 columns; whatsapp sources from atrativo sub_state and falha from GET /api/v1/failures; metrics still read the envelope total; unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: painel-actions.ts — transition DropAction edges (security boundary)</name>
  <files>dashboard/lib/painel-actions.ts, dashboard/lib/__tests__/painel-actions.test.ts</files>
  <read_first>
    - dashboard/lib/painel-actions.ts (full file — DropAction union :40-44, mapDrop :54-77, mapRetry :80-84, runAction :91-107, usePainelMutations :132-173)
    - dashboard/lib/engine-api.ts transition() (Task 1 output)
    - brave/api/routers/cms.py _ALLOWED_EDGES (plan 03 — the server allow-list this MUST mirror edge-for-edge)
    - dashboard/lib/__tests__/painel-actions.test.ts:46-52,119-124 (ALL_COLUMNS exhaustive null-mapping test)
    - 17.1-RESEARCH.md Finding 4 (NEW edges) + Finding 5 gotcha 5 (drawer "Etapa do pipeline" routes through the SAME map)
  </read_first>
  <behavior>
    - DropAction union gains { kind: "transition"; to: PainelColumnKey; expected: PainelColumnKey; entity; id }.
    - mapDrop emits a transition action for exactly the edges in the server _ALLOWED_EDGES (e.g. atrativo dlq→rio reopen, force in_progress/rio→dlq), and KEEPS returning null for mar→*, into-nascente, same-column, and any edge not in the server allow-list.
    - runAction gains a transition branch calling engine-api transition(); null mappings never call any endpoint.
  </behavior>
  <action>
    Extend dashboard/lib/painel-actions.ts: widen the DropAction union with the transition kind; extend
    mapDrop to emit { kind:"transition", to, expected: card.column, entity, id } for the new edges that
    mirror the plan-03 server _ALLOWED_EDGES EXACTLY (document the paired-contract in a comment), while
    every edge NOT in the server allow-list — notably mar→*, into-nascente, same-column — still returns
    null (revert + toast, never an unmapped call). Add a runAction transition branch calling engine-api
    transition(entity, id, { to, expected }). Keep usePainelMutations invalidating
    ["destinos"]/["atrativos"]/["engine","status"] (+ no new keys needed). The drawer "Etapa do
    pipeline" select must route through this same mapDrop (gotcha 5) — do not add a parallel path.
    Extend dashboard/lib/__tests__/painel-actions.test.ts for the 6-column model: prove the exhaustive
    invariant that every (column, target) pair NOT in the allow-list returns null (no unmapped path ever
    called — T-17-05-01 lineage), and that each new allowed edge produces the correct transition action.
  </action>
  <acceptance_criteria>
    - `grep -c '"transition"' dashboard/lib/painel-actions.ts` returns ≥2 (union + runAction branch)
    - test asserts every mar→X drop returns null (no transition action)
    - test asserts an atrativo dlq→rio drop returns a transition action
    - `cd dashboard && bun run test painel-actions` passes
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bun run test painel-actions</automated>
  </verify>
  <done>mapDrop maps the new full-pipeline edges to the real transition endpoint and mirrors the server allow-list; every unmapped edge (incl. mar→*) still reverts+toasts; exhaustive unit test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (mapDrop) | the client security allow-list that turns a drop into ONE real call or nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-06-01 | Tampering | client invents an unmapped transition | mitigate | mapDrop returns null for any edge absent from the server allow-list; exhaustive test proves no unmapped path is called |
| T-17.1-06-02 | Integrity | backward drop out of mar depublishes | mitigate | mar→* always null (revert+toast); server also 409s (plan 03) |
| T-17.1-06-03 | Info disclosure | LGPD phone into board/falha cards | mitigate | toPainelCards allow-list excludes phone; falha cards read failure reason only |
| T-17.1-06-04 | Info disclosure | TA cookies in injectTASession logs | mitigate | client posts to BFF; values never logged client-side; backend already guards (tripadvisor_session.py) |
| T-17.1-06-SC | Tampering | npm installs | accept | no installs |
</threat_model>

<verification>
- `cd dashboard && bun run test painel-data painel-actions` green.
- Exhaustive null-mapping test passes for the 6-column model (no unmapped path callable).
</verification>

<success_criteria>
The board is 6 columns with real whatsapp + falha sourcing; the client allow-list mirrors the server transition allow-list and keeps mar→* a revert+toast no-op; transition/injectTASession/fetchFailures clients exist; unit tests green.
</success_criteria>

<output>
Create `.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-06-SUMMARY.md` when done.
</output>
