---
phase: "07"
plan: "05"
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
files_modified:
  - scripts/loadtest_destinos_atrativos.py
autonomous: true
requirements:
  - PLACE-07
  - PLACE-08

must_haves:
  truths:
    - "D-05: scripts/loadtest_destinos_atrativos.py exists — mirrors ingest_destinos.py structure (from __future__ import annotations, asyncio, os, sys, sqlalchemy, main(), if __name__ == __main__)"
    - "D-05: harness step 1 ingests destinos via MturSeedIngest.produce(uf) for chosen UF(s), step 2 promotes up to 10 DLQ destinos to Mar via validate_and_promote_rio (imported from brave.core.dlq.service), step 3 calls DiscoveryAgent.produce_for_destino(mar_record, target_count=10) for each promoted Mar destino, step 4 prints summary (MarRecord count + RioRecord count per parent_mar_id)"
    - "D-05: harness does NOT auto-reset the DB — it prints a WARNING if active Mar destinos for the target UF(s) already exist, instructing the operator to run TRUNCATE manually before a fresh baseline run"
    - "D-05: acceptance bar printed in summary — 10 mar_records (entity_type=destination) and ≥10 rio_records (entity_type=attraction) per parent_mar_id"
    - "D-07: harness imports and uses RealPlacesClient + RealLLMClient (real externals) guarded by BRAVE_PLACES_API_KEY + BRAVE_LLM_OPENROUTER_API_KEY env vars; prints operator note about raising BRAVE_LLM_USD_DAILY_BUDGET if CostGuardError fires"

  artifacts:
    - path: scripts/loadtest_destinos_atrativos.py
      provides: "End-to-end real load-test harness (10 destinos × ≥10 atrativos)"

  key_links:
    - from: scripts/loadtest_destinos_atrativos.py
      to: brave.core.dlq.service.validate_and_promote_rio
      via: "direct import + call in step 2"
      pattern: "validate_and_promote_rio"
    - from: scripts/loadtest_destinos_atrativos.py
      to: brave.lanes.atrativos.discovery_agent.DiscoveryAgent.produce_for_destino
      via: "direct call in step 3"
      pattern: "produce_for_destino"
    - from: scripts/loadtest_destinos_atrativos.py
      to: "DB summary query"
      via: "select MarRecord count + RioRecord count grouped by parent_mar_id"
      pattern: "parent_mar_id"
---

<objective>
Deliver the load-test harness that drives the full real end-to-end flow — ingest destinos, promote 10 to Mar, run targeted atrativos discovery for each, and print a summary — so an operator can verify the 10×10 acceptance bar with live credentials.

Purpose: Plans 07-01/02/03 fix the underlying bugs. This harness exercises all four fixes together against a real DB + real Google Places + real DeepSeek. It is the operator's acceptance gate for Phase 7. It must NOT auto-truncate the DB (destructive — operator resets manually) but must warn clearly when the DB is non-clean.

Output: scripts/loadtest_destinos_atrativos.py mirroring scripts/ingest_destinos.py structure. Operator runs: `set -a; source .env; set +a && .venv/bin/python -m scripts.loadtest_destinos_atrativos BA`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-RESEARCH.md
@.planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-PATTERNS.md
@scripts/ingest_destinos.py
@brave/core/dlq/service.py

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

From scripts/ingest_destinos.py (structure to mirror exactly):
- main() with sys.argv[1:] or ["BA"] default UF list
- BRAVE_DB_URL env check → sys.exit(1) if missing
- create_engine + sessionmaker pattern
- asyncio.run() for async tasks
- Routing summary at the end via select(RioRecord.uf, RioRecord.routing, func.count(RioRecord.id))

From brave/core/dlq/service.py (created in Plan 07-02):
```python
def validate_and_promote_rio(
    session: Session,
    rio: RioRecord,
    config: ScoreConfig | None = None,
) -> MarRecord | None: ...
```

From brave/core/models.py (MarRecord):
- MarRecord.entity_type == "destination" (for destino count)
- MarRecord.superseded_by_id.is_(None) (active only)
- RioRecord.entity_type == "attraction" (for atrativo count)
- RioRecord.parent_mar_id (str UUID, for grouping)
- RioRecord.routing == "rio" or "dlq" (atrativos land in rio/dlq after process_nascente_record)

From brave/lanes/atrativos/discovery_agent.py (DiscoveryAgent):
```python
class DiscoveryAgent:
    def __init__(self, places_client, llm_client, session, config): ...
    async def produce_for_destino(self, parent_mar: MarRecord, target_count: int = 10) -> int: ...
```

From brave/clients/places.py (RealPlacesClient):
```python
class RealPlacesClient:
    def __init__(self, api_key: str, ibge_lookup: dict | None = None) -> None: ...
```
ibge_lookup should be built from MturClient.fetch_municipalities() (pass all UFs or all rows).

From brave/clients/llm.py (RealLLMClient):
```python
class RealLLMClient:
    def __init__(self, config: LLMConfig) -> None: ...
```

From brave/config/settings.py:
- AppConfig.run_real_externals (must be True for real clients)
- LLMConfig (usd_daily_budget defaults to 10.0)
- ScoreConfig
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/loadtest_destinos_atrativos.py</name>
  <files>scripts/loadtest_destinos_atrativos.py</files>

  <read_first>
    - scripts/ingest_destinos.py (full file — already in context; mirror structure exactly)
    - .planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-PATTERNS.md §scripts/loadtest_destinos_atrativos.py (full harness skeleton, warning pattern, summary pattern)
    - .planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-RESEARCH.md §RQ-4 (validate_and_promote_rio usage), §RQ-3 (produce_for_destino), §RQ-7 (cost guard note for operator)
    - brave/clients/mtur.py (MturClient.fetch_municipalities signature — needed to build ibge_lookup)
  </read_first>

  <action>
    Create scripts/loadtest_destinos_atrativos.py. Mirror the structure of scripts/ingest_destinos.py exactly.

    Module docstring:
    "Real end-to-end load-test harness: ingest destinos → promote 10 to Mar → targeted atrativos discovery.

    Drives the full real flow against a live DB + Google Places + DeepSeek LLM.
    Acceptance bar: 10 Mar destination records + ≥10 Rio attraction records per parent_mar_id.

    Prerequisites:
      BRAVE_DB_URL         — PostgreSQL connection string
      BRAVE_PLACES_API_KEY — Google Places API (New) key
      BRAVE_LLM_OPENROUTER_API_KEY — OpenRouter key for DeepSeek extraction
      RUN_REAL_EXTERNALS=true — enables real client construction
      BRAVE_LLM_USD_DAILY_BUDGET=10.0 (default) — raise to 50.0 if CostGuardError fires

    Usage:
      set -a; source .env; set +a
      .venv/bin/python -m scripts.loadtest_destinos_atrativos BA
      # defaults to BA if no UF given

    WARNING: This script writes to the real database.
    For a clean baseline run TRUNCATE manually first:
      TRUNCATE nascente_records, rio_records, mar_records, consent_log CASCADE;
    "

    Imports (in this order):
      from __future__ import annotations
      import asyncio
      import os
      import sys
      from sqlalchemy import create_engine, func, select
      from sqlalchemy.orm import sessionmaker
      from brave.clients.llm import RealLLMClient
      from brave.clients.mtur import MturClient
      from brave.clients.places import RealPlacesClient, build_mtur_ibge_lookup
      from brave.config.settings import AppConfig, LLMConfig, ScoreConfig
      from brave.core.dlq.service import validate_and_promote_rio
      from brave.core.models import MarRecord, RioRecord
      from brave.lanes.atrativos.discovery_agent import DiscoveryAgent
      from brave.lanes.destinos.mtur import MturSeedIngest

    main() function body:

    Env checks:
      ufs = [a.upper() for a in sys.argv[1:]] or ["BA"]
      target_destinos = int(os.environ.get("LOAD_TARGET_DESTINOS", "10"))
      target_atrativos = int(os.environ.get("ATRATIVO_TARGET_COUNT", "10"))
      db_url = os.environ.get("BRAVE_DB_URL")
      if not db_url: print("ERROR: BRAVE_DB_URL not set..."); sys.exit(1)
      places_key = os.environ.get("BRAVE_PLACES_API_KEY")
      if not places_key: print("ERROR: BRAVE_PLACES_API_KEY not set..."); sys.exit(1)
      llm_key = os.environ.get("BRAVE_LLM_OPENROUTER_API_KEY")
      if not llm_key: print("ERROR: BRAVE_LLM_OPENROUTER_API_KEY not set..."); sys.exit(1)
      os.environ["RUN_REAL_EXTERNALS"] = "true"  # set before constructing real clients

    Cost guard operator note (print before running):
      print("NOTE: Default BRAVE_LLM_USD_DAILY_BUDGET=10.0 (~$0.02 for 10 destinos × 10 atrativos DeepSeek extractions).")
      print("      If CostGuardError fires, set BRAVE_LLM_USD_DAILY_BUDGET=50.0 and re-run.")

    Client construction:
      mtur_client = MturClient()
      llm_config = LLMConfig()
      score_config = ScoreConfig()
      engine = create_engine(db_url, echo=False)
      SessionFactory = sessionmaker(bind=engine)

    Build ibge_lookup from all mtur rows (all UFs for name→IBGE resolution):
      all_mtur_rows = []
      for uf in ["AC","AL","AP","AM","BA","CE","DF","ES","GO","MA","MT","MS","MG","PA","PB","PR","PE","PI","RJ","RN","RS","RO","RR","SC","SP","SE","TO"]:
          try:
              all_mtur_rows.extend(asyncio.run(mtur_client.fetch_municipalities(uf)))
          except Exception:
              pass  # best-effort; 2024 sample only has BA/RJ/SP
      ibge_lookup = build_mtur_ibge_lookup(all_mtur_rows)

    with SessionFactory() as session:

      Step 0 — DB non-clean warning:
        existing_mar = session.scalar(select(func.count(MarRecord.id)).where(MarRecord.entity_type=="destination", MarRecord.superseded_by_id.is_(None)))
        if existing_mar and existing_mar > 0:
          print(f"\nWARNING: {existing_mar} active Mar destinos already exist.")
          print("For a clean baseline: TRUNCATE nascente_records, rio_records, mar_records, consent_log CASCADE;\n")

      Step 1 — Ingest destinos:
        for uf in ufs:
          asyncio.run(MturSeedIngest(mtur_client, session, score_config).produce(uf))
          session.commit()
          print(f"[{uf}] Step 1: destinos ingested")

      Step 2 — Promote up to target_destinos DLQ destinos to Mar:
        promoted = []
        for uf in ufs:
          dlq_rows = list(session.scalars(select(RioRecord).where(RioRecord.routing=="dlq", RioRecord.entity_type=="destination", RioRecord.uf==uf).limit(target_destinos - len(promoted))).all())
          for rio in dlq_rows:
            if len(promoted) >= target_destinos:
              break
            mar = validate_and_promote_rio(session, rio, score_config)
            if mar:
              promoted.append(mar)
          session.commit()
        print(f"Step 2: {len(promoted)} destinos promoted to Mar")

      Step 3 — Targeted atrativos discovery for each promoted Mar destino:
        places_client = RealPlacesClient(api_key=places_key, ibge_lookup=ibge_lookup)
        llm_client = RealLLMClient(config=llm_config)
        for mar in promoted:
          agent = DiscoveryAgent(places_client, llm_client, session, score_config)
          count = asyncio.run(agent.produce_for_destino(mar, target_count=target_atrativos))
          session.commit()
          canonical = mar.canonical or {}
          nome = canonical.get("municipio") or canonical.get("name", str(mar.id))
          print(f"  [{nome}] atrativos created in Rio: {count}")

      Step 4 — Summary:
        print("\n=== LOAD TEST SUMMARY ===")
        mar_count = session.scalar(select(func.count(MarRecord.id)).where(MarRecord.entity_type=="destination", MarRecord.superseded_by_id.is_(None)))
        print(f"Mar destination records (active): {mar_count}")
        atr_rows = session.execute(select(RioRecord.parent_mar_id, func.count(RioRecord.id)).where(RioRecord.entity_type=="attraction").group_by(RioRecord.parent_mar_id).order_by(RioRecord.parent_mar_id)).all()
        print(f"Attraction Rio records by parent_mar_id:")
        for parent_id, cnt in atr_rows:
          status = "OK" if cnt >= target_atrativos else "FAIL"
          print(f"  {parent_id}: {cnt} atrativos [{status}]")
        total_atrativos = sum(cnt for _, cnt in atr_rows)
        parents_ok = sum(1 for _, cnt in atr_rows if cnt >= target_atrativos)
        print(f"\nAcceptance: {mar_count} Mar destinos, {parents_ok}/{len(atr_rows)} parents with >={target_atrativos} atrativos")
        if mar_count >= target_destinos and parents_ok >= target_destinos:
          print("ACCEPTANCE: PASS")
        else:
          print("ACCEPTANCE: FAIL — check logs for quarantine reasons")

    if __name__ == "__main__": main()
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import ast
with open('scripts/loadtest_destinos_atrativos.py') as f: src = f.read()
ast.parse(src)
assert 'validate_and_promote_rio' in src
assert 'produce_for_destino' in src
assert 'TRUNCATE' in src, 'Missing DB reset warning'
assert 'CostGuardError' in src, 'Missing cost guard operator note'
assert 'ACCEPTANCE' in src, 'Missing acceptance summary'
print('loadtest_destinos_atrativos.py syntax + content OK')
"</automated>
  </verify>

  <done>scripts/loadtest_destinos_atrativos.py parses (syntax valid), imports validate_and_promote_rio and produce_for_destino, prints DB non-clean warning, runs 4 steps (ingest → promote → discover → summary), prints ACCEPTANCE: PASS/FAIL. Operator can run with real keys to verify 10×10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → real API keys | BRAVE_PLACES_API_KEY + BRAVE_LLM_OPENROUTER_API_KEY in env; harness requires them before constructing real clients |
| harness → live PostgreSQL DB | Direct DB writes (not through FastAPI auth layer); operator-local only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Information Disclosure | BRAVE_PLACES_API_KEY logged in script output | mitigate | script prints key existence check ("set" / "not set") but NEVER prints the key value; structlog in RealPlacesClient already omits keys |
| T-07-14 | Denial of Service | Harness auto-truncates DB destroying production data | mitigate | D-05 explicit requirement: no auto-truncate; harness only prints WARNING + manual TRUNCATE command; operator confirms manually |
| T-07-15 | Denial of Service | CostGuardError halts harness mid-run | accept | operator note printed before run; $10 budget covers ~$0.02 for the 10×10 test; operator raises budget if needed |
| T-07-16 | Tampering | harness bypasses the DLQ auth boundary (calls validate_and_promote_rio directly) | accept | harness is operator-local script (not network-exposed); same function is also called via authenticated /api/v1/dlq/{id}/validate in production; no new trust boundary introduced |
| T-07-17 | Information Disclosure | COMP-03 — only place_id persisted from Google | accept | enforced in store_raw + DiscoveryAgent payload (place_id_cache + canonical["place_id"] only); harness does not change this |
| T-07-SC | Tampering | No new package installs in this plan | accept | All imports already in project dependencies |
</threat_model>

<verification>
1. Script syntax: `.venv/bin/python -c "import ast; ast.parse(open('scripts/loadtest_destinos_atrativos.py').read())"` exits 0
2. Import check (without real keys — will fail at client construction, not import):
   `.venv/bin/python -c "from scripts.loadtest_destinos_atrativos import main; print('import OK')"` exits 0
3. Content assertions: validate_and_promote_rio + produce_for_destino + TRUNCATE warning + ACCEPTANCE print all present
4. Existing suite unaffected: `.venv/bin/python -m pytest tests/unit/ -x --tb=short` exits 0
</verification>

<success_criteria>
- scripts/loadtest_destinos_atrativos.py exists and parses cleanly
- 4-step flow: MturSeedIngest → validate_and_promote_rio × 10 → DiscoveryAgent.produce_for_destino × 10 → summary
- DB non-clean warning printed; TRUNCATE command shown; no auto-reset
- Cost guard operator note printed before run
- Acceptance gate: prints ACCEPTANCE: PASS when 10 Mar destinos + ≥10 Rio atrativos per parent are found
- Operator can run: `.venv/bin/python -m scripts.loadtest_destinos_atrativos BA` with real keys to execute the full Phase 7 acceptance UAT
</success_criteria>

<output>
Create `.planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-05-SUMMARY.md` when done.
</output>
