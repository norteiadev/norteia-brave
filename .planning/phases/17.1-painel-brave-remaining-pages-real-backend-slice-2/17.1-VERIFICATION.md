---
phase: 17.1-painel-brave-remaining-pages-real-backend-slice-2
verified: 2026-06-28T14:25:54Z
status: passed
resolved: 2026-06-28T15:00:00Z
score: 8/8 must-haves verified
overrides_applied: 0
resolution: >-
  The sole blocking gap (test-isolation regression in test_migration_0006 from the
  0007 head bump) was fixed in commit d5df295 — both migration tests now downgrade to
  absolute parent revisions (0005 / 0006) and restore to `head`, making them order- and
  head-independent. Verified against a real BRAVE_DB_URL Postgres: 4 passed. Status
  flipped failed → passed.
gaps:
  - truth: "Offline-test mandate — backend suite green; dashboard MSW green"
    status: partial
    reason: >-
      The new Alembic migration 0007 (Phase 17.1) bumps the head from 0006 to
      0007. The pre-existing tests/integration/test_migration_0006.py::test_migration_0006_up_down
      uses a relative `command.downgrade(cfg, "-1")` and assumes the DB starts at
      revision 0006. When integration tests share a real Postgres (operator's
      standard `.env` sets BRAVE_DB_URL), earlier integration tests leave the DB
      at head 0007, so `downgrade -1` runs 0007->0006 (not 0006->0005) and the
      `mar_ready` column survives — the assertion `mar_ready not in columns_after`
      fails. Reproduced this run: `1 failed, 765 passed, 1 skipped`. Truly offline
      (no BRAVE_DB_URL) both migration tests skip and the suite is green; the
      dashboard MSW suite is fully green (276 passed, 42 files). The migration
      0007 itself and its own up/down test are correct (passes). This is a
      test-isolation regression attributable to this phase's head bump, and it
      breaks the phase's own pre-verification gate ("Both full suites green",
      17.1-VALIDATION.md).
    artifacts:
      - path: "tests/integration/test_migration_0006.py:69"
        issue: "Relative `command.downgrade(cfg, '-1')` + `upgrade(cfg, '0006')` (no-op when DB already past 0006) assumes the DB head is 0006; breaks now that 0007 is head when integration tests run against a shared real Postgres."
      - path: "tests/integration/test_migration_0007.py:73"
        issue: "Same fragile relative `downgrade -1` pattern — will re-trigger this class of failure for the next migration; should pin absolute revisions."
    missing:
      - "Make the migration up/down integration tests isolation-safe: pin absolute downgrade targets (0006->0005, 0007->0006) and/or reset the DB to a known base in a fixture, so a shared real-Postgres run of the full integration suite stays green. (One-line-per-test fix; no product code change.)"
deferred: []
---

# Phase 17.1: Painel Brave — remaining pages + real backend (slice 2) Verification Report

**Phase Goal:** Complete the Painel Brave CMS — all six views against REAL data; destinos+atrativos synchronization through the Kanban end-to-end (6 stage columns + full-pipeline drag firing real backend transitions); NET-NEW Duplicados + Varreduras backends; Origem modal (TA cURL inject) + Motor depth toggle; cross-wire Mapeamento/Conversas/Custo/drawer into the finished shell.
**Verified:** 2026-06-28T14:25:54Z
**Status:** failed (1 blocking gap — test-isolation regression on the offline-test mandate)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All 6 views real in the shell; nav grouped Processamento/Operação; no "Em breve" for the six | ✓ VERIFIED | `app/painel/page.tsx` `PainelBody` switch mounts PainelView/PainelDuplicados/PainelMapeamento/PainelVarreduras/PainelConversas/PainelCusto (no placeholder; docstring "all six views real"). `nav.ts` NAV_GROUPS = Processamento{painel,duplicados,mapeamento,varreduras} + Operação{conversas,custo}. |
| 2 | Sync via Kanban — drag fires real per-entity transition endpoints; client mapDrop mirrors server _ALLOWED_EDGES; mar→* blocked; unmapped drop reverts+toasts | ✓ VERIFIED | `painel-actions.ts` DESTINO_EDGES/ATRATIVO_EDGES exactly mirror server `cms.py _ALLOWED_EDGES` + `atrativos.py _ATRATIVO_ALLOWED_EDGES`; `mapDrop` returns null for any unlisted/mar→*/same-column pair → toast UNAVAILABLE; `runAction` → `transition()`. Server 409s mar→* and unmapped edges before mutation (`cms.py:450-453`). 14 painel-actions + 20 backend transition tests pass. |
| 3 | Duplicados backend — GET /dedup/pairs (compute-on-read field similarity, NO pgvector cosine) + resolve merge(union provenance, NO 409)/keep/discard, audited | ✓ VERIFIED | `brave/api/routers/dedup.py`: `_compute_field_diff`/`_token_similarity` pure Python (no cosine_distance/pgvector operator); merge unions candidate source_ref into `mar.provenance["merged_source_refs"]`, routes candidate→descarte, no new Mar, no 409; `write_audit` on merge/keep/discard. 12 offline tests pass. |
| 4 | Varreduras backend — RunHistory + migration 0007 + engine/sweep write points (no phantom rows; finalize best-effort) + GET /runs + reprocess | ✓ VERIFIED | `models.py` RunHistory(runs_history); `alembic/versions/0007_add_runs_history.py` (down_revision 0006); `engine.py:140-156` INSERT only after depth/source 422 + start_run() success, run_id→Redis, best-effort; `pipeline.py:1953-1990` `_finalize_run_history` in finally try/except; `runs.py` GET /runs (on-read synced/failed) + PATCH reprocess (202, audited). 16 offline + own migration up/down test pass. |
| 5 | 6-column board — nascente/rio/whatsapp/mar/dlq/falha; whatsapp from sub_state aguardando_consulta_whatsapp; falha from GET /failures | ✓ VERIFIED | `painel-data.ts` COLUMN_DEFS = 6 keys; `routingToColumn(in_progress→rio)`; atrativo `sub_state===aguardando_consulta_whatsapp → whatsapp`; `failureToCard` projects `GET /api/v1/failures` (workers.py:88) into draggable falha cards (no payload/phone). 19 painel-data tests pass. |
| 6 | Origem modal + TA inject + Motor — injectTASession wired, 422/503 distinct, TTL from real expires_in; Motor start requires depth | ✓ VERIFIED | `engine-api.ts` `injectTASession`→POST /tripadvisor/session; `PainelOrigem.tsx` parseTACurl→strict body, 422→"422" state, 503→"503" state (distinct toasts+inline); `PainelTopbar.tsx` pill/expiry driven by real `expires_in` (warn ≤5min), START opens depth menu → `startEngine({depth})` (no depthless start). PainelOrigem (6) + PainelTopbar (10) tests pass. |
| 7 | Contract discipline — typed Pydantic + mirrored MSW (no pact-js) | ✓ VERIFIED | dedup.py/runs.py Pydantic models `extra="forbid"`; mirrored MSW `mocks/handlers/dedup.ts` + `runs.ts` at double-prefixed BFF paths; `grep pact` in package.json → none. |
| 8 | Offline-test mandate — backend offline green; migration tests skip-safe; dashboard MSW green | ✗ FAILED | Dashboard MSW: 276 passed (42 files) ✓. Backend truly offline (no BRAVE_DB_URL): green, both migration tests skip-safe ✓. Backend with BRAVE_DB_URL set (operator .env): **1 failed, 765 passed, 1 skipped** — `test_migration_0006_up_down` breaks via test-isolation pollution caused by the new 0007 head bump (see Gaps). |

**Score:** 7/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `brave/api/routers/dedup.py` | dedup pairs + resolve | ✓ VERIFIED | Registered in main.py:70-72; substantive; offline-tested |
| `brave/api/routers/runs.py` | runs list + reprocess | ✓ VERIFIED | Registered main.py:75-77; on-read counts; audited reprocess |
| `brave/core/models.py` RunHistory | runs_history table | ✓ VERIFIED | Full column set incl. `lane` (used by runs.py reprocess) |
| `alembic/versions/0007_add_runs_history.py` | migration | ✓ VERIFIED | down_revision 0006; create_table + index; own up/down test passes |
| `brave/api/routers/cms.py` transition_destino | destino edges + mar→* 409 | ✓ VERIFIED | `_ALLOWED_EDGES` 6 edges, no mar→*; optimistic-concurrency 409 |
| `brave/api/routers/atrativos.py` transition_atrativo | atrativo edges + whatsapp delegate | ✓ VERIFIED | `_ATRATIVO_ALLOWED_EDGES`; into-whatsapp delegates to gate approve |
| `dashboard/lib/painel-data.ts` | 6-column model | ✓ VERIFIED | COLUMN_DEFS 6 keys; whatsapp/falha sourcing |
| `dashboard/lib/painel-actions.ts` | client allow-list | ✓ VERIFIED | Exact server twin; null→revert+toast |
| `dashboard/lib/engine-api.ts` | transition + injectTASession | ✓ VERIFIED | Both clients + fetchFailures re-export |
| `dashboard/components/painel/PainelDuplicados.tsx` | Duplicados view | ✓ VERIFIED | useQuery pairs + resolve mutation; 3 tests |
| `dashboard/components/painel/PainelVarreduras.tsx` | Varreduras table | ✓ VERIFIED | runs table + 7-day cards + reprocess; 4 tests |
| `dashboard/components/painel/PainelOrigem.tsx` | Origem modal | ✓ VERIFIED | cURL parse + 422/503 + TTL; 6 tests |
| `dashboard/app/painel/page.tsx` | 6-view switcher | ✓ VERIFIED | All six mounted; no placeholder |

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| mapDrop (client) | _ALLOWED_EDGES / _ATRATIVO_ALLOWED_EDGES (server) | edge-for-edge twin keyed by (expected,to) | ✓ WIRED |
| board drag | PATCH /{destinos\|atrativos}/{id}/transition | engine-api `transition()` → runAction | ✓ WIRED |
| PainelDuplicados | GET/PATCH /api/v1/dedup/pairs | dedup-api `fetchDedupPairs`/`resolveDedupPair` | ✓ WIRED |
| PainelVarreduras | GET/PATCH /api/v1/runs | runs-api `fetchRuns`/`reprocessRun` | ✓ WIRED |
| PainelOrigem cURL | POST /api/v1/tripadvisor/session | `injectTASession` | ✓ WIRED |
| Motor START | POST /engine/start (depth) | `startEngine({depth})` | ✓ WIRED |
| falha column | GET /api/v1/failures | `fetchFailures` → failureToCard | ✓ WIRED |
| engine_start | runs_history INSERT (after 422/start_run) | best-effort + run_id→Redis | ✓ WIRED |
| engine_sweep_run finally | runs_history UPDATE | `_finalize_run_history` (try/except) | ✓ WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| New backend feature tests offline | `pytest test_dedup_* test_runs_* test_transitions test_migration_0007` | 48 passed, 1 skipped (DB up/down skip-safe) | ✓ PASS |
| Full dashboard suite | `cd dashboard && bun run test` | 276 passed (42 files) | ✓ PASS |
| Full backend suite (BRAVE_DB_URL present) | `pytest` | 1 failed, 765 passed, 1 skipped | ✗ FAIL |
| Routers registered | grep main.py | dedup_router + runs_router include_router | ✓ PASS |

### Probe Execution

No project `scripts/*/tests/probe-*.sh` declared for this phase. N/A.

### Requirements Coverage

| Requirement | Source | Description | Status | Evidence |
|-------------|--------|-------------|--------|----------|
| UI-PAINEL-2 (slice 2) | all 7 plans | all views + dedup-pairs backend + runs-history backend + full-pipeline kanban sync | ✓ SATISFIED (functional) | Truths 1-7 verified; only the offline-suite isolation gate (truth 8) is red |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `nav.ts` | 6-9 | Stale docstring "Only the painel view is implemented this slice; the other five render an 'Em breve' placeholder" | ℹ️ Info | Cosmetic only — NAV_GROUPS below correctly defines all 6 grouped views; page.tsx renders all six. Worth updating for accuracy. |
| `dedup.py` | 235 | `similarity_source="embedding_stub"` | ℹ️ Info | Intentional, labeled placeholder (CONTEXT A1 — real embedding cosine deferred). Does not block the Duplicados goal (list+resolve works). |

No 🛑 debt markers (TBD/FIXME/XXX) introduced. No hardcoded-empty render stubs (falha/dedup/runs views all render real backend-sourced data; mock payloads live only in MSW handlers).

### Human Verification Required

Deferred to HUMAN-UAT (from VALIDATION.md Manual-Only — not blocking this automated verification):
- Visual fidelity of all six views + 6-column board to `Painel Brave.dc.html` (pixel/feel).
- Live drag→Mar actually reaches norteia-api (opt-in `RUN_REAL_EXTERNALS` real run — crosses the collector→platform boundary, mocked in CI).

### Gaps Summary

The phase's **functional goal is fully delivered**: all six Painel views are real and wired to live endpoints, the Kanban is the 6-column sync surface with a full-pipeline client allow-list that is the exact twin of two server-side audited transition endpoints (mar→* 409-blocked both sides), and both NET-NEW backends (Duplicados compute-on-read + Varreduras runs_history/migration 0007) exist, are registered, audited, and pass their offline tests. The Origem cURL inject (distinct 422/503), real-`expires_in` TA pill, and depth-required Motor are wired. Contract discipline holds (typed Pydantic + mirrored MSW, no pact-js). Dashboard suite is fully green (276).

**One blocking gap on the offline-test mandate (truth 8):** the new migration 0007 bumps the Alembic head 0006→0007. The pre-existing `test_migration_0006_up_down` uses a relative `downgrade -1` and assumes the DB head is 0006; when the integration suite runs against a shared real Postgres (the operator's standard condition — `.env` sets BRAVE_DB_URL), the DB is left at 0007 before that test, so its `downgrade -1` runs 0007→0006 instead of 0006→0005 and `mar_ready` survives → the suite is **red (1 failed, 765 passed)**. Truly offline (no DB) the suite is green and both migration tests skip-safe, but the phase's own pre-verification gate is "Both full suites green" and that is not met in the standard run. The migration 0007 and its own up/down test are correct — this is a test-isolation regression, fixable by pinning absolute downgrade targets in the migration up/down tests (no product-code change).

---

## VERIFICATION: FAILED

**7/8 must-haves verified.** All functional deliverables (Truths 1-7) are real, substantive, wired, and data-flowing — the Painel Brave CMS is complete: six real views, full-pipeline Kanban sync through audited per-entity transition endpoints (client mapDrop = exact server allow-list twin; mar→* 409 both sides), NET-NEW Duplicados (compute-on-read, no pgvector cosine, merge-by-provenance no-409) and Varreduras (RunHistory + migration 0007 + best-effort engine/sweep write points + on-read runs list + audited reprocess) backends, Origem cURL inject with distinct 422/503 + real-TTL pill, depth-required Motor, and typed-Pydantic⇄MSW contract discipline (no pact-js). Dashboard suite green (276/276).

**One blocking gap (Truth 8 — offline-test mandate):** the backend full suite is **red (1 failed, 765 passed, 1 skipped)** in the operator's standard `.env`/BRAVE_DB_URL run. Root cause: this phase's new migration 0007 head bump exposed a fragile relative `downgrade -1` in the pre-existing `tests/integration/test_migration_0006.py::test_migration_0006_up_down`, which now downgrades 0007→0006 instead of 0006→0005 under shared-DB ordering, leaving `mar_ready` present and failing the assertion. Fix is a one-line-per-test isolation change (pin absolute downgrade revisions / reset to a known base in `test_migration_0006.py:69` and `test_migration_0007.py:73`); no product code is wrong and the 0007 migration applies/reverses correctly on its own. Re-run the full backend suite green to flip to PASSED.
