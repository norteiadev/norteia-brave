---
phase: 17.1-painel-brave-remaining-pages-real-backend-slice-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brave/api/routers/dedup.py
  - brave/api/main.py
  - tests/unit/test_dedup_pairs.py
  - tests/unit/test_dedup_resolve.py
autonomous: true
requirements: [UI-PAINEL-2]

must_haves:
  truths:
    - "GET /api/v1/dedup/pairs returns candidate↔Mar pairs with matched/diverged field labels and a labeled similarity"
    - "PATCH /api/v1/dedup/pairs/{candidate_rio_id}/resolve performs merge | keep | discard and writes an audit row"
    - "merge unions the candidate provenance into the EXISTING Mar (no new Mar row) and routes the candidate Rio to descarte"
    - "Pair listing never compares across UF (territorial-key blocked)"
  artifacts:
    - path: "brave/api/routers/dedup.py"
      provides: "Duplicados list + resolve endpoints with typed Pydantic response models"
      contains: "list_dedup_pairs"
    - path: "tests/unit/test_dedup_pairs.py"
      provides: "Offline field-diff + pairing unit tests (MagicMock session)"
    - path: "tests/unit/test_dedup_resolve.py"
      provides: "Offline merge/keep/discard dispatch + audit unit tests"
  key_links:
    - from: "brave/api/main.py"
      to: "brave/api/routers/dedup.py"
      via: "app.include_router(dedup_router)"
      pattern: "include_router"
    - from: "brave/api/routers/dedup.py"
      to: "brave.observability.audit.write_audit"
      via: "audit on every resolve"
      pattern: "write_audit"
---

<objective>
Build the NET-NEW Duplicados backend: a compute-on-read endpoint that lists dedup candidate↔Mar
pairs (territorial-key blocked, field similarity computed in Python) and a resolve endpoint
(merge / keep / discard) that mutates Rio/Mar and audits every action.

Purpose: Duplicados is one of the two views that has no backend today. Embeddings are a zero stub
(RESEARCH #1 / Pitfall 1), so similarity is computed in Python from normalized/canonical fields and
the whole read path stays offline-unit-testable.
Output: brave/api/routers/dedup.py (list + resolve, typed Pydantic models), router registered in
main.py, two offline unit test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-CONTEXT.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-RESEARCH.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dedup.py list_dedup_pairs (compute-on-read) + register router</name>
  <files>brave/api/routers/dedup.py, brave/api/main.py, tests/unit/test_dedup_pairs.py</files>
  <read_first>
    - brave/api/routers/cms.py:19-35 (router/imports preamble), cms.py:483-540 (list_atrativos paginated-envelope + Query params)
    - brave/core/rio/dedup.py:82-91 (territorial-key blocking invariant), :102-117 (zero-stub compute_embedding — do NOT trust cosine)
    - brave/core/models.py:140 (RioRecord.normalized), :176-229 (MarRecord fields: source_ref unique-active-only, canonical JSON, provenance JSON non-null, superseded_by_id, rio relationship), :343 (ix_rio_territorial_key)
    - brave/core/mar/service.py:46-50 (canonical field allow-list kept by promote_to_mar)
    - brave/api/routers/tripadvisor_session.py:113 (TASweepProgressResponse — extra="forbid" typed-contract analog)
    - brave/api/main.py:48-67 (lazy-import + include_router registration pattern)
    - tests/unit/test_dedup.py (MagicMock-session offline test pattern)
  </read_first>
  <behavior>
    - Pairing only matches candidate (RioRecord.routing in_progress|dlq) to an ACTIVE Mar row sharing (uf, municipio_id, entity_type); never across UF.
    - matched_fields/diverged_fields are derived by diffing rio.normalized vs mar.canonical over the canonical allow-list keys; equal→matched, both-present-and-unequal→diverged.
    - similarity is a labeled placeholder with similarity_source ("embedding_stub" | "name_overlap"); no embedding.cosine_distance call in this read path.
    - Response is a paginated envelope: items / total / offset / limit.
  </behavior>
  <action>
    Create brave/api/routers/dedup.py mirroring the cms.py router preamble. Declare typed Pydantic
    models with model_config extra="forbid": DedupPairItem (fields: candidate_id, mar_id,
    candidate_rio_id, mar_rio_id, uf, municipio, entity_type, similarity: float, similarity_source:
    str, matched_fields: list[str], diverged_fields: list[dict]) and DedupPairsResponse (items, total,
    offset, limit) — field-for-field the RESEARCH #1 list schema, mirroring the extra="forbid"
    discipline of TASweepProgressResponse.
    Implement GET /api/v1/dedup/pairs with dependencies=[Depends(require_bearer)], Query params
    uf: str|None, offset: int (ge=0), limit: int (ge=1, le=500), db: Session = Depends(get_db).
    Select candidate RioRecords where routing in ("in_progress","dlq") and municipio_id is not None,
    optional uf filter; count via select(func.count()).select_from(subquery). For each candidate find
    the active Mar row (MarRecord.superseded_by_id.is_(None)) on the SAME territorial key by joining
    MarRecord -> rio_id -> RioRecord and matching uf+municipio_id+entity_type (copy the block keys from
    dedup.py:82-91 — NEVER compare across UF, CR-02). Compute similarity, matched_fields and
    diverged_fields in PYTHON by diffing rio.normalized vs mar.canonical over the canonical allow-list
    (mar/service.py:46-50); set similarity_source="embedding_stub" with similarity computed from
    name/municipio/UF token overlap (do NOT call embedding.cosine_distance — Pitfall 2 forces a
    silently-skipped integration test). Return DedupPairsResponse.
    Register the router in brave/api/main.py with a lazy import + app.include_router(dedup_router),
    matching the main.py:48-67 pattern.
    Write tests/unit/test_dedup_pairs.py (MagicMock session like tests/unit/test_dedup.py): assert the
    field-diff produces correct matched/diverged labels for a known normalized-vs-canonical pair, that
    a cross-UF candidate is never paired, and that the response envelope shape (items/total/offset/limit)
    is exact. Keep it fully offline (no DB, no pgvector operator).
  </action>
  <acceptance_criteria>
    - `grep -n "extra" brave/api/routers/dedup.py` shows extra="forbid" on the response models
    - `grep -c "cosine_distance" brave/api/routers/dedup.py` returns 0 (no pgvector operator in read path)
    - `grep -c "include_router(dedup" brave/api/main.py` returns ≥1
    - `.venv/bin/python -m pytest tests/unit/test_dedup_pairs.py -x` passes (RUN_REAL_EXTERNALS unset)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_dedup_pairs.py -x</automated>
  </verify>
  <done>GET /api/v1/dedup/pairs returns territorial-key-blocked pairs with Python-computed matched/diverged labels and a labeled similarity; router registered; offline unit test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resolve_pair (merge / keep / discard) + audit</name>
  <files>brave/api/routers/dedup.py, tests/unit/test_dedup_resolve.py</files>
  <read_first>
    - brave/api/routers/cms.py:330-378 (descarte_destino — audit + commit posture to copy verbatim), :365-366 (routing="descarte" + dlq_reason set)
    - brave/core/models.py:176-208 (MarRecord: source_ref unique ACTIVE-only via uq_mar_active_source_ref; canonical JSON; provenance JSON non-null) — union into provenance, do NOT supersede / create a 2nd active row
    - brave/observability/audit.py:22-74 (write_audit signature)
    - brave/api/routers/dedup.py (this plan's Task 1 output)
    - 17.1-CONTEXT.md "Research resolutions" A2 (LOCKED — merge unions provenance into existing Mar + discards candidate Rio; NO 409 on differing sources; OVERRIDES RESEARCH Pitfall 4)
  </read_first>
  <behavior>
    - discard: candidate.routing="descarte", dlq_reason="dedup_discarded"; audit action "dedup_discarded"; no Mar change.
    - merge (LOCKED A2): load the EXISTING MarRecord by body.mar_id; UNION the candidate's source_ref into that Mar's provenance JSON (append to provenance["merged_source_refs"]); do NOT create a 2nd active Mar, do NOT change the existing Mar's own source_ref, do NOT call promote_to_mar. Then route the candidate Rio → routing="descarte" (removes it from the pending dedup pool). Audit action "dedup_merged". NO 409 on differing sources.
    - keep: no row change; write a "dedup_kept" audit row that IS the suppression marker (no schema change).
    - 404 when candidate RioRecord (or, for merge, the target MarRecord) is missing.
  </behavior>
  <action>
    Add to brave/api/routers/dedup.py a ResolveBody Pydantic model (action: Literal["merge","keep",
    "discard"], mar_id: uuid.UUID; extra="forbid") and PATCH /api/v1/dedup/pairs/{candidate_rio_id}/resolve
    with dependencies=[Depends(require_steward_or_bearer)], copying the descarte_destino audit+commit
    posture (cms.py:335-378). Load rio via db.get(RioRecord, candidate_rio_id); 404 if None; capture
    before = {"routing": rio.routing}.
    For discard: set rio.routing="descarte", rio.dlq_reason="dedup_discarded"; action="dedup_discarded".
    For merge (LOCKED CONTEXT A2 — this OVERRIDES RESEARCH Pitfall 4, which is stale): load the existing
    target via mar = db.get(MarRecord, body.mar_id) (404 if None). UNION the candidate provenance into
    the existing Mar by reassigning a fresh dict so SQLAlchemy detects the JSON change, e.g.
    merged = list(mar.provenance.get("merged_source_refs", [])); merged.append(rio.source_ref);
    mar.provenance = {**mar.provenance, "merged_source_refs": merged}. Do NOT mutate mar.source_ref
    (the partial unique uq_mar_active_source_ref stays satisfied), do NOT create a new MarRecord, do NOT
    call promote_to_mar. Then set rio.routing="descarte" (discard the candidate Rio from the pending
    pool). action="dedup_merged". There is NO source_ref 409 guard.
    For keep: no mutation; action="dedup_kept" (the audit row IS the suppression marker, A3 — no schema
    change).
    In all branches call write_audit(session=db, action=action, entity_type=rio.entity_type,
    record_id=rio.id, before_state=before, after_state={"routing": rio.routing}, actor="steward") then
    db.commit(); return {"status":"ok","action": body.action}.
    Write tests/unit/test_dedup_resolve.py (MagicMock session): assert merge unions the candidate
    source_ref into the existing Mar's provenance["merged_source_refs"], routes the candidate Rio
    routing=="descarte", writes audit action=="dedup_merged", and creates NO new MarRecord (never calls
    promote_to_mar / no session.add of a MarRecord); assert discard sets routing=="descarte" with
    action=="dedup_discarded"; assert keep writes a dedup_kept audit and mutates no routing.
  </action>
  <acceptance_criteria>
    - `grep -c "dedup_merged\|dedup_kept\|dedup_discarded" brave/api/routers/dedup.py` returns ≥3
    - `grep -c "merged_source_refs" brave/api/routers/dedup.py` returns ≥1 (provenance union)
    - `grep -c "promote_to_mar\|HTTPException(409" brave/api/routers/dedup.py` returns 0 (no supersession, no source-mismatch 409 in merge)
    - test asserts NO new MarRecord is created on merge and the existing Mar provenance contains the candidate source_ref
    - `.venv/bin/python -m pytest tests/unit/test_dedup_resolve.py -x` passes
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_dedup_resolve.py -x</automated>
  </verify>
  <done>resolve endpoint unions the candidate provenance into the existing Mar and discards the candidate Rio on merge (no new Mar row, no 409), routes discard to descarte, suppresses on keep, and audits every action; offline unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard→BFF→collector | operator-authenticated mutation of dedup state crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-01-01 | Elevation | resolve endpoint | mitigate | require_steward_or_bearer on PATCH; require_bearer on GET (deps.py) |
| T-17.1-01-02 | Integrity | merge creates a duplicate active Mar | mitigate | merge unions into the existing Mar's provenance + discards the candidate Rio (A2); never adds a 2nd active row, never touches mar.source_ref (uq_mar_active_source_ref stays satisfied) |
| T-17.1-01-03 | Repudiation | resolve actions | mitigate | write_audit on every merge/keep/discard (audit.py:22) |
| T-17.1-01-04 | Info disclosure | LGPD phone in pairs | mitigate | pairs read canonical/normalized over the canonical allow-list only; no phone fields |
| T-17.1-01-SC | Tampering | package installs | accept | no installs this plan (RESEARCH: stack reuse only) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_dedup_pairs.py tests/unit/test_dedup_resolve.py -x` green with RUN_REAL_EXTERNALS unset.
- No pgvector operator in the offline read path (grep cosine_distance == 0).
- merge creates no new MarRecord and no 409 (grep promote_to_mar/HTTPException(409 == 0).
- dedup router included in main.py.
</verification>

<success_criteria>
Duplicados list + resolve endpoints exist, are Bearer/steward-guarded, audited, territorial-key blocked, and fully offline-unit-tested; merge follows LOCKED A2 (provenance union into existing Mar + candidate discard, no new Mar row, no 409).
</success_criteria>

<output>
Create `.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-01-SUMMARY.md` when done.
</output>
