---
phase: 08-ops-cms-destinos-atrativos-crud-process-observability-cores
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - brave/api/main.py
  - dashboard/lib/destinos-api.ts
  - dashboard/components/cms/DestinoList.tsx
  - dashboard/components/cms/DetailPanel.tsx
  - dashboard/app/destinos/page.tsx
  - dashboard/app/destinos/[id]/page.tsx
  - dashboard/mocks/handlers/destinos.ts
  - dashboard/components/cms/__tests__/DestinoList.test.tsx
autonomous: true
requirements: [D-03]

must_haves:
  truths:
    - "D-03: cms_router and workers_router registered in brave/api/main.py (app.include_router for both)"
    - "D-03: /destinos page renders DestinoList with TanStack Table v8 columns (name, uf, score/StageBadge, routing/StageBadge); row click navigates to /destinos/[id] or selects in master-detail"
    - "D-03: /destinos/[id] page renders DetailPanel with score_breakdown (reusing ScoreBreakdownPanel), audit_log via JourneyStepper, and action buttons (Promover/Reprocessar/Descartar)"
    - "D-03: destinos-api.ts exports destinoKeys, fetchDestinoList, fetchDestinoDetail, promoteDestino, descarteDestino, reprocessDestino typed functions using apiFetch"
    - "D-03: MSW destinos.ts handlers use double-prefix BASE = 'http://localhost:3000/api/api/v1/destinos'; provide list/detail/action variants"
    - "Vitest tests: DestinoList renders items with StageBadge (routing=dlq amber, routing=mar green); empty state renders 'Sem destinos'; 401 shows session-expired message"
  artifacts:
    - path: "brave/api/main.py"
      provides: "Router registration for cms_router + workers_router"
      contains: "cms_router"
    - path: "dashboard/lib/destinos-api.ts"
      provides: "Type-safe API client for destinos endpoints"
      exports: ["destinoKeys", "fetchDestinoList", "fetchDestinoDetail", "promoteDestino"]
    - path: "dashboard/components/cms/DestinoList.tsx"
      provides: "TanStack Table v8 destinos list with filters and StageBadge"
      exports: ["DestinoList"]
    - path: "dashboard/components/cms/DetailPanel.tsx"
      provides: "Generic detail panel (injected fetcher + actions) reused by both destinos and atrativos"
      exports: ["DetailPanel"]
    - path: "dashboard/app/destinos/page.tsx"
      provides: "Master-detail /destinos page"
    - path: "dashboard/app/destinos/[id]/page.tsx"
      provides: "Full-detail /destinos/[id] page"
    - path: "dashboard/mocks/handlers/destinos.ts"
      provides: "MSW handler factories for destinos endpoints"
      exports: ["destinosListSuccess", "destinosListEmpty", "destinoDetailSuccess", "destinoPromoteSuccess"]
  key_links:
    - from: "dashboard/app/destinos/page.tsx"
      to: "dashboard/lib/destinos-api.ts"
      via: "fetchDestinoList + destinoKeys"
      pattern: "fetchDestinoList"
    - from: "dashboard/components/cms/DestinoList.tsx"
      to: "dashboard/components/cms/StageBadge.tsx"
      via: "StageBadge import (from plan 08-03)"
      pattern: "StageBadge"
    - from: "dashboard/components/cms/DetailPanel.tsx"
      to: "dashboard/components/cms/JourneyStepper.tsx"
      via: "JourneyStepper import (from plan 08-03)"
      pattern: "JourneyStepper"
---

<objective>
Register new routers in main.py, then build the destinos frontend: API client, list+detail components, pages, MSW handlers, and Vitest tests. Also create the generic DetailPanel reused by atrativos in plan 08-05.

Purpose: Give operators a browsable /destinos CMS listing with status badges, drill-down detail with score breakdown and journey, and promote/reject/reprocess actions. DetailPanel is designed to be action-agnostic so plan 08-05 reuses it for atrativos without modification.

Output: main.py (router registrations), destinos-api.ts, DestinoList.tsx, DetailPanel.tsx, destinos pages, MSW handlers, Vitest tests.
</objective>

<execution_context>
@/Users/leandro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leandro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/leandro/Projects/norteia/norteia-brave/.planning/PROJECT.md
@/Users/leandro/Projects/norteia/norteia-brave/.planning/phases/08-ops-cms-destinos-atrativos-crud-process-observability-cores-/08-RESEARCH.md
@/Users/leandro/Projects/norteia/norteia-brave/.planning/phases/08-ops-cms-destinos-atrativos-crud-process-observability-cores-/08-PATTERNS.md
@/Users/leandro/Projects/norteia/norteia-brave/.planning/phases/08-ops-cms-destinos-atrativos-crud-process-observability-cores-/08-04-SUMMARY.md

<interfaces>
<!-- Existing patterns to mirror exactly. -->

From dashboard/lib/dlq-api.ts (query key + fetcher pattern):
```typescript
import { apiFetch } from "@/lib/api-client";
// apiFetch<T>(path: string, init?: RequestInit): Promise<T>
// Path WITHOUT leading slash; BFF prepends /api/ internally → /api/{path}
// MSW intercepts at http://localhost:3000/api/{path}

export const dlqKeys = {
  all: ["dlq"] as const,
  list: (uf: string) => ["dlq", "list", uf] as const,
  detail: (id: string) => ["dlq", "detail", id] as const,
};
```

From dashboard/components/dlq/QueueList.tsx (TanStack Table v8 pattern):
```typescript
import { flexRender, getCoreRowModel, useReactTable, type ColumnDef } from "@tanstack/react-table";
import { useQuery } from "@tanstack/react-query";
// Table instantiation: useReactTable({ data, columns, getCoreRowModel: getCoreRowModel() })
// Row click: <TableRow onClick={() => onSelect?.(row.original.id)} className="cursor-pointer">
```

From dashboard/components/dlq/ReviewPanel.tsx (detail panel pattern):
```typescript
// Props: { rioId: string | null; ... }
// Loading state: <Skeleton> x 3
// Error state: 401 → "Sessão expirada", other → "Não foi possível carregar" + retry button
// Content: header + ScoreBreakdownPanel + actions
```

From dashboard/mocks/handlers/dlq.ts (MSW double-prefix critical):
```typescript
const BASE = "http://localhost:3000/api/api/v1/dlq";
//                                  ^^^^^^^^^^ BFF mount prefix (REQUIRED — Pitfall 5)
```

From dashboard/mocks/server.ts (handler contract — DO NOT MODIFY):
```typescript
// server.ts exports: setupServer(...handlers) where handlers is the empty barrel from handlers/index.ts.
// The global array MUST stay empty. Per-suite mocking is done via server.use(<handler>()) in each test file.
// Do NOT import new handler files into server.ts or handlers/index.ts.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Register routers in main.py + build destinos API client + MSW handlers</name>
  <files>brave/api/main.py, dashboard/lib/destinos-api.ts, dashboard/mocks/handlers/destinos.ts, dashboard/components/cms/__tests__/DestinoList.test.tsx</files>
  <read_first>
    - brave/api/main.py — current router registrations; append cms_router + workers_router at bottom with comment "Phase 8: CMS + Workers observability"
    - dashboard/lib/dlq-api.ts — full file; copy query key factory + qs helper + apiFetch pattern
    - dashboard/mocks/handlers/dlq.ts — full file; copy handler factory pattern; note BASE URL double-prefix (line 18)
    - 08-PATTERNS.md — destinos-api.ts section (DestinoListItem interface, destinoKeys, fetchers), destinos.ts handler section
  </read_first>
  <action>
    Step 1 — brave/api/main.py:
    Read the file. After the last app.include_router() call (sweep_router line), append:
    ```
    # Phase 8: CMS CRUD + Workers observability
    from brave.api.routers.cms import router as cms_router
    from brave.api.routers.workers import router as workers_router
    app.include_router(cms_router)
    app.include_router(workers_router)
    ```

    Step 2 — dashboard/lib/destinos-api.ts:
    Create the file. Mirror dlq-api.ts structure exactly. Types: DestinoListItem { id: string; entity_type: string; uf: string | null; routing: string; score: number | null; name: string | null; canonical_key: string | null; validation_pending: boolean; mar_id: string | null; published_at: string | null }. DestinoDetail extends DestinoListItem with: score_breakdown: Record<string, unknown>; normalized: Record<string, unknown>; source: string | null; audit_log: AuditLogRow[]; child_atrativos: { total: number; by_sub_state: Record<string, number> }. AuditLogRow: { action: string; actor: string | null; after_state: Record<string, unknown> | null; created_at: string | null }.

    destinoKeys: { all, list(filters), detail(id) }. qs helper (copy from dlq-api.ts). fetchDestinoList(params). fetchDestinoDetail(id). promoteDestino(id). descarteDestino(id). reprocessDestino(id): all return Promise<...> via apiFetch with method PATCH for mutations.

    API paths: "api/v1/destinos" (list), "api/v1/destinos/${id}" (detail), "api/v1/destinos/${id}/promote" (promote PATCH), "api/v1/destinos/${id}/descarte" (descarte PATCH), "api/v1/destinos/${id}/reprocess" (reprocess PATCH).

    Step 3 — dashboard/mocks/handlers/destinos.ts:
    Create the file. BASE = "http://localhost:3000/api/api/v1/destinos" (double-prefix — Pitfall 5). sampleDestinos array (2 items: one routing=dlq validation_pending=true, one routing=mar). sampleDestinoDetail object with all DestinoDetail fields including audit_log with 2 rows. Factory functions: destinosListSuccess (with uf filter logic in handler), destinosListEmpty, destinosListError(statusCode), destinoDetailSuccess, destinoPromoteSuccess (202), destinoDescarteSuccess, destinoReprocessSuccess. Export destinoHandlers default array.

    Step 4 — (server.ts not modified in this plan):
    The handlers barrel (dashboard/mocks/handlers/index.ts) intentionally stays empty. Per the documented contract, each test suite applies its own handlers via server.use(<handler>()) — no global registration needed.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from brave.api.main import app; routes = [r.path for r in app.routes]; assert any('/api/v1/destinos' in str(r) for r in routes), 'cms routes missing'; assert any('/api/v1/workers' in str(r) for r in routes), 'workers routes missing'; print('main.py routes OK')"</automated>
  </verify>
  <done>
    main.py imports and registers cms_router + workers_router; `from brave.api.main import app` succeeds; app.routes includes /api/v1/destinos, /api/v1/atrativos, /api/v1/workers, /api/v1/failures. destinos-api.ts exports all typed functions. destinos.ts MSW handlers use double-prefix BASE URL. Test file created (server.use() per-suite pattern).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DestinoList, DetailPanel components + /destinos pages + Vitest tests</name>
  <files>
    dashboard/components/cms/DestinoList.tsx,
    dashboard/components/cms/DetailPanel.tsx,
    dashboard/app/destinos/page.tsx,
    dashboard/app/destinos/[id]/page.tsx
  </files>
  <read_first>
    - dashboard/components/dlq/QueueList.tsx — full file; copy TanStack Table v8 structure, row selection, UF filter tabs pattern, loading/error/empty states (lines 1-307)
    - dashboard/components/dlq/ReviewPanel.tsx — full file; copy detail panel structure, loading/error/401 states, action injection pattern (lines 1-183)
    - dashboard/components/dlq/ScoreBreakdownPanel.tsx — import path + props interface (accepts score_breakdown dict)
    - dashboard/app/dlq/page.tsx — master-detail layout + DlqActions pattern (lines 1-155)
    - 08-PATTERNS.md — DestinoList.tsx section (columns), DetailPanel.tsx section, destinos page section (master-detail layout + action bar)
    - 08-RESEARCH.md — Q8 (react-table + TanStack Query mutation invalidation pattern)
  </read_first>
  <action>
    Component 1 — dashboard/components/cms/DestinoList.tsx ("use client"):
    Imports: useQuery from @tanstack/react-query, flexRender/getCoreRowModel/useReactTable/ColumnDef from @tanstack/react-table, useState/useMemo, StageBadge from @/components/cms/StageBadge, fetchDestinoList/destinoKeys/DestinoListItem from @/lib/destinos-api, Table/TableBody/TableCell/TableHead/TableHeader/TableRow from @/components/ui/table, Skeleton from @/components/ui/skeleton, cn from @/lib/utils.

    Props: DestinoListProps { selectedId: string | null; onSelect: (id: string) => void }.

    State: uf (string|undefined), routing (string|undefined). useQuery with queryKey=destinoKeys.list({uf, routing}), queryFn=fetchDestinoList({uf, routing}), staleTime=30_000.

    Columns (useMemo): name (font-mono text-[12px]), uf (short text), score (tabular-nums toFixed(1) or "—"), routing badge (<StageBadge routing={row.original.routing} score={row.original.score} />), source (neutral chip or "—"), validationPending (only shows "Aguardando" chip if true via <StageBadge validationPending={...} />).

    Table: useReactTable({data: query.data?.items ?? [], columns, getCoreRowModel}). Render: loading → Skeleton rows (3), error → "Não foi possível carregar" + retry, empty → "Sem destinos para este filtro". Row click → onSelect(row.original.id). Selected row → bg-muted.

    Filter bar above table: UF select (native <select> or simple buttons for BA/RJ/SP/SC/CE + "Todos") and routing filter (Mar/DLQ/Descarte/Todos). Both setState(value || undefined) to clear.

    Component 2 — dashboard/components/cms/DetailPanel.tsx ("use client"):
    Generic panel accepting: { rioId: string | null; fetchDetail: (id: string) => Promise<unknown>; queryKeys: { detail: (id: string) => readonly unknown[] }; entityType: "destination" | "attraction"; actions?: (detail: unknown) => ReactNode }.

    useQuery with queryKey=rioId ? queryKeys.detail(rioId) : ["detail", "none"], queryFn, enabled=rioId!=null. Loading → Skeleton. Error → check if status 401 → "Sessão expirada" else "Não foi possível carregar" + retry. Null rioId → "Selecione um item à esquerda" placeholder.

    Content: header with name (from detail.normalized?.name or detail.canonical_key), StageBadge (routing + subState + score + source). ScoreBreakdownPanel (import from @/components/dlq/ScoreBreakdownPanel; pass score_breakdown). JourneyStepper (import from @/components/cms/JourneyStepper; pass entityType, routing, subState, score, auditLog). Separator. Actions slot: {actions?.(detail)}.

    Page 1 — dashboard/app/destinos/page.tsx ("use client"):
    Master-detail layout (copy dlq/page.tsx structure). useState selectedId. Left panel: DestinoList. Right panel: DetailPanel with fetchDetail=fetchDestinoDetail, queryKeys=destinoKeys, entityType="destination", actions={(detail) => <DestinoActions detail={detail} onDone={() => setSelectedId(null)} />}.

    DestinoActions component (inline in page.tsx): useMutation wrappers for promoteDestino/descarteDestino/reprocessDestino from destinos-api.ts. useQueryClient(); onSuccess: qc.invalidateQueries({queryKey: destinoKeys.all}). Toast on success/error (import toast from sonner). Promote button + Reprocessar button + Descartar button behind AlertDialog (copy AlertDialog pattern from dlq/page.tsx). Disable all buttons while any mutation isPending.

    Page 2 — dashboard/app/destinos/[id]/page.tsx ("use client"):
    Full-page detail for a single destino. params: { id: string } (Next.js dynamic segment). useQuery(destinoKeys.detail(id), fetchDestinoDetail(id)). Render DetailPanel in full-width mode (no left-panel split). Back link to /destinos.

    Vitest test file — dashboard/components/cms/__tests__/DestinoList.test.tsx:
    Import renderWithProviders (or use @testing-library/react render + QueryClientProvider + MSW). beforeEach: server.use(destinosListSuccess()). Test 1: renders table rows with StageBadge (find badge with text "DLQ" for validation_pending item). Test 2: empty state renders "Sem destinos" when destinosListEmpty(). Test 3: 401 → "Sessão expirada" message. afterEach: server.resetHandlers().
  </action>
  <verify>
    <automated>cd /Users/leandro/Projects/norteia/norteia-brave/dashboard && bun run test --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    DestinoList.tsx and DetailPanel.tsx build without TypeScript errors. /destinos page compiles. /destinos/[id] page compiles. Vitest tests: 3 passing (list renders, empty state, 401 handling). bun run test exits 0 (no regressions in existing tests).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → BFF → FastAPI | Operator Bearer flows through BFF; cms_router + workers_router now reachable via main.py |
| DestinoActions PATCH calls → BFF | apiFetch sends Bearer automatically; BFF validates before forwarding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Tampering | PATCH promote/descarte/reprocess from DestinoActions | mitigate | apiFetch includes Bearer (set at BFF login); BFF validates before forwarding; FastAPI require_steward_or_bearer blocks unauthorized calls |
| T-08-13 | Information Disclosure | DestinoDetail.normalized in DetailPanel | accept | Destinos have no PII fields; normalized contains only geographic/name data |
| T-08-SC | Tampering | npm/pip installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `.venv/bin/python -c "from brave.api.main import app"` succeeds; app.routes includes /api/v1/destinos and /api/v1/workers
- `grep -n "cms_router\|workers_router" brave/api/main.py` returns 2 import lines + 2 include_router lines
- `grep -n "http://localhost:3000/api/api/v1/destinos" dashboard/mocks/handlers/destinos.ts` returns BASE line (double-prefix Pitfall 5)
- `cd dashboard && bun run test` exits 0; Vitest shows DestinoList tests passing
- `cd dashboard && bun run build` exits 0
</verification>

<success_criteria>
main.py registers cms_router and workers_router. destinos-api.ts typed API client with TanStack query keys. DestinoList uses TanStack Table v8 with StageBadge in routing column. DetailPanel is action-agnostic (injected actions slot) with ScoreBreakdownPanel + JourneyStepper. /destinos master-detail page with promote/descarte/reprocess actions (AlertDialog for destructive). /destinos/[id] full-detail page. MSW handlers with double-prefix URL. Vitest: 3 tests pass (renders, empty, 401).
</success_criteria>

<output>
Create `.planning/phases/08-ops-cms-destinos-atrativos-crud-process-observability-cores-/08-04-SUMMARY.md` when done.
</output>
