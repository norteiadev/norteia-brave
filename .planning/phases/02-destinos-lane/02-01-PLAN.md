---
phase: 02-destinos-lane
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/contract/test_pact_norteia_api.py
autonomous: true
requirements: [DEST-01, DEST-05]
must_haves:
  truths:
    - "Implements CONTEXT.md decision D-10: (IBGE passthrough, no local municipality table) — see 02-CONTEXT.md"
    - "The Pact contract carries ibge_code in canonical so norteia-api can resolve IBGE → municipality_id"
    - "The Pact consumer tests pass with the updated payload shape"
  artifacts:
    - path: "tests/contract/test_pact_norteia_api.py"
      provides: "Updated Pact DESTINATION_PAYLOAD with ibge_code in canonical dict"
      contains: "ibge_code"
    - path: "tests/contract/pacts/norteia-brave-norteia-api.json"
      provides: "Regenerated Pact JSON artefact reflecting ibge_code field"
      contains: "ibge_code"
  key_links:
    - from: "tests/contract/test_pact_norteia_api.py"
      to: "brave/clients/norteia_api.py"
      via: "NorteiaApiClient.push_destination(payload)"
      pattern: "ibge_code.*canonical|canonical.*ibge_code"
---

<objective>
Update the frozen Pact consumer contract to carry `ibge_code` inside the `canonical` dict of every destination push payload, satisfying D-10 (IBGE passthrough to norteia-api for municipality resolution).

Purpose: The Phase 1 frozen contract has `canonical.municipio` as a name string only. norteia-api cannot uniquely resolve a Brazilian municipality without the IBGE 7-digit code (there are multiple "Santa Cruz" in Brazil). This is a breaking change to the Pact shape; it must land before any real push is built so the Laravel team can update their provider verification once.

Output: Updated DESTINATION_PAYLOAD constant in the Pact test; regenerated Pact JSON artefact; all three Pact interactions verified passing; norteia-api coordination note documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-destinos-lane/02-CONTEXT.md
@.planning/phases/02-destinos-lane/02-RESEARCH.md

<interfaces>
<!-- Frozen Pact DESTINATION_PAYLOAD (tests/contract/test_pact_norteia_api.py lines 51-68) -->
```python
DESTINATION_PAYLOAD = {
    "source": "mtur",
    "source_ref": "mtur:BA:123",
    "entity_type": "destination",
    "canonical": {
        "name": "Trancoso",
        "uf": "BA",
        "municipio": "Porto Seguro",
        # MISSING: ibge_code — must add per D-10
    },
    "reliability_score": 87.5,
    "score_version": "v1.0",
    "provenance": {...},
}
```

<!-- NorteiaApiClient.push_destination signature (brave/clients/norteia_api.py) -->
Accepts a dict payload that it POSTs verbatim to /api/internal/territorial/destinations.
No validation of canonical keys — the Pact shape is the authoritative spec.

<!-- _build_push_payload (brave/tasks/pipeline.py lines 201-250) -->
```python
canonical = mar_record.canonical or {}
# canonical is stored as JSONB in MarRecord — it carries whatever promote_to_mar stored.
# Producers must store ibge_code in canonical when they write to Nascente/Mar.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ibge_code to Pact DESTINATION_PAYLOAD and re-run contract tests</name>
  <files>tests/contract/test_pact_norteia_api.py</files>

  <read_first>
    - tests/contract/test_pact_norteia_api.py — read the full file; understand the three test functions and DESTINATION_PAYLOAD shape (lines 51-68)
    - brave/tasks/pipeline.py lines 201-250 — _build_push_payload; canonical dict is passed through from MarRecord.canonical; understand how canonical is constructed
    - brave/core/mar/service.py — promote_to_mar; where canonical JSONB is populated from RioRecord.normalized
    - .planning/phases/02-destinos-lane/02-RESEARCH.md §"Pact Contract Gap (RISK-01)" — the exact risk and recommended resolution
  </read_first>

  <behavior>
    - The updated DESTINATION_PAYLOAD has canonical = {"name": ..., "uf": ..., "municipio": ..., "ibge_code": "2927408"}
    - test_push_destination_contract passes with the updated payload
    - test_push_destination_idempotent_contract passes with the updated payload
    - test_pact_file_written_and_valid passes (interactions >= 2 still holds)
    - The Pact JSON artefact at tests/contract/pacts/norteia-brave-norteia-api.json contains "ibge_code"
  </behavior>

  <action>
    Update DESTINATION_PAYLOAD in tests/contract/test_pact_norteia_api.py: add "ibge_code": "2927408" to the canonical dict. Keep all other fields unchanged. Update the source_ref to "mtur:BA:2927408" for consistency (IBGE code in source_ref matches canonical.ibge_code). Update the Pact response body assertion source_ref accordingly. Re-run the three contract interactions — all must pass.

    Also add a module-level comment above DESTINATION_PAYLOAD explaining the change: "D-10 (Phase 2): ibge_code added to canonical to support IBGE → municipality_id resolution in norteia-api. Breaking change from Phase 1 frozen contract — coordinate with norteia-api Laravel team (Trilha 5) to update provider verification."

    Do NOT change the NorteiaApiClient implementation — it passes the payload dict through. Do NOT change _build_push_payload — canonical is built from MarRecord.canonical JSONB which producers will populate. This plan only updates the contract fixture and spec.
  </action>

  <verify>
    <automated>cd /Users/leandro/Projects/norteia/norteia-brave && python -m pytest tests/contract/test_pact_norteia_api.py -v --enable-socket 2>&1 | tail -20</automated>
  </verify>

  <done>
    - DESTINATION_PAYLOAD.canonical has "ibge_code" key
    - All three Pact tests pass (pytest exit 0)
    - tests/contract/pacts/norteia-brave-norteia-api.json exists and grep for "ibge_code" returns >=1 match
    - Module-level coordination comment is present above DESTINATION_PAYLOAD
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → Pact mock server | Pact mock server binds to localhost; test-only, no real network |
| norteia-brave (consumer) → norteia-api (provider) | The Pact JSON artefact is the contract artifact shared with the Laravel team; corrupted contract = silent mismatch in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | Pact JSON artefact | mitigate | Pact JSON is generated by the test suite and committed to the repo; only the test suite may regenerate it. IBGE code is a plain string — no injection vector. |
| T-02-01-02 | Information Disclosure | ibge_code in push payload | accept | IBGE codes are public Brazilian government data (open-data portal). No PII, no secrets. |
| T-02-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan — slopcheck not required. |
</threat_model>

<verification>
```bash
cd /Users/leandro/Projects/norteia/norteia-brave
python -m pytest tests/contract/ -v --enable-socket
grep -l "ibge_code" tests/contract/pacts/norteia-brave-norteia-api.json && echo "PASS: ibge_code in pact artefact"
```
</verification>

<success_criteria>
- All three Pact consumer tests pass (pytest exit 0)
- `canonical.ibge_code` is present in DESTINATION_PAYLOAD and in the generated Pact JSON
- source_ref format uses the IBGE code: "mtur:BA:2927408"
- No changes to NorteiaApiClient or _build_push_payload (contract shape is the only change)
- Coordination comment for norteia-api Laravel team is present
</success_criteria>

<output>
Create `.planning/phases/02-destinos-lane/02-01-SUMMARY.md` when done
</output>
