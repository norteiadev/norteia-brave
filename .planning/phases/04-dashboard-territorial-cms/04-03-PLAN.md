---
phase: 04-dashboard-territorial-cms
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - brave/api/routers/dashboard.py
  - brave/api/main.py
  - tests/integration/test_dashboard_endpoints.py
autonomous: true
requirements: [DASH-01]
must_haves:
  truths:
    - "An operator can read a single DLQ record's full detail: Nascente payload + Rio normalized + §7.6 per-criterion breakdown + signals + WhatsApp log, in one call"
    - "The detail endpoint is guarded by the Bearer dependency"
    - "A non-existent rio_id returns 404"
  artifacts:
    - path: "brave/api/routers/dashboard.py"
      provides: "GET /api/v1/dlq/{rio_id} detail endpoint surfacing score_breakdown + payload + signals + whatsapp log"
      contains: "def get_dlq_detail"
    - path: "brave/api/main.py"
      provides: "dashboard_router registration"
      contains: "dashboard_router"
  key_links:
    - from: "brave/api/routers/dashboard.py"
      to: "RioRecord.score_breakdown"
      via: "db.get(RioRecord, rio_id) + nascente join"
      pattern: "score_breakdown"
    - from: "brave/api/main.py"
      to: "dashboard_router"
      via: "app.include_router"
      pattern: "include_router\\(dashboard_router\\)"
---

<objective>
DLQ slice — backend half (DASH-01). Add the `GET /api/v1/dlq/{rio_id}` detail read endpoint the DLQ review panel needs: the existing `GET /api/v1/dlq` list deliberately omits `score_breakdown`, `normalized`, and the Nascente payload. The detail endpoint surfaces them by joining `NascenteRecord`, plus the per-record WhatsApp/steward event log from `AuditLog`. Create the new `brave/api/routers/dashboard.py` and register it. Read-only, Bearer-guarded, offline-tested.

Purpose: DASH-01's per-criterion explainability has no backing endpoint (RESEARCH §1) — this fills it so the DLQ UI slice (plan 04) has a real contract to fetch and MSW-mock against.
Output: `dashboard.py` with `get_dlq_detail`, registered in `main.py`, with offline pytest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-dashboard-territorial-cms/04-CONTEXT.md
@.planning/phases/04-dashboard-territorial-cms/04-PATTERNS.md
@.planning/phases/04-dashboard-territorial-cms/04-RESEARCH.md
@.planning/phases/04-dashboard-territorial-cms/04-01-SUMMARY.md

<interfaces>
<!-- Shapes the endpoint aggregates. From brave/core/models.py + dlq.py. -->

RioRecord (rio_records): routing, sub_state, normalized(JSON), score(Numeric), score_breakdown(JSON — per-criterion §7.6 origem/completude/corroboração/atualidade/validação-humana), score_version, dlq_reason, processed_at, uf, entity_type, canonical_key, nascente_id→NascenteRecord (.nascente relationship, models.py line 154)
NascenteRecord (nascente_records): source, source_ref, entity_type, uf, payload(JSON raw), content_hash, version
AuditLog (audit_log): action, entity_type, record_id, before_state, after_state, actor, created_at

404 idiom (copy-exact from dlq.py lines 100-102 / 147-149): obj = db.get(Model, id); if obj is None: raise HTTPException(404, ...)
require_bearer: from brave.api.deps (added in plan 01) — guard read endpoints with it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/v1/dlq/{rio_id} detail endpoint in new dashboard.py</name>
  <files>brave/api/routers/dashboard.py, tests/integration/test_dashboard_endpoints.py</files>
  <read_first>
    - brave/api/routers/dlq.py (list lines 50-83 omits score_breakdown; 404 idiom lines 100-102/147-149)
    - brave/api/routers/metrics.py (read-aggregation imports + DI shape)
    - brave/core/models.py (RioRecord, NascenteRecord, AuditLog; .nascente relationship line 154)
    - brave/api/deps.py (require_bearer, get_db)
    - .planning/phases/04-dashboard-territorial-cms/04-PATTERNS.md (DLQ detail section + 404 / read-aggregation shared patterns)
  </read_first>
  <behavior>
    - Test: GET /api/v1/dlq/{rio_id} with a valid Bearer returns 200 with keys id, routing, sub_state, dlq_reason, score, score_version, score_breakdown, normalized, nascente_payload, whatsapp_log
    - Test: score_breakdown contains the five §7.6 criteria keys when present on the Rio record
    - Test: whatsapp_log is the AuditLog rows for this rio_id ordered by created_at
    - Test: a random/unknown rio_id → 404
    - Test: missing Bearer header → 401 before any DB work
  </behavior>
  <action>Create `brave/api/routers/dashboard.py` with `router = APIRouter()` (imports per PATTERNS dashboard.py section: FastAPI APIRouter/Depends/Query/HTTPException/status, sqlalchemy func/select, Session, `from brave.api.deps import get_db, require_bearer`, models AuditLog/MarRecord/NascenteRecord/PoisonQuarantine/RioRecord/LLMGeneration). Add `@router.get("/api/v1/dlq/{rio_id}", dependencies=[Depends(require_bearer)])` `def get_dlq_detail(rio_id: uuid.UUID, db: Session = Depends(get_db)) -> dict`: `rio = db.get(RioRecord, rio_id)`; 404 if None (copy the dlq.py idiom); `nascente = db.get(NascenteRecord, rio.nascente_id)`; build the response dict with id(str), routing, sub_state, dlq_reason, score(float-or-None), score_version, `score_breakdown` (rio.score_breakdown or {} — the §7.6 per-criterion panel source for DASH-01), `normalized` (rio.normalized or {}), `nascente_payload` (nascente.payload if nascente else {}), `signals` (extract from normalized/payload per the SignalAgent shape), and `whatsapp_log` (AuditLog rows where `record_id == rio.id`, ordered by `created_at`, as a list of dicts). Coerce Numeric→float. Per D-01 this is read-only, no pipeline logic.</action>
  <verify>
    <automated>pytest tests/integration/test_dashboard_endpoints.py -k dlq_detail -x</automated>
  </verify>
  <acceptance_criteria>
    - `brave/api/routers/dashboard.py` contains `def get_dlq_detail(` decorated with `dependencies=[Depends(require_bearer)]`
    - `grep -n "score_breakdown" brave/api/routers/dashboard.py` returns a match
    - `python -c "import brave.api.routers.dashboard"` exits 0
    - dlq_detail test (200 shape, 404 unknown id, 401 no-Bearer) passes
  </acceptance_criteria>
  <done>GET /api/v1/dlq/{rio_id} returns the full per-criterion DLQ detail behind the Bearer gate; unknown ids 404; missing auth 401.</done>
</task>

<task type="auto">
  <name>Task 2: Register dashboard_router in main.py</name>
  <files>brave/api/main.py</files>
  <read_first>
    - brave/api/main.py (router import + include_router idiom, lines 16-37)
    - brave/api/routers/dashboard.py (from Task 1)
  </read_first>
  <action>In `brave/api/main.py` add `from brave.api.routers.dashboard import router as dashboard_router` alongside the other router imports, and `app.include_router(dashboard_router)` in the Phase 4 block (add a comment `# Phase 4: Dashboard read-aggregation surface (D-01, DASH-01..05)`). This is the wiring step — the dashboard router will accrete the monitor/cost/funnels/conversations endpoints in later plans on the same router.</action>
  <verify>
    <automated>python -c "from brave.api.main import app; print([r.path for r in app.routes if 'dlq' in r.path])"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "include_router(dashboard_router)" brave/api/main.py` returns a match
    - `python -c "from brave.api.main import app"` exits 0
    - The route `/api/v1/dlq/{rio_id}` is present in `app.routes`
  </acceptance_criteria>
  <done>The dashboard router is registered and the DLQ detail route is live on the app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BFF → FastAPI read surface | Operator reads DLQ detail; must be Bearer-gated, no data the operator shouldn't see |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Spoofing | get_dlq_detail | mitigate | guarded by `Depends(require_bearer)`; 401 before any DB work |
| T-04-10 | Information Disclosure | DLQ detail payload | mitigate | returns only Nascente payload + Rio normalized + score breakdown + audit log for the operator's review; no secrets/tokens in the response; this lane (destinos/atrativos pre-contact) carries no phone PII in the DLQ detail — phone masking enforced in plan 07 conversation/gate endpoints |
| T-04-11 | Tampering | read endpoint | accept | endpoint is read-only (db.get + select), no writes, no pipeline mutation |
| T-04-SC | Tampering | python deps (none added) | accept | no new package installs |
</threat_model>

<verification>
- `pytest tests/integration/test_dashboard_endpoints.py -k dlq_detail -x` passes.
- `python -c "from brave.api.main import app"` exits 0 with the new route registered.
</verification>

<success_criteria>
DASH-01 backend half: a single Bearer-guarded read endpoint returns the full DLQ detail (Nascente payload + Rio normalized + §7.6 per-criterion breakdown + signals + WhatsApp log), 404 on unknown ids, proven offline.
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-territorial-cms/04-03-SUMMARY.md` when done.
</output>
