---
phase: 17.1
slug: painel-brave-remaining-pages-real-backend-slice-2
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-27
---

# Phase 17.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Backend: pytest 9.x (+respx, fakeredis, @pytest.mark.integration for pgvector/db). Dashboard: Vitest 4.x + MSW 2.x. |
| **Config file** | Backend: `pyproject.toml`/`pytest.ini`; Dashboard: `dashboard/vitest.config.ts` |
| **Quick run command** | Backend: `.venv/bin/python -m pytest <touched test>`; Dashboard: `cd dashboard && bun run test <file>` |
| **Full suite command** | Backend: `.venv/bin/python -m pytest` (unset RUN_REAL_EXTERNALS); Dashboard: `cd dashboard && bun run test` |
| **Estimated runtime** | Backend ~60–120s; Dashboard ~30–60s |

---

## Sampling Rate

- **After every task commit:** Run the quick run command for the touched suite.
- **After every plan wave:** Run the full suite for the side(s) touched in that wave.
- **Before `/gsd:verify-work`:** Both full suites green; backend run with RUN_REAL_EXTERNALS unset.
- **Max feedback latency:** ~120 seconds.

---

## Per-Task Verification Map

Filled by the planner per task. Required shape (no 3 consecutive tasks without an automated verify):

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | Status |
|---------|------|------|-------------|-----------|-------------------|--------|
| 17.1-01-* | Duplicados backend | 1 | dedup-pairs | unit (compute-on-read sim, offline) | `.venv/bin/python -m pytest brave/api/routers/test_dedup*.py` | ⬜ pending |
| 17.1-02-* | Varreduras backend | 1 | runs-history | unit + `@pytest.mark.integration` migration | `.venv/bin/python -m pytest brave/.../test_runs*.py` | ⬜ pending |
| 17.1-03-* | Transition endpoint | 1 | full-pipeline sync | unit (edge allow-list + audit) | `.venv/bin/python -m pytest brave/api/routers/test_transition*.py` | ⬜ pending |
| 17.1-04-* | Frontend views/wiring | 2–3 | all views | Vitest + MSW component/handler tests | `cd dashboard && bun run test` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Backend: new test files alongside each new router (`test_dedup_pairs.py`, `test_runs_history.py`, `test_transition.py`) — offline by default, pgvector/db paths gated behind `@pytest.mark.integration` (skip-safe without `BRAVE_DB_URL`).
- [ ] Dashboard: MSW handlers mirroring the new Pydantic response shapes (dedup-pairs, runs, transition, TA-inject) + Vitest tests for Duplicados/Varreduras views and extended `mapDrop`.
- [ ] No new framework install — existing pytest + Vitest/MSW infra covers all requirements.

*Critical: integration tests must skip silently without `BRAVE_DB_URL` (see project memory — a missing DB URL can mask regressions).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual fidelity to design `Painel Brave.dc.html` (palette, spacing, 6-col board) | UI redesign slice 2 | Pixel/feel not unit-assertable | Load `/painel`, compare each view to the design canvas |
| Live drag→Mar publish actually reaches norteia-api | full-pipeline sync | Crosses the collector→platform boundary (mocked in CI) | Opt-in real run with `RUN_REAL_EXTERNALS`; observe Mar push |

---

## Validation Sign-Off

- [ ] All tasks have an automated verify or a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
