"""Contract tests — pact-python 3.x consumer tests for norteia-api push contract.

Generates Pact JSON files that verify the Mar push shape (D-16).
Uses pact-python 3.4.x API (pact.v3 module — NOT the deprecated 2.x API).
"""
