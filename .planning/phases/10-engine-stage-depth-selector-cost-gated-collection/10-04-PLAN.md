---
phase: 10-engine-stage-depth-selector-cost-gated-collection
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/components/cms/StageBadge.tsx
  - dashboard/components/cms/__tests__/StageBadge.test.tsx
autonomous: true
requirements: [ENG-06, ENG-07]
must_haves:
  truths:
    - "StageBadge renders a distinct 'nascente' variant for a record that exists only in the Nascente layer (no Rio row yet)"
    - "The nascente badge uses Norteia CSS-var tokens (no hardcoded hex), consistent with the existing badges"
    - "Existing StageBadge variants (routing/subState/score/source/validationPending) are unchanged"
  artifacts:
    - path: "dashboard/components/cms/StageBadge.tsx"
      provides: "A nascente badge variant (prop-driven) with PT-BR label"
      contains: "nascente"
  key_links:
    - from: "dashboard/components/cms/StageBadge.tsx"
      to: "Badge (CSS-var token classes)"
      via: "nascente prop renders the Nascente chip"
      pattern: "nascente"
---

<objective>
Add the per-entity **"nascente" StageBadge variant** (ENG-06): the visual primitive for a record that has a Nascente row but no Rio row yet — exactly the records the `Apenas nascente` depth produces. This is the locked "stage is implicit by table membership; the badge covers the visual need" decision — a self-contained, prop-driven badge variant, no backend/schema change.

Purpose: Give the CMS a way to render "nascente-only" stage visually, completing the cost-checkpoint UX (operators can see records parked at the free layer).
Output: A new `nascente` variant on `StageBadge` with a PT-BR label using the existing CSS-var token system, plus Vitest coverage. No new endpoint/field is introduced (keeps to the hard scope fence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-engine-stage-depth-selector-cost-gated-collection/10-CONTEXT.md

<interfaces>
<!-- StageBadge is prop-driven; add a new optional variant prop. No backend changes. -->
From dashboard/components/cms/StageBadge.tsx (existing):
  interface StageBadgeProps { routing?; subState?; score?; source?; validationPending?; className? }
  ROUTING_CLASS / SUB_STATE_CLASS use only CSS-var tokens:
    e.g. "border-transparent bg-[var(--status-mar)]/15 text-[var(--status-mar)]"
  Render order: routing → subState → score → source → validationPending (each guarded by its prop)
  Existing tokens available: --status-mar, --status-dlq, --status-descarte, --color-primary

From dashboard/components/cms/__tests__/StageBadge.test.tsx (existing):
  render(<StageBadge ... />) per-variant; assert label text + presence; mirror this style.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the nascente variant to StageBadge + Vitest</name>
  <read_first>
    - dashboard/components/cms/StageBadge.tsx (full — match the Badge + CSS-var token pattern exactly)
    - dashboard/components/cms/__tests__/StageBadge.test.tsx (full — mirror the per-variant render/assert style)
    - dashboard/app/globals.css (or wherever --status-* / --color-primary tokens are declared) to confirm an existing token to reuse for the nascente chip
  </read_first>
  <behavior>
    - StageBadgeProps gains an optional `nascente?: boolean` prop.
    - When nascente is true, a chip with the PT-BR label "Nascente" renders, styled with an existing CSS-var token (reuse --color-primary or --status-* family; no hardcoded hex), visually distinct from the routing/subState chips.
    - All existing variants render exactly as before when nascente is absent/false.
    - The nascente chip slots into the render sequence (e.g. first, before routing) and composes with other props if both are passed.
  </behavior>
  <action>
    Add `nascente?: boolean` to StageBadgeProps. In the component body add a guarded Badge block (mirroring the existing ones: `variant="outline"`, font-mono text-[12px] font-semibold, a CSS-var token class such as `border-transparent bg-[var(--color-primary)]/15 text-[var(--color-primary)]`) that renders the label "Nascente" when `nascente` is truthy. Place it at the start of the returned <span> so a nascente-only record reads stage-first. Do not alter ROUTING_CLASS/SUB_STATE_CLASS/scoreClass or any existing branch. No new token needs to be defined — reuse an existing CSS var.
  </action>
  <verify>
    <automated>cd dashboard && bun run test -- StageBadge</automated>
  </verify>
  <acceptance_criteria>
    - New Vitest cases assert: `render(<StageBadge nascente />)` shows the text "Nascente"; `render(<StageBadge />)` (no props) renders no "Nascente" text; the nascente chip class references a CSS var (e.g. matches /var\(--/) and contains no hex color literal.
    - Existing StageBadge tests still pass unchanged.
    - `grep -c 'nascente' dashboard/components/cms/StageBadge.tsx` returns at least 1 (the prop + branch).
    - `grep -E "#[0-9a-fA-F]{3,6}" dashboard/components/cms/StageBadge.tsx | grep -c .` returns 0 (no hardcoded hex introduced).
    - `cd dashboard && bun run test` passes for StageBadge (offline; component-only, no MSW/network needed).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure presentational component; no untrusted input crosses a boundary, no external/network call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | (n/a) | StageBadge nascente variant | accept | Presentational, prop-driven, no data flow or auth surface; renders a static label. No mitigation required. |
| T-10-SC | Tampering | npm installs | mitigate | No new npm packages; reuses the existing Badge primitive and CSS-var tokens. If any install is proposed, stop and require the Package Legitimacy Gate. |
</threat_model>

<verification>
- `cd dashboard && bun run test` green for StageBadge (new + existing cases).
- nascente variant renders the PT-BR "Nascente" label using a CSS-var token; no hex literals.
- No backend/schema/endpoint change (scope fence honored).
</verification>

<success_criteria>
- A prop-driven `nascente` StageBadge variant exists and renders a distinct "Nascente" chip.
- CSS-var token styling, consistent with all other StageBadge variants.
- Existing variants untouched; 100% offline Vitest coverage.
</success_criteria>

<output>
Create `.planning/phases/10-engine-stage-depth-selector-cost-gated-collection/10-04-SUMMARY.md` when done.
</output>
