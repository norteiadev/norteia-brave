---
phase: 05-auto-discovery-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brave/tasks/pipeline.py
  - tests/integration/test_sweep_uf.py
autonomous: true
requirements: [ORCH-01, ORCH-04]
must_haves:
  truths:
    - "The beat entry sweep-{uf}-daily → brave.sweep_uf resolves to a real registered task (no unregistered-task error when it fires) [D-01]"
    - "sweep_uf(uf) composes MturSeedIngest.produce(uf) + DesmembramentoAgent.produce(uf) and commits — the recurring Destinos discovery [D-01]"
    - "sweep_uf is producer-only: records land in DLQ/Mar/descarte by §7.6 automatically; it does NOT auto-validate or add a scoring branch [D-02]"
    - "Re-running sweep_uf for the same UF is a no-op (store_raw dedup by source_ref/content_hash) — replay-safe [D-01, ORCH-01]"
    - "A poison producer failure lands a PoisonQuarantine row (quarantine wrapper), not a lost record [D-01]"
    - "sweep_uf runs 100% offline with FakeMturClient + FakeLLMClient when run_real_externals=False; CI stays keyless [D-06, ORCH-04]"
    - "NotebookLM is NOT run inside sweep_uf (manual report ingest only) [Deferred]"
  artifacts:
    - path: "brave/tasks/pipeline.py"
      provides: "sweep_uf shared_task with name='brave.sweep_uf'"
      contains: "name=\"brave.sweep_uf\""
    - path: "tests/integration/test_sweep_uf.py"
      provides: "offline idempotency + quarantine + composition tests for sweep_uf"
      contains: "def test_"
  key_links:
    - from: "brave/tasks/beat_schedule.py"
      to: "brave/tasks/pipeline.py:sweep_uf"
      via: "Celery task name 'brave.sweep_uf' auto-discovered via autodiscover_tasks"
      pattern: "name=\"brave.sweep_uf\""
    - from: "brave/tasks/pipeline.py:sweep_uf"
      to: "brave/lanes/destinos/mtur.py:MturSeedIngest.produce + brave/lanes/destinos/desmembramento.py:DesmembramentoAgent.produce"
      via: "asyncio.run(producer.produce(uf))"
      pattern: "asyncio\\.run\\(.*produce\\(uf\\)\\)"
---

<objective>
Implement the missing `brave.sweep_uf(uf)` Celery task so the existing
`beat_schedule.py` 27-UF daily fan-out (sweep-{uf}-daily → "brave.sweep_uf")
resolves to a real recurring Destinos producer instead of raising an
unregistered-task error. The task composes the two destino producers
(idempotent Mtur seed re-ingest + Desmembramento LLM discovery) and lets §7.6
route records automatically. This closes ORCH-01 and the Destinos half of
ORCH-04 (offline-testable).

Purpose: The beat schedule promises a 24/7 Destinos sweep but points at a
phantom task; this delivers the real producer the schedule already expects.
Output: `sweep_uf` task in `pipeline.py` + an offline integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-auto-discovery-orchestration/05-CONTEXT.md
@.planning/phases/05-auto-discovery-orchestration/05-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly, no exploration. -->

Producer constructors (verified, brave/lanes/destinos/):
- MturSeedIngest(mtur_client, session, config)  — mtur.py:95. produce(uf) is async → None.
    Internally calls store_raw + process_nascente_record (mtur.py:149,158): producer-only, runs Rio.
- DesmembramentoAgent(llm_client, mtur_client, session, config)  — desmembramento.py:128.
    NOTE arg order: llm FIRST, then mtur. produce(uf) async → None. Only acts on
    categoria == "Oferta Principal" municipalities (desmembramento.py:157). origem=40 firewall.
- MturClient()  — no-arg offline CSV reader (brave/clients/mtur.py). Raises FileNotFoundError if
    data/mtur/municipios_mtur_*.csv missing → must be caught by the quarantine path.

Existing task to copy verbatim (decorator + session lifecycle + quarantine wrapper):
- discover_atrativo_task  — brave/tasks/pipeline.py:626-720.
- _get_session() -> (Session, engine)  — pipeline.py:220 (reads BRAVE_DB_URL, raises PermanentError if unset).
- quarantine_poison(session, nascente_id, task_name, error, payload)  — re-exported pipeline.py:241
    (import from brave.core.quarantine to respect the D-18 boundary).
- Real-vs-fake LLM selection — pipeline.py:666-671 (RealLLMClient from brave.clients.llm when
    AppConfig().run_real_externals else FakeLLMClient from tests.fakes.fake_llm).
- ScoreConfig, AppConfig from brave.config.settings (run_real_externals default False).

Fakes for the offline test (tests/fakes/):
- FakeMturClient(fixtures=[...]) — returns municipalities filtered by uf, records .calls (fake_mtur.py:22).
- FakeLLMClient(fixture_result=...) — async extract() (fake_llm.py:19).
- db_session fixture — tests/conftest.py:89.

Test analog: tests/integration/test_celery_tasks.py:15-71 (@pytest.mark.integration,
db_session, "call twice → exactly one record", PoisonQuarantine row assertion).
Producer composition analog: tests/unit/test_mtur_lane.py, tests/unit/test_desmembramento.py.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement brave.sweep_uf task (ORCH-01, D-01/D-02)</name>
  <read_first>
    - brave/tasks/pipeline.py:220-241 (_get_session, quarantine_poison re-export)
    - brave/tasks/pipeline.py:626-720 (discover_atrativo_task — the EXACT analog: decorator, session lifecycle, quarantine wrapper, real-vs-fake selection)
    - brave/lanes/destinos/mtur.py:86-162 (MturSeedIngest ctor + produce; store_raw + process_nascente_record inline)
    - brave/lanes/destinos/desmembramento.py:128-160 (DesmembramentoAgent ctor arg order + produce; Oferta-Principal-only)
    - brave/clients/mtur.py (MturClient no-arg CSV reader, FileNotFoundError path)
    - brave/config/settings.py:235-236 (run_real_externals)
    - .planning/phases/05-auto-discovery-orchestration/05-PATTERNS.md "sweep_uf(uf) — NEW task" section (verbatim composition guidance)
  </read_first>
  <behavior>
    - When run_real_externals=False, sweep_uf("BA") drives FakeMtur + FakeLLM producers and creates RioRecord rows for BA destinos (origem=100 seed + origem=40 desmembramento), routed by §7.6.
    - Calling sweep_uf("BA") twice creates no duplicate Nascente/Rio rows (store_raw dedup by content_hash) — idempotent/replay-safe.
    - A producer raising FileNotFoundError / PermanentError writes a PoisonQuarantine row with task_name="brave.sweep_uf" and payload={"uf": uf}; the record is not silently lost.
    - sweep_uf adds NO scoring/validation branch of its own — records land in DLQ/Mar/descarte purely via the producers' existing process_nascente_record (D-02).
  </behavior>
  <action>
    Add a new `@shared_task` named `sweep_uf` to brave/tasks/pipeline.py, placed near the other
    Destinos/Atrativos tasks. Copy the decorator block from discover_atrativo_task verbatim but set
    `name="brave.sweep_uf"` (this exact string is what beat_schedule.py:45 already expects — DO NOT
    rename the beat entry), keep `bind=True, max_retries=3, default_retry_delay=60, acks_late=True,
    reject_on_worker_lost=True`, and set `time_limit=600` (per-município fan-out needs headroom).
    Signature: `def sweep_uf(self, uf: str) -> None:`.

    Body: open `_get_session()`, then in the try block instantiate `config = ScoreConfig()` and
    `app_config = AppConfig()`. First run the Mtur seed: import `MturClient` from brave.clients.mtur,
    build `MturSeedIngest(MturClient(), session, config)` and `asyncio.run(seed.produce(uf))`
    (idempotent re-ingest — store_raw dedups). Then run Desmembramento: select the LLM client exactly
    like discover_atrativo_task (RealLLMClient from brave.clients.llm when app_config.run_real_externals
    else FakeLLMClient from tests.fakes.fake_llm), build
    `DesmembramentoAgent(llm_client, MturClient(), session, config)` — note llm FIRST then mtur — and
    `asyncio.run(desm.produce(uf))`. Then `session.commit()`. Do NOT run NotebookLM here (manual ingest
    only, per the Deferred decision) and do NOT add any scoring/validation branch — the producers'
    internal process_nascente_record already routes records (D-02).

    Copy the quarantine wrapper from discover_atrativo_task verbatim: `except PermanentError` →
    rollback + fresh-session `quarantine_poison(..., task_name="brave.sweep_uf", payload={"uf": uf})`;
    generic `except Exception` → `self.retry(...)` then on `MaxRetriesExceededError` the same
    quarantine_poison block. Import quarantine_poison from brave.core.quarantine (D-18 boundary).
    `finally`: session.close() + engine.dispose(). Wrap MturClient()'s FileNotFoundError into the
    PermanentError/quarantine path so a missing CSV is quarantined, not crashed.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from brave.tasks.pipeline import sweep_uf; assert sweep_uf.name == 'brave.sweep_uf'; print('ok')"</automated>
  </verify>
  <done>
    `sweep_uf.name == "brave.sweep_uf"`; the task imports and is decorated with acks_late=True,
    reject_on_worker_lost=True, time_limit=600; body composes MturSeedIngest + DesmembramentoAgent and
    commits; quarantine wrapper mirrors discover_atrativo_task with task_name="brave.sweep_uf".
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline integration test for sweep_uf (ORCH-04, D-06)</name>
  <read_first>
    - tests/integration/test_celery_tasks.py:1-71 (the EXACT test analog: @pytest.mark.integration, db_session, twice→one-record, PoisonQuarantine assertion)
    - tests/unit/test_mtur_lane.py, tests/unit/test_desmembramento.py (producer composition assertions, fixtures)
    - tests/fakes/fake_mtur.py:22-59 (FakeMturClient(fixtures=[...]) filtered-by-uf + .calls)
    - tests/fakes/fake_llm.py:19-95 (FakeLLMClient(fixture_result=...))
    - tests/conftest.py:89-112 (db_session, fake_redis fixtures)
    - the sweep_uf task written in Task 1
  </read_first>
  <behavior>
    - test_sweep_uf_ingests_destinos: invoking sweep_uf inline (sweep_uf.run("BA") or the underlying function with fakes injected) creates RioRecords for BA with entity_type="destination".
    - test_sweep_uf_idempotent: running the sweep twice produces no duplicate Nascente/Rio rows for the same source_ref.
    - test_sweep_uf_quarantines_poison: a producer that raises lands a PoisonQuarantine row with task_name="brave.sweep_uf".
    - test_sweep_uf_no_notebooklm: the sweep never instantiates / calls a NotebookLM client (assert via the fake-client set used, or that no source="notebooklm" Nascente row is created by the sweep).
  </behavior>
  <action>
    Create tests/integration/test_sweep_uf.py mirroring test_celery_tasks.py's structure
    (@pytest.mark.integration, db_session fixture). Because sweep_uf builds its own producers from
    AppConfig().run_real_externals, drive the offline path: ensure run_real_externals is False
    (default) so FakeLLMClient is selected, and supply FakeMturClient fixtures the way test_mtur_lane
    does. If sweep_uf instantiates MturClient() directly (real CSV reader), structure the test to use
    the bundled data/mtur CSV OR factor the producer construction so the test can inject fakes — prefer
    the lowest-friction approach already used by the destinos lane tests (the CSV reader is offline and
    keyless, so calling the real MturClient against the bundled fixture CSV is acceptable and keeps the
    test keyless). Assert: (1) RioRecords with entity_type="destination" exist for the swept UF after
    one run; (2) a second run adds zero new Nascente rows for the same source_ref (idempotency);
    (3) a forced producer failure writes a PoisonQuarantine row with task_name="brave.sweep_uf";
    (4) no source="notebooklm" Nascente row is produced by the sweep. No test may hit a real network or
    require a key (D-06 / ORCH-04).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/integration/test_sweep_uf.py -x -q</automated>
  </verify>
  <done>
    All tests in test_sweep_uf.py pass offline (keyless). Idempotency, quarantine, destino-ingest, and
    no-NotebookLM assertions are present and green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| beat scheduler → sweep_uf | scheduled internal trigger; no external input beyond the UF code |
| sweep_uf → external producers (Mtur CSV, OpenRouter/DeepSeek LLM) | crosses the client boundary; gated by run_real_externals + cost guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | sweep_uf LLM/Places fan-out (replay amplification) | mitigate | store_raw content_hash dedup makes a retried sweep a no-op; the existing USD cost guard (OBS-02) caps DeepSeek spend; acks_late + idempotent body prevent double-charge runaway |
| T-05-02 | Elevation of Privilege | sweep_uf bypassing the §7.6 gate / auto-promoting | mitigate | sweep_uf is producer-only (D-02) — no scoring/validation branch; promotion to Mar stays behind §7.6 routing + the human DLQ steward gate (unchanged) |
| T-05-03 | Tampering | malformed producer output / missing Mtur CSV | mitigate | quarantine wrapper routes PermanentError (incl. FileNotFoundError) to PoisonQuarantine; Desmembramento keeps its instructor 2nd-layer validator (origem=40 firewall) |
| T-05-SC | Tampering | npm/pip installs | accept | no new packages added in this plan; all producers + fakes already vendored (RESEARCH Package Legitimacy Gate satisfied in prior phases) |
</threat_model>

<verification>
- `sweep_uf.name == "brave.sweep_uf"` so the beat entry resolves.
- Offline test suite green and keyless: `.venv/bin/python -m pytest tests/integration/test_sweep_uf.py -x -q`.
- No new external network calls in the offline path (run_real_externals=False uses fakes).
</verification>

<success_criteria>
- The phantom beat task is gone: `brave.sweep_uf` is registered and composes both destino producers.
- Re-run is a no-op (store_raw dedup); poison is quarantined, not lost.
- Producer-only (no auto-validate) — §7.6 + human DLQ gate stays the promotion path.
- 100% offline, keyless.
</success_criteria>

<output>
Create `.planning/phases/05-auto-discovery-orchestration/05-01-SUMMARY.md` when done.
</output>
