---
phase: 01-brave-core-score-gate-boundary-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - brave/core/score/__init__.py
  - brave/core/score/schemas.py
  - brave/core/score/engine.py
  - brave/core/score/simulation.py
  - brave/core/nascente/__init__.py
  - brave/core/nascente/service.py
  - brave/core/rio/__init__.py
  - brave/core/rio/dedup.py
  - brave/core/rio/normalize.py
  - brave/core/rio/label.py
  - brave/core/rio/routing.py
  - brave/core/mar/__init__.py
  - brave/core/mar/service.py
  - brave/observability/llm_tracker.py
  - brave/observability/cost_guard.py
  - brave/observability/audit.py
  - brave/tasks/celery_app.py
  - brave/tasks/beat_schedule.py
  - brave/tasks/pipeline.py
  - brave/api/main.py
  - brave/api/deps.py
  - brave/api/routers/__init__.py
  - brave/api/routers/health.py
  - brave/api/routers/metrics.py
  - brave/api/routers/audit.py
  - brave/api/routers/dlq.py
  - brave/api/routers/webhook.py
  - tests/fakes/fake_llm.py
  - tests/fakes/fake_norteia_api.py
  - tests/fakes/fake_places.py
  - tests/unit/test_score_engine.py
  - tests/unit/test_score_simulation.py
  - tests/unit/test_routing.py
  - tests/unit/test_dedup.py
  - tests/integration/test_nascente_service.py
  - tests/integration/test_rio_pipeline.py
  - tests/integration/test_celery_tasks.py
  - tests/integration/test_cost_guard.py
autonomous: true
requirements:
  - CORE-02
  - CORE-03
  - CORE-05
  - CORE-06
  - CORE-07
  - CORE-08
  - CORE-09
  - CORE-10
  - CORE-11
  - CORE-12
  - SCORE-01
  - SCORE-02
  - SCORE-03
  - OBS-01
  - OBS-02
  - OBS-03
  - OBS-04
  - TEST-01
  - TEST-03

must_haves:
  truths:
    - "compute_score(ScoreInput, ScoreConfig) is a pure function (zero I/O) that returns a ScoreResult with correct routing for Mar (≥85), DLQ (51-84.9), descarte (≤50)"
    - "simulate_distribution(config, samples) returns stats dict showing %mar, %dlq, %descarte for a set of cold-start inputs (validacao_humana=0)"
    - "Nascente.store_raw writes an immutable row to nascente_records with content_hash, version, source, entity_type, uf per D-04"
    - "Rio pipeline (dedup → normalize → label → score → route) processes a Nascente record and sets routing on the RioRecord"
    - "Two-stage dedup: exact content_hash check first, then territorial-key blocked pgvector fuzzy within same UF+municipio_id per D-07"
    - "Celery tasks process_nascente and push_mar are idempotent (re-run is a no-op)"
    - "Poison messages after max_retries land in poison_quarantine, NOT in the review DLQ"
    - "USD cost guard halts before dispatching an LLM call when daily Redis counter exceeds usd_daily_budget"
    - "FastAPI /api/v1/health returns 200; /api/v1/dlq returns DLQ records; /api/v1/metrics returns layer counts"
    - "All 8 client fakes implement the corresponding Protocol (structural typing) and all unit/integration tests pass with --disable-socket"
    - "Decisions covered by this plan: D-02, D-05, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-20, D-21 (routing values in Rio, celery-redbeat, HNSW search, faked LLM Protocol, pinned-slug/data_collection deny, validate-or-quarantine, pure score engine, score_version stamp, simulation harness, llm_generations + enforcing cost guard, FastAPI metrics/DLQ/audit surface)"
  artifacts:
    - path: "brave/core/score/engine.py"
      provides: "compute_score pure function"
      exports: ["compute_score"]
    - path: "brave/core/score/simulation.py"
      provides: "simulate_distribution histogram harness"
      exports: ["simulate_distribution"]
    - path: "brave/core/nascente/service.py"
      provides: "store_raw, get_nascente functions"
      exports: ["store_raw", "get_nascente"]
    - path: "brave/core/rio/dedup.py"
      provides: "find_duplicate with territorial-key blocking"
      exports: ["find_duplicate"]
    - path: "brave/core/rio/routing.py"
      provides: "process_nascente_record, route_by_score, reprocess_record"
      exports: ["process_nascente_record", "route_by_score", "reprocess_record"]
    - path: "brave/observability/cost_guard.py"
      provides: "pre_dispatch_check, record_spend, CostGuardError"
      exports: ["pre_dispatch_check", "record_spend", "CostGuardError"]
    - path: "brave/tasks/pipeline.py"
      provides: "process_nascente, push_mar Celery tasks"
      exports: ["process_nascente", "push_mar", "reprocess_record_task"]
    - path: "brave/api/main.py"
      provides: "FastAPI app with all Phase 1 routers"
      exports: ["app"]
    - path: "tests/fakes/fake_llm.py"
      provides: "FakeLLMClient implementing LLMClientProtocol"
    - path: "tests/fakes/fake_norteia_api.py"
      provides: "FakeNorteiaApiClient implementing NorteiaApiClientProtocol"
  key_links:
    - from: "brave/core/score/engine.py"
      to: "brave/core/rio/routing.py"
      via: "route_by_score calls compute_score"
      pattern: "compute_score"
    - from: "brave/tasks/pipeline.py"
      to: "brave/core/rio/routing.py"
      via: "process_nascente task calls process_nascente_record"
    - from: "brave/observability/cost_guard.py"
      to: "brave/observability/llm_tracker.py"
      via: "pre_dispatch_check uses Redis counter; LLMTracker calls pre_dispatch_check before invoking client"
    - from: "brave/api/routers/webhook.py"
      to: "brave/core/mar/service.py"
      via: "error-report endpoint calls reopen_from_error_report"
---

<objective>
Implement the three processing layers (Nascente service, Rio pipeline, Mar service), the pure §7.6 score engine with histogram simulation harness, all 8 client fakes, the Celery orchestration layer (idempotent tasks, poison quarantine, celery-redbeat beat), the observability stack (llm_tracker, cost_guard, audit, structlog), and the FastAPI surface (health, metrics, DLQ, audit, webhook routers).

Purpose: This plan completes the functional pipeline spine. After it finishes, a fixture payload can be ingested into Nascente, processed through Rio with score §7.6, routed to Mar/DLQ/descarte, and the full suite runs offline with no external calls.

Output: Passing `pytest -q --disable-socket` (unit + integration) covering score boundary cases, dedup, routing, Celery idempotency, cost guard, and FastAPI endpoints.
</objective>

<decisions_covered>
## Decisions Covered (from 01-CONTEXT.md)
This plan implements: D-02, D-05, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-20, D-21 — routing values within Rio (D-02), celery-redbeat single-beat orchestration (D-05), territorial-key-blocked two-stage dedup (D-07), HNSW vector search (D-08), faked LLM client via Protocol (D-09), pinned-slug/data_collection=deny usage (D-10), validate-or-quarantine second layer (D-11), pure zero-I/O score engine (D-12), `score_version` stamping (D-13), score-distribution simulation harness (D-14), `llm_generations` + enforcing pre-dispatch USD cost guard (D-20), FastAPI metrics/DLQ/audit surface (D-21).
</decisions_covered>

<execution_context>
@/Users/leandro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leandro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-brave-core-score-gate-boundary-contract/01-CONTEXT.md
@.planning/phases/01-brave-core-score-gate-boundary-contract/01-RESEARCH.md
@.planning/phases/01-brave-core-score-gate-boundary-contract/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs, derived from Plan 01. -->
<!-- Read brave/core/models.py and brave/config/settings.py after Plan 01 executes. -->
<!-- The contracts below are what Plan 01 produces; do not re-implement. -->

From brave/config/settings.py (produced by Plan 01):
  class ScoreConfig(BaseSettings):
      weight_origem: float = 30.0
      weight_completude: float = 20.0
      weight_corroboracao: float = 20.0
      weight_atualidade: float = 15.0
      weight_validacao_humana: float = 15.0
      threshold_mar: float = 85.0
      threshold_dlq: float = 51.0
      score_version: str = "v1.0"
      model_config = SettingsConfigDict(env_prefix="BRAVE_SCORE_")

  class LLMConfig(BaseSettings):
      openrouter_base_url: str = "https://openrouter.ai/api/v1"
      deepseek_primary_slug: str = "deepseek/deepseek-chat"
      provider_data_collection: str = "deny"
      usd_daily_budget: float = 10.0
      model_config = SettingsConfigDict(env_prefix="BRAVE_LLM_")

  class DBConfig(BaseSettings):
      url: str  # required
      redis_url: str = "redis://localhost:6379/0"
      model_config = SettingsConfigDict(env_prefix="BRAVE_DB_")

From brave/core/models.py (produced by Plan 01):
  NascenteRecord: id (UUID PK), source (str), source_ref (str, indexed), entity_type (str),
    uf (str), payload (JSON/JSONB), content_hash (str, indexed), version (int), ingested_at,
    superseded_by_id (FK nullable self-ref)
  RioRecord: id (UUID PK), nascente_id (FK), entity_type, uf, municipio_id, routing (default "in_progress"),
    dlq_reason, sub_state, normalized (JSON), embedding (Vector(1536)), score, score_breakdown (JSON),
    score_version, processed_at, canonical_key (unique)
  MarRecord: id (UUID PK), rio_id (FK), entity_type, source_ref (unique), canonical (JSON),
    provenance (JSON), reliability_score, score_version, parent_mar_id (FK nullable), superseded_by_id (FK nullable), published_at

From brave/clients/base.py (produced by Plan 01):
  LLMClientProtocol: async def extract(self, prompt: str, schema: type, mode: str = "tools") -> Any
  NorteiaApiClientProtocol: async def push_destination(self, payload: dict) -> dict
                             async def push_attraction(self, payload: dict) -> dict
  PlacesClientProtocol: async def text_search(self, query: str, uf: str) -> list[dict]
                         async def place_details(self, place_id: str) -> dict
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: §7.6 score engine, simulation harness, and all 8 client fakes</name>
  <read_first>
    .planning/phases/01-brave-core-score-gate-boundary-contract/01-RESEARCH.md
    — Sections "Pattern 1: Pure Score Engine with Config-Driven Weights", "Pattern 5: Score Distribution Simulation Harness", "Pattern 2: Client Boundary — Protocol + Fake + Real Swap", "§7.6 ScoreConfig via pydantic-settings"
    .planning/phases/01-brave-core-score-gate-boundary-contract/01-CONTEXT.md
    — Decisions D-09 (instructor Mode.Tools), D-10 (data_collection deny), D-11 (validate-or-quarantine), D-12 (pure score function), D-13 (score_version stamp), D-14 (histogram simulation harness first)
    .planning/research/PITFALLS.md
    — Pitfall 1 (DLQ landfill, simulation harness), Pitfall 6 (score_version)
  </read_first>
  <files>
    brave/core/score/__init__.py,
    brave/core/score/schemas.py,
    brave/core/score/engine.py,
    brave/core/score/simulation.py,
    tests/fakes/fake_llm.py,
    tests/fakes/fake_norteia_api.py,
    tests/fakes/fake_places.py,
    tests/unit/test_score_engine.py,
    tests/unit/test_score_simulation.py
  </files>
  <behavior>
    Score engine:
    - Test: compute_score with origem_value=100, completude_value=100, corroboracao_value=100, atualidade_value=100, validacao_humana_value=100, default config → score=100.0, routing="mar"
    - Test: compute_score with origem_value=100, completude_value=80, corroboracao_value=60, atualidade_value=40, validacao_humana_value=0, default config → score=30+16+12+6+0=64.0, routing="dlq"
    - Test: compute_score with origem_value=40, completude_value=20, corroboracao_value=0, atualidade_value=0, validacao_humana_value=0, default config → score=12+4+0+0+0=16.0, routing="descarte"
    - Test: compute_score result carries score_version == config.score_version
    - Test: compute_score threshold boundary: score exactly 85.0 → routing="mar"; score exactly 84.9 → routing="dlq"; score exactly 51.0 → routing="dlq"; score exactly 50.9 → routing="descarte"
    - Test: compute_score is a pure function — calling it twice with same inputs produces identical result (no side effects)

    Simulation harness:
    - Test: simulate_distribution with 100 cold-start samples (origem_value=40, completude_value=30, corroboracao_value=0, atualidade_value=0, validacao_humana_value=0) returns dict with keys total, mar_pct, dlq_pct, descarte_pct, mean, stdev — dlq_pct > 50 (cold-start DLQ landfill warning validation)
    - Test: simulate_distribution output totals to 100% (mar_pct + dlq_pct + descarte_pct == 100.0)

    Client fakes:
    - Test: FakeLLMClient.extract returns the pre-configured fixture result and appends to .calls list
    - Test: FakeNorteiaApiClient.push_destination records the call and returns {"id": str, "source_ref": str}
    - Test: FakeLLMClient and FakeNorteiaApiClient satisfy their Protocol interfaces (structural typing — assign to annotated variable of Protocol type without error)
  </behavior>
  <action>
    Create brave/core/score/schemas.py with three Pydantic BaseModel classes:
    - ScoreInput: origine_value (float 0-100), completude_value (float 0-100), corroboracao_value (float 0-100), atualidade_value (float 0-100), validacao_humana_value (float 0-100). All fields validated 0 ≤ value ≤ 100.
    - ScoreBreakdown: origem (float), completude (float), corroboracao (float), atualidade (float), validacao_humana (float)
    - ScoreResult: score (float), routing (Literal["mar", "dlq", "descarte"]), score_version (str), breakdown (ScoreBreakdown)

    Create brave/core/score/engine.py with a single function compute_score(inp: ScoreInput, config: ScoreConfig) -> ScoreResult. The function must be pure (no imports of db session, no side effects, no global state mutation). Calculate each criterion: origem_pts = inp.origem_value * config.weight_origem / 100, same pattern for all five. Sum to total. Route: if total >= config.threshold_mar → "mar", elif total >= config.threshold_dlq → "dlq", else "descarte". Return ScoreResult with score=round(total, 2), routing, score_version=config.score_version, breakdown=ScoreBreakdown(origem=origem_pts, ...). Never import SQLAlchemy, httpx, or any I/O library in this file.

    Create brave/core/score/simulation.py with simulate_distribution(config: ScoreConfig, samples: list[ScoreInput]) -> dict. Compute scores for all samples using compute_score. Count routing outcomes. Compute mean/stdev using statistics stdlib. Return {"total": n, "mar_pct": float, "dlq_pct": float, "descarte_pct": float, "mean": float, "stdev": float}. Note: stdev requires n >= 2; handle n==1 by returning stdev=0.0. Include a generate_cold_start_samples(n: int, origem_value: float = 40.0) -> list[ScoreInput] helper that produces n samples representative of cold-start conditions (completude 20-50 random, corroboracao=0, atualidade=0, validacao_humana=0).

    Create tests/fakes/fake_llm.py with FakeLLMClient. Constructor accepts fixture_result: Any and optionally raise_on_call: Exception | None = None. Implements async def extract(self, prompt: str, schema: type, mode: str = "tools") -> Any by appending to self.calls: list[dict] and returning fixture_result (or raising if raise_on_call set). The class structurally satisfies LLMClientProtocol.

    Create tests/fakes/fake_norteia_api.py with FakeNorteiaApiClient. Maintains self.push_destination_calls: list[dict] and self.push_attraction_calls: list[dict]. push_destination(payload) appends payload and returns {"id": str(uuid4()), "source_ref": payload.get("source_ref", "")}. push_attraction same pattern. Optionally accepts should_fail: bool for error-path tests.

    Create tests/fakes/fake_places.py with FakePlacesClient. Constructor accepts fixture_results: dict[str, list[dict]] keyed by query. Returns fixture or empty list/dict. Stub only for Phase 1; real impl deferred to Phase 3.

    Create tests/unit/test_score_engine.py with parametrized @pytest.mark.parametrize covering all behavior cases above plus boundary cases: score exactly at 85.0, 84.9, 51.0, 50.9 thresholds.

    Create tests/unit/test_score_simulation.py covering simulation harness behavior cases.
  </action>
  <verify>
    <automated>pytest tests/unit/test_score_engine.py tests/unit/test_score_simulation.py -v --disable-socket</automated>
  </verify>
  <acceptance_criteria>
    1. pytest tests/unit/test_score_engine.py -v exits 0 with all parametrized cases passing (including all 4 threshold boundaries).
    2. pytest tests/unit/test_score_simulation.py -v exits 0.
    3. `python -c "from brave.core.score.engine import compute_score; import inspect; assert len(inspect.getsource(compute_score).split('import')) == 1 or True"` — compute_score contains no sqlalchemy/httpx/redis import (grep check: grep -n "sqlalchemy\|httpx\|redis\|Session" brave/core/score/engine.py exits 1 — no matches).
    4. `grep -c "from brave.clients.base import LLMClientProtocol" tests/fakes/fake_llm.py` >= 1 — fake imports and structurally satisfies Protocol.
    5. Cold-start simulation: `python -c "from brave.core.score.simulation import simulate_distribution, generate_cold_start_samples; from brave.config.settings import ScoreConfig; s=simulate_distribution(ScoreConfig(), generate_cold_start_samples(200)); print(s); assert s['dlq_pct'] > 0"` — DLQ landfill warning visible in output.
    6. Score version stamp: every ScoreResult.score_version matches the ScoreConfig.score_version used (assert in test).
  </acceptance_criteria>
  <done>Pure score engine with all 5-criterion boundary cases tested; simulation harness shows cold-start DLQ distribution; all 8 client fakes created; tests pass offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rio pipeline, Nascente/Mar services, Celery tasks, observability, and FastAPI surface</name>
  <read_first>
    .planning/phases/01-brave-core-score-gate-boundary-contract/01-RESEARCH.md
    — Sections "Pattern 3: Celery Task Idempotency and Poison Quarantine", "Pattern 4: Two-Stage Dedup with Territorial Key Blocking", "System Architecture Diagram", "FastAPI surface routers"
    .planning/phases/01-brave-core-score-gate-boundary-contract/01-CONTEXT.md
    — Decisions D-02 (DLQ routing column), D-03 (supersession), D-05 (celery-redbeat), D-06 (orchestration interface), D-07/D-08 (dedup + HNSW), D-20 (llm_generations + cost guard pre-dispatch), D-21 (FastAPI metrics/DLQ/audit)
    .planning/research/PITFALLS.md
    — Pitfall 7 (Celery operational — idempotency, poison quarantine, beat singleton)
    — Pitfall 8 (cost guard enforcing, not advisory)
    — Pitfall 11 (offline CI guard — pytest-socket)
  </read_first>
  <files>
    brave/core/nascente/__init__.py,
    brave/core/nascente/service.py,
    brave/core/rio/__init__.py,
    brave/core/rio/dedup.py,
    brave/core/rio/normalize.py,
    brave/core/rio/label.py,
    brave/core/rio/routing.py,
    brave/core/mar/__init__.py,
    brave/core/mar/service.py,
    brave/observability/llm_tracker.py,
    brave/observability/cost_guard.py,
    brave/observability/audit.py,
    brave/tasks/celery_app.py,
    brave/tasks/beat_schedule.py,
    brave/tasks/pipeline.py,
    brave/api/main.py,
    brave/api/deps.py,
    brave/api/routers/__init__.py,
    brave/api/routers/health.py,
    brave/api/routers/metrics.py,
    brave/api/routers/audit.py,
    brave/api/routers/dlq.py,
    brave/api/routers/webhook.py,
    tests/unit/test_routing.py,
    tests/unit/test_dedup.py,
    tests/integration/test_nascente_service.py,
    tests/integration/test_rio_pipeline.py,
    tests/integration/test_celery_tasks.py,
    tests/integration/test_cost_guard.py
  </files>
  <behavior>
    Nascente:
    - Test: store_raw(session, source="mtur", source_ref="mtur:BA:1", entity_type="destination", uf="BA", payload={"name":"Trancoso"}) creates NascenteRecord with correct fields, content_hash is SHA-256 of deterministic JSON, version=1
    - Test: calling store_raw twice with identical (source, source_ref, payload) returns the same record (idempotent — no duplicate row)
    - Test: NascenteRecord is never mutated — calling store_raw with updated payload creates a new version row and sets superseded_by_id on the old row

    Rio dedup:
    - Test: find_duplicate returns None when no existing records for same UF+municipio_id
    - Test: find_duplicate returns existing record when content_hash matches exactly (regardless of embedding)
    - Test: find_duplicate NEVER compares records with different uf values (territorial-key block enforcement — homonym São Domingos/BA vs São Domingos/SE cannot be merge candidates)

    Routing:
    - Test: route_by_score with score=87.0 sets RioRecord.routing="mar"
    - Test: route_by_score with score=65.0 sets RioRecord.routing="dlq" and sets dlq_reason non-empty
    - Test: route_by_score with score=30.0 sets RioRecord.routing="descarte"
    - Test: reprocess_record resets RioRecord.routing to "in_progress" and re-runs scoring (idempotent — calling twice produces same result)

    Celery idempotency:
    - Test: process_nascente called twice with same nascente_id produces exactly one RioRecord (idempotent)
    - Test: after max_retries failures, PoisonQuarantine row exists for the task; RioRecord.routing is NOT "dlq" (poison ≠ review DLQ)

    Cost guard:
    - Test: pre_dispatch_check raises CostGuardError when Redis daily counter >= usd_daily_budget
    - Test: pre_dispatch_check passes (no raise) when counter < budget
    - Test: record_spend increments Redis counter by the spent USD amount

    FastAPI:
    - Test: GET /api/v1/health returns {"status": "ok"} with 200
    - Test: GET /api/v1/metrics returns JSON with keys nascente_count, rio_count (by routing), mar_count (all ints)
    - Test: GET /api/v1/dlq returns list of DLQ records (routing="dlq" rows)
    - Test: POST /webhook/error-report with a valid X-Webhook-Secret header and valid JSON body {"source_ref": "mtur:BA:1"} returns 202 and resets RioRecord.routing to "dlq" for the corresponding MarRecord's source_ref
    - Test: POST /webhook/error-report with a missing or wrong X-Webhook-Secret header returns 401 and does NOT touch the MarRecord/RioRecord (no reopen on bad secret)
  </behavior>
  <action>
    Create brave/core/nascente/service.py with:
    - store_raw(session: Session, source: str, source_ref: str, entity_type: str, uf: str, payload: dict) -> NascenteRecord. Compute content_hash = hashlib.sha256(json.dumps(payload, sort_keys=True).encode()).hexdigest(). Check for existing row: SELECT ... WHERE source == source AND source_ref == source_ref AND content_hash == content_hash. If found, return existing (idempotent). If new payload for existing source_ref, supersede: create new version row (version=old.version+1), set old.superseded_by_id = new.id, flush. Return new row. Never update existing rows.
    - get_nascente(session: Session, nascente_id: UUID) -> NascenteRecord | None

    Create brave/core/rio/dedup.py with:
    - find_duplicate(session: Session, uf: str, municipio_id: str | None, entity_type: str, content_hash: str, embedding: list[float] | None = None) -> RioRecord | None. Stage 1: exact hash check against NascenteRecord. Stage 2: if embedding provided, query RioRecord WHERE uf==uf AND municipio_id==municipio_id AND entity_type==entity_type (territorial-key block) ORDER BY embedding.cosine_distance(embedding) LIMIT 10. Return first candidate with cosine similarity > 0.95. NEVER compare across UF boundary. If municipio_id is None, skip vector search (no block defined → no fuzzy dedup).
    - compute_embedding(text: str) -> list[float]: returns a deterministic stub embedding (np.zeros(1536).tolist()) for Phase 1; real embedding via LLMClient in Phase 2. Note this is intentional — Phase 1 doesn't call real embedding APIs.

    Create brave/core/rio/normalize.py with:
    - normalize_name(name: str) -> str: strip whitespace, titlecase, remove double spaces.
    - normalize_coordinates(lat: float | None, lon: float | None) -> tuple[float | None, float | None]: round to 6 decimal places.
    - normalize_address(address: str | None) -> str | None: strip and titlecase.

    Create brave/core/rio/label.py with:
    - label_entity(entity_type: str, normalized: dict) -> dict: returns normalized dict with a "labels" key added. Phase 1 stub: labels = {"entity_type": entity_type, "taxonomy_version": "v1.0"}. Real NLP labeling deferred to Phase 2 when lane data arrives.

    Create brave/core/rio/routing.py with:
    - route_by_score(session: Session, rio_record: RioRecord, config: ScoreConfig) -> RioRecord: calls compute_score(ScoreInput from rio_record.normalized), sets rio_record.routing, rio_record.score, rio_record.score_breakdown, rio_record.score_version, sets dlq_reason if routing=="dlq". Acquires SELECT FOR UPDATE lock on rio_record before state transition.
    - process_nascente_record(session: Session, nascente: NascenteRecord, config: ScoreConfig, llm_client: LLMClientProtocol | None = None) -> RioRecord: orchestrates dedup → normalize → label → route. Creates RioRecord if not exists (idempotency: check canonical_key uniqueness). Returns existing if already processed.
    - reprocess_record(session: Session, rio_id: UUID, config: ScoreConfig) -> RioRecord: fetches RioRecord, resets routing to "in_progress", re-runs route_by_score. Idempotent.

    Create brave/core/mar/service.py with:
    - promote_to_mar(session: Session, rio_record: RioRecord) -> MarRecord: creates MarRecord with source_ref from rio_record, provenance={"score_breakdown": rio_record.score_breakdown, "score_version": rio_record.score_version}, reliability_score=rio_record.score. Idempotent: if MarRecord with source_ref already exists, update reliability_score + supersede old row (new supersedes old via superseded_by_id).
    - reopen_from_error_report(session: Session, source_ref: str) -> RioRecord | None: finds active MarRecord by source_ref, creates/resets its linked RioRecord to routing="dlq" with dlq_reason="community_error_report". Returns the DLQ'd RioRecord.

    Create brave/observability/cost_guard.py with:
    - CostGuardError(Exception)
    - pre_dispatch_check(redis_client: Redis, config: LLMConfig) -> None: key = "brave:cost:daily:{today}", get counter, if >= config.usd_daily_budget raise CostGuardError(f"Daily budget {config.usd_daily_budget} USD exceeded").
    - record_spend(redis_client: Redis, usd_amount: float) -> float: INCRBYFLOAT key by usd_amount, set TTL to end-of-day seconds if key newly created. Returns new counter value.

    Create brave/observability/llm_tracker.py with:
    - LLMTracker wrapping a client: async def track_and_call(self, lane: str, model_slug: str, session: Session, redis_client: Redis, config: LLMConfig, call_fn: Callable) -> Any: calls pre_dispatch_check, invokes call_fn, writes LLMGeneration row with lane/model_slug/resolved_provider/tokens/usd_cost. Resolved provider defaults to model_slug in Phase 1 (real provider from OpenRouter response headers in Phase 2+).

    Create brave/observability/audit.py with:
    - write_audit(session: Session, action: str, entity_type: str | None = None, record_id: UUID | None = None, before_state: dict | None = None, after_state: dict | None = None, actor: str = "pipeline") -> None: inserts AuditLog row, also emits structlog JSON log entry with same fields.

    Create brave/tasks/celery_app.py with:
    - Celery app: app = Celery("norteia_brave"), configure with Redis broker/backend from DBConfig.redis_url, enable celery-redbeat: app.config_from_object with redbeat_redis_url, redbeat_key_prefix="brave". Set task_acks_late=True, task_reject_on_worker_lost=True, task_time_limit=300, worker_prefetch_multiplier=1.

    Create brave/tasks/beat_schedule.py with RedBeatSchedulerEntry for sweep_uf tasks (one per UF from a UF_LIST constant). In Phase 1, just define the structure; actual UF sweep tasks are stubs (no real producer yet).

    Create brave/tasks/pipeline.py with three Celery tasks:
    - @shared_task(bind=True, max_retries=3, default_retry_delay=60, name="brave.process_nascente") def process_nascente(self, nascente_id: str) -> None: idempotency check (if RioRecord exists for nascente_id, return), call process_nascente_record. Catch TransientError → self.retry(exc=exc). Catch PermanentError or unhandled after max_retries → quarantine_poison(nascente_id, task_name, error).
    - @shared_task(bind=True, max_retries=3, name="brave.push_mar") def push_mar(self, rio_id: str) -> None: fetch RioRecord, if routing != "mar" return (idempotent), call promote_to_mar. Actual NorteiaApi push deferred to Plan 03 (the real client ships there); task records intent only in Phase 1 via audit log.
    - @shared_task(bind=True, name="brave.reprocess_record") def reprocess_record_task(self, rio_id: str) -> None: calls reprocess_record.
    - quarantine_poison(nascente_id, task_name, error): inserts PoisonQuarantine row.

    Create brave/api/main.py with FastAPI() app that includes routers for health, metrics, dlq, audit, webhook.

    Create brave/api/deps.py with get_db() yielding a SQLAlchemy Session, get_redis() yielding Redis, get_config() returning AppConfig.

    Create brave/api/routers/health.py: GET /api/v1/health → {"status": "ok", "db": "ok", "redis": "ok"} (checks DB connection and Redis ping).

    Create brave/api/routers/metrics.py: GET /api/v1/metrics → aggregates SELECT COUNT(*) from nascente_records, rio_records grouped by routing, mar_records. Returns {"nascente_count": int, "rio_count": {"in_progress": int, "mar": int, "dlq": int, "descarte": int}, "mar_count": int}.

    Create brave/api/routers/dlq.py:
    - GET /api/v1/dlq → list of RioRecord where routing="dlq" (optional ?uf= and ?entity_type= filter params; default limit=50)
    - PATCH /api/v1/dlq/{rio_id}/reprocess → calls reprocess_record_task.delay(rio_id); writes audit log; returns 202
    - PATCH /api/v1/dlq/{rio_id}/descarte → sets routing="descarte", dlq_reason="steward_rejected"; audit log; returns 200

    Create brave/api/routers/audit.py: GET /api/v1/audit → paginated list of AuditLog entries (default limit=100, offset=0).

    Create brave/api/routers/webhook.py: POST /webhook/error-report → FIRST enforce a static shared-secret: require header X-Webhook-Secret equal to config webhook_secret (read from env BRAVE_WEBHOOK_SECRET via AppConfig / a WebhookConfig pydantic-settings field with env_prefix="BRAVE_WEBHOOK_"); if the header is missing or does not match, return 401 BEFORE any DB work or rate-limit increment (constant-time compare via hmac.compare_digest; never log the secret). Then validate JSON body has "source_ref" (Pydantic model ErrorReportRequest), call reopen_from_error_report. Returns 202 if found, 404 if source_ref not in Mar. Rate limiting: check request IP against Redis key "brave:webhook:ratelimit:{ip}" (max 10 calls/minute per IP — enforced by INCR + EXPIRE; return 429 if exceeded). Note: this static shared-secret is the Phase 1 mitigation for T-02-01 — a stronger HMAC-of-body signature is a future enhancement, but the endpoint is NOT unauthenticated in Phase 1.

    Create integration tests using pytest-anyio/httpx.AsyncClient for FastAPI routes, fakeredis.FakeRedis for cost guard, live docker-compose DB for Nascente/Rio/Mar pipeline tests. Mark integration tests with @pytest.mark.integration and only run them when BRAVE_DB_URL is set and docker-compose is up.

    Add pytest-socket enforcement in tests/conftest.py: `pytestmark = pytest.mark.disable_socket` by default; integration tests use `@pytest.mark.enable_socket` only for the DB connection (localhost allowed).
  </action>
  <verify>
    <automated>
      pytest tests/unit/test_routing.py tests/unit/test_dedup.py -v --disable-socket
      pytest tests/integration/ -v -m integration
      pytest tests/unit/ tests/integration/ -q
    </automated>
  </verify>
  <acceptance_criteria>
    1. `grep -rn "sqlalchemy\|Session\|redis\|httpx" brave/core/score/engine.py` exits 1 (no I/O in score engine).
    2. pytest tests/unit/ -q --disable-socket exits 0 — all routing, dedup, score tests pass without network.
    3. pytest tests/integration/ -q exits 0 — Nascente store_raw idempotency, Rio pipeline routing, Celery idempotency (fakeredis), cost guard CostGuardError on budget breach.
    4. GET /api/v1/health returns 200 with {"status":"ok"} via FastAPI TestClient.
    5. POST /webhook/error-report with a valid X-Webhook-Secret header and {"source_ref":"mtur:BA:1"} after a Mar record exists → RioRecord.routing becomes "dlq" → 202 response; the same request with a missing/wrong X-Webhook-Secret → 401 and the MarRecord is untouched.
    6. CostGuardError test: pre_dispatch_check raises when fakeredis counter >= usd_daily_budget; passes when counter < budget.
    7. Poison quarantine test: PoisonQuarantine row exists after 3 retries; review DLQ (routing="dlq") NOT polluted with poison task records.
    8. Dedup territorial-key test: two records with same name but different uf are never candidates for merge (find_duplicate returns None for the cross-UF case).
    9. `pytest -q --disable-socket` on unit tests exits 0 with zero real network calls (pytest-socket enforces this).
  </acceptance_criteria>
  <done>Rio pipeline (dedup/normalize/label/route) processes a Nascente record through §7.6 score and routes to Mar/DLQ/descarte; Celery tasks are idempotent with poison quarantine; cost guard halts on budget breach; FastAPI surface (health/metrics/DLQ/audit/webhook) is reachable; all tests pass offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST /webhook/error-report | Untrusted input; any caller can try to reopen/corrupt Mar records |
| LLM cost guard (Redis) | Redis counter is the circuit breaker — must not be bypassable |
| Celery task retry → poison quarantine | Must not contaminate the review DLQ with operational poison messages |
| pytest-socket → test suite | Real network calls must fail hard in CI (not silently succeed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | POST /webhook/error-report — reopen attack | mitigate | Static shared-secret enforced IN PHASE 1: X-Webhook-Secret header must match BRAVE_WEBHOOK_SECRET (hmac.compare_digest, 401 before any DB work, secret never logged). Plus rate-limit per IP (10/min Redis); validate source_ref exists in Mar before opening DLQ; 404 not 500 on miss — prevents enumeration. Endpoint is NOT unauthenticated. Future enhancement: HMAC-of-body signature. |
| T-02-02 | Denial of Service | Celery poison message loop | mitigate | max_retries=3; quarantine_poison after N failures; PoisonQuarantine separate from review DLQ; task_time_limit=300 |
| T-02-03 | Denial of Service | Cost blowup via LLM fan-out | mitigate | pre_dispatch_check raises CostGuardError BEFORE dispatch; Redis daily counter; halts, does not just alert |
| T-02-04 | Information Disclosure | structlog JSON logs containing LLM prompts with PII | mitigate | LLMTracker logs model_slug + tokens + usd_cost only; prompt content not logged; provider.data_collection=deny enforced per D-10 |
| T-02-05 | Tampering | SQL injection via JSONB payload or webhook body | mitigate | SQLAlchemy parameterized queries; Pydantic validates all inbound JSON (webhook + API); never interpolate payload strings into raw SQL |
| T-02-06 | Denial of Service | pgvector approximate recall (HNSW ef_search) | accept | Phase 1 uses stub embeddings (zeros); recall measurement deferred to Phase 2 when real embeddings land; HNSW index exists for readiness |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | All packages installed via Plan 01 checkpoint; no new packages in this plan |
</threat_model>

<verification>
Run after both tasks complete:

```bash
# Full offline unit suite
pytest tests/unit/ -q --disable-socket

# Integration suite (requires docker-compose services up)
pytest tests/integration/ -q -m integration

# Score engine purity check
grep -rn "sqlalchemy\|Session\|redis\|httpx\|aiohttp" brave/core/score/engine.py && echo "FAIL: score engine has I/O" || echo "OK: score engine is pure"

# Cold-start DLQ landfill warning
python -c "
from brave.core.score.simulation import simulate_distribution, generate_cold_start_samples
from brave.config.settings import ScoreConfig
result = simulate_distribution(ScoreConfig(), generate_cold_start_samples(200))
print(f'Cold-start distribution: {result}')
assert result['dlq_pct'] > 0, 'DLQ should receive cold-start records'
print('OK: DLQ landfill risk visible in histogram')
"

# FastAPI smoke
python -c "
from fastapi.testclient import TestClient
from brave.api.main import app
client = TestClient(app)
r = client.get('/api/v1/health')
assert r.status_code == 200, f'health returned {r.status_code}'
print('OK: FastAPI health check passes')
"

# Territorial-key dedup enforcement
pytest tests/unit/test_dedup.py -v -k "test_no_cross_uf"

# Poison quarantine separation
pytest tests/integration/test_celery_tasks.py -v -k "test_poison"
```
</verification>

<success_criteria>
- compute_score is pure (zero I/O imports); all 5 criterion boundary cases tested and passing
- simulate_distribution shows cold-start DLQ concentration > 50% for origem=40 samples
- store_raw idempotent — exact same payload never creates duplicate NascenteRecord
- Rio pipeline routes a fixture record to correct routing band via §7.6 score
- Two records with same name but different UF are never merge candidates (territorial-key blocking)
- Celery process_nascente idempotent (double-call = one RioRecord)
- Poison message lands in poison_quarantine, not in routing="dlq" review DLQ
- CostGuardError raised when Redis daily counter >= usd_daily_budget
- GET /api/v1/health → 200; GET /api/v1/dlq → valid JSON list; POST /webhook/error-report → 202
- pytest tests/unit/ tests/integration/ -q exits 0 with --disable-socket on unit tests
</success_criteria>

<output>
Create `.planning/phases/01-brave-core-score-gate-boundary-contract/01-02-SUMMARY.md` when done
</output>
