---
phase: 04-dashboard-territorial-cms
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - dashboard/package.json
  - dashboard/bun.lock
  - dashboard/next.config.ts
  - dashboard/tsconfig.json
  - dashboard/vitest.config.ts
  - dashboard/components.json
  - dashboard/app/layout.tsx
  - dashboard/app/page.tsx
  - dashboard/app/providers.tsx
  - dashboard/app/globals.css
  - dashboard/app/login/page.tsx
  - dashboard/app/api/[...path]/route.ts
  - dashboard/lib/auth.ts
  - dashboard/lib/api-client.ts
  - dashboard/mocks/handlers/index.ts
  - dashboard/mocks/server.ts
  - dashboard/vitest.setup.ts
  - dashboard/app/api/__tests__/bff.test.ts
  - dashboard/app/login/__tests__/login.test.tsx
autonomous: true
requirements: [DASH-06]
must_haves:
  truths:
    - "Implements D-03 (Next.js 16 App Router + Route-Handler BFF), D-05 (Tailwind v4 + shadcn/ui scaffold), and D-07 (Vitest + MSW offline harness)"
    - "An operator can authenticate in the browser with a Bearer token and reach a FastAPI endpoint through the BFF"
    - "The backend service secret never reaches the browser — the BFF injects it server-side"
    - "A missing/invalid browser token returns 401 from the BFF before forwarding to FastAPI"
    - "The dashboard test suite runs fully offline (MSW) under Bun, no real FastAPI"
  artifacts:
    - path: "dashboard/app/api/[...path]/route.ts"
      provides: "BFF Route Handler proxying to FastAPI with the server-held secret"
      contains: "Authorization"
    - path: "dashboard/lib/auth.ts"
      provides: "Browser-Bearer validation at the BFF edge"
    - path: "dashboard/app/providers.tsx"
      provides: "TanStack Query QueryClientProvider"
      contains: "QueryClientProvider"
    - path: "dashboard/app/login/page.tsx"
      provides: "Login/token gate (Entrar)"
  key_links:
    - from: "dashboard/app/api/[...path]/route.ts"
      to: "FastAPI"
      via: "fetch injecting server-held Bearer secret"
      pattern: "fetch\\("
    - from: "dashboard/lib/auth.ts"
      to: "browser Bearer token"
      via: "validate before forward, 401 on bad token"
      pattern: "401"
---

<objective>
Foundation slice — frontend half (Walking-Skeleton-equivalent for the dashboard, RESEARCH §6 slice 0). Scaffold the greenfield `dashboard/` Next.js 16 app (App Router, Bun, Tailwind v4, shadcn init, TanStack Query provider, Vitest+MSW harness) and the BFF auth layer: a Next.js Route Handler that validates the browser Bearer token, then proxies to FastAPI injecting the server-held service secret (D-02/D-03). Plus a login/token gate.

Purpose: Proves one real authenticated round-trip (browser Bearer → BFF → FastAPI `/health` or `/metrics`) and establishes every greenfield convention later slices build on (DASH-06).
Output: A running `dashboard/` app with the BFF proxy, login gate, and an offline Vitest+MSW harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-dashboard-territorial-cms/04-CONTEXT.md
@.planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md
@.planning/phases/04-dashboard-territorial-cms/04-RESEARCH.md
@.planning/phases/04-dashboard-territorial-cms/04-PATTERNS.md
@.planning/phases/04-dashboard-territorial-cms/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold dashboard app + shadcn + TanStack Query + Vitest/MSW harness</name>
  <files>dashboard/package.json, dashboard/bun.lock, dashboard/next.config.ts, dashboard/tsconfig.json, dashboard/vitest.config.ts, dashboard/components.json, dashboard/app/layout.tsx, dashboard/app/page.tsx, dashboard/app/providers.tsx, dashboard/app/globals.css, dashboard/vitest.setup.ts, dashboard/mocks/server.ts, dashboard/mocks/handlers/index.ts</files>
  <read_first>
    - .planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md (preset: new-york/neutral/CSS-vars, Geist Sans+Mono, dark-default, density tokens, shadcn block allowlist)
    - .planning/phases/04-dashboard-territorial-cms/04-RESEARCH.md (§5 stack versions + App Router server/client boundary + Vitest 4 + MSW 2 setupServer Bun gotcha)
    - dashboard/ (confirm it is greenfield — .gitkeep only)
  </read_first>
  <action>Initialize the Next.js 16 App Router app under `dashboard/` with Bun 1.3 (`bun create next-app` equivalent — App Router, TypeScript, Tailwind v4) targeting Node 22. **This `bun install` + `npx shadcn@latest init` is the ONE sanctioned network event of the phase** (the single greenfield scaffold install); pin exact versions and commit `bun.lock` (RESEARCH §6 R6) so every subsequent run is reproducible-offline. The offline-by-default test mandate (D-07) governs the TEST SUITE (MSW, no real FastAPI) — it does NOT forbid this one-time scaffold install. Run `npx shadcn@latest init` with the locked preset from UI-SPEC: new-york style, neutral base, CSS-variables mode; this writes `components.json` and `app/globals.css` with the CSS-variable theme; default the theme to dark via `next-themes` (UI-SPEC). Configure `next/font` with Geist Sans (UI) + Geist Mono (data) per UI-SPEC typography. Add `app/providers.tsx` exporting a client component that wraps children in TanStack Query v5 `QueryClientProvider` (singleton client on the client, per-request on the server per RESEARCH §5); wire it into `app/layout.tsx`. Set up the test harness: `vitest.config.ts` (jsdom/happy-dom environment for component tests), `vitest.setup.ts`, and MSW 2 `mocks/server.ts` using `setupServer` (Node, not the browser worker — RESEARCH §5 Bun gotcha) + an empty `mocks/handlers/index.ts` barrel (each later slice owns its own `mocks/handlers/<slice>.ts` module and applies it per-suite via `server.use(...)`, so no two slices share a handlers file). Add a `test` script running `bunx vitest`. Add only shadcn blocks from the UI-SPEC Registry Safety allowlist (button/input/label/card/table/dialog/alert-dialog/sheet/tabs/badge/skeleton/sonner/select/dropdown-menu/tooltip/separator/scroll-area/chart/sidebar/form/progress). Do NOT add third-party registries.</action>
  <verify>
    <automated>cd dashboard && bun install && bunx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/package.json` declares next 16.x, react 19.x, @tanstack/react-query 5.x, vitest 4.x, msw 2.x
    - `dashboard/components.json` exists with style "new-york" and baseColor "neutral"
    - `dashboard/app/providers.tsx` contains `QueryClientProvider`
    - `dashboard/bun.lock` exists and is committed
    - `cd dashboard && bunx vitest run` exits 0 (harness boots even with zero tests)
    - `cd dashboard && bunx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The greenfield dashboard app boots, shadcn is initialized with the locked preset, TanStack Query provider is wired, and the offline Vitest+MSW harness runs.</done>
</task>

<task type="auto">
  <name>Task 2: BFF auth Route Handler + login gate + offline auth tests</name>
  <files>dashboard/app/api/[...path]/route.ts, dashboard/lib/auth.ts, dashboard/lib/api-client.ts, dashboard/app/login/page.tsx, dashboard/app/api/__tests__/bff.test.ts, dashboard/app/login/__tests__/login.test.tsx</files>
  <read_first>
    - .planning/phases/04-dashboard-territorial-cms/04-RESEARCH.md (§3 BFF bridge: browser Bearer → Route Handler validate → fetch FastAPI with server-held secret; MSW 401 case)
    - .planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md (login copy "Entrar", 401 copy "Sessão expirada ou token inválido", error copy)
    - .planning/phases/04-dashboard-territorial-cms/04-01-SUMMARY.md (the FastAPI Bearer contract this BFF presents to)
    - dashboard/app/providers.tsx (from Task 1)
  </read_first>
  <action>Create the BFF proxy at `dashboard/app/api/[...path]/route.ts` (catch-all Route Handler, GET+PATCH+POST): read the browser's `Authorization: Bearer <operator-token>`, validate it in `dashboard/lib/auth.ts` via constant-time compare against a server env operator token (`DASHBOARD_OPERATOR_TOKEN`); return 401 BEFORE forwarding if the browser token is missing/invalid. On success, `fetch` the FastAPI base URL (`BRAVE_API_URL` env) at the same `path`, injecting the server-held `Authorization: Bearer <service-token>` from `BRAVE_DASHBOARD_BEARER_TOKEN` (server env, never shipped to client — mirrors the FastAPI gate from plan 01), forward method/body/query, and relay the FastAPI status + JSON back. The service secret must never appear in any response body, header, or log returned to the browser. Add `dashboard/lib/api-client.ts` with typed fetch wrappers + TanStack Query keys that call the BFF (relative `/api/...`), never FastAPI directly. Create `dashboard/app/login/page.tsx` — a token-entry gate with the **"Entrar"** primary CTA (UI-SPEC blue `--primary`), storing the operator token for subsequent BFF calls; on a 401 from the BFF, redirect here with the UI-SPEC copy "Sessão expirada ou token inválido". Write offline tests: `app/api/__tests__/bff.test.ts` invokes the Route Handler asserting (a) missing/bad browser token → 401 before any fetch, (b) valid browser token → forwards with the injected service secret (MSW intercepts the FastAPI URL), (c) the service secret never leaks into the response; `app/login/__tests__/login.test.tsx` renders the login gate and asserts the 401 redirect copy. All offline via MSW (D-07).</action>
  <verify>
    <automated>cd dashboard && bunx vitest run app/api app/login</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/app/api/[...path]/route.ts` exists and contains a `fetch(` to the FastAPI base injecting `Authorization`
    - `dashboard/lib/auth.ts` returns 401 on missing/invalid browser token before forwarding
    - `dashboard/app/login/page.tsx` renders the "Entrar" CTA
    - `cd dashboard && bunx vitest run app/api app/login` exits 0
    - BFF test asserts the service secret (`BRAVE_DASHBOARD_BEARER_TOKEN`) is absent from the response returned to the browser
  </acceptance_criteria>
  <done>A browser Bearer token authenticates to the BFF, which forwards to FastAPI with a server-held secret that never reaches the browser; the login gate handles 401; all proven offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → BFF | Operator token validated here; untrusted browser input crosses |
| BFF → FastAPI | Server-held service secret injected here; must never cross back to the browser |

<!-- Network-event note (INFO): the one-time `bun install` + `npx shadcn@latest init` at scaffold time is the SINGLE sanctioned network event for this phase; `bun.lock` is committed so it is reproducible. The offline-by-default mandate (D-07) constrains the TEST SUITE, not this greenfield install — verifiers should not read it as forbidding the scaffold. -->

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Spoofing | BFF browser-token check (lib/auth.ts) | mitigate | constant-time compare of browser token vs DASHBOARD_OPERATOR_TOKEN; 401 before any forward |
| T-04-06 | Information Disclosure | service secret leakage | mitigate | BRAVE_DASHBOARD_BEARER_TOKEN read only server-side in the Route Handler; never placed in any response/header/log; test asserts absence in browser-facing response |
| T-04-07 | Tampering | proxied path/method | mitigate | catch-all forwards only to the configured BRAVE_API_URL base; no open redirect / SSRF to arbitrary host |
| T-04-08 | Repudiation | unauthenticated mutation forwarding | mitigate | BFF refuses to forward mutations without a valid browser token (401 first) |
| T-04-SC | Tampering | npm package installs (next/shadcn/tanstack/msw) | mitigate | the one-time `bun install` + `npx shadcn init` is the single sanctioned network event (offline mandate D-07 governs the test suite, not the scaffold); pin exact versions + commit bun.lock; only UI-SPEC-allowlisted shadcn blocks; no third-party registries |
</threat_model>

<verification>
- `cd dashboard && bunx vitest run` exits 0 (whole suite offline).
- `cd dashboard && bunx tsc --noEmit` exits 0.
- BFF test proves: 401 before forward, secret injected server-side, secret never in browser response.
</verification>

<success_criteria>
DASH-06 frontend half: the dashboard app is scaffolded with the locked stack/preset, the BFF validates the browser Bearer and proxies to FastAPI with a server-held secret that never reaches the browser, and the login gate + auth flow are tested fully offline.
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-territorial-cms/04-02-SUMMARY.md` when done.
</output>


## CONTEXT Decision Traceability

Implements CONTEXT.md decisions: D-02 (BFF auth Route Handler), D-03 (Next.js 16 App Router scaffold), D-05 (Tailwind v4 + shadcn/ui), D-07 (Vitest + MSW offline harness)
