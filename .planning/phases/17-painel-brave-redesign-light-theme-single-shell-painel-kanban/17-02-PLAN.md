---
phase: 17-painel-brave-redesign-light-theme-single-shell-painel-kanban
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - dashboard/lib/painel-data.ts
  - dashboard/lib/__tests__/painel-data.test.ts
autonomous: true
requirements: [UI-PAINEL-1]

must_haves:
  truths:
    - "usePainelBoard returns a unified PainelCard[] built from destinos + atrativos lists fetched through the BFF"
    - "buildMetrics derives total-no-escopo / sincronizados (routing=mar) / falhas (routing=descarte) / progresso% per entity type"
    - "buildColumns places each card into one of 5 stage columns by its routing"
    - "filterCards applies the type filter (Tudo/Destinos/Atrativos) and UF scope client-side"
    - "No PII (phone_e164) ever enters a PainelCard — only list-safe fields are mapped"
  artifacts:
    - path: "dashboard/lib/painel-data.ts"
      provides: "PainelCard model, pure selectors (toPainelCards/routingToColumn/filterCards/buildColumns/buildMetrics), usePainelBoard hook, BR_UFS, COLUMN_DEFS"
      min_lines: 90
    - path: "dashboard/lib/__tests__/painel-data.test.ts"
      provides: "Unit coverage for every pure selector + the hook over MSW"
      min_lines: 60
  key_links:
    - from: "dashboard/lib/painel-data.ts usePainelBoard"
      to: "lib/destinos-api fetchDestinoList + lib/atrativos-api fetchAtrativoList"
      via: "TanStack useQuery"
      pattern: "fetchDestinoList|fetchAtrativoList"
---

<objective>
Build the Painel data layer: a unified `PainelCard` model + pure, unit-testable selectors that turn the existing destinos/atrativos list endpoints into Kanban columns and metric counts, plus a `usePainelBoard` hook that fetches both lists through the BFF. This is the single source the metric cards, filters, board, and drag-drop plans all consume — NO new backend endpoints.

Purpose: Isolate all data shaping in one tested module so the view/board plans stay presentational and the drag-drop plan has a clean column model.
Output: `dashboard/lib/painel-data.ts` + its test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-painel-brave-redesign-light-theme-single-shell-painel-kanban/17-CONTEXT.md
@dashboard/lib/destinos-api.ts
@dashboard/lib/atrativos-api.ts
@dashboard/components/cms/StageBadge.tsx
@dashboard/mocks/handlers/destinos.ts
@dashboard/mocks/handlers/atrativos.ts

<interfaces>
From dashboard/lib/destinos-api.ts:
- interface DestinoListItem { id; entity_type; uf: string|null; routing: string; score: number|null; name: string|null; canonical_key: string|null; validation_pending: boolean; mar_id: string|null; published_at: string|null }
- function fetchDestinoList(params:{uf?;routing?;offset?;limit?}): Promise<{items:DestinoListItem[]; total; offset; limit}>
- const destinoKeys = { all:["destinos"], list:(f)=>["destinos","list",f], detail:(id)=>["destinos","detail",id] }

From dashboard/lib/atrativos-api.ts:
- interface AtrativoListItem { id; entity_type:"attraction"; uf:string|null; routing:string; sub_state:string|null; score:number|null; name:string|null; validation_pending:boolean; mar_id:string|null; parent_mar_id:string|null; contacts_summary:{phone_masked:string|null; website:string|null}|null }
- function fetchAtrativoList(params:{uf?;sub_state?;parent_mar_id?;routing?;offset?;limit?}): Promise<{items:AtrativoListItem[]; total; offset; limit}>
- const atrativoKeys = { all:["atrativos"], list:(f)=>["atrativos","list",f], detail:(id)=>["atrativos","detail",id] }

Routing values seen in list items: "in_progress" | "mar" | "dlq" | "descarte" (records sitting only in Nascente are not surfaced by these rio-backed lists).

StageBadge score bands (REUSE the exact thresholds): score>=85 → green/mar, score>=40 → amber/dlq, <40 → red/descarte. municipality is read from a card's normalized payload on detail; the LIST items do NOT carry municipality — see action for the município source decision.

MSW (reuse, no new handler file): destinosListSuccess(items?), atrativosListSuccess(items?), sampleDestinos, sampleAtrativos from mocks/handlers/destinos.ts + atrativos.ts. Double-prefix base http://localhost:3000/api/api/v1/...
Test helper: a fresh QueryClient (retry:false) wrapping renderHook — mirror components/cms/__tests__/test-utils.tsx.
</interfaces>
</context>

<feature>
  <name>Painel data layer — unified cards, columns, metrics</name>
  <files>dashboard/lib/painel-data.ts, dashboard/lib/__tests__/painel-data.test.ts</files>
  <behavior>
    Pure selectors (RED first — write these tests before implementation):
    - `routingToColumn(routing: string): PainelColumnKey` — "mar"→"mar", "descarte"→"descarte", "dlq"→"dlq", "in_progress"→"in_progress"; any other/empty → "nascente".
    - `toPainelCards(destinos, atrativos): PainelCard[]` — maps each list item into a PainelCard. PII guard: only map id, type ("destino"|"atrativo"), name, uf, municipality (see source note), routing, column, score, source, duplicate flag, error. NEVER read phone_e164. For atrativos, `duplicate = validation_pending` (slice-1 dedup hint). `column = routingToColumn(routing)`.
    - `filterCards(cards, { type, ufs }): PainelCard[]` — type "all" keeps both; "destino"/"atrativo" filters by card.type; `ufs` empty → all UFs, else keep cards whose uf ∈ ufs.
    - `buildColumns(cards): { key; label; cards: PainelCard[] }[]` — 5 columns in order: nascente "Nascente", in_progress "Em processamento", mar "Sincronizado", dlq "Revisão", descarte "Descarte". Each column gets the cards whose column===key.
    - `buildMetrics(cards, type): { total; mar; falha; pct }` — over cards of the given entity type: total = count; mar = count(column==="mar"); falha = count(column==="descarte"); pct = total>0 ? Math.round(mar/total*100) : 0.
    - Test cases: a mixed sample (2 destinos: 1 mar 1 dlq; 2 atrativos: 1 in_progress 1 descarte) → buildColumns counts per column; buildMetrics("destino") = {total:2, mar:1, falha:0, pct:50}; buildMetrics("atrativo") = {total:2, mar:0, falha:1, pct:0}; filterCards by type and by ufs; routingToColumn unknown → "nascente"; assert no card object contains a phone_e164 key.

    Hook (GREEN, over MSW):
    - `usePainelBoard()` — two `useQuery`s: destinoKeys.list({}) → fetchDestinoList({ limit: 500 }); atrativoKeys.list({}) → fetchAtrativoList({ limit: 500 }). Returns `{ cards: PainelCard[]; isPending; isError; refetch }` where cards = toPainelCards(d.items, a.items). isPending = either pending; isError = either error.
    - Test: server.use(destinosListSuccess(), atrativosListSuccess()); renderHook(usePainelBoard) → waitFor cards.length === 4 (2 sample destinos + 2 sample atrativos) with correct type tags.
  </behavior>
  <implementation>Create `dashboard/lib/painel-data.ts`. Export: `type PainelEntityType = "destino"|"atrativo"`; `type PainelColumnKey = "nascente"|"in_progress"|"mar"|"dlq"|"descarte"`; `type TypeFilter = "all"|"destino"|"atrativo"`; `interface PainelCard { id; type: PainelEntityType; name: string|null; uf: string|null; municipality: string|null; routing: string; column: PainelColumnKey; score: number|null; source: string|null; duplicate: boolean; error: string|null }`; `const COLUMN_DEFS: { key: PainelColumnKey; label: string }[]`; `const BR_UFS: string[]` (the 27 codes — copy the array from EngineControl.tsx so the filters plan imports it from here, not from the component). Município source note: the LIST endpoints do not return município; set `municipality` from `canonical_key` parse for destinos when present (e.g. last segment) else null, and null for atrativos (the record-edit drawer with detail município is a later slice). `error` comes from no list field today → set null (the ⚠ falha reason is shown for column==="descarte" cards with a generic label in the card plan). `source` for destinos = null (list has no source field) — leave null; for atrativos = null. Keep selectors pure (no React). Put the hook at the bottom. Write the test FIRST asserting the pure-selector cases, watch them fail, then implement; then add the hook test over MSW.</implementation>
</feature>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| list endpoint payload → PainelCard | Untrusted/PII-bearing fields must be dropped at the mapping boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Information Disclosure | toPainelCards mapping | mitigate | Map an explicit allow-list of fields; never copy phone_e164/contacts_summary into PainelCard; unit-assert no phone key present |
| T-17-02-02 | Spoofing | data fetch | mitigate | All fetches via lib/*-api (apiFetch/bff Bearer); no direct FastAPI calls |
| T-17-02-SC | Tampering | bun installs | mitigate | No new packages — pure TS + existing TanStack Query |
</threat_model>

<verification>
- `cd dashboard && bun run test lib/__tests__/painel-data.test.ts` passes (RED→GREEN evidenced by commits).
- `cd dashboard && bun run test` full suite stays green.
</verification>

<success_criteria>
- All pure selectors + usePainelBoard tested; metrics/columns match the design semantics; no PII in PainelCard.
</success_criteria>

<output>
Create `.planning/phases/17-painel-brave-redesign-light-theme-single-shell-painel-kanban/17-02-SUMMARY.md` when done.
</output>
