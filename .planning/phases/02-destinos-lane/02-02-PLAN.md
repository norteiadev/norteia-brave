---
phase: 02-destinos-lane
plan: "02"
type: execute
wave: 0
depends_on: []
files_modified:
  - brave/config/settings.py
  - scripts/calibrate_destinos.py
autonomous: true
requirements: [DEST-04, DEST-03, TEST-02]
must_haves:
  truths:
    - "Simulation harness confirms that Mtur (origem=100) and NotebookLM (origem=80) records land in DLQ (51-84.9) with the calibrated atualidade/completude mappings"
    - "DesmembramentoAgent (origem=40) records land in DLQ (not descarte) with the calibrated threshold_dlq"
    - "ScoreConfig has a documented, calibrated threshold_dlq that prevents the descarte black-hole"
  artifacts:
    - path: "scripts/calibrate_destinos.py"
      provides: "Runnable calibration script that prints distribution for all three producers"
      min_lines: 40
    - path: "brave/config/settings.py"
      provides: "Updated ScoreConfig with calibrated threshold_dlq (lowered from 51 to resolve descarte black-hole)"
      contains: "threshold_dlq"
  key_links:
    - from: "scripts/calibrate_destinos.py"
      to: "brave/core/score/simulation.py"
      via: "simulate_distribution + generate_cold_start_samples"
      pattern: "simulate_distribution|generate_cold_start_samples"
    - from: "brave/config/settings.py"
      to: "brave/core/score/engine.py"
      via: "ScoreConfig.threshold_dlq consumed by route_by_score"
      pattern: "threshold_dlq"
---

<objective>
Run the Phase 1 simulation harness to determine safe atualidade/completude values for each producer, resolve the DesmembramentoAgent descarte black-hole by calibrating threshold_dlq, and document the calibration rationale in a runnable script so it can be re-run on real BA data.

Purpose: The score math proves that with default threshold_dlq=51, origem=40 records score ≤47.0 at cold start — they ALWAYS hit descarte (violating DEST-04). Even Mtur records with low atualidade_value can hit descarte. This calibration task must complete before any lane fan-out is wired. The "DLQ landfill vs descarte black-hole" risk (RESEARCH.md §Score Calibration Analysis) is resolved here, not discovered at runtime.

Output: scripts/calibrate_destinos.py (runnable calibration script); updated ScoreConfig.threshold_dlq default; calibration rationale documented inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-destinos-lane/02-CONTEXT.md
@.planning/phases/02-destinos-lane/02-RESEARCH.md

<interfaces>
<!-- ScoreConfig (brave/config/settings.py lines 15-38) -->
```python
class ScoreConfig(BaseSettings):
    weight_origem: float = 30.0
    weight_completude: float = 20.0
    weight_corroboracao: float = 20.0
    weight_atualidade: float = 15.0
    weight_validacao_humana: float = 15.0
    threshold_mar: float = 85.0
    threshold_dlq: float = 51.0   # ← will be lowered
    score_version: str = "v1.0"
    model_config = SettingsConfigDict(env_prefix="BRAVE_SCORE_")
```

<!-- simulation.py public API -->
```python
simulate_distribution(config: ScoreConfig, samples: list[ScoreInput]) -> dict
# returns: {total, mar_pct, dlq_pct, descarte_pct, mean, stdev}

generate_cold_start_samples(n: int, origem_value: float = 40.0) -> list[ScoreInput]
# corroboracao=0, validacao_humana=0, completude 60-100, atualidade 0-50
```

<!-- Score math (from RESEARCH.md §7.6 Score Calibration Analysis) -->
# Desmembramento cold-start (origem=40, corroboracao=0, validacao=0):
# max score = 40*0.3 + 100*0.2 + 0*0.2 + 100*0.15 + 0*0.15 = 47.0
# → always descarte with threshold_dlq=51

# Mtur cold-start descarte risk (origem=100, completude=70, atualidade=30):
# score = 30 + 14 + 0 + 4.5 + 0 = 48.5 → descarte

# Mtur after validation + corroboracao=50 (origem=100, completude=100, atualidade=70):
# score = 30 + 20 + 10 + 10.5 + 15 = 85.5 → Mar ✓
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write calibration script and run simulation</name>
  <files>scripts/calibrate_destinos.py</files>

  <read_first>
    - brave/core/score/simulation.py — full file; understand simulate_distribution and generate_cold_start_samples signatures
    - brave/config/settings.py — ScoreConfig; understand threshold_dlq and env_prefix
    - .planning/phases/02-destinos-lane/02-RESEARCH.md §"§7.6 Score Calibration Analysis" — all three producer math analyses and the open question on threshold_dlq
    - brave/core/score/engine.py — compute_score; understand route_by_score thresholds
    - brave/core/score/schemas.py — ScoreInput fields
  </read_first>

  <behavior>
    - Script is runnable: `python scripts/calibrate_destinos.py` exits 0 without BRAVE_DB_URL
    - Script prints a table with three rows: mtur (orig=100), notebooklm (orig=80), desmembramento (orig=40)
    - Each row shows: producer name, origin value, sample_count, mar_pct, dlq_pct, descarte_pct, mean score
    - Script runs simulation with BOTH default config (threshold_dlq=51) AND proposed calibrated config (threshold_dlq=40)
    - Script prints an explicit PASS/FAIL gate: "GATE: Desmembramento lands in DLQ with threshold_dlq=40: PASS"
    - Script includes a clear inline comment explaining WHY threshold_dlq must be lowered
  </behavior>

  <action>
    Create scripts/calibrate_destinos.py. Import simulate_distribution, generate_cold_start_samples from brave.core.score.simulation; import ScoreConfig from brave.config.settings; import ScoreInput from brave.core.score.schemas.

    The script runs two simulation passes:
    1. Default config (ScoreConfig()) — demonstrates the descarte black-hole: shows 100% descarte for origem=40 samples
    2. Calibrated config (ScoreConfig(threshold_dlq=40)) — shows origem=40 cold-start records landing in DLQ

    For each pass and each producer (mtur/notebooklm/desmembramento), call generate_cold_start_samples(500, origem_value=X) then simulate_distribution(config, samples). Print a formatted table.

    Include a GATE check at the bottom: with threshold_dlq=40, assert desmembramento dlq_pct > 0 (at least some records reach DLQ). Print "GATE PASS" or "GATE FAIL" accordingly.

    Also simulate the "post-validation" path: generate 100 samples with validacao_humana=100 for mtur (origem=100) to verify Mar promotion works when corroboracao is added. Use ScoreInput directly (not generate_cold_start_samples) to set corroboracao=50, validacao_humana=100. This validates that the corroboration boost (D-02) is the mechanism for Mar promotion.

    Note: Runs without a DB connection — pure Python computation.
  </action>

  <verify>
    <automated>cd /Users/leandro/Projects/norteia/norteia-brave && python scripts/calibrate_destinos.py 2>&1 | tail -30</automated>
  </verify>

  <done>
    - Script runs and prints distribution table (no ImportError, no exceptions)
    - Output shows "GATE PASS" for desmembramento DLQ landing at threshold_dlq=40
    - Output shows Mtur cold-start lands in DLQ at threshold_dlq=40 for reasonable completude/atualidade values
    - Post-validation simulation shows Mtur + corroboracao=50 + validacao=100 lands in Mar (>=85)
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply calibrated threshold_dlq to ScoreConfig default</name>
  <files>brave/config/settings.py</files>

  <read_first>
    - brave/config/settings.py — full file; ScoreConfig field definitions
    - scripts/calibrate_destinos.py (just written) — the GATE output confirms threshold_dlq=40 is correct
    - brave/core/score/simulation.py — score_version; confirm "v1.0" remains valid after threshold change
    - tests/unit/test_score_engine.py — existing boundary tests; understand what thresholds they test against to avoid regressions
    - tests/unit/test_score_simulation.py — simulation tests; check if any test hardcodes threshold_dlq=51
  </read_first>

  <action>
    Update ScoreConfig in brave/config/settings.py: change the default for threshold_dlq from 51.0 to 40.0.

    Add a doc comment on the threshold_dlq field explaining the calibration: "Lowered from 51.0 to 40.0 in Phase 2 (D-05 calibration) so that DesmembramentoAgent cold-start records (origem=40, corroboracao=0) land in DLQ instead of descarte. Re-calibrate on real BA data before national fan-out. Env override: BRAVE_SCORE_THRESHOLD_DLQ."

    Also bump score_version from "v1.0" to "v1.1" and add a doc comment: "Bumped to v1.1 in Phase 2 due to threshold_dlq calibration (40.0 from 51.0). Old scored records from Phase 1 tests remain valid — score values are unchanged, only the DLQ/descarte boundary moved."

    Search existing tests for any hardcoded 51.0 or "v1.0" score_version assertions. If found, update them to match the new defaults (40.0 / v1.1) — document each change as a comment: "# Updated: Phase 2 threshold_dlq calibration (D-05)".

    The change is purely a default value — operators can still override via BRAVE_SCORE_THRESHOLD_DLQ env var.
  </action>

  <verify>
    <automated>cd /Users/leandro/Projects/norteia/norteia-brave && python -m pytest tests/unit/test_score_engine.py tests/unit/test_score_simulation.py -v 2>&1 | tail -30</automated>
  </verify>

  <done>
    - ScoreConfig.threshold_dlq default is 40.0 (not 51.0)
    - ScoreConfig.score_version is "v1.1"
    - doc comment explains calibration rationale and env override
    - All existing score engine and simulation tests pass (pytest exit 0)
    - scripts/calibrate_destinos.py re-run confirms GATE PASS still holds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calibration script → score engine | Pure Python computation; no I/O, no external calls |
| ScoreConfig env override | BRAVE_SCORE_THRESHOLD_DLQ can be set by any process with env access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | ScoreConfig threshold_dlq env override | mitigate | threshold_dlq is a calibration knob, not a security gate. Lowering it further (e.g., to 0) does not bypass security — it only routes more records to DLQ for human review, which is conservative. Accept as config design. |
| T-02-02-02 | Elevation of Privilege | Lower threshold_dlq routes more records to DLQ → steward workload | accept | DLQ is a human review queue; more records in DLQ increases steward load but cannot auto-promote to Mar (requires explicit validate action). |
| T-02-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages. slopcheck not required. |
</threat_model>

<verification>
```bash
cd /Users/leandro/Projects/norteia/norteia-brave
python scripts/calibrate_destinos.py | grep "GATE"
python -m pytest tests/unit/test_score_engine.py tests/unit/test_score_simulation.py -v
python -c "from brave.config.settings import ScoreConfig; c=ScoreConfig(); assert c.threshold_dlq == 40.0, f'expected 40.0, got {c.threshold_dlq}'; print('threshold_dlq OK')"
```
</verification>

<success_criteria>
- scripts/calibrate_destinos.py runs and prints GATE PASS for desmembramento at threshold_dlq=40
- ScoreConfig.threshold_dlq default is 40.0 with calibration rationale comment
- ScoreConfig.score_version is "v1.1"
- All existing unit tests pass (no regressions from threshold change)
- Calibration rationale is documented in code for future re-calibration on real BA data
</success_criteria>

<output>
Create `.planning/phases/02-destinos-lane/02-02-SUMMARY.md` when done
</output>
