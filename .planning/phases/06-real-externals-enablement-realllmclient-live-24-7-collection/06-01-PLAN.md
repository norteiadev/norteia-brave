---
phase: "06"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - brave/clients/places.py
  - brave/clients/apify.py
  - brave/clients/whatsapp.py
  - tests/integration/test_atrativos_lane_e2e.py
autonomous: true
requirements:
  - TEST-01
  - CORE-11

must_haves:
  truths:
    - "D-06: toggle footgun fixed — operator docs/error strings say RUN_REAL_EXTERNALS (no BRAVE_ prefix)"
    - "Operator docs for places.py, apify.py, whatsapp.py all say RUN_REAL_EXTERNALS (no BRAVE_ prefix)"
    - "test_atrativos_lane_e2e.py module docstring says RUN_REAL_EXTERNALS (no BRAVE_ prefix)"
    - "grep -r BRAVE_RUN_REAL_EXTERNALS brave/ tests/ returns zero matches"
  artifacts:
    - path: "brave/clients/places.py"
      contains: "Set RUN_REAL_EXTERNALS=true to enable real API calls."
    - path: "brave/clients/apify.py"
      contains: "Set RUN_REAL_EXTERNALS=true to enable real API calls."
    - path: "brave/clients/whatsapp.py"
      contains: "requires RUN_REAL_EXTERNALS=true"
    - path: "tests/integration/test_atrativos_lane_e2e.py"
      contains: "RUN_REAL_EXTERNALS must be absent"
  key_links:
    - from: "brave/config/settings.py"
      to: "AppConfig.run_real_externals"
      via: "env_prefix=''"
      pattern: "RUN_REAL_EXTERNALS"
---

<objective>
Fix the D-06 env-var footgun: 7 string occurrences of `BRAVE_RUN_REAL_EXTERNALS` across three client
files and one integration test docstring must be replaced with `RUN_REAL_EXTERNALS` — the real env var
name as set by `AppConfig` (which uses `env_prefix=""`).

Purpose: An operator following the wrong `BRAVE_RUN_REAL_EXTERNALS` name in docs will silently get
fake clients even when they intend to run real. This is a pure string/doc fix — no behavior change.

Output: 7 updated strings across 4 files; the footgun is eliminated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@brave/config/settings.py

<interfaces>
<!-- From brave/config/settings.py lines 230–245 (AppConfig env_prefix) -->
<!-- AppConfig has env_prefix="" so run_real_externals resolves from bare RUN_REAL_EXTERNALS -->
<!--
class AppConfig(BaseSettings):
    ...
    run_real_externals: bool = False
    model_config = SettingsConfigDict(env_prefix="")
-->

<!-- RESEARCH.md Pattern 8 — exact occurrences to fix (VERIFIED: grep of codebase) -->
<!--
File: brave/clients/places.py, line 95
  OLD: "Set BRAVE_RUN_REAL_EXTERNALS=true to enable real API calls."
  NEW: "Set RUN_REAL_EXTERNALS=true to enable real API calls."

File: brave/clients/apify.py, line 89
  OLD: "Set BRAVE_RUN_REAL_EXTERNALS=true to enable real API calls."
  NEW: "Set RUN_REAL_EXTERNALS=true to enable real API calls."

File: brave/clients/whatsapp.py, line 13
  OLD: requires BRAVE_RUN_REAL_EXTERNALS=true
  NEW: requires RUN_REAL_EXTERNALS=true

File: brave/clients/whatsapp.py, line 66
  OLD: Requires BRAVE_RUN_REAL_EXTERNALS=true
  NEW: Requires RUN_REAL_EXTERNALS=true

File: brave/clients/whatsapp.py, line 121
  OLD: RuntimeError: If BRAVE_RUN_REAL_EXTERNALS is not True.
  NEW: RuntimeError: If RUN_REAL_EXTERNALS is not True.

File: brave/clients/whatsapp.py, line 129
  OLD: "TwilioWhatsAppClient.send_template requires BRAVE_RUN_REAL_EXTERNALS=true."
  NEW: "TwilioWhatsAppClient.send_template requires RUN_REAL_EXTERNALS=true."

File: tests/integration/test_atrativos_lane_e2e.py, line 7
  OLD: BRAVE_RUN_REAL_EXTERNALS must be absent / False.
  NEW: RUN_REAL_EXTERNALS must be absent / False.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix BRAVE_RUN_REAL_EXTERNALS → RUN_REAL_EXTERNALS in all 7 occurrences (D-06)</name>
  <files>brave/clients/places.py, brave/clients/apify.py, brave/clients/whatsapp.py, tests/integration/test_atrativos_lane_e2e.py</files>
  <read_first>
    - brave/clients/places.py (full — read once to locate line 95 before editing)
    - brave/clients/apify.py (full — read once to locate line 89 before editing)
    - brave/clients/whatsapp.py (full — read once to locate lines 13, 66, 121, 129 before editing)
    - tests/integration/test_atrativos_lane_e2e.py lines 1–20 (locate line 7 before editing)
  </read_first>
  <action>
    Apply exactly 7 string-only edits. No code logic changes. No new imports. No behavior changes.
    All edits replace `BRAVE_RUN_REAL_EXTERNALS` with `RUN_REAL_EXTERNALS` in strings/docstrings only
    (per D-06 and RESEARCH.md Pattern 8).

    Edit 1 — brave/clients/places.py, line 95:
      Replace: "Set BRAVE_RUN_REAL_EXTERNALS=true to enable real API calls."
      With:    "Set RUN_REAL_EXTERNALS=true to enable real API calls."

    Edit 2 — brave/clients/apify.py, line 89:
      Replace: "Set BRAVE_RUN_REAL_EXTERNALS=true to enable real API calls."
      With:    "Set RUN_REAL_EXTERNALS=true to enable real API calls."

    Edit 3 — brave/clients/whatsapp.py, line 13:
      Replace: requires BRAVE_RUN_REAL_EXTERNALS=true
      With:    requires RUN_REAL_EXTERNALS=true

    Edit 4 — brave/clients/whatsapp.py, line 66:
      Replace: Requires BRAVE_RUN_REAL_EXTERNALS=true
      With:    Requires RUN_REAL_EXTERNALS=true

    Edit 5 — brave/clients/whatsapp.py, line 121:
      Replace: RuntimeError: If BRAVE_RUN_REAL_EXTERNALS is not True.
      With:    RuntimeError: If RUN_REAL_EXTERNALS is not True.

    Edit 6 — brave/clients/whatsapp.py, line 129:
      Replace: "TwilioWhatsAppClient.send_template requires BRAVE_RUN_REAL_EXTERNALS=true."
      With:    "TwilioWhatsAppClient.send_template requires RUN_REAL_EXTERNALS=true."

    Edit 7 — tests/integration/test_atrativos_lane_e2e.py, line 7:
      Replace: BRAVE_RUN_REAL_EXTERNALS must be absent / False.
      With:    RUN_REAL_EXTERNALS must be absent / False.

    Rationale (do not include in code): AppConfig has model_config=SettingsConfigDict(env_prefix=""),
    so run_real_externals resolves from bare RUN_REAL_EXTERNALS, not BRAVE_RUN_REAL_EXTERNALS.
    The wrong name in docs silently keeps fake clients active when the operator sets the wrong var.
  </action>
  <verify>
    <automated>grep -r "BRAVE_RUN_REAL_EXTERNALS" brave/ tests/ | grep -v "^Binary" | wc -l | grep -qx "0" && echo "PASS: no BRAVE_RUN_REAL_EXTERNALS found" || echo "FAIL: occurrences remain"</automated>
  </verify>
  <acceptance_criteria>
    - grep -r BRAVE_RUN_REAL_EXTERNALS brave/ tests/ returns zero matches
    - brave/clients/places.py contains the string "Set RUN_REAL_EXTERNALS=true to enable real API calls."
    - brave/clients/apify.py contains the string "Set RUN_REAL_EXTERNALS=true to enable real API calls."
    - brave/clients/whatsapp.py contains "requires RUN_REAL_EXTERNALS=true" (lowercase 'requires')
    - brave/clients/whatsapp.py contains "Requires RUN_REAL_EXTERNALS=true" (capital 'Requires')
    - brave/clients/whatsapp.py contains "TwilioWhatsAppClient.send_template requires RUN_REAL_EXTERNALS=true."
    - tests/integration/test_atrativos_lane_e2e.py contains "RUN_REAL_EXTERNALS must be absent / False."
    - Existing tests still pass (no logic was changed): .venv/bin/python -m pytest tests/integration/test_atrativos_lane_e2e.py -x exits 0
  </acceptance_criteria>
  <done>All 7 BRAVE_RUN_REAL_EXTERNALS occurrences replaced; grep confirms zero remaining; existing atrativos lane e2e test still passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator docs → env var | Operator reads error string and sets env var; wrong name means real calls never trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all edits are string-only in existing files |
| T-06-01-01 | Spoofing | Env var name confusion (BRAVE_RUN_REAL_EXTERNALS vs RUN_REAL_EXTERNALS) | mitigate | D-06: replace all 7 wrong-name occurrences in docs/error strings so operator sets the correct var |
</threat_model>

<verification>
After plan execution:
- grep -r "BRAVE_RUN_REAL_EXTERNALS" brave/ tests/ returns zero matches
- .venv/bin/python -m pytest tests/integration/test_atrativos_lane_e2e.py -x exits 0 (no regressions)
- .venv/bin/python -m pytest tests/ -x --ignore=tests/integration/test_real_llm_smoke.py exits 0 (full offline suite green)
</verification>

<success_criteria>
- Zero occurrences of BRAVE_RUN_REAL_EXTERNALS in brave/ and tests/ tree
- All 4 modified files have RUN_REAL_EXTERNALS in their previously-wrong strings
- Full offline test suite remains green
</success_criteria>

<output>
Create `.planning/phases/06-real-externals-enablement-realllmclient-live-24-7-collection/06-01-SUMMARY.md` when done
</output>
