---
milestone: v1.0
audited: 2026-06-19T00:00:00Z
status: gaps_found
scores:
  requirements: 44/44   # all verified offline by phase VERIFICATIONs
  phases: 8/8           # 5 passed, 3 human_needed (offline-green, live confirmation pending)
  integration: 5/6      # 1 BLOCKER (offline-branch tests.fakes import)
  flows: 5/6            # destinos+atrativos task execution blocked under default config
gaps:
  requirements: []      # no requirement unsatisfied by 3-source matrix; all VERIFIED offline
  integration:
    - id: INT-BLOCKER-01
      severity: blocker
      summary: "Production Celery tasks import tests.fakes.* in their default (run_real_externals=False) branch; wheel ships only `brave`, so every sweep/FSM task raises ModuleNotFoundError under the documented default config. Masked only because the repo .env forces RUN_REAL_EXTERNALS=true."
      where: "brave/tasks/pipeline.py:662,673,809,896,1001,1002,1249,1441; pyproject.toml [tool.hatch.build.targets.wheel] packages=['brave']; brave/config/settings.py:236 run_real_externals=False"
      affected_reqs: ["ORCH-01","ORCH-02","ORCH-03","ORCH-04","ATR-01","ATR-02","ATR-03","ATR-04","DEST-01","DEST-02","DEST-03","DEST-04","DEST-05","CORE-10","CORE-11","TEST-03"]
      fix: "Add NullPlacesClient/NullLLMClient/NullApifyClient under brave/clients/ (mirror NullWhatsAppClient) and use them in the offline branch instead of tests.fakes.*."
  flows:
    - flow: "Destinos sweep_uf → producers → DLQ → validate → push"
      breaks_at: "task execution under default run_real_externals=False (INT-BLOCKER-01)"
    - flow: "Atrativos FSM auto-advance (discover→contacts→signals)"
      breaks_at: "task execution under default run_real_externals=False (INT-BLOCKER-01)"
tech_debt:
  - phase: 03-atrativos-lane-whatsapp-compliance
    items:
      - "Twilio RequestValidator signature validation deferred to prod hardening (T-03-03-05); X-Webhook-Secret mitigation in place — defence-in-depth, not a gate bypass."
      - "human_needed: real Twilio BSP end-to-end dry-run (live infra)."
      - "human_needed: CR-04 concurrent inbound-webhook double-send stress check (load test)."
      - "human_needed: WR-06 async/sync session boundary under worker restart (kill-worker experiment)."
  - phase: 06-real-externals-enablement
    items:
      - "human_needed: RUN_REAL_EXTERNALS smoke against live OpenRouter key."
  - phase: 07-real-places-hardening
    items:
      - "human_needed: live load-test (loadtest_destinos_atrativos.py) with real Places+DeepSeek keys → ACCEPTANCE: PASS (G1/G2 closed in-harness per gap_closure_result)."
  - phase: cross-phase
    items:
      - "WARNING: DLQ validate/validate-batch swallow push_destination_task.delay failure silently (dlq.py:165,224) — record sits in Mar unpublished on broker-down with no log; sibling gate/webhook paths log + 503 correctly."
      - "WARNING: orphaned CMS edit routes PATCH /api/v1/{atrativos,destinos}/{rio_id}/edit (cms.py) — no dashboard consumer; edit→re-score for DLQ still reachable via /validate."
nyquist:
  compliant_phases: ["08"]
  partial_phases: []
  missing_phases: ["01","02","03","04","05","06","07"]
  overall: partial
---

# Milestone v1.0 — Audit Report

**Milestone:** norteia-brave — Pipeline Brave (Collector), foundational milestone
**Audited:** 2026-06-19
**Status:** ⚠ gaps_found — 1 production-packaging BLOCKER in cross-phase task wiring

## Summary

All 8 phases verified; all 44 v1 requirements VERIFIED offline by their phase
VERIFICATION reports. The pipeline core, score engine, both lanes, dashboard↔API,
Bearer auth, Pact contract, and the human-gate invariant (no auto-send) are wired
end-to-end and pass the full offline suite (backend + 127/127 dashboard).

One cross-phase wiring defect blocks sign-off: the Celery sweep/FSM tasks import
`tests.fakes.*` in their **default** offline branch, but the production wheel ships
only the `brave` package. Under the documented default (`run_real_externals=False`)
every sweep/discovery/FSM task crashes with `ModuleNotFoundError: No module named
'tests'`. It is masked today only because the repo `.env` forces
`RUN_REAL_EXTERNALS=true`. This is a real packaging/wiring break, not a test artifact.

## Phase Verification

| Phase | Status | Score | Note |
|-------|--------|-------|------|
| 01 brave-core-score-gate-boundary-contract | passed | 5/5 | re-verified after gap closure |
| 02 destinos-lane | passed | 5/5 | + SECURITY.md |
| 03 atrativos-lane-whatsapp-compliance | human_needed | 9/9 | all offline checks VERIFIED; 3 live-infra items |
| 04 dashboard-territorial-cms | passed | 6/6 | DASH-03 gap closed by 04-10 |
| 05 auto-discovery-orchestration | passed | 21/21 | — |
| 06 real-externals-enablement | human_needed | 7/7 | 1 live-OpenRouter smoke |
| 07 real-places-hardening | human_needed | 7/7 | G1/G2 closed in-harness; 1 live load-test |
| 08 ops-cms-process-observability | passed | 13/13 | re-verified; VALIDATION.md present |

No phase is `gaps_found`. The 3 `human_needed` phases are code-complete and
offline-green; each awaits a single live-infrastructure confirmation.

## Requirements Coverage (3-source cross-reference)

- **VERIFICATION.md:** all 44 REQ-IDs appear and are marked VERIFIED/SATISFIED across phases 1–5 (CORE/SCORE/OBS/CNTR/TEST → P1; DEST/TEST-02 → P2; ATR/COMP → P3; DASH → P4; ORCH → P5).
- **SUMMARY frontmatter:** `requirements_completed` is empty (`null`) in every SUMMARY — frontmatter cross-check unavailable; fell back to VERIFICATION tables.
- **REQUIREMENTS.md traceability:** ATR-01..06 and COMP-01..03 are still checked `[ ]` / "Pending", but Phase 3 VERIFICATION marks all nine **SATISFIED** offline. These are **stale checkboxes**, not unsatisfied requirements — update them on completion.

**Result:** 44/44 satisfied at code/offline level. 0 orphaned, 0 unsatisfied by the matrix. The `gaps_found` status is driven by the integration blocker, not by a requirement gap.

## Cross-Phase Integration

**Wired & verified:** pipeline core consumed by both lanes; score engine serves destino+atrativo; destinos sweep chain; atrativos FSM auto-advance terminating at the human gate (outreach dispatches only from steward `/approve`); dashboard→BFF→FastAPI with fail-closed Bearer auth; Pact contract; all 11 Celery tasks register, both beat task names resolve, 54 beat entries (27 UF × 2), no phantom tasks.

**BLOCKER — INT-BLOCKER-01:** offline branch imports `tests.fakes.*` (absent from wheel) under default config → `ModuleNotFoundError`. Fix: add `NullPlacesClient`/`NullLLMClient`/`NullApifyClient` under `brave/clients/` and use them in the offline branch (mirror the existing `NullWhatsAppClient`, which handles this correctly). Files: `brave/tasks/pipeline.py` (8 sites), `pyproject.toml`, `brave/config/settings.py`.

**WARNING — silent push-drop:** `brave/api/routers/dlq.py:165,224` swallow `push_destination_task.delay` failures; a broker-down leaves a Mar record unpublished with no log.

**WARNING — orphaned edit routes:** `PATCH /api/v1/{atrativos,destinos}/{rio_id}/edit` have no dashboard consumer.

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 01 | missing | — | `/gsd:validate-phase 01` |
| 02 | missing | — | `/gsd:validate-phase 02` |
| 03 | missing | — | `/gsd:validate-phase 03` |
| 04 | missing | — | `/gsd:validate-phase 04` |
| 05 | missing | — | `/gsd:validate-phase 05` |
| 06 | missing | — | `/gsd:validate-phase 06` |
| 07 | missing | — | `/gsd:validate-phase 07` |
| 08 | exists | true | — |

Only Phase 08 carries a VALIDATION.md (`nyquist_compliant: true`). Phases 01–07 predate the Nyquist gate — discovery only, not auto-run.
