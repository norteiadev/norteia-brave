---
phase: 15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li
plan: 07
type: execute
wave: 4
depends_on: ["15-03", "15-04", "15-06"]
files_modified:
  - brave/tasks/pipeline.py
  - scripts/ta_bulk_sweep.py
  - tests/unit/tasks/test_sweep_tripadvisor.py
autonomous: true
requirements: [TA-12]
must_haves:
  truths:
    - "A bulk national branch in sweep_tripadvisor paginates geoId 294280 via produce_paginated"
    - "The slice and full run share ONE page-range-parameterized code path (start_page/max_pages)"
    - "A mid-run 403/429 stops cleanly: rollback, needs_bootstrap marker, progress state stopped_needs_bootstrap, no retry"
    - "On completion the progress state is marked done"
    - "A re-run resumes from the page after the last completed offset (does not restart at page 1)"
    - "The existing per-UF destinos+atrativos sweep path is unchanged"
    - "An operator can trigger a small page-range slice end-to-end"
  artifacts:
    - path: "brave/tasks/pipeline.py"
      provides: "sweep_tripadvisor bulk national branch (resume read, produce_paginated, progress, fail-fast)"
      contains: "produce_paginated"
    - path: "scripts/ta_bulk_sweep.py"
      provides: "Thin operator entrypoint for a page-range slice (and later the full run)"
      contains: "max_pages"
  key_links:
    - from: "brave/tasks/pipeline.py::sweep_tripadvisor (bulk branch)"
      to: "brave/lanes/tripadvisor/atrativos.py::produce_paginated"
      via: "asyncio.run on the national geoId"
      pattern: "produce_paginated"
    - from: "sweep_tripadvisor bulk branch"
      to: "brave/lanes/tripadvisor/sweep_progress.py"
      via: "start / record (via producer) / mark_done / stop_needs_bootstrap"
      pattern: "sweep_progress"
---

<objective>
Wire the bulk national run into `sweep_tripadvisor`: a DISTINCT branch (national geoId 294280) that
reads the resume offset, starts progress, drives `produce_paginated` (15-06), marks done on completion,
and on a mid-run 403/429 reuses the EXISTING fail-fast block (rollback + `_mark_needs_bootstrap`)
plus `sweep_progress.stop_needs_bootstrap`. The slice (`max_pages=5-10`) and the full run
(`max_pages=334`) share this ONE page-range-parameterized path — the full national run is a later
TRIGGER, not new code (CONTEXT/phase intent). A thin operator entrypoint lets the operator run a
small slice end-to-end now. The existing per-UF destinos+atrativos sweep path stays unchanged.

Purpose: Make the slice runnable end-to-end (fetch → extract → bulk-ingest → Nascente → live panel)
with resume safety, deferring full-run geocode-resilience batching.
Output: bulk branch + resume + fail-fast progress wiring + operator slice trigger + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-PATTERNS.md
@brave/tasks/pipeline.py

<interfaces>
<!-- The sweep_tripadvisor structure to branch within. -->
brave/tasks/pipeline.py::sweep_tripadvisor(self, uf, depth=None) (:924-1085):
  run_rio = depth != collection_engine.NASCENTE (:949).
  Client/geocoder selection on app_config.run_real_externals (:956-978) — REUSE verbatim
    (real TripAdvisorClient + NominatimGeocoderClient vs Null* offline).
  IBGE load (:980-986). Per-UF path = destinos producer + destino_rio_map + atrativos.produce (:988-1030) — DO NOT change.
  FAIL-FAST block :1032-1046: except (SessionMissingError, SessionExpiredError) → session.rollback();
    _mark_needs_bootstrap(); logger.warning(error_type=type(exc).__name__); return. ADD sweep_progress.stop_needs_bootstrap(rc) here.
  _mark_needs_bootstrap() — module helper that obtains the sync redis client; obtain `rc` the same way for progress calls.
brave/lanes/tripadvisor/atrativos.py: produce_paginated(geo_id, start_page, max_pages, redis, *, run_rio) (15-06);
  TripAdvisorAtrativosIngest(ta_client, session, config, ibge_records, destino_rio_map=None, geocoder=...) — bulk uses destino_rio_map=None.
brave/lanes/tripadvisor/sweep_progress.py (15-03): start(rc, pages_total, resume_from_offset=0), mark_done(rc),
  stop_needs_bootstrap(rc), get_resume_offset(rc)->int, get_progress(rc) (carries pages_done).
National geoId = 294280; pages_total = 334 (full); for a slice pass max_pages directly.
Resume contract: start_page = 1 when progress pages_done==0, else (get_resume_offset(rc)//30) + 2 (page AFTER last completed offset).
scripts/ta_bootstrap (existing) — style analog for scripts/ta_bulk_sweep.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sweep_tripadvisor bulk national branch + operator slice trigger</name>
  <read_first>
    - brave/tasks/pipeline.py (sweep_tripadvisor :924-1085 — client/geocoder selection :956-978, per-UF path :988-1030, fail-fast :1032-1046, _mark_needs_bootstrap)
    - brave/lanes/tripadvisor/atrativos.py (produce_paginated + TripAdvisorAtrativosIngest constructor — 15-06)
    - brave/lanes/tripadvisor/sweep_progress.py (start/mark_done/stop_needs_bootstrap/get_resume_offset/get_progress — 15-03)
    - scripts/ta_bootstrap (existing operator-helper style)
  </read_first>
  <action>
    Add bulk params to `sweep_tripadvisor(self, uf, depth=None, *, bulk_national=False, start_page=1,
    max_pages=None, geo_id=294280)`. When `bulk_national` is True, take a DISTINCT branch that does NOT run
    the destinos producer / build `destino_rio_map`: reuse the existing client + geocoder selection (:956-978)
    and IBGE load, obtain the sync redis client `rc` (same way `_mark_needs_bootstrap` does), compute the resume
    start_page (1 when `sweep_progress.get_progress(rc)["pages_done"]==0`, else `(sweep_progress.get_resume_offset(rc)//30)+2`),
    call `sweep_progress.start(rc, pages_total=334, resume_from_offset=...)`, build
    `TripAdvisorAtrativosIngest(..., destino_rio_map=None, geocoder=geocoder)`, then
    `asyncio.run(atrativos_ingest.produce_paginated(geo_id, start_page, max_pages or 334, rc, run_rio=run_rio))`,
    then `sweep_progress.mark_done(rc)` and `session.commit()` (note produce_paginated already commits per page —
    this is the terminal commit). Add `sweep_progress.stop_needs_bootstrap(rc)` INSIDE the existing
    `except (SessionMissingError, SessionExpiredError)` block (do not change its rollback/_mark_needs_bootstrap/
    log-error-class/return behavior). Keep `run_rio` depth-gating and the operator-gated posture (no auto-Mar,
    no WhatsApp). Leave the per-UF path (bulk_national=False) byte-for-byte unchanged. Create
    `scripts/ta_bulk_sweep.py` (mirror scripts/ta_bootstrap style) — an operator CLI with `--start-page` and
    `--max-pages` args that dispatches the bulk branch (inline run or `.delay`) for a slice; default a small
    slice cap. Log only offsets/counts/error-class — never cookies/session.
  </action>
  <acceptance_criteria>
    - `grep -q 'bulk_national' brave/tasks/pipeline.py` and `grep -q 'produce_paginated' brave/tasks/pipeline.py`.
    - The per-UF branch (bulk_national=False) is unchanged: existing test_sweep_tripadvisor.py tests for the destinos+atrativos path still pass.
    - `python scripts/ta_bulk_sweep.py --help` exits 0 and documents --start-page / --max-pages.
    - Bulk branch logs reference no cookies/user_agent/session_id (source grep).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/tasks/test_sweep_tripadvisor.py -x -q && .venv/bin/python scripts/ta_bulk_sweep.py --help >/dev/null && echo TRIGGER_OK</automated>
  </verify>
  <done>Bulk national branch paginates geoId 294280 via produce_paginated with resume + progress + fail-fast; per-UF path intact; operator slice trigger present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resume + fail-fast progress tests for the bulk branch</name>
  <read_first>
    - tests/unit/tasks/test_sweep_tripadvisor.py (existing fail-fast + client-selection test patterns; savepoint-isolated session fixture)
    - brave/lanes/tripadvisor/sweep_progress.py (get_progress/get_resume_offset/stop_needs_bootstrap)
    - tests/fakes/fake_tripadvisor.py (fetch_attractions_paginated yields fixture_pages; can raise SessionExpiredError)
  </read_first>
  <behavior>
    - With a fake client yielding 2 pages, bulk_national=True ingests both, progress ends state="done", pages_done=2.
    - With a fake client that raises SessionExpiredError on page 2, the sweep stops: needs_bootstrap marker set,
      progress state="stopped_needs_bootstrap", page-1 records remain durable (committed per page), no retry.
    - A re-run after the mid-run stop computes start_page from the resume offset (page after last completed), not page 1.
    - bulk_national=False path is unaffected (regression assertion).
  </behavior>
  <action>
    Extend `tests/unit/tasks/test_sweep_tripadvisor.py` (offline: fakeredis + fake client + fake geocoder +
    savepoint-isolated session per the repo convention) with: a happy-path bulk run asserting progress
    state="done"/pages_done==2; a mid-run SessionExpiredError test asserting state="stopped_needs_bootstrap",
    needs_bootstrap marker set, page-1 records durable, and no retry; and a resume test asserting the second
    invocation starts at the page after `get_resume_offset` (e.g. resume_offset=30 → start_page 3). Assert the
    per-UF (bulk_national=False) path still behaves as before.
  </action>
  <acceptance_criteria>
    - Happy-path test: progress get_progress(rc)["state"]=="done", pages_done==2, Nascente attraction rows > 0.
    - Fail-fast test: state=="stopped_needs_bootstrap", needs_bootstrap marker present, page-1 Nascente rows durable, client called no more than the page that raised.
    - Resume test: with resume_offset=30, the bulk branch invokes produce_paginated with start_page==3.
    - Existing per-UF sweep tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/tasks/test_sweep_tripadvisor.py -x -q</automated>
  </verify>
  <done>Resume, fail-fast, and done-state behaviors are proven offline; per-UF path regression-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collector → TripAdvisor (long sequential run) | Session expiry mid-run; secrets must not leak; partial progress must be durable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07-01 | Information Disclosure | fail-fast + bulk-branch logs | mitigate | Reuse the existing block: log only `type(exc).__name__`/uf/offsets — never exc str (may carry cookie fragments) or cookies/session_id (T-12-04-01). |
| T-15-07-02 | Tampering / DoS | mid-run 403 data loss + retry storm | mitigate | Per-page commit (15-06) + resume from last_completed_offset; fail-fast returns with no retry, no quarantine (RESEARCH Pitfall 3/5). |
| T-15-07-03 | Elevation of Privilege | depth gating / Mar / WhatsApp | mitigate | Bulk branch keeps run_rio depth gating; never auto-promotes to Mar; TA attractions never enter WhatsApp (CONTEXT/Phase 11 locked). |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
Run the `<verify>` commands with `RUN_REAL_EXTERNALS` UNSET. Bulk happy-path, fail-fast, resume, and per-UF
regression tests green. Slice CLI `--help` works. (A real ~5-10 page operator slice is the Manual-Only verification.)
</verification>

<success_criteria>
The slice runs end-to-end offline (fake transport): pages paginate, cards bulk-ingest to Nascente, progress goes
running→done, mid-run expiry stops cleanly with a resumable offset, and the per-UF path is untouched — ready for
the operator Level-3 live slice.
</success_criteria>

<output>
Create `.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-07-SUMMARY.md` when done
</output>
