---
phase: 17.1-painel-brave-remaining-pages-real-backend-slice-2
plan: 02
type: execute
wave: 2
depends_on: ["17.1-01"]
files_modified:
  - brave/core/models.py
  - alembic/versions/0007_add_runs_history.py
  - brave/api/routers/engine.py
  - brave/tasks/pipeline.py
  - brave/api/routers/runs.py
  - brave/api/main.py
  - tests/integration/test_migration_0007.py
  - tests/unit/test_runs_write_path.py
  - tests/unit/test_runs_history.py
autonomous: true
requirements: [UI-PAINEL-2]

must_haves:
  truths:
    - "Starting the engine persists a runs_history row (only after the depth/source/already-running guards pass)"
    - "The sweep orchestrator finalizes the row (ended_at, ufs_dispatched, status) without ever aborting the sweep on a write failure"
    - "GET /api/v1/runs lists runs filtered by uf/source/depth with on-read synced/failed counts"
    - "PATCH /api/v1/runs/{run_id}/reprocess re-dispatches the run scope and audits it"
  artifacts:
    - path: "brave/core/models.py"
      provides: "RunHistory SQLAlchemy model"
      contains: "class RunHistory"
    - path: "alembic/versions/0007_add_runs_history.py"
      provides: "runs_history table migration (down_revision 0006)"
      contains: "runs_history"
    - path: "brave/api/routers/runs.py"
      provides: "list runs + reprocess endpoints"
      contains: "api/v1/runs"
    - path: "tests/unit/test_runs_write_path.py"
      provides: "Offline unit test of the engine_start INSERT guards + finalize best-effort swallow"
  key_links:
    - from: "brave/api/routers/engine.py"
      to: "brave.core.models.RunHistory"
      via: "INSERT after start_run() succeeds"
      pattern: "RunHistory"
    - from: "brave/tasks/pipeline.py"
      to: "runs_history row"
      via: "finalize UPDATE in finally block"
      pattern: "run_id"
    - from: "brave/api/main.py"
      to: "brave/api/routers/runs.py"
      via: "include_router(runs_router)"
      pattern: "include_router"
---

<objective>
Build the NET-NEW Varreduras backend: a persisted runs_history envelope table written at engine-start
and finalized when the sweep returns to idle, plus list (filter by uf/source/depth with on-read
synced/failed aggregation) and reprocess endpoints.

Purpose: engine runs live only in Redis today (no DB audit trail). This adds the durable trail
Varreduras renders. synced/failed are computed on-read over the run window because async producer
tasks don't return counts (RESEARCH #2).
Output: RunHistory model + 0007 migration, write points in engine_start + engine_sweep_run, runs.py
(list + reprocess) registered in main.py, migration + write-path + list unit tests (integration paths
skip-safe).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-CONTEXT.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-RESEARCH.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RunHistory model + 0007 migration + migration test</name>
  <files>brave/core/models.py, alembic/versions/0007_add_runs_history.py, tests/integration/test_migration_0007.py</files>
  <read_first>
    - brave/core/models.py:243-269 (LLMGeneration table-style analog), :21-41 (already-present imports), :356 (ConsentLog)
    - alembic/versions/0006_add_rio_mar_ready.py (entire file — revision header + create_table/create_index structure; non-CONCURRENTLY index)
    - tests/integration/test_migration_0006.py:15 (pytestmark integration; offline revision-metadata test + skipif-no-BRAVE_DB_URL up/down test)
    - 17.1-RESEARCH.md Finding 2 "Recommended schema" + "Alembic migration steps" (exact column list)
  </read_first>
  <action>
    Add class RunHistory(Base) to brave/core/models.py mirroring the LLMGeneration declaration style
    (no new imports needed — JSON/DateTime/Integer/String/func/UUID already imported). Columns per
    RESEARCH #2: id (UUID pk default uuid4), started_at (DateTime tz, server_default=func.now(), index),
    ended_at (nullable), ufs (JSON), source (String(32)), depth (String(32)), lane (String(32),
    server_default="both"), ufs_total (Integer default 0), ufs_dispatched (Integer default 0), total
    (Integer nullable), synced (Integer nullable), failed (Integer nullable), status (String(16),
    server_default="running").
    Create alembic/versions/0007_add_runs_history.py mirroring 0006 exactly: revision="0007",
    down_revision="0006", branch_labels=None, depends_on=None; upgrade() op.create_table("runs_history",
    ...) with the full RESEARCH #2 column list + op.create_index("ix_runs_history_started_at",
    "runs_history", ["started_at"]) (plain, non-CONCURRENTLY — inside the Alembic txn); downgrade()
    drops index then table.
    Write tests/integration/test_migration_0007.py mirroring test_migration_0006.py: pytestmark =
    pytest.mark.integration at module top; one offline test asserting revision=="0007" and
    down_revision=="0006" (runs WITHOUT a DB); one @pytest.mark.skipif(not BRAVE_DB_URL) up/down test.
  </action>
  <acceptance_criteria>
    - `grep -c "class RunHistory" brave/core/models.py` returns 1
    - `grep -c "runs_history" alembic/versions/0007_add_runs_history.py` returns ≥1
    - `grep -c "0007" alembic/versions/0007_add_runs_history.py` returns ≥1 and down_revision is "0006"
    - `.venv/bin/python -m pytest tests/integration/test_migration_0007.py -x` passes (offline metadata test runs; DB up/down skips safely without BRAVE_DB_URL)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/integration/test_migration_0007.py -x</automated>
  </verify>
  <done>RunHistory model + 0007 migration exist (down_revision 0006, non-CONCURRENTLY index); migration test asserts revision metadata offline and applies/rolls back when BRAVE_DB_URL is set.</done>
</task>

<task type="auto">
  <name>Task 2: write points — engine_start INSERT + engine_sweep_run finalize UPDATE + offline write-path test</name>
  <files>brave/api/routers/engine.py, brave/tasks/pipeline.py, tests/unit/test_runs_write_path.py</files>
  <read_first>
    - brave/api/routers/engine.py:72-75 (engine_status already injects db + redis), :87-149 (engine_start body), :108 (depth 422), :117 (source 422), :123 (start_run success), :135 (engine_sweep_run.delay)
    - brave/tasks/pipeline.py:1864 (engine_sweep_run), :1902 (own redis client), :1911/:1932 (dispatched counter), :1914 (get_state read), :1942-1944 (finally block)
    - brave/api/routers/tripadvisor_session.py:292-335 (best-effort try/except posture — write must never abort the operation)
    - tests/unit/test_dedup.py (MagicMock-session offline test pattern); existing fakeredis usage in tests/ for engine state
    - 17.1-RESEARCH.md Finding 2 "Write points (exact)" + Pitfall 3
  </read_first>
  <behavior>
    - engine_start inserts NO RunHistory row when start is rejected by the depth 422, the source 422, or the start_run() 409 (already-running) guard.
    - A RunHistory row IS inserted exactly once, only AFTER start_run() returns True; run_id is persisted to Redis and passed into engine_sweep_run.delay.
    - The finalize UPDATE in engine_sweep_run's finally block swallows any write failure (try/except) and never aborts the sweep.
  </behavior>
  <action>
    In brave/api/routers/engine.py engine_start: add db: Session = Depends(get_db) to the signature
    (mirror engine_status :72-75). Pitfall 3: INSERT the RunHistory row ONLY AFTER the depth 422 (:108),
    source 422 (:117), AND start_run() returns True (:123) — never before (rejected starts must not
    create phantom rows). Set status="running", ufs, source, depth, lane, ufs_total=len(ufs). Capture
    run_id; persist it to Redis under key brave:engine:run_id; pass run_id into
    engine_sweep_run.delay(..., run_id=run_id) (:135). Wrap the INSERT so it can never abort a valid start.
    In brave/tasks/pipeline.py engine_sweep_run: accept the new run_id kwarg; open a DB session the same
    way the task opens its redis client (:1902). In the finally block (after mark_idle, :1942-1944)
    UPDATE the row by run_id: ended_at=now(), ufs_dispatched=dispatched (:1932), status="concluido" (or
    "parcial" if Stop drained early — detectable via the same get_state read at :1914). Wrap the UPDATE
    in try/except so a runs-history write failure NEVER aborts the sweep (best-effort posture, mirror
    tripadvisor_session.py:292-335).
    Write tests/unit/test_runs_write_path.py — a FOCUSED OFFLINE unit test (fakeredis + a mock/inline
    session — NOT @pytest.mark.integration): (a) assert NO RunHistory is added when engine_start is
    rejected by a bad depth (422), a bad source (422), and a start_run()-returns-False 409 — in each
    case session.add is never called with a RunHistory; (b) assert exactly one RunHistory row is added
    only after start_run() returns True, and run_id is written to brave:engine:run_id; (c) assert the
    finalize UPDATE path swallows a raised write error (mock the session to raise on the UPDATE) and the
    sweep function still returns/completes without propagating. Use MagicMock/fakeredis so it runs in the
    default offline suite.
  </action>
  <acceptance_criteria>
    - `grep -c "RunHistory" brave/api/routers/engine.py` returns ≥1 and the INSERT is after the start_run() guard (verified by reading)
    - `grep -c "run_id" brave/tasks/pipeline.py` returns ≥1 (finalize UPDATE keyed by run_id)
    - `grep -c "brave:engine:run_id" brave/api/routers/engine.py` returns ≥1
    - test asserts no RunHistory.add on any rejected start AND a swallowed finalize-write error does not abort the sweep
    - `.venv/bin/python -m pytest tests/unit/test_runs_write_path.py -x` passes (offline; RUN_REAL_EXTERNALS unset)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_runs_write_path.py -x</automated>
  </verify>
  <done>engine_start inserts a runs_history row only after all guards pass (proven offline); the sweep finalizes the row in a best-effort finally block that cannot abort the sweep (proven offline).</done>
</task>

<task type="auto">
  <name>Task 3: runs.py list (on-read aggregation) + reprocess + register router</name>
  <files>brave/api/routers/runs.py, brave/api/main.py, tests/unit/test_runs_history.py</files>
  <read_first>
    - brave/api/routers/cms.py:483-540 (list paginated-envelope + Query-filter analog)
    - brave/api/routers/sweep.py:31-54 (prod-vs-offline broker fallback to copy verbatim), :62 (POST /sweep per UF)
    - brave/api/routers/engine.py:42-68 (func.count window-aggregation idiom)
    - brave/core/models.py (RunHistory from Task 1; MarRecord.published_at :224; RioRecord.processed_at :147; PoisonQuarantine :310)
    - brave/observability/audit.py:22 (write_audit), brave/api/main.py:48-67 (router registration)
    - 17.1-RESEARCH.md Finding 2 "Endpoint shapes" + A4 (on-read window approximation)
  </read_first>
  <action>
    Create brave/api/routers/runs.py. Declare typed Pydantic response models (extra="forbid"): RunItem
    (id, started_at, ended_at, ufs, source, depth, total, synced, failed, status) and RunsResponse
    (items, total, offset, limit). Implement GET /api/v1/runs with dependencies=[Depends(require_bearer)],
    Query filters uf/source/depth + offset/limit, same paginated-envelope shape as list_atrativos.
    Compute synced/failed ON-READ over [started_at, ended_at] per RESEARCH #2: synced = count of
    MarRecord with published_at in window; failed = count of RioRecord routed dlq/descarte with
    processed_at in window PLUS PoisonQuarantine rows in window (document the A4 approximation in a
    docstring). Implement PATCH /api/v1/runs/{run_id}/reprocess (status_code=202,
    dependencies=[Depends(require_steward_or_bearer)]): re-dispatch the run's (ufs, source, depth) scope
    REUSING the sweep.py:31-54 prod-vs-offline broker fallback verbatim (task.delay per UF; on broker
    error, raise 503 only when AppConfig().run_real_externals else task.run inline); write_audit(action=
    "run_reprocessed", ...). Register the router in main.py with a lazy import + include_router.
    Write tests/unit/test_runs_history.py (offline): assert the on-read window aggregation counts
    synced/failed correctly against a MagicMock/in-memory session, the list envelope shape is exact, and
    the reprocess dispatch uses the offline inline fallback (no broker) without raising.
  </action>
  <acceptance_criteria>
    - `grep -c "api/v1/runs" brave/api/routers/runs.py` returns ≥2 (list + reprocess)
    - `grep -c "run_real_externals" brave/api/routers/runs.py` returns ≥1 (broker fallback)
    - `grep -c "include_router(runs" brave/api/main.py` returns ≥1
    - `.venv/bin/python -m pytest tests/unit/test_runs_history.py -x` passes
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_runs_history.py -x</automated>
  </verify>
  <done>GET /api/v1/runs lists runs with on-read synced/failed; reprocess re-dispatches the scope with the offline broker fallback and audits; router registered; offline unit test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard→BFF→collector | operator lists/reprocesses runs here |
| Celery task→DB | the sweep orchestrator finalizes a run row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-02-01 | Elevation | runs reprocess | mitigate | require_steward_or_bearer; require_bearer on GET |
| T-17.1-02-02 | DoS | runs-history write aborts the live sweep | mitigate | finalize UPDATE wrapped best-effort (try/except) — never aborts the sweep; proven by tests/unit/test_runs_write_path.py |
| T-17.1-02-03 | Integrity | phantom run rows for rejected starts | mitigate | INSERT only after depth/source 422 + start_run() success (Pitfall 3); proven by tests/unit/test_runs_write_path.py |
| T-17.1-02-04 | Repudiation | reprocess action | mitigate | write_audit(action="run_reprocessed") |
| T-17.1-02-SC | Tampering | package installs | accept | no installs this plan |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_runs_write_path.py tests/unit/test_runs_history.py tests/integration/test_migration_0007.py -x` green; integration paths skip-safe without BRAVE_DB_URL.
- runs router included in main.py; engine_start write gated behind all guards (offline-proven).
</verification>

<success_criteria>
runs_history table + write path persist engine runs durably; list (on-read synced/failed) + reprocess endpoints are guarded, audited, and offline-tested; the write-path guards (no phantom row, best-effort finalize) are proven by a focused offline unit test; the finalize write can never abort a sweep.
</success_criteria>

<output>
Create `.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-02-SUMMARY.md` when done.
</output>
