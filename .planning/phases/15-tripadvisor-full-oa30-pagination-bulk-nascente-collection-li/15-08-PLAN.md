---
phase: 15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li
plan: 08
type: execute
wave: 2
depends_on: ["15-03"]
files_modified:
  - dashboard/lib/ta-sweep-api.ts
  - dashboard/mocks/handlers/ta-sweep.ts
  - dashboard/components/engine/TASweepProgress.tsx
  - dashboard/components/engine/__tests__/TASweepProgress.test.tsx
  - dashboard/app/processo/page.tsx
autonomous: true
requirements: [TA-12]
must_haves:
  truths:
    - "A new dashboard panel polls the sweep progress endpoint every 10s and renders live progress"
    - "The panel shows a pages_done/pages_total progress bar, attractions ingested, current offset, errors, and a terminal-state pill"
    - "The panel is mounted on /processo beside EngineControl"
    - "An unauthorized (401) response is handled without crashing"
  artifacts:
    - path: "dashboard/components/engine/TASweepProgress.tsx"
      provides: "Live TripAdvisor sweep progress panel (10s poll, data-testid discipline)"
      contains: "refetchInterval"
    - path: "dashboard/lib/ta-sweep-api.ts"
      provides: "fetchTASweepProgress + TASweepProgress type + taSweepKeys"
      contains: "tripadvisor/sweep/progress"
    - path: "dashboard/mocks/handlers/ta-sweep.ts"
      provides: "MSW handler at the double /api/api/ BFF prefix"
      contains: "/api/api/v1/tripadvisor/sweep/progress"
  key_links:
    - from: "dashboard/components/engine/TASweepProgress.tsx"
      to: "dashboard/lib/ta-sweep-api.ts::fetchTASweepProgress"
      via: "useQuery queryFn"
      pattern: "fetchTASweepProgress"
    - from: "dashboard/app/processo/page.tsx"
      to: "TASweepProgress"
      via: "panel mount beside EngineControl"
      pattern: "<TASweepProgress"
---

<objective>
Add the locked NEW dashboard live-progress panel by MIRRORING the existing EngineControl panel +
engine-api client + engine MSW handler + EngineControl test exactly (10s poll, BFF double `/api/api/`
prefix, data-testid discipline, MSW + Vitest). No novel UI — the panel renders the sweep progress JSON
from `GET /api/v1/tripadvisor/sweep/progress` (15-03).

Purpose: Give the operator a real-time visual of the running sweep (pages/334, attractions ingested,
offset, errors, terminal state) while slice/full runs execute.
Output: ta-sweep-api.ts + ta-sweep.ts handler + TASweepProgress.tsx + test + /processo mount.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-PATTERNS.md

<interfaces>
<!-- Mirror targets (PATTERNS.md pins exact line ranges). Do not design novel UI. -->
dashboard/lib/engine-api.ts: apiFetch usage, fetchTASessionStatus (:124-127), taSessionKeys,
  ENGINE_REFETCH_INTERVAL_MS (:74, =10_000). apiFetch takes the bare FastAPI path "api/v1/...".
dashboard/lib/api-client.ts (:10-38): bff() maps "api/v1/..." → "/api/api/v1/..." (double prefix).
dashboard/mocks/handlers/engine.ts (:15-16 TA_BASE, :64-73 taSessionStatus): MSW handlers hardcode the
  double "/api/api/" prefix — a single "/api/" 404s in tests.
dashboard/components/engine/EngineControl.tsx: useQuery poll (:92-97), progress-bar markup (:329-344),
  sessionLabel/sessionColor pill (:73-84), CountTile (:368-375), "use client" + data-testid on every readable element.
dashboard/components/engine/__tests__/EngineControl.test.tsx: server.resetHandlers in beforeEach,
  server.use(...) per-test override, renderWithClient (from ../../cms/__tests__/test-utils), findByTestId + waitFor.
dashboard/app/processo/page.tsx: <EngineControl /> mount (:96).

Shared JSON contract (must match 15-03 TASweepProgressResponse EXACTLY):
  { state: "running"|"done"|"stopped_needs_bootstrap"|"idle"; pages_done: number; pages_total: number;
    attractions_ingested: number; current_offset: number; error_count: number; started_at?: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ta-sweep-api.ts data layer + ta-sweep.ts MSW handler</name>
  <read_first>
    - dashboard/lib/engine-api.ts (fetchTASessionStatus :124-127, taSessionKeys, ENGINE_REFETCH_INTERVAL_MS :74)
    - dashboard/lib/api-client.ts (:10-38 bff double-prefix mapping)
    - dashboard/mocks/handlers/engine.ts (:15-16 TA_BASE, :64-73 taSessionStatus)
    - .planning/phases/15-...-li/15-PATTERNS.md (ta-sweep-api.ts + ta-sweep.ts sections — exact excerpts)
  </read_first>
  <action>
    Create `dashboard/lib/ta-sweep-api.ts`: export the `TASweepProgress` interface matching the 15-03 response
    shape EXACTLY, `taSweepKeys = { status: ["ta","sweep","progress"] as const }`, and
    `fetchTASweepProgress(): Promise<TASweepProgress>` calling `apiFetch<TASweepProgress>("api/v1/tripadvisor/sweep/progress")`
    (bare FastAPI path — apiFetch/bff add the prefix; callers never write the double prefix). Reuse
    `ENGINE_REFETCH_INTERVAL_MS` (10_000). Create `dashboard/mocks/handlers/ta-sweep.ts`: a `taSweepProgress(overrides)`
    factory mirroring `taSessionStatus`, registering `http.get` at
    `http://localhost:3000/api/api/v1/tripadvisor/sweep/progress` (MANDATORY double `/api/api/` BFF prefix) returning
    a default running snapshot; plus a `taSweepUnauthorized` 401 variant mirroring the engine unauthorized handler.
  </action>
  <acceptance_criteria>
    - `grep -q 'api/v1/tripadvisor/sweep/progress' dashboard/lib/ta-sweep-api.ts`.
    - `grep -q '/api/api/v1/tripadvisor/sweep/progress' dashboard/mocks/handlers/ta-sweep.ts` (double prefix).
    - The TASweepProgress TS interface field names match the 15-03 TASweepProgressResponse exactly (state/pages_done/pages_total/attractions_ingested/current_offset/error_count/started_at).
    - `cd dashboard && bun run test ta-sweep` type-checks/imports cleanly (handler usable in tests).
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bun run test TASweepProgress</automated>
  </verify>
  <done>Data layer + MSW handler exist with the correct BFF double prefix and a contract matching the endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: TASweepProgress.tsx panel + test + /processo mount</name>
  <read_first>
    - dashboard/components/engine/EngineControl.tsx (useQuery :92-97, progress bar :329-344, pill :73-84, CountTile :368-375)
    - dashboard/components/engine/__tests__/EngineControl.test.tsx (server.use override, renderWithClient, findByTestId/waitFor)
    - dashboard/app/processo/page.tsx (:96 EngineControl mount)
    - dashboard/lib/ta-sweep-api.ts + dashboard/mocks/handlers/ta-sweep.ts (from Task 1)
  </read_first>
  <action>
    Create `dashboard/components/engine/TASweepProgress.tsx` ("use client") mirroring EngineControl:
    `useQuery({ queryKey: taSweepKeys.status, queryFn: fetchTASweepProgress, refetchInterval:
    ENGINE_REFETCH_INTERVAL_MS, refetchOnWindowFocus: false })`. Render (every readable element gets a
    `data-testid`): a progress bar for `pages_done/pages_total` (copy EngineControl's bar markup, swap the
    ufs_done/ufs_total fields), CountTiles for attractions_ingested / current_offset / error_count, and a
    terminal-state pill driven by `state` (running / done / stopped_needs_bootstrap → label + color, mirror
    sessionLabel/sessionColor). Use PT-BR labels and CSS-var color tokens (no hex), matching the existing
    panels. Create `__tests__/TASweepProgress.test.tsx` mirroring EngineControl.test: assert the 5/334 bar,
    attractions count, terminal-state pill, and a 401-safe render (server.use(taSweepUnauthorized)). Mount
    `<TASweepProgress />` in `dashboard/app/processo/page.tsx` immediately after `<EngineControl />` (import alongside it).
  </action>
  <acceptance_criteria>
    - `grep -q '<TASweepProgress' dashboard/app/processo/page.tsx`.
    - Vitest: panel renders pages_done/pages_total (5/334) progress, attractions_ingested tile, current_offset, error_count, and the correct terminal-state pill for each state.
    - Vitest: a 401 response renders a safe fallback (no crash / no thrown error).
    - Every rendered value has a data-testid (asserted via findByTestId in the test).
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bun run test TASweepProgress</automated>
  </verify>
  <done>Panel mirrors EngineControl, polls 10s, renders live progress + terminal pill, mounted on /processo; Vitest green offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → BFF → FastAPI | Operator bearer token is validated at the BFF before forwarding; panel reads only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08-01 | Information Disclosure | progress fetch via BFF | mitigate | apiFetch routes through the BFF which validates the bearer before forwarding; panel is read-only and renders no cookie/session data. |
| T-15-08-02 | Denial of Service | poll cadence | mitigate | Reuse the 10s ENGINE_REFETCH_INTERVAL_MS — no faster polling (RESEARCH anti-pattern). |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs — reuses @tanstack/react-query, msw, vitest already pinned. |
</threat_model>

<verification>
Run `cd dashboard && bun run test TASweepProgress`. Panel + handler tests green offline (MSW). No backend needed.
</verification>

<success_criteria>
A real-time sweep progress panel mirrors EngineControl exactly, polls the authenticated endpoint, renders pages/
attractions/offset/errors/terminal-state, and is mounted on /processo — fully offline-tested.
</success_criteria>

<output>
Create `.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-08-SUMMARY.md` when done
</output>
