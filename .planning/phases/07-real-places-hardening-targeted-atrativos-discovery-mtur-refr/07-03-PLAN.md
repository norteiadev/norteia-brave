---
phase: "07"
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - brave/lanes/atrativos/discovery_agent.py
  - tests/unit/lanes/test_discovery_agent.py
autonomous: true
requirements:
  - PLACE-03
  - PLACE-04

must_haves:
  truths:
    - "D-02: _resolve_parent_destino has an early-return guard: if not municipio_ibge or not municipio_ibge.strip(): return None — the DB is never queried for an empty ibge, preventing source_ref.contains('') from matching every MarRecord"
    - "D-03: DiscoveryAgent.produce_for_destino(parent_mar, target_count=10) exists — queries 'pontos turísticos em {municipio_nome} {uf}' and 'o que fazer em {municipio_nome} {uf}', skips _resolve_parent_destino entirely, injects parent_mar.id directly as parent_mar_id in each payload, returns int (count of rio records created)"
    - "D-03: produce_for_destino reuses the existing LLM extraction + store_raw + process_nascente_record + advance_sub_state pipeline from produce() — same payload structure, same keys (origem_value, completude_value, place_id_cache, canonical, parent_mar_id, entity_type='attraction')"
    - "D-08: test_discovery_agent.py — test_empty_ibge_guard_quarantines: places result with municipio_ibge='' → session.scalar NOT called, quarantine_poison called with error containing 'parent_destino_absent'; test_produce_for_destino_links_to_known_parent: produce_for_destino with FakePlacesClient fixture for 'pontos turísticos em Porto Seguro BA' → store_raw called with payload['parent_mar_id'] == str(parent_mar.id); test_produce_for_destino_quarantines_on_missing_municipio_nome: parent_mar canonical has no municipio/name → returns 0, no store_raw called"

  artifacts:
    - path: brave/lanes/atrativos/discovery_agent.py
      provides: "Empty-ibge guard + produce_for_destino targeted method"
      contains: "produce_for_destino"
    - path: tests/unit/lanes/test_discovery_agent.py
      provides: "Offline tests for D-02 guard + D-03 targeted discovery"
      contains: "test_empty_ibge_guard_quarantines"

  key_links:
    - from: "DiscoveryAgent.produce_for_destino"
      to: "places_client.text_search"
      via: "queries: pontos turísticos em {municipio_nome} {uf}"
      pattern: "pontos turísticos em"
    - from: "DiscoveryAgent.produce_for_destino"
      to: "store_raw payload"
      via: "parent_mar_id=str(parent_mar.id) — no _resolve_parent_destino"
      pattern: "parent_mar_id"
    - from: "_resolve_parent_destino"
      to: "quarantine_poison"
      via: "early return None on empty municipio_ibge → caller quarantines"
      pattern: "not municipio_ibge"
---

<objective>
Fix the _resolve_parent_destino contains("") bug so empty municipio_ibge never matches an arbitrary parent, and add DiscoveryAgent.produce_for_destino so the harness can drive ≥10 targeted atrativos per Mar destino.

Purpose: With Plan 07-01 in place, text_search results now carry municipio_ibge — but the old fallback logic still fires source_ref.contains("") when ibge is empty. The early-return guard (D-02) eliminates the mislink. The produce_for_destino method (D-03) provides targeted per-município queries that bypass _resolve_parent_destino entirely since the parent is already known.

Output: discovery_agent.py with the empty-ibge guard on _resolve_parent_destino and a new produce_for_destino method on DiscoveryAgent. Extended test_discovery_agent.py with 3 new offline tests covering the guard and the targeted path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-RESEARCH.md
@.planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-PATTERNS.md
@brave/lanes/atrativos/discovery_agent.py
@tests/unit/lanes/test_discovery_agent.py
@tests/fakes/fake_places.py

<interfaces>
<!-- Interfaces the executor needs. Extracted from codebase. -->

From brave/lanes/atrativos/discovery_agent.py (current):

_resolve_parent_destino signature (lines 102-106):
```python
def _resolve_parent_destino(
    session: Session,
    uf: str,
    municipio_ibge: str,
) -> MarRecord | None:
```
Insert at line ~130 (before the existing SQL query): `if not municipio_ibge or not municipio_ibge.strip(): return None`

DiscoveryAgent produce() method (lines 194-364):
- Pattern for produce_for_destino: same LLM extraction + store_raw + process_nascente_record + advance_sub_state pipeline
- payload dict structure (lines 289-314): origem_value=60.0, completude_value, corroboracao_value=0.0, atualidade_value=0.0, validacao_humana_value=0.0, place_id_cache, canonical dict, parent_mar_id, municipio_id, name, entity_type, source_note
- Exception handling: quarantine_poison on places search fail + on llm extraction fail + continue

From tests/unit/lanes/test_discovery_agent.py (lines 32-44 — existing _make_places_result):
```python
def _make_places_result(
    place_id: str = "ChIJtest001",
    municipio_ibge: str = "2919207",
    municipio_nome: str = "Porto Seguro",
) -> dict[str, Any]:
    ...
```
Existing tests use FakePlacesClient(fixture_results={"atrativos em BA": [...]})

From tests/fakes/fake_places.py:
- FakePlacesClient(fixture_results: dict[str, list[dict]]) — keys are exact query strings
- For targeted discovery: keys must be "pontos turísticos em Porto Seguro BA" and "o que fazer em Porto Seguro BA"

MarRecord (brave/core/models.py) canonical field is JSONB with keys including "municipio", "uf", "ibge_code", "name"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add empty-ibge guard to _resolve_parent_destino and produce_for_destino method</name>
  <files>brave/lanes/atrativos/discovery_agent.py</files>

  <read_first>
    - brave/lanes/atrativos/discovery_agent.py (full file — already in context; lines 102-154 _resolve_parent_destino, lines 194-365 produce())
    - .planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-RESEARCH.md §RQ-2 (_resolve_parent_destino fix), §RQ-3 (produce_for_destino design with code example)
    - .planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-PATTERNS.md §brave/lanes/atrativos/discovery_agent.py (exact diff: guard insert location, produce_for_destino skeleton, payload pattern)
  </read_first>

  <behavior>
    - _resolve_parent_destino: when municipio_ibge is empty or whitespace-only, return None immediately (before the SQL query is executed)
    - produce_for_destino(self, parent_mar: MarRecord, target_count: int = 10) -> int:
      - extracts municipio_nome from parent_mar.canonical.get("municipio") or canonical.get("name", "")
      - extracts uf from parent_mar.canonical.get("uf", "")
      - extracts municipio_ibge from parent_mar.canonical.get("ibge_code", "")
      - if not municipio_nome or not uf: log warning, return 0
      - runs two queries in order: "pontos turísticos em {municipio_nome} {uf}" then "o que fazer em {municipio_nome} {uf}"
      - short-circuits the outer loop when created >= target_count
      - for each place: deduplicates by place_id (seen_place_ids set), skips empty place_id
      - does NOT call _resolve_parent_destino — injects parent_mar directly
      - runs the same LLM extraction + store_raw + process_nascente_record + advance_sub_state pipeline as produce()
      - payload["parent_mar_id"] = str(parent_mar.id), same other keys as produce()
      - quarantines on places search exception or LLM extraction exception (continue)
      - returns count of Rio records created (int)
  </behavior>

  <action>
    Read discovery_agent.py fully (already in context). Make these precise changes:

    1. Empty-ibge guard in _resolve_parent_destino (insert after the docstring and before line ~130):
       Add as the FIRST executable line of the function body (before the `from sqlalchemy import and_` import):
       `if not municipio_ibge or not municipio_ibge.strip(): return None`
       Add a comment: `# D-02 guard: empty ibge → source_ref.contains("") matches any record — never query`

    2. Add TYPE_CHECKING import for MarRecord if not already present at module level (it's already in the models import). The MarRecord type is already imported at the top (line 29).

    3. Add produce_for_destino as a new method on DiscoveryAgent class, after the produce() method (at the end of the class). The method:

       Signature:
       async def produce_for_destino(self, parent_mar: MarRecord, target_count: int = 10) -> int:

       Docstring: "Run targeted Google Places discovery for a single Mar destino municipality.\n\nBypasses _resolve_parent_destino — parent is already known.\nUses municipality-specific queries to guarantee per-destino volume.\n\nArgs:\n    parent_mar:   Active MarRecord with entity_type='destination'.\n    target_count: Desired minimum attraction count (default 10).\n\nReturns:\n    Count of Rio records created in this call (int)."

       Body:
       - canonical = parent_mar.canonical or {}
       - municipio_nome = canonical.get("municipio") or canonical.get("name", "")
       - uf = canonical.get("uf", "")
       - municipio_ibge = canonical.get("ibge_code", "")
       - if not municipio_nome or not uf: log warning("produce_for_destino_missing_fields", parent_mar_id=str(parent_mar.id)); return 0
       - search_queries = [f"pontos turísticos em {municipio_nome} {uf}", f"o que fazer em {municipio_nome} {uf}"]
       - created = 0; seen_place_ids: set[str] = set()
       - Outer loop over search_queries: if created >= target_count, break
       - Try text_search; except → quarantine_poison (task_name="brave.discover_atrativo", error=f"places_search_failed: {exc}", payload={"uf": uf, "query": query}); continue
       - Inner loop over places: if created >= target_count, break; get place_id; skip if empty or in seen_place_ids; add to seen
       - Get place_name + formatted_address + municipio_nome + municipio_ibge from place dict (fallback to values from canonical)
       - Build DISCOVERY_PROMPT (same format string, same fields) — use municipio_nome + municipio_ibge from place dict (not canonical) for the prompt, since they come from the Places result
       - Try LLM extract; except → quarantine_poison; continue
       - Build same payload dict as produce() (lines 289-314) with parent_mar_id=str(parent_mar.id), municipio_id=municipio_ibge
       - store_raw(source="places_discovery", source_ref=f"places:{uf}:{place_id}", entity_type="attraction", uf=uf, payload=payload)
       - write_audit (same action="atrativo_discovered" pattern)
       - process_nascente_record + advance_sub_state (same as produce())
       - log info("atrativo_ingested_targeted", ...)
       - created += 1
       - return created

    Preserve: all existing methods, imports, DISCOVERY_PROMPT, _compute_completude unchanged.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import inspect
from brave.lanes.atrativos.discovery_agent import DiscoveryAgent, _resolve_parent_destino
sig = inspect.signature(DiscoveryAgent.produce_for_destino)
assert 'parent_mar' in sig.parameters
assert 'target_count' in sig.parameters
import ast, sys
with open('brave/lanes/atrativos/discovery_agent.py') as f:
    src = f.read()
assert 'not municipio_ibge' in src, 'Missing empty-ibge guard'
assert 'produce_for_destino' in src
print('discovery_agent.py changes OK')
"</automated>
  </verify>

  <done>_resolve_parent_destino returns None immediately on empty municipio_ibge; DiscoveryAgent.produce_for_destino method exists with correct signature; both methods importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend test_discovery_agent.py with D-02/D-03 offline tests</name>
  <files>tests/unit/lanes/test_discovery_agent.py</files>

  <read_first>
    - tests/unit/lanes/test_discovery_agent.py (full file — read to find: existing fixture helpers _make_places_result, existing test structure, how FakePlacesClient + FakeLLMClient are wired, how store_raw + quarantine_poison are patched)
    - tests/fakes/fake_places.py (FakePlacesClient fixture_results key format)
    - brave/lanes/atrativos/discovery_agent.py (the file modified in Task 1)
    - .planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-PATTERNS.md §tests/unit/lanes/test_discovery_agent.py (new test stubs + FakePlacesClient fixture extension)
  </read_first>

  <behavior>
    - test_empty_ibge_guard_quarantines_without_db_query: places result with municipio_ibge=""; mock session.scalar asserted NOT called; quarantine_poison mock called with error containing "parent_destino_absent"
    - test_produce_for_destino_links_to_known_parent: FakePlacesClient with fixture_results={"pontos turísticos em Porto Seguro BA": [_make_places_result(municipio_ibge="2927408", municipio_nome="Porto Seguro")]}; mock_parent_mar with canonical={"municipio":"Porto Seguro","uf":"BA","ibge_code":"2927408"}; after produce_for_destino(mock_parent_mar); store_raw mock called; call kwargs payload["parent_mar_id"] == str(mock_parent_mar.id); session.scalar NOT called (no _resolve_parent_destino)
    - test_produce_for_destino_returns_zero_on_missing_municipio: parent_mar canonical={"uf":"BA"} (no "municipio" or "name"); produce_for_destino returns 0; store_raw NOT called
  </behavior>

  <action>
    Read tests/unit/lanes/test_discovery_agent.py fully. Add three new tests at the END of the file, after the last existing test. Do not modify existing tests.

    Test 1 — test_empty_ibge_guard_quarantines_without_db_query:
    - Patch store_raw, quarantine_poison, process_nascente_record at the same import paths used by existing tests
    - Construct DiscoveryAgent with FakePlacesClient(fixture_results={"atrativos em BA": [_make_places_result(municipio_ibge="", municipio_nome="")]}) and FakeLLMClient
    - Mock session.scalar to ensure it's not called
    - await agent.produce(uf="BA")
    - Assert session.scalar was never called (the guard prevents the DB query)
    - Assert quarantine_poison was called with error="parent_destino_absent"

    Test 2 — test_produce_for_destino_links_to_known_parent:
    - Patch store_raw, quarantine_poison, process_nascente_record, write_audit, advance_sub_state at discovery_agent module paths
    - Construct FakePlacesClient(fixture_results={"pontos turísticos em Porto Seguro BA": [_make_places_result(place_id="ChIJtest001", municipio_ibge="2927408", municipio_nome="Porto Seguro")]})
    - mock_parent_mar = MagicMock(); mock_parent_mar.id = uuid.uuid4(); mock_parent_mar.canonical = {"municipio": "Porto Seguro", "uf": "BA", "ibge_code": "2927408"}
    - Construct DiscoveryAgent(fake_places, fake_llm, mock_session, ScoreConfig())
    - await agent.produce_for_destino(mock_parent_mar, target_count=1)
    - Assert store_raw was called exactly once
    - Assert store_raw call's payload kwarg contains parent_mar_id == str(mock_parent_mar.id)
    - Assert session.scalar was never called (no _resolve_parent_destino invoked)

    Test 3 — test_produce_for_destino_returns_zero_on_missing_municipio:
    - Patch store_raw
    - mock_parent_mar = MagicMock(); mock_parent_mar.canonical = {"uf": "BA"}  # no municipio/name
    - Construct DiscoveryAgent
    - result = await agent.produce_for_destino(mock_parent_mar)
    - assert result == 0
    - assert store_raw.call_count == 0

    Use the same @pytest.mark.asyncio decorator, same import style, same patching targets as the existing tests in the file. Do not change any existing test fixtures or test functions.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/lanes/test_discovery_agent.py -v --tb=short 2>&1 | tail -25</automated>
  </verify>

  <done>All existing tests in test_discovery_agent.py still pass; 3 new tests (empty-ibge guard, targeted link, zero-return) pass; total test count in file increased by 3; no real network calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Places API result → parent Mar record | municipio_ibge from Places response used to look up parent — guard prevents mislink on empty value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | source_ref.contains("") linking atrativo to wrong parent | mitigate | D-02 empty-ibge guard: `if not municipio_ibge: return None` — unit-tested in test_empty_ibge_guard_quarantines_without_db_query |
| T-07-08 | Spoofing | produce_for_destino injects parent_mar_id without re-verifying from DB | accept | parent_mar passed from harness/task which queried MarRecord directly; the targeted path trusts the caller; misuse would require attacker control of the caller's MarRecord query |
| T-07-09 | Information Disclosure | DISCOVERY_PROMPT logs municipio_nome + place_id in structlog | accept | no PII in municipality names or place_ids; COMP-03 only requires not persisting non-place_id Google fields in the DB (already enforced in store_raw) |
| T-07-SC | Tampering | No new package installs in this plan | accept | Only existing imports; no new dependencies |
</threat_model>

<verification>
1. Guard present: `grep -n "not municipio_ibge" brave/lanes/atrativos/discovery_agent.py` returns a line inside _resolve_parent_destino
2. Method present: `grep -n "def produce_for_destino" brave/lanes/atrativos/discovery_agent.py` returns one line
3. Tests pass: `.venv/bin/python -m pytest tests/unit/lanes/test_discovery_agent.py -v` all pass
4. No regressions: `.venv/bin/python -m pytest tests/unit/ -x -v` exits 0
</verification>

<success_criteria>
- _resolve_parent_destino returns None immediately on empty municipio_ibge; SQL is not executed
- DiscoveryAgent.produce_for_destino(parent_mar, target_count) exists and returns int
- 3 new offline tests pass covering D-02 guard and D-03 targeted path
- No existing tests broken
</success_criteria>

<output>
Create `.planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-03-SUMMARY.md` when done.
</output>
