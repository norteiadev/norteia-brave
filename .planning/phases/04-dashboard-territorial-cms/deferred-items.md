Pre-existing test-isolation flake (NOT introduced by 04-03): tests/integration/test_atrativos_gate.py quality-rating-webhook + inbound tests fail when run AFTER test_dashboard_endpoints.py in the same session (module-scoped client fixture + shared get_redis() singleton leaking fakeredis state across modules). Each passes in isolation. Out of scope for 04-03 (dashboard DLQ-detail read endpoint). Owner: a future gate/test-harness fix.
