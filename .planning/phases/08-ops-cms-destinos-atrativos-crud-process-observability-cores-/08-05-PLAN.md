---
phase: 08-ops-cms-destinos-atrativos-crud-process-observability-cores
plan: 05
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-03
  - 08-04
files_modified:
  - dashboard/lib/atrativos-api.ts
  - dashboard/components/cms/AtrativoList.tsx
  - dashboard/app/atrativos/page.tsx
  - dashboard/app/atrativos/[id]/page.tsx
  - dashboard/mocks/handlers/atrativos.ts
  - dashboard/components/cms/__tests__/AtrativoList.test.tsx
autonomous: true
requirements: [D-04]

must_haves:
  truths:
    - "D-04: /atrativos page renders AtrativoList with TanStack Table v8 columns (name, uf, sub_state/StageBadge, score/StageBadge, validation_pending chip); filter bar: uf + sub_state + parent_mar_id"
    - "D-04: /atrativos/[id] renders DetailPanel (reused from 08-04) with entityType='attraction', JourneyStepper for atrativo 7-step journey, and action buttons (Advance/Descartar)"
    - "D-04: AtrativoList MSW handlers use double-prefix BASE = 'http://localhost:3000/api/api/v1/atrativos'; sampleAtrativos include phone_masked (never phone_e164)"
    - "D-04: atrativos-api.ts exports atrativoKeys, fetchAtrativoList, fetchAtrativoDetail, advanceAtrativo, descartarAtrativo typed functions"
    - "Vitest tests: AtrativoList renders sub_state badge (discovered=navy chip); phone_e164 never rendered in any component; advance action calls PATCH /{id}/advance with body {expected_state, next_state}"
  artifacts:
    - path: "dashboard/lib/atrativos-api.ts"
      provides: "Typed API client for atrativos endpoints including advance mutation"
      exports: ["atrativoKeys", "fetchAtrativoList", "fetchAtrativoDetail", "advanceAtrativo", "descartarAtrativo"]
    - path: "dashboard/components/cms/AtrativoList.tsx"
      provides: "TanStack Table v8 atrativos list with sub_state and StageBadge"
      exports: ["AtrativoList"]
    - path: "dashboard/app/atrativos/page.tsx"
      provides: "Master-detail /atrativos page with advance/descartar actions"
    - path: "dashboard/app/atrativos/[id]/page.tsx"
      provides: "Full-detail /atrativos/[id] page with journey stepper"
    - path: "dashboard/mocks/handlers/atrativos.ts"
      provides: "MSW handler factories; sample data has phone_masked not phone_e164"
      exports: ["atrativosListSuccess", "atrativoDetailSuccess", "atrativoAdvanceSuccess"]
  key_links:
    - from: "dashboard/app/atrativos/page.tsx"
      to: "dashboard/components/cms/DetailPanel.tsx"
      via: "DetailPanel reuse from 08-04 with entityType='attraction' + advance actions"
      pattern: "DetailPanel"
    - from: "dashboard/mocks/handlers/atrativos.ts"
      to: "dashboard/mocks/server.ts"
      via: "atrativoHandlers spread into server setup"
      pattern: "atrativoHandlers"
---

<objective>
Build the atrativos CMS frontend: API client, list component, pages (/atrativos, /atrativos/[id]), MSW handlers, and Vitest tests. Reuses DetailPanel (from 08-04) and StageBadge/JourneyStepper (from 08-03) without modification.

Purpose: Operators can list all atrativos across all FSM stages (with sub_state badge showing pipeline position), drill into a record to see the 7-step journey, and advance the sub_state or descartar — all with PII masked (phone_masked, not phone_e164) throughout.

Output: atrativos-api.ts, AtrativoList.tsx, atrativos pages, MSW handlers (phone_masked only), Vitest tests.
</objective>

<execution_context>
@/Users/leandro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leandro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/leandro/Projects/norteia/norteia-brave/.planning/PROJECT.md
@/Users/leandro/Projects/norteia/norteia-brave/.planning/phases/08-ops-cms-destinos-atrativos-crud-process-observability-cores-/08-RESEARCH.md
@/Users/leandro/Projects/norteia/norteia-brave/.planning/phases/08-ops-cms-destinos-atrativos-crud-process-observability-cores-/08-PATTERNS.md

<interfaces>
<!-- Types and contracts reused or extended in this plan. -->

From dashboard/lib/destinos-api.ts (pattern to mirror — built in 08-04):
```typescript
// atrativoKeys mirrors destinoKeys pattern
// fetchAtrativoList(params) mirrors fetchDestinoList
// Mutation: advanceAtrativo(id, body: {expected_state: string; next_state: string}) → PATCH /api/v1/atrativos/{id}/advance with JSON body
```

AtrativoListItem interface (mirrors DestinoListItem):
```typescript
interface AtrativoListItem {
  id: string;
  entity_type: "attraction";
  uf: string | null;
  routing: string;
  sub_state: string | null;
  score: number | null;
  name: string | null;
  validation_pending: boolean;       // sub_state === 'aguardando_consulta_whatsapp'
  mar_id: string | null;
  parent_mar_id: string | null;
  contacts_summary: {
    phone_masked: string | null;     // NEVER phone_e164 — already masked by backend
    website: string | null;
  } | null;
}
```

AtrativoDetail (from GET /api/v1/atrativos/{id}):
```typescript
interface AtrativoDetail extends AtrativoListItem {
  score_breakdown: Record<string, unknown>;
  normalized: Record<string, unknown>;  // _safe_normalized applied by backend — no phone_e164
  audit_log: AuditLogRow[];
  parent_destino: { mar_id: string; name: string } | null;
}
```

sub_state FSM values (from RESEARCH Q4, state_machine.py):
"discovered" | "contacts_found" | "signals_gathered" | "aguardando_consulta_whatsapp" | "whatsapp_in_progress"

advance body: { expected_state: string; next_state: string }

From dashboard/components/cms/DetailPanel.tsx (built in 08-04):
```typescript
// Props: entityType: "destination" | "attraction" → JourneyStepper gets this
// actions prop receives the detail object, renders action buttons
// Reuse exactly — no changes needed for atrativos
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: atrativos-api.ts + MSW handlers (atrativos.ts) + AtrativoList tests</name>
  <files>dashboard/lib/atrativos-api.ts, dashboard/mocks/handlers/atrativos.ts, dashboard/components/cms/__tests__/AtrativoList.test.tsx</files>
  <read_first>
    - dashboard/lib/destinos-api.ts (built in 08-04) — mirror pattern exactly; swap destino→atrativo, add advance mutation with JSON body, remove promote/reprocess
    - dashboard/mocks/handlers/destinos.ts (built in 08-04) — mirror handler factory pattern; BASE uses double-prefix
    - 08-PATTERNS.md — atrativos-api.ts section + atrativos.ts handler section; note phone_masked in sample data
    - 08-RESEARCH.md — Pitfall 5 (MSW double-prefix), PII masking contract
  </read_first>
  <action>
    Step 1 — dashboard/lib/atrativos-api.ts:
    Mirror destinos-api.ts. Define AtrativoListItem interface (per interfaces section above — phone_masked, NOT phone_e164). Define AtrativoDetail extending AtrativoListItem. Define AuditLogRow (same as destinos-api.ts — can import if exported there, or re-declare). atrativoKeys: { all, list(filters), detail(id) }. qs helper (copy or import). Fetchers:
    - fetchAtrativoList(params: {uf?: string; sub_state?: string; parent_mar_id?: string; routing?: string; offset?: number; limit?: number}): apiFetch("api/v1/atrativos" + qs)
    - fetchAtrativoDetail(id: string): apiFetch("api/v1/atrativos/${id}")
    - advanceAtrativo(id: string, body: {expected_state: string; next_state: string}): apiFetch("api/v1/atrativos/${id}/advance", {method: "PATCH", headers: {"Content-Type": "application/json"}, body: JSON.stringify(body)})
    - descartarAtrativo(id: string): apiFetch("api/v1/atrativos/${id}/descarte", {method: "PATCH"})

    Step 2 — dashboard/mocks/handlers/atrativos.ts:
    BASE = "http://localhost:3000/api/api/v1/atrativos" (double-prefix — Pitfall 5).
    sampleAtrativos: 2 items — one sub_state="discovered" routing="in_progress", one sub_state="aguardando_consulta_whatsapp" validation_pending=true. CRITICAL: contacts_summary has phone_masked (e.g., "**1234") NOT phone_e164. Never include phone_e164 in sample data.
    sampleAtrativoDetail: full detail object with audit_log (3 rows: atrativo_discovered, sub_state_advanced contacts_found, sub_state_advanced signals_gathered), parent_destino: { mar_id: "...", name: "Salvador" }, normalized: { name: "Mercado Modelo", phone_masked: "**1234" } — never phone_e164.
    Handler factories: atrativosListSuccess (with sub_state + uf filter logic), atrativosListEmpty, atrativosListError, atrativoDetailSuccess, atrativoAdvanceSuccess (200 {status: "ok", sub_state: "contacts_found"}), atrativoDescarteSuccess (200 {status: "ok", routing: "dlq"}). Export atrativoHandlers default array.

    Step 3 — (server.ts not modified in this plan):
    The handlers barrel (dashboard/mocks/handlers/index.ts) intentionally stays empty. Per the documented contract, each test suite applies its own handlers via server.use(<handler>()) — no global registration. In AtrativoList.test.tsx, use server.use(atrativosListSuccess()) inside beforeEach.
  </action>
  <verify>
    <automated>cd /Users/leandro/Projects/norteia/norteia-brave/dashboard && grep -n "phone_e164" mocks/handlers/atrativos.ts; echo "exit $?"</automated>
  </verify>
  <done>
    atrativos-api.ts exports all typed functions with no phone_e164 reference. atrativos.ts MSW handlers use double-prefix BASE; sample data has phone_masked only (grep phone_e164 in atrativos.ts returns 0 lines). Test file created with server.use() per-suite pattern.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AtrativoList component + /atrativos pages + Vitest tests</name>
  <files>
    dashboard/components/cms/AtrativoList.tsx,
    dashboard/app/atrativos/page.tsx,
    dashboard/app/atrativos/[id]/page.tsx
  </files>
  <read_first>
    - dashboard/components/cms/DestinoList.tsx (built in 08-04) — copy structure; swap destino types/keys for atrativo; add sub_state filter to filter bar
    - dashboard/app/destinos/page.tsx (built in 08-04) — copy master-detail layout; replace DestinoList with AtrativoList, DestinoActions with AtrativoActions (advance body + descartar)
    - dashboard/components/cms/DetailPanel.tsx (built in 08-04) — import and use with entityType="attraction"
    - 08-PATTERNS.md — AtrativoList.tsx section (sub_state column uses StageBadge subState prop), advance action
    - 08-RESEARCH.md — Q5 (atrativos PATCH actions — advance body shape, 409 conflict response)
  </read_first>
  <action>
    Component — dashboard/components/cms/AtrativoList.tsx ("use client"):
    Mirror DestinoList.tsx. Props: AtrativoListProps { selectedId: string | null; onSelect: (id: string) => void }. State: uf, sub_state (string|undefined), parent_mar_id (string|undefined). useQuery: atrativoKeys.list({uf, sub_state, parent_mar_id}).

    Columns: name, uf, sub_state badge (<StageBadge subState={row.original.sub_state} />), score (<StageBadge score={row.original.score} />), routing (<StageBadge routing={row.original.routing} />), validationPending (show <StageBadge validationPending /> if row.original.validation_pending). Do NOT display any contacts_summary.phone_masked in the list (leave contacts to detail view).

    Filter bar: UF select/buttons + sub_state select (Todos/Descoberto/Contatos/Sinais/Aguardando WA/Em outreach) + optional parent_mar_id text input. Both reset to undefined when "Todos" selected.

    Loading/error/empty states: same pattern as DestinoList.tsx ("Sem atrativos para este filtro").

    Page 1 — dashboard/app/atrativos/page.tsx ("use client"):
    Master-detail layout mirroring destinos/page.tsx. Left: AtrativoList. Right: DetailPanel(fetchDetail=fetchAtrativoDetail, queryKeys=atrativoKeys, entityType="attraction", actions={(detail) => <AtrativoActions detail={detail} />}).

    AtrativoActions component (inline or co-located in page):
    - advanceMutation: useMutation calling advanceAtrativo(rioId, {expected_state: detail.sub_state, next_state: nextSubState}). nextSubState is derived from the FSM progression: discovered→contacts_found→signals_gathered→aguardando_consulta_whatsapp. Show advance button only if sub_state is a non-terminal FSM state (not aguardando_consulta_whatsapp, not whatsapp_in_progress, not null).
    - descartarMutation: useMutation calling descartarAtrativo(rioId). Behind AlertDialog (same pattern as DestinoActions).
    - onSuccess: qc.invalidateQueries({queryKey: atrativoKeys.all}). Toast on success/error.
    - 409 conflict from advance: toast.error("Estado já avançado — recarregue a página").

    Page 2 — dashboard/app/atrativos/[id]/page.tsx ("use client"):
    Full-page detail. params: { id: string }. useQuery(atrativoKeys.detail(id), fetchAtrativoDetail). Renders DetailPanel full-width with entityType="attraction". Show parent_destino link: "Destino pai: {name}" → link to /destinos/{parent_mar_id}. Back link to /atrativos.

    Vitest test file — dashboard/components/cms/__tests__/AtrativoList.test.tsx:
    beforeEach: server.use(atrativosListSuccess()). Test 1: renders sub_state badge for "discovered" item (look for StageBadge output — navy-tinted chip). Test 2: empty state "Sem atrativos". Test 3: phone_e164 never appears in the rendered DOM (query by text "phone_e164" returns null). afterEach: server.resetHandlers().
  </action>
  <verify>
    <automated>cd /Users/leandro/Projects/norteia/norteia-brave/dashboard && bun run test --reporter=verbose 2>&1 | grep -E "PASS|FAIL|atrativos|destinos" | head -20</automated>
  </verify>
  <done>
    AtrativoList renders with sub_state + score StageBadge columns; filter bar includes sub_state selector. /atrativos master-detail page with AtrativoActions (advance guided by FSM progression, descartar with AlertDialog). /atrativos/[id] full-detail with parent_destino link. Vitest: 3 tests pass (sub_state badge renders, empty state, no phone_e164 in DOM). All existing tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AtrativoList component → API | contacts_summary rendered from API response; must not expose phone_e164 |
| AtrativoActions advance mutation → BFF → FastAPI | advance body {expected_state, next_state} passed through; backend validates expected_state against actual sub_state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Information Disclosure | AtrativoList + AtrativoDetail rendering normalized/contacts | mitigate | Sample MSW data uses phone_masked only; never phone_e164 in any frontend type definition or sample; backend already applies _safe_normalized before responding |
| T-08-15 | Tampering | AtrativoActions advance with arbitrary next_state | mitigate | Backend advance_sub_state checks expected_state match (409 on mismatch); next_state constrained by FSM progression in frontend action logic; backend re-validates via state_machine |
| T-08-SC | Tampering | npm/pip installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `grep -n "phone_e164" dashboard/lib/atrativos-api.ts dashboard/mocks/handlers/atrativos.ts dashboard/components/cms/AtrativoList.tsx dashboard/app/atrativos/page.tsx` — returns 0 matches across all files
- `grep -n "http://localhost:3000/api/api/v1/atrativos" dashboard/mocks/handlers/atrativos.ts` — returns BASE line (double-prefix)
- `cd dashboard && bun run test` exits 0; AtrativoList tests passing
- `cd dashboard && bun run build` exits 0
</verification>

<success_criteria>
atrativos-api.ts exports typed functions with phone_masked interface (never phone_e164). AtrativoList.tsx TanStack Table with sub_state StageBadge + score StageBadge + uf + routing columns; sub_state filter in filter bar. /atrativos master-detail with advance (FSM-guided next_state) + descartar (AlertDialog) actions; 409 conflict handled with toast. /atrativos/[id] full-detail with parent_destino link. MSW handlers double-prefix URL; sample data phone_masked only. Vitest: 3 tests pass.
</success_criteria>

<output>
Create `.planning/phases/08-ops-cms-destinos-atrativos-crud-process-observability-cores-/08-05-SUMMARY.md` when done.
</output>
