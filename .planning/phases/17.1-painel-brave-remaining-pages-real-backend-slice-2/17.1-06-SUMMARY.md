---
phase: 17.1-painel-brave-remaining-pages-real-backend-slice-2
plan: 06
subsystem: dashboard
tags: [painel, kanban, transition, security-boundary, msw, whatsapp, falha, UI-PAINEL-2]
requires:
  - "brave/api/routers/cms.py _ALLOWED_EDGES + atrativos.py _ATRATIVO_ALLOWED_EDGES (plan 17.1-03 — the server allow-list this client mirrors)"
  - "brave/api/routers/workers.py GET /api/v1/failures (PoisonQuarantine — falha cards)"
  - "brave/api/routers/tripadvisor_session.py POST /tripadvisor/session (injectTASession target)"
provides:
  - "dashboard/lib/engine-api.ts — transition() + injectTASession() clients (+ fetchFailures/FailureItem re-export)"
  - "dashboard/lib/painel-data.ts — 6-column model (nascente/rio/whatsapp/mar/dlq/falha) + whatsapp/falha sourcing"
  - "dashboard/lib/painel-actions.ts — mapDrop full-pipeline transition allow-list (client security boundary)"
affects:
  - "plan 17.1-07 (Origem modal) consumes injectTASession(); the view-switcher already renders PainelView/PainelBoard"
tech-stack:
  added: []
  patterns:
    - "client mapDrop allow-list == exact twin of the server _ALLOWED_EDGES (paired contract, keyed by (expected,to))"
    - "ONE generic audited transition endpoint per entity — all board stage drags route through it"
    - "re-export the canonical fetchFailures/FailureItem (lib/workers-api.ts) rather than duplicating a drift-prone type"
key-files:
  created: []
  modified:
    - dashboard/lib/engine-api.ts
    - dashboard/lib/painel-data.ts
    - dashboard/lib/painel-actions.ts
    - dashboard/components/painel/PainelBoard.tsx
    - dashboard/components/painel/RecordCard.tsx
    - dashboard/mocks/handlers/destinos.ts
    - dashboard/mocks/handlers/atrativos.ts
    - dashboard/lib/__tests__/painel-data.test.ts
    - dashboard/lib/__tests__/painel-actions.test.ts
    - dashboard/components/painel/__tests__/PainelBoard.test.tsx
    - dashboard/components/painel/__tests__/PainelView.test.tsx
    - dashboard/components/painel/__tests__/PainelDrawer.test.tsx
    - dashboard/components/painel/__tests__/RecordCard.test.tsx
decisions:
  - "PainelColumnKey keeps `descarte` as a NON-rendered key (COLUMN_DEFS has exactly 6 rendered columns) so routingToColumn('descarte') stays unchanged and the drawer 'Descartar' path keeps a valid target — 'total 6' read as the 6 rendered stage columns"
  - "All board stage drags (incl. promote/descarte) route through the ONE generic transition endpoint — the truest edge-for-edge mirror of the server; mapDrop emits only kind:transition|null, mapRetry keeps destino reprocess"
  - "fetchFailures + FailureItem re-exported from lib/workers-api.ts (already existed) instead of re-declaring in engine-api.ts — single source of truth for the /failures shape"
metrics:
  duration: ~45min
  tasks: 3
  files: 13
  completed: 2026-06-28
---

# Phase 17.1 Plan 06: Board 6-Column Model + Client Transition Allow-List Summary

The board moves from 5 to 6 stage columns (nascente, rio, whatsapp, mar, dlq, falha) with real WhatsApp (atrativo sub_state) + falha (PoisonQuarantine) sourcing, and the client `mapDrop` is extended into the full-pipeline security boundary — emitting a single generic, audited `transition` call for exactly the (expected→to) edges in the server allow-list and reverting+toasting for everything else (mar→*, into-nascente, same-column, falha→*).

## What Was Built

**Task 1 — engine-api clients** (`feat`, commit `29ac93e`)
- `transition(entity, rioId, { to, expected })` → `PATCH api/v1/{destinos|atrativos}/{id}/transition` (mirrors the backend `TransitionBody`, `extra="forbid"`).
- `injectTASession(body)` → `POST api/v1/tripadvisor/session` (strict `SessionInjectBody` shape; `ApiError.status` surfaced so the Origem modal in 17.1-07 can split 422 invalid_session vs 503 canary_unverified).
- Re-export of the canonical `fetchFailures` + `FailureItem`/`FailuresData` from `lib/workers-api.ts` + `engineKeys.failures` query key.

**Task 2 — painel-data 6-column model** (TDD: RED `6068afb` → GREEN `f6c91f8`)
- `PainelColumnKey` gains `rio`/`whatsapp`/`falha`; `COLUMN_DEFS` = 6 ordered pt-BR labels (Nascente, Rio · validação, WhatsApp · contato, Mar · publicado, DLQ · revisão, Falha).
- `routingToColumn` maps `in_progress → rio` (server twin `_ROUTING_TO_COLUMN`); `mar`/`dlq`/`descarte` unchanged; unknown → `nascente`.
- `toPainelCards(destinos, atrativos, failures = [])`: an atrativo in sub_state `aguardando_consulta_whatsapp` buckets into `whatsapp`; `FailureItem[]` projects into real, draggable `falha` cards (error = quarantine reason, no payload/phone — LGPD allow-list intact). `usePainelBoard` now also queries `fetchFailures`. Metrics still read the envelope `total`.
- `PainelBoard` `COLUMN_DOT` updated to the 6 keys.

**Task 3 — painel-actions transition allow-list** (TDD: RED `9746b47` → GREEN `c9343cb`)
- `DropAction` gains `{ kind:"transition"; entity; id; to; expected }`. `mapDrop` emits a transition for exactly the (expected→to) pairs in the server `_ALLOWED_EDGES` (destino) / `_ATRATIVO_ALLOWED_EDGES` (atrativo), restricted to board-reachable targets; every other pair — mar→*, into-nascente, same-column, falha→* — returns `null` (revert+toast, never an unmapped call). `runAction` gains a transition branch calling `engine-api transition()`; `mapRetry` keeps destino reprocess. An exhaustive unit test proves no unmapped (source,target) board pair is ever callable.

## Verification

- `cd dashboard && bun run test painel-data painel-actions PainelBoard PainelView PainelDrawer` → 51/51 green.
- Full dashboard suite `bun run test` → 265/265 green (40 files).
- `bunx tsc --noEmit` → no NEW errors (2 pre-existing, unrelated — see Deferred Issues).
- `bun run lint` → 0 errors (4 pre-existing TanStack-Table warnings, unrelated files).
- Acceptance greps: engine-api `injectTASession|fetchFailures|export function transition` = 3; painel-data `"rio"|"whatsapp"|"falha"` ≥3 + COLUMN_DEFS exactly 6 + `aguardando_consulta_whatsapp` ≥1; painel-actions `"transition"` ≥2.

## Deviations from Plan

The plan's `files_modified` named 6 files, but the locked 6-column model + the `mar→*`-null security rule necessarily broke downstream component tests/components keyed to the old 5-column shape. All changes below are direct, blocking consequences (Rule 1/3), not scope creep.

**1. [Rule 3 — Reuse over duplicate] fetchFailures/FailureItem re-exported, not re-declared**
- **Found during:** Task 1. `lib/workers-api.ts` already owns `fetchFailures` + a `FailureItem` matching the real `/failures` response (`id, task_name, error_message, quarantined_at`). The plan's `read_first` assumed a richer `entity_type/uf/name` shape that the endpoint does NOT return (payload is deliberately omitted — workers.py T-08-08).
- **Fix:** Re-exported the canonical client/type from engine-api (satisfies the acceptance grep) and projected falha cards from the real fields (type inferred from `task_name`; name = task_name; error = error_message). No second drift-prone type.

**2. [Rule 1 — Bug] RecordCard falha affordance keyed to the wrong column**
- **Found during:** Task 3. `RecordCard.tsx` gated the ⚠ falha line + ↺ Reprocessar on `card.column === "descarte"`, but `descarte` is no longer a rendered column — the error column is now `falha`, so the retry affordance would never appear.
- **Fix:** `isFalha = card.column === "falha"`. (`dashboard/components/painel/RecordCard.tsx`, commit `c9343cb`.)

**3. [Rule 3 — Blocking] Downstream tests + MSW handlers updated for the new model**
- **Found during:** Tasks 2–3. `PainelBoard.test.tsx` (5-col keys), `PainelView.test.tsx` (dropped onto a `descarte` column + asserted a mar→descarte descarte PATCH — now impossible under mar→*-null), `PainelDrawer.test.tsx` (mar card + `/descarte` endpoint), and `RecordCard.test.tsx` (descarte falha card) all asserted the old shape.
- **Fix:** Updated each to the 6-column + generic-transition reality (drag dlq→mar / mar→dlq, assert `/transition`, falha cards from `/failures`); added `destinoTransitionSuccess()`/`atrativoTransitionSuccess()` MSW handlers and `failuresEmpty()` wiring (the board now queries `/failures` and `onUnhandledRequest:"error"` requires it mocked). Commits `f6c91f8` (board) + `c9343cb` (actions/view/drawer/record).

## Deferred Issues

- **2 pre-existing tsc errors, unrelated to this plan** (verified present on the baseline with changes stashed; out of scope per SCOPE BOUNDARY):
  - `components/engine/__tests__/EngineControl.test.tsx(18,3)` — `EngineStatus.depth` `undefined` vs `EngineDepth | null`.
  - `mocks/handlers/mar-ready.ts(64,60)` — `params.id` possibly `undefined`.

## Notes / Out of Scope

- The atrativo `(whatsapp→whatsapp)` gate_approve server edge is intentionally NOT reachable via a board drag (same-column → null by design); the WhatsApp gate is driven by its dedicated gate UI. Documented in the `painel-actions.ts` header.
- The drawer "Etapa do pipeline" field stays a read-only label (not a select) — no parallel mutation path was added (gotcha 5). The drawer's Descartar/Promover buttons already route through `mapDrop`, so they now emit generic transitions for rio/dlq cards and revert+toast for mar cards (correct new behavior).
- `injectTASession` is wired but not yet called — the Origem modal that consumes it is plan 17.1-07.

## Self-Check: PASSED
- FOUND: dashboard/lib/engine-api.ts (transition/injectTASession/fetchFailures re-export)
- FOUND: dashboard/lib/painel-data.ts (6-column model)
- FOUND: dashboard/lib/painel-actions.ts (transition allow-list)
- FOUND: commit 29ac93e (Task 1)
- FOUND: commit 6068afb + f6c91f8 (Task 2 RED/GREEN)
- FOUND: commit 9746b47 + c9343cb (Task 3 RED/GREEN)
