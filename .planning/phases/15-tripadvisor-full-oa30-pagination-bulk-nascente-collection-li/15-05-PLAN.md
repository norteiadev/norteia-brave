---
phase: 15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li
plan: 05
type: execute
wave: 2
depends_on: ["15-02"]
files_modified:
  - brave/clients/nominatim.py
  - brave/lanes/tripadvisor/ibge.py
  - tests/unit/lanes/tripadvisor/test_geo_national.py
autonomous: true
requirements: [TA-12]
must_haves:
  truths:
    - "A national geocode (name + Brazil, no UF input) returns lat/lon/osm_id/municipio_name or None"
    - "A national município resolver maps geocoded coordinates to the nearest IBGE municipality across all states"
    - "The resolved municipality carries its uf, so the bulk lane can derive UF without a per-UF input"
    - "Only LGPD-safe geo fields are returned/persisted (no display_name/street/address PII)"
  artifacts:
    - path: "brave/clients/nominatim.py"
      provides: "NominatimGeocoderClient.geocode_national (national query, 4-key LGPD-safe result)"
      contains: "geocode_national"
    - path: "brave/lanes/tripadvisor/ibge.py"
      provides: "resolve_municipio_national(lat, lng, records, max_distance_km) -> IbgeMunicipio | None"
      contains: "resolve_municipio_national"
  key_links:
    - from: "brave/lanes/tripadvisor/ibge.py::resolve_municipio_national"
      to: "brave/lanes/tripadvisor/ibge.py::haversine_km"
      via: "nearest-seat distance over all IBGE records"
      pattern: "haversine_km"
---

<objective>
Resolve the BLOCKING national-UF gap (RESEARCH Open Question 1 / A1, operator-locked in CONTEXT):
the all-Brazil g294280 listing has no per-UF context and AttractionsFusion cards carry no lat/lng or
state. Provide the two reuse-friendly primitives the bulk lane (15-06) needs to derive uf + município
from a geocoded card WITHOUT a per-UF input or a parent destino:

1. `NominatimGeocoderClient.geocode_national` — national forward-geocode (`"{name}, Brazil"`), same
   4-key LGPD-safe result shape as `geocode`, reusing the existing cache + rate-limit + retry pattern.
2. `resolve_municipio_national` — pure haversine over ALL 5570 IBGE records (not UF-filtered), returning
   the nearest `IbgeMunicipio` (which carries `.uf` and `.ibge_code`).

Purpose: Reuse the existing Nominatim + IBGE resolver to derive uf+município from coords (the locked
resolution), so bulk records reach Nascente fully município-resolved with no parent destino.
Output: geocode_national + resolve_municipio_national + offline tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-RESEARCH.md

<interfaces>
<!-- Copy-from analogs. -->
brave/clients/nominatim.py — geocode(self, location_id, name, uf) (:139-235): Redis cache key
  f"brave:geo:{location_id}", rate-limit asyncio.sleep, httpx GET self._config.base_url with
  params {q, format, limit, countrycodes:"br", addressdetails:1}, address precedence
  municipality→city→town→village→county, returns {"lat","lon","osm_id","municipio_name"} or None.
  LGPD: ONLY those 4 keys; logs only location_id + resolved municipio (never name/address payload).
brave/lanes/tripadvisor/ibge.py — IbgeMunicipio dataclass (ibge_code, nome, uf, lat, lng);
  haversine_km(lat1,lon1,lat2,lon2) (:85-104); resolve_municipio(name, uf, records, *, threshold,
  max_distance_km, candidate_lat, candidate_lng) (:112+). resolve_municipio_national mirrors the
  haversine step but over ALL records (no uf filter).
tests/fakes/fake_nominatim.py — FakeGeocoderClient.geocode_national (added in 15-02) for offline tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NominatimGeocoderClient.geocode_national</name>
  <read_first>
    - brave/clients/nominatim.py (geocode :139-235 — cache/rate-limit/retry/LGPD pattern to reuse)
    - brave/clients/base.py (GeocoderClientProtocol.geocode_national stub added in 15-02)
  </read_first>
  <behavior>
    - geocode_national(location_id, name): national query q=f"{name}, Brazil" (no UF), same httpx params
      (format=json, limit=1, countrycodes="br", addressdetails=1), returns {"lat","lon","osm_id","municipio_name"} or None.
    - Reuses the Redis cache (distinct key namespace so a national result never collides with a per-UF geocode), rate-limit, and tenacity retry.
    - Empty Nominatim result → negative-cache sentinel + return None.
    - Returns ONLY the 4 LGPD-safe keys; logs only location_id + resolved municipio (never name/address).
  </behavior>
  <action>
    Add `async def geocode_national(self, location_id: str, name: str) -> dict[str, Any] | None` mirroring
    `geocode` but with `q = f"{name}, Brazil"` and no UF. Use a distinct cache key (e.g.
    `f"{NOMINATIM_CACHE_KEY_PREFIX}natl:{location_id}"`) so it does not collide with the per-UF geocode cache.
    Keep the rate-limit (`min_request_interval`), the tenacity `@retry(_is_retryable)`, the address-precedence
    chain (municipality→city→town→village→county), the negative `{"__no_match": true}` sentinel, and the exact
    4-key LGPD-safe return. Reuse the existing structlog discipline (location_id + municipio only). Keep the
    `run_real_externals=False` guard inherited from `__init__`.
  </action>
  <acceptance_criteria>
    - `grep -c 'def geocode_national' brave/clients/nominatim.py` == 1.
    - test (respx + fakeredis): geocode_national("loc1","Instituto Inhotim") returns a 4-key dict (lat/lon/osm_id/municipio_name); second call hits the cache (no second HTTP request).
    - Empty Nominatim response → returns None and writes the __no_match sentinel.
    - Returned dict has EXACTLY the 4 keys (no display_name/street/address).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_geo_national.py -k geocode_national -x -q</automated>
  </verify>
  <done>National geocode returns the 4-key LGPD-safe result, cached + rate-limited; offline tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resolve_municipio_national (haversine over all IBGE records)</name>
  <read_first>
    - brave/lanes/tripadvisor/ibge.py (IbgeMunicipio dataclass; haversine_km :85-104; resolve_municipio haversine step)
  </read_first>
  <behavior>
    - resolve_municipio_national(candidate_lat, candidate_lng, records, *, max_distance_km=50.0): returns the
      IbgeMunicipio whose seat coordinates are nearest to the candidate, within max_distance_km; None beyond that.
    - The returned record carries .uf (the derived state) and .ibge_code (município) — no per-UF input needed.
    - None candidate coords → None.
  </behavior>
  <action>
    Add `resolve_municipio_national(candidate_lat: float | None, candidate_lng: float | None,
    records: list[IbgeMunicipio], *, max_distance_km: float = 50.0) -> IbgeMunicipio | None` to `ibge.py`.
    Compute `haversine_km` from the candidate to every record (no UF filter), pick the minimum, and return it
    only if within `max_distance_km` (≥50 km relaxed radius — IBGE coords are the município seat; natural
    attractions sit 15-25 km out, per Phase 14). Return None when coords are None or nothing is within radius.
    Pure math reusing the existing `haversine_km` — no external library.
  </action>
  <acceptance_criteria>
    - `grep -q 'def resolve_municipio_national' brave/lanes/tripadvisor/ibge.py`.
    - test: a coordinate near a known município seat resolves to that IbgeMunicipio with the correct .uf and .ibge_code.
    - test: a coordinate in the ocean / >50 km from any seat returns None; None coords return None.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_geo_national.py -k resolve_national -x -q</automated>
  </verify>
  <done>National resolver maps coords to the nearest IBGE município (with uf) within a relaxed radius; offline tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collector → Nominatim (OSM) | Outbound geocode; LGPD requires only coordinate/place-ref fields persist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Information Disclosure | geocode_national result + logs | mitigate | Return only the 4 LGPD-safe keys (lat/lon/osm_id/municipio_name); log only location_id + municipio — never name/display_name/street/address (reuse decision #8 discipline). |
| T-15-05-02 | Denial of Service | Nominatim public-instance policy | mitigate | Reuse the existing ≥1 req/s rate-limit + Redis cache + negative sentinel; national cache key namespaced to avoid collisions. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
Run the `<verify>` commands with `RUN_REAL_EXTERNALS` UNSET (respx + fakeredis cover the network/cache). Both green.
</verification>

<success_criteria>
The bulk lane can geocode a card nationally and derive uf+ibge_code from coordinates, reusing the existing
Nominatim client + IBGE resolver — resolving the A1 blocker with no parent-destino dependency.
</success_criteria>

<output>
Create `.planning/phases/15-tripadvisor-full-oa30-pagination-bulk-nascente-collection-li/15-05-SUMMARY.md` when done
</output>
