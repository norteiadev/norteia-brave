---
phase: 03-atrativos-lane-whatsapp-compliance
plan: 05
type: execute
wave: 5
depends_on:
  - "03-02"
  - "03-03"
  - "03-04"
files_modified:
  - tests/integration/test_atrativos_lane_e2e.py
autonomous: true
requirements:
  - ATR-01
  - ATR-02
  - ATR-03
  - ATR-04
  - ATR-05
  - ATR-06
  - COMP-01
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "End-to-end offline integration test: Places sweep → DiscoveryAgent → Nascente → score → DLQ (aguardando_consulta_whatsapp) → gate approve → outreach_task (NullWhatsAppClient) → LangGraph reply → re-score → Mar → push_attraction"
    - "Parent-destino absent: DiscoveryAgent skips ingestion and quarantines with parent_destino_absent reason — verified in e2e test (D-03)"
    - "CLOSED_PERMANENTLY: SignalAgent routes to descarte before scoring — verified in e2e test (D-05)"
    - "Compliance gate blocks: opted-out contact cannot receive outreach even after gate approve — ComplianceError raised (D-11)"
    - "Opt-out flow: 'SAIR' keyword in inbound reply writes consent_log.opted_out=True and routes RioRecord to DLQ"
    - "Owner-confirmed atrativo (existe=sim, funcionando=sim) reaches Mar with routing='mar' and push_attraction_task succeeds (D-10)"
    - "All 5 Phase 3 success criteria verified by the offline suite without real externals"
    - "test_sc4 exercises the sub_state FSM across the full discovery → contacts_found → signals_gathered → aguardando_consulta_whatsapp pipeline, verifying D-01 (Celery+Redis idempotent FSM) and D-02 (sub_state single source of truth with audit rows on every transition) hold end-to-end"
    - "place_id-only persistence verified in test_sc1: NascenteRecord.payload has place_id_cache key and no raw Places address data (D-04)"
  artifacts:
    - path: "tests/integration/test_atrativos_lane_e2e.py"
      provides: "Full Phase 3 offline integration suite (7 test scenarios)"
  key_links:
    - from: "test_atrativos_lane_e2e.py"
      to: "brave/lanes/atrativos/discovery_agent.DiscoveryAgent"
      via: "direct instantiation with FakePlacesClient + FakeLLMClient + real DB session"
      pattern: "DiscoveryAgent"
    - from: "test_atrativos_lane_e2e.py"
      to: "brave/compliance/gate.send_path_gate"
      via: "invoked before simulated outreach; opted-out path tested via consent_log fixture"
      pattern: "send_path_gate"
    - from: "test_atrativos_lane_e2e.py"
      to: "brave/tasks/pipeline.push_attraction_task"
      via: "called synchronously (no broker) after owner validation in e2e happy path"
      pattern: "push_attraction_task"
---

<objective>
Phase acceptance gate — full offline end-to-end integration suite verifying all 5 Phase 3 success criteria and all 9 requirements (ATR-01..06, COMP-01..03) against a real Postgres+Redis test database (docker-compose) with fake external clients. This plan produces a single integration test file with 7 scenario tests that collectively prove the entire Atrativos lane works end-to-end.

Purpose: Before Phase 3 is declared complete, every observable truth from the phase goal must be machine-verifiable in a single pytest run. This is the phase acceptance gate.
Output: tests/integration/test_atrativos_lane_e2e.py — 7 tests, all green, all offline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-atrativos-lane-whatsapp-compliance/03-CONTEXT.md
@.planning/phases/03-atrativos-lane-whatsapp-compliance/03-RESEARCH.md
@.planning/phases/03-atrativos-lane-whatsapp-compliance/03-01-SUMMARY.md
@.planning/phases/03-atrativos-lane-whatsapp-compliance/03-02-SUMMARY.md
@.planning/phases/03-atrativos-lane-whatsapp-compliance/03-03-SUMMARY.md
@.planning/phases/03-atrativos-lane-whatsapp-compliance/03-04-SUMMARY.md

<interfaces>
<!-- Previous phase 02 integration test as structural template. -->

From tests/integration/test_destinos_lane_e2e.py (read this file in full during task):
```python
# structural pattern: pytest fixtures for DB session, fakeredis, fake clients
# test_ functions call agents/tasks directly with real DB + fake externals
# assert DB state after each step
```

From tests/fakes/fake_places.py:
```python
SIGNAL_FIXTURE_OPEN = {"place_id": "ChIJtest001", "business_status": "OPERATIONAL", ...}
SIGNAL_FIXTURE_CLOSED = {"place_id": "ChIJtest002", "business_status": "CLOSED_PERMANENTLY", ...}
```

Phase 3 success criteria (from ROADMAP.md):
1. DiscoveryAgent sweeps + resolves parent destino from Mar + persists place_id only
2. SignalAgent maps Places fields to §7.6 inputs (CLOSED_* → descarte; reviews → atualidade; weekday_text → completude)
3. Human WhatsApp gate approves borderline atrativos with enforced volume ramp
4. WhatsAppAgent runs outreach: Sonnet ask → DeepSeek extract → owner-validation → re-score → Mar/DLQ
5. LGPD + BSP enforced as hard offline-tested gates before first send
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end integration test suite (7 scenarios)</name>

  <read_first>
    - tests/integration/test_destinos_lane_e2e.py (full — DB fixture pattern, fake client injection, assert DB state shape)
    - tests/integration/test_fastapi_endpoints.py (full — TestClient pattern, fixture shapes for RioRecord seeding)
    - tests/conftest.py (full — db_session, engine, fakeredis fixtures)
    - tests/fakes/fake_places.py (full — SIGNAL_FIXTURE_OPEN, SIGNAL_FIXTURE_CLOSED, FakePlacesClient)
    - tests/fakes/fake_llm.py (full — FakeLLMClient, how to configure return values)
    - tests/fakes/fake_whatsapp.py (full — FakeWhatsAppClient, sent_messages tracking)
    - tests/fakes/fake_apify.py (full — FakeApifyClient)
    - tests/fakes/fake_norteia_api.py (full — FakeNorteiaApiClient)
    - brave/core/models.py (ConsentLog, RioRecord, MarRecord, NascenteRecord — all column names)
    - brave/core/rio/routing.py (process_nascente_record, reprocess_record — for direct call in tests)
    - brave/compliance/gate.py (send_path_gate, ComplianceError)
    - .planning/phases/03-atrativos-lane-whatsapp-compliance/03-RESEARCH.md (sections: Score Input Mapping for atrativos origin_value=60; cold-start score estimate ~28 → descarte; owner validation → re-score path)
  </read_first>

  <behavior>
    Seven test scenarios, each as a standalone pytest test function in tests/integration/test_atrativos_lane_e2e.py:

    test_sc1_discovery_happy_path:
      - Seed: MarRecord with entity_type="destination" for UF="BA", municipio_ibge="2900702"
      - Fake: FakePlacesClient returns [{place_id:"ChIJtest001", ...AtrativoResult fields}]; FakeLLMClient returns AtrativoResult for "Praia de Itapoã" with municipio_ibge="2900702"
      - Call: DiscoveryAgent(fake_places, fake_llm, session, config).produce("BA")
      - Assert: NascenteRecord created with source="places_discovery", source_ref="places:BA:ChIJtest001", entity_type="attraction"
      - Assert: NascenteRecord payload has "place_id_cache" key; does NOT have "address" key (only place_id persisted per COMP-03/D-04)
      - Assert: corresponding RioRecord created with sub_state="discovered"
      - Verifies: ATR-02, COMP-03

    test_sc2_discovery_skips_absent_parent_destino:
      - Seed: No MarRecord for BA/2900702
      - Call: DiscoveryAgent.produce("BA")
      - Assert: NascenteRecord NOT created (store_raw not called)
      - Assert: QuarantineRecord (or audit row) has error containing "parent_destino_absent"
      - Verifies: ATR-02 (D-03 precondition), ATR-01

    test_sc3_signal_agent_hard_descarte_closed_place:
      - Seed: RioRecord with sub_state="contacts_found", entity_type="attraction"
      - Fake: FakePlacesClient.place_details returns SIGNAL_FIXTURE_CLOSED (business_status="CLOSED_PERMANENTLY")
      - Call: SignalAgent(fake_places, fake_apify, session).run(rio)
      - Assert: rio.routing == "descarte", rio.sub_state is None
      - Assert: AuditLog row with action="hard_descarte" exists
      - Verifies: ATR-04 (D-05 hard descarte)

    test_sc4_full_pipeline_borderline_reaches_gate:
      - Seed: MarRecord (parent destino); seed rich FakePlacesClient results (OPERATIONAL, recent reviews)
      - Call chain: DiscoveryAgent → find_contacts_task equivalent → SignalAgent → process_nascente_record
      - Assert: After scoring (with origin=60, completude from fields, atualidade from reviews), RioRecord lands in DLQ with sub_state="aguardando_consulta_whatsapp" (borderline score <85 but >40)
      - Assert: RioRecord.routing == "dlq"
      - Note: To reliably reach aguardando state, set FakeLLMClient extraction completude_value=70 and use FakePlacesClient with recent review (atualidade=100); score = 18+14+0+15+0 = 47 — still descarte at calibrated threshold. Adjust: set origin_value=80 via config or use a richer fixture to reach borderline band. Follow RESEARCH.md score mapping to construct a fixture that scores between 40 and 85.
      - Verifies: ATR-01 (sub_state FSM D-01/D-02), ATR-03 (contacts found), ATR-04 (signals)

    test_sc5_compliance_gate_blocks_opted_out_contact:
      - Seed: RioRecord sub_state="whatsapp_in_progress"; ConsentLog row with opted_out=True for phone="+5511999990001"
      - Call: send_path_gate(session, fakeredis, rio, "+5511999990001", "norteia_verification", {"body": "Olá da Norteia"}, settings) 
      - Assert: raises ComplianceError with message containing "opted_out"
      - Verifies: COMP-01 (D-11 opt-out check), ATR-05

    test_sc6_opt_out_keyword_closes_conversation:
      - Seed: RioRecord sub_state="whatsapp_in_progress"; ConsentLog row (not opted_out)
      - Call: Invoke recv_reply_node logic (or resume_conversation_task with message_text="SAIR")
      - Assert: ConsentLog row for that phone has opted_out=True, opted_out_keyword="SAIR"
      - Assert: RioRecord.routing == "dlq", sub_state set appropriately (whatsapp ended)
      - Verifies: COMP-01 (opt-out), COMP-02 (BSP opt-out honoring), ATR-06

    test_sc7_owner_validation_reaches_mar:
      - Seed: RioRecord with sufficient score data; ConsentLog row (not opted_out); fakeredis (fresh)
      - Simulate outreach completion: set normalized["owner_validation"] = {"existe": "sim", "funcionando": "sim"}; call reprocess_record with validacao_humana_value=100
      - Assert: if score reaches ≥85 → routing == "mar"; MarRecord exists for that source_ref
      - Assert: FakeNorteiaApiClient.push_attraction called with entity_type="attraction"
      - Verifies: ATR-06 (D-10 owner-validation → Mar), ATR-05 (gate approved path)
  </behavior>

  <files>
    tests/integration/test_atrativos_lane_e2e.py
  </files>

  <action>
    Create tests/integration/test_atrativos_lane_e2e.py following the structural pattern of tests/integration/test_destinos_lane_e2e.py. Use the same pytest fixture approach: db_session (real Postgres via docker-compose), fakeredis.FakeRedis() for Redis, FakePlacesClient, FakeLLMClient, FakeApifyClient, FakeWhatsAppClient, FakeNorteiaApiClient from tests/fakes/.

    For each scenario above: set up DB state (seed MarRecord/RioRecord/ConsentLog as needed), invoke the relevant agent/service/function directly (not via Celery broker — call the underlying function), assert DB state after call.

    For test_sc4 (borderline reaches gate): construct a FakeLLMClient that returns an AtrativoResult with completude_value=80 and FakePlacesClient with SIGNAL_FIXTURE_OPEN including a review with publishTime within 30 days. Set origin_value=70 via a custom ScoreConfig (override origen_weight or adjust origin_value). Target a score in the 40–84 range. If the Phase 2 calibration (threshold_dlq=40) means a cold-start atrativo scores below 40, adjust the fixture to add corroboration_value=20 (Apify confirms). Verify the test asserts sub_state matches a DLQ/aguardando state.

    For test_sc7 (owner → Mar): directly manipulate RioRecord.normalized to set the owner validation signals; call reprocess_record(session, rio.id, config) with ScoreConfig that has validacao_humana weight; assert routing=="mar" and MarRecord exists. If score still doesn't reach 85 with test fixture, adjust completude_value/atualidade_value in the seed to ensure ≥85 is reachable after validacao_humana=100 is added.

    Mark all tests with @pytest.mark.integration to allow running the unit suite separately with pytest -m "not integration".

    Add a docstring at the top of the file mapping each test to the Phase 3 success criterion and requirement IDs it verifies.
  </action>

  <verify>
    <automated>cd /Users/leandro/Projects/norteia/norteia-brave && python -m pytest tests/integration/test_atrativos_lane_e2e.py -x -v -m integration 2>&1 | tail -40</automated>
  </verify>

  <acceptance_criteria>
    - pytest tests/integration/test_atrativos_lane_e2e.py exits 0; all 7 tests pass
    - test_sc1: NascenteRecord.payload["place_id_cache"] exists; NascenteRecord.payload does NOT contain raw Places address data
    - test_sc2: NascenteRecord count == 0 after DiscoveryAgent.produce("BA") with no MarRecord parent
    - test_sc3: rio.routing == "descarte" and rio.sub_state is None after CLOSED_PERMANENTLY signal
    - test_sc5: send_path_gate raises ComplianceError (opted_out contact cannot be messaged)
    - test_sc6: ConsentLog.opted_out == True and opted_out_keyword == "SAIR" after opt-out
    - test_sc7: MarRecord exists with entity_type="attraction" after owner validation; FakeNorteiaApiClient.push_attraction_calls has at least 1 entry
    - All tests use fake clients only (grep "BRAVE_RUN_REAL_EXTERNALS" tests/integration/test_atrativos_lane_e2e.py returns 0 — no real externals needed)
  </acceptance_criteria>

  <done>7 offline integration tests verify all Phase 3 success criteria and all 9 requirement IDs (ATR-01..06, COMP-01..03) against a real Postgres+Redis docker-compose DB using fake external clients. Phase 3 acceptance gate is passed when this suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test → DB | Real Postgres (docker-compose); test fixtures isolated via transaction rollback or per-test schema |
| Integration test → Redis | fakeredis in-process; no real Redis broker needed for unit-level tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Information Disclosure | Test fixtures containing real PII | mitigate | All fixture phone numbers use "+5511999990001" format (clearly synthetic); no real business data in test fixtures |
| T-03-05-02 | Tampering | Integration test not covering compliance gate bypass path | mitigate | test_sc5 directly tests the opted-out path via send_path_gate; proves gate is not bypassable via the approved integration path |
</threat_model>

<verification>
- pytest tests/integration/test_atrativos_lane_e2e.py -x -v exits 0 (7 tests, all green)
- No test function uses BRAVE_RUN_REAL_EXTERNALS=true
- test_sc3 asserts rio.routing == "descarte" for CLOSED_PERMANENTLY
- test_sc5 asserts ComplianceError raised for opted-out contact
- test_sc7 asserts MarRecord exists with entity_type="attraction"
- pytest -m "not integration" tests/ still passes all unit tests (integration tests properly marked)
</verification>

<success_criteria>
All 5 Phase 3 success criteria are machine-verifiable in a single offline pytest run. The test suite covers: (1) discovery + parent resolution + place_id-only persistence; (2) signal hard descarte for closed places; (3) compliance gate blocking opted-out contacts; (4) opt-out keyword detection closing conversations; (5) owner-validation → Mar promotion. Phase 3 can be declared complete when this suite passes green.
</success_criteria>

<output>
Create `.planning/phases/03-atrativos-lane-whatsapp-compliance/03-05-SUMMARY.md` when done.
</output>
