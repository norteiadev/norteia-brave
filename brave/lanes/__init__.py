"""Lanes package — pluggable producer lanes.

Each lane is a distinct data source that writes to Nascente.
Lanes import from core; core NEVER imports from lanes (D-18).

Phase 1: stub only (base.py Lane Protocol).
Phase 2: Destinos lane (Mtur + NotebookLM + Desmembramento).
Phase 3: Atrativos lane (Discovery + Contact + Signal + WhatsApp).
"""
