---
phase: 04-dashboard-territorial-cms
plan: 05
type: execute
wave: 4
depends_on: ["04-02", "04-03"]
files_modified:
  - brave/api/routers/dashboard.py
  - tests/integration/test_dashboard_endpoints.py
  - dashboard/app/monitor/page.tsx
  - dashboard/components/monitor/MonitorTiles.tsx
  - dashboard/components/monitor/ThroughputChart.tsx
  - dashboard/components/monitor/AlertsPanel.tsx
  - dashboard/lib/monitor-api.ts
  - dashboard/mocks/handlers/monitor.ts
  - dashboard/components/monitor/__tests__/MonitorTiles.test.tsx
  - dashboard/components/monitor/__tests__/ThroughputChart.test.tsx
autonomous: true
requirements: [DASH-02]
must_haves:
  truths:
    - "The Brave monitor shows volume per layer, approval/rejection/DLQ rates, throughput, and failure alerts"
    - "The monitor view polls the endpoint at a sane interval (no manual refresh)"
    - "Failure alerts surface when PoisonQuarantine count > 0 or the WhatsApp quality flag is RED"
  artifacts:
    - path: "brave/api/routers/dashboard.py"
      provides: "GET /api/v1/monitor aggregating counts + rates + throughput + alerts"
      contains: "def get_monitor"
    - path: "dashboard/components/monitor/MonitorTiles.tsx"
      provides: "Per-layer volume + rate tiles"
  key_links:
    - from: "brave/api/routers/dashboard.py"
      to: "AuditLog + PoisonQuarantine + RioRecord"
      via: "group_by action counts + window throughput + quarantine count"
      pattern: "PoisonQuarantine"
    - from: "dashboard/components/monitor/MonitorTiles.tsx"
      to: "GET /api/v1/monitor"
      via: "useQuery refetchInterval polling"
      pattern: "refetchInterval"
---

<objective>
Monitor slice (DASH-02, §15.7). Add `GET /api/v1/monitor` — counts **plus** approval/rejection/DLQ rates (from `AuditLog` action counts over a window), throughput (`RioRecord.processed_at` per interval), and failure alerts (`PoisonQuarantine` count, RED WhatsApp quality flag) — then build the monitor view: tiles + Recharts charts that poll via TanStack Query (no WebSocket this milestone, CONTEXT deferred).

Purpose: The existing `/metrics` returns counts only (RESEARCH §1); DASH-02 needs rates/throughput/alerts/audit. This slice fills the endpoint and renders it end-to-end.
Output: `get_monitor` on `dashboard.py` + the monitor view + offline pytest + Vitest/MSW tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md
@.planning/phases/04-dashboard-territorial-cms/04-PATTERNS.md
@.planning/phases/04-dashboard-territorial-cms/04-RESEARCH.md
@.planning/phases/04-dashboard-territorial-cms/04-03-SUMMARY.md
@.planning/phases/04-dashboard-territorial-cms/04-02-SUMMARY.md

<interfaces>
<!-- Shapes the endpoint aggregates. From models.py + metrics.py/audit.py patterns. -->
AuditLog(action, created_at): actions dlq_validated / dlq_rejected / dlq_reprocessed → rates via group_by(action) over a window
RioRecord(processed_at, routing): throughput = count over processed_at window; routing counts for volume
PoisonQuarantine(id, quarantined_at): failure-alert count
WhatsApp quality flag: Redis flag (RED → alert) — from atrativos_gate quality-rating (Phase 3)
metrics.py existing: {nascente_count, rio_count:{in_progress,mar,dlq,descarte}, mar_count}
Read-aggregation idioms (PATTERNS): db.scalar(select(func.count(...))) single; db.execute(select(col, func.count(...)).group_by(col)).fetchall() grouped; pre-seed {v:0} for empty groups
Guard with Depends(require_bearer). dashboard.py already exists (plan 03) — ADD to it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/v1/monitor endpoint</name>
  <files>brave/api/routers/dashboard.py, tests/integration/test_dashboard_endpoints.py</files>
  <read_first>
    - brave/api/routers/metrics.py (counts + group_by, pre-seed empties)
    - brave/api/routers/audit.py (windowed/ordered read shape)
    - brave/api/routers/dashboard.py (existing from plan 03 — add the route here)
    - brave/core/models.py (AuditLog, PoisonQuarantine, RioRecord)
    - .planning/phases/04-dashboard-territorial-cms/04-PATTERNS.md (Monitor rates section)
  </read_first>
  <behavior>
    - Test: GET /api/v1/monitor with a valid Bearer returns 200 with keys volume (per-layer counts), rates (approval/rejection/dlq), throughput, alerts
    - Test: alerts.failures reflects PoisonQuarantine count; alerts.quality reflects the RED flag
    - Test: rates derive from AuditLog action counts (dlq_validated/dlq_rejected/dlq_reprocessed) over the window
    - Test: empty DB → all counts 0 (pre-seeded), endpoint still 200
    - Test: missing Bearer → 401 before any DB work
  </behavior>
  <action>Add `@router.get("/api/v1/monitor", dependencies=[Depends(require_bearer)])` `def get_monitor(since: ... = Query(...), db: Session = Depends(get_db))` to the existing `brave/api/routers/dashboard.py`. Compose: `volume` = per-layer counts (reuse the metrics.py `db.scalar(select(func.count()))` + routing `group_by` shape, pre-seed `{routing:0}`); `rates` = AuditLog action counts (`group_by(AuditLog.action)` filtered on a `created_at` window) for dlq_validated/dlq_rejected/dlq_reprocessed, computed as proportions; `throughput` = `func.count(RioRecord.id)` over a `processed_at` window; `alerts` = `{failures: func.count(PoisonQuarantine.id), quality: <RED flag from Redis quality-rating>}`. Read-only, no pipeline logic (D-01). Use the get_redis dep for the quality flag (mirror how atrativos_gate reads it).</action>
  <verify>
    <automated>pytest tests/integration/test_dashboard_endpoints.py -k monitor -x</automated>
  </verify>
  <acceptance_criteria>
    - `brave/api/routers/dashboard.py` contains `def get_monitor(` decorated `dependencies=[Depends(require_bearer)]`
    - `grep -n "PoisonQuarantine" brave/api/routers/dashboard.py` returns a match
    - monitor test (200 shape with volume/rates/throughput/alerts, empty-DB pre-seed, 401 no-Bearer) passes
  </acceptance_criteria>
  <done>GET /api/v1/monitor returns volume + rates + throughput + alerts behind the Bearer gate, proven offline.</done>
</task>

<task type="auto">
  <name>Task 2: Monitor view — tiles + Recharts + polling</name>
  <files>dashboard/components/monitor/MonitorTiles.tsx, dashboard/components/monitor/ThroughputChart.tsx, dashboard/components/monitor/AlertsPanel.tsx, dashboard/lib/monitor-api.ts, dashboard/app/monitor/page.tsx, dashboard/mocks/handlers/monitor.ts, dashboard/components/monitor/__tests__/MonitorTiles.test.tsx, dashboard/components/monitor/__tests__/ThroughputChart.test.tsx</files>
  <read_first>
    - .planning/phases/04-dashboard-territorial-cms/04-UI-SPEC.md (Display-size volume tiles, destructive failure-alert tile, chart color rules, empty/loading/error copy)
    - .planning/phases/04-dashboard-territorial-cms/04-RESEARCH.md (§5 useQuery refetchInterval, Recharts 3 via shadcn chart)
    - brave/api/routers/dashboard.py (the /monitor shape from Task 1)
    - dashboard/app/providers.tsx (from plan 02)
  </read_first>
  <action>Build `dashboard/components/monitor/MonitorTiles.tsx` (per-layer volume as Display-size numerals + approval/rejection/DLQ rate captions, UI-SPEC typography), `dashboard/components/monitor/ThroughputChart.tsx` (Recharts 3 area/bar via the shadcn `chart` wrapper, primary blue series), and `dashboard/components/monitor/AlertsPanel.tsx` (failure-alert tile in destructive red when PoisonQuarantine>0 or quality RED — UI-SPEC reserves destructive for failure alerts). All fetch `GET /api/v1/monitor` through the BFF with `useQuery({ refetchInterval })` at a 5–15s interval (RESEARCH §5, planner discretion: use 10s). Wire `dashboard/app/monitor/page.tsx`. Loading = Skeleton tiles; empty = "Sem dados no período"; error = "Não foi possível carregar" + retry. Add `dashboard/lib/monitor-api.ts` (query key + fetcher). Tests: MonitorTiles renders volume/rates and the alert tile turns destructive when failures>0; ThroughputChart renders the series; all MSW states (success/empty/error/401).</action>
  <verify>
    <automated>cd dashboard && bunx vitest run components/monitor && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "refetchInterval" dashboard/components/monitor` returns a match
    - `dashboard/components/monitor/AlertsPanel.tsx` renders a destructive tile when failures > 0
    - `cd dashboard && bunx vitest run components/monitor` exits 0 (all four MSW states)
    - `cd dashboard && bunx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The Brave monitor renders volume/rates/throughput/alerts and polls live, all offline-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BFF → FastAPI /monitor | Bearer-gated operational read; aggregate counts only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Spoofing | get_monitor | mitigate | `Depends(require_bearer)`; 401 before DB work |
| T-04-16 | Information Disclosure | monitor aggregates | accept | returns aggregate counts/rates only, no PII, no record-level secrets |
| T-04-17 | Denial of Service | polling load | accept | single internal operator, 10s interval; no external exposure; aggregate queries are bounded by window |
| T-04-SC | Tampering | python deps (none added) | accept | no new package installs |
</threat_model>

<verification>
- `pytest tests/integration/test_dashboard_endpoints.py -k monitor -x` passes.
- `cd dashboard && bunx vitest run components/monitor` exits 0.
</verification>

<success_criteria>
DASH-02 complete: the Brave monitor shows volume per layer, approval/rejection/DLQ rates, failure alerts, and throughput, polling live, all proven offline.
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-territorial-cms/04-05-SUMMARY.md` when done.
</output>
