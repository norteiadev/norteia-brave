---
phase: "14"
plan: "02"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - brave/lanes/tripadvisor/atrativos.py
  - tests/unit/lanes/tripadvisor/test_atrativos.py
autonomous: false
requirements: [TA-15]

must_haves:
  truths:
    - "A coordless AttractionsFusion card that previously quarantined as ibge_unmatched now resolves to the correct município when a geocoder is injected"
    - "ibge_unmatched quarantine fires only after both name-match AND geo-enrichment fail"
    - "When geocoder=None, atrativos._ingest_one behavior is byte-identical to Phase-13 (no regression)"
    - "max_distance_km=15.0 default in resolve_municipio is untouched; 50.0 is passed only from the geo-enrichment call site"
    - "A real MG sweep with RUN_REAL_EXTERNALS=1 yields Nascente entity_type='attraction' > 0 with municípios resolved (Level-3 gate)"
  artifacts:
    - path: "brave/lanes/tripadvisor/atrativos.py"
      provides: "TripAdvisorAtrativosIngest with optional geocoder arg; async _ingest_one; geo-enrichment block before quarantine"
      contains: "geocoder: GeocoderClientProtocol | None = None"
    - path: "tests/unit/lanes/tripadvisor/test_atrativos.py"
      provides: "Regression tests: test_coordless_resolves_via_geo, test_quarantine_after_both_fail, test_no_geocoder_unchanged"
  key_links:
    - from: "brave/lanes/tripadvisor/atrativos.py"
      to: "brave/clients/nominatim.py"
      via: "GeocoderClientProtocol injection into __init__"
      pattern: "geocoder.*GeocoderClientProtocol"
    - from: "brave/lanes/tripadvisor/atrativos.py"
      to: "brave/lanes/tripadvisor/ibge.py"
      via: "resolve_municipio called twice: first with default radius, second with max_distance_km=50.0"
      pattern: "resolve_municipio.*max_distance_km=50\\.0"
---

<objective>
Wire the Nominatim geocoder into atrativos._ingest_one as an optional enrichment step before the ibge_unmatched quarantine, promote _ingest_one to async, add three regression/no-regression tests, and run the Level-3 human checkpoint to confirm a real MG sweep yields Nascente attraction count > 0.

Purpose: Closes the Phase-13 carry-forward gap (TA-15) — without this, every real AttractionsFusion card quarantines as ibge_unmatched even when the Phase-13 fetch wiring is correct.

Output: atrativos.py with geo-enrichment; test_atrativos.py with regression tests; Level-3 operator checkpoint confirming real data flows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-coordless-attraction-geo-resolution-nominatim/14-CONTEXT.md
@.planning/phases/14-coordless-attraction-geo-resolution-nominatim/14-RESEARCH.md
@.planning/phases/14-coordless-attraction-geo-resolution-nominatim/14-PATTERNS.md
@.planning/phases/14-coordless-attraction-geo-resolution-nominatim/14-VALIDATION.md
@.planning/phases/14-coordless-attraction-geo-resolution-nominatim/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: atrativos.py — optional geocoder arg + async _ingest_one + geo-enrichment block</name>
  <files>brave/lanes/tripadvisor/atrativos.py</files>

  <read_first>
    - brave/lanes/tripadvisor/atrativos.py (entire file — read once; capture: __init__ signature lines ~73–104, produce loop lines ~120–133, _ingest_one signature + ibge_match block lines ~135–188; quarantine_poison call lines ~180–188)
    - brave/lanes/tripadvisor/ibge.py (resolve_municipio signature — confirm it accepts candidate_lat, candidate_lng, max_distance_km params; find default max_distance_km value to ensure it stays 15.0)
    - 14-PATTERNS.md (atrativos.py section — __init__ extension, produce loop await change, _ingest_one geo-enrichment insertion; exact line references)
    - 14-CONTEXT.md (locked decision #4: quarantine ibge_unmatched ONLY after BOTH fail; decision #3: haversine fallback 50km; decision #2: primary = name from address)
  </read_first>

  <behavior>
    - TripAdvisorAtrativosIngest.__init__ gains last param geocoder: "GeocoderClientProtocol | None" = None; stores as self._geocoder; all existing callers without geocoder arg continue to work unchanged
    - produce() loop changes self._ingest_one(...) to await self._ingest_one(...)
    - _ingest_one promoted from def to async def
    - After the existing resolve_municipio(name, uf, self._ibge_records, candidate_lat=lat, candidate_lng=lng) call that returns None AND self._geocoder is not None: call geo = await self._geocoder.geocode(location_id, name, uf); if geo is not None: call resolve_municipio(geo.get("municipio_name") or name, uf, self._ibge_records, candidate_lat=geo["lat"], candidate_lng=geo["lon"], max_distance_km=50.0)
    - quarantine_poison with task_name="brave.ta.atrativos.ibge_unmatched" fires ONLY if ibge_match is still None after both attempts
    - Default max_distance_km in ibge.py's resolve_municipio signature is NOT changed (anti-pattern: would break Phase-11/13 tests)
    - GeocoderClientProtocol imported under TYPE_CHECKING (D-18 import cleanliness)
  </behavior>

  <action>
    1. Read brave/lanes/tripadvisor/atrativos.py in full to identify exact line numbers for: (a) imports/TYPE_CHECKING block, (b) __init__ signature and body end, (c) produce loop line calling _ingest_one, (d) _ingest_one def line, (e) the ibge_match assignment and quarantine_poison block.

    2. In the TYPE_CHECKING import block, add GeocoderClientProtocol from brave.clients.base (per D-18 import rule — TYPE_CHECKING only, no runtime circular import).

    3. Extend __init__ signature: append geocoder: "GeocoderClientProtocol | None" = None as the last parameter after destino_rio_map. In the body, add self._geocoder = geocoder. No other __init__ changes.

    4. In produce(): change the call site from self._ingest_one(uf, entity, run_rio=run_rio) to await self._ingest_one(uf, entity, run_rio=run_rio). produce is already async — this is a single-character + keyword change.

    5. Change _ingest_one's def to async def. No other callers besides produce and test_atrativos.py exist (grep confirms).

    6. Insert the geo-enrichment block BETWEEN the existing ibge_match assignment and the existing quarantine_poison block, following the exact pattern in 14-PATTERNS.md atrativos.py geo-enrichment insertion section. The block is: if ibge_match is None and self._geocoder is not None: geo = await self._geocoder.geocode(location_id, name, uf); if geo is not None: ibge_match = resolve_municipio(geo.get("municipio_name") or name, uf, self._ibge_records, candidate_lat=geo["lat"], candidate_lng=geo["lon"], max_distance_km=50.0). Do NOT change the existing first resolve_municipio call or its arguments.

    7. Verify ibge.py resolve_municipio's default max_distance_km remains 15.0 — do not change it.
  </action>

  <verify>
    <automated>env -u RUN_REAL_EXTERNALS .venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_atrativos.py -x -p no:warnings 2>&1 | tail -10</automated>
  </verify>

  <done>
    - brave/lanes/tripadvisor/atrativos.py: __init__ has geocoder=None last param; _ingest_one is async; geo-enrichment block between first ibge_match and quarantine_poison; max_distance_km=50.0 at geo-enrichment call site only
    - ibge.py resolve_municipio default max_distance_km is unchanged (still 15.0)
    - Existing test_atrativos.py tests pass (no regression — geocoder=None default means no behavior change for old tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_atrativos.py — regression + both-fail + no-geocoder-unchanged tests</name>
  <files>tests/unit/lanes/tripadvisor/test_atrativos.py</files>

  <read_first>
    - tests/unit/lanes/tripadvisor/test_atrativos.py (entire file — read once; extract: imports block, _IBGE_RECORDS fixture, _DESTINO_RIO_MAP, _GEO_ID_MG constant, _make_card helper, _make_fake_client helper, _make_config helper, existing test class structure; identify the test calling _ingest_one so it can be made async-aware)
    - tests/fakes/fake_nominatim.py (FakeGeocoderClient signature + geocode_calls attribute — from 14-01-PLAN.md output)
    - 14-PATTERNS.md (test_atrativos.py section — _make_coordless_card helper, TestAtrativosGeoEnrichment class with all three test methods, exact assertions per method)
    - 14-VALIDATION.md (Per-Requirement Verification Map — exact pytest node IDs: test_coordless_resolves_via_geo, test_quarantine_after_both_fail, test_no_geocoder_unchanged)
  </read_first>

  <behavior>
    - test_coordless_resolves_via_geo: builds a card with name="Cachoeira do Tabuleiro" and NO lat/lng; injects FakeGeocoderClient returning {lat:-19.047, lon:-43.426, osm_id:123, municipio_name:"Conceição do Mato Dentro"} for locationId="312332"; asserts store_raw is called (not quarantined); asserts payload["municipio_id"] == "3117900"; asserts fake_geo.geocode_calls has 1 entry with location_id="312332"
    - test_quarantine_after_both_fail: builds a coordless card with name="Cachoeira do Tabuleiro"; IBGE records contain only "São Paulo/SP" (no MG records); FakeGeocoderClient returns None for the locationId; asserts quarantine_poison is called with task_name="brave.ta.atrativos.ibge_unmatched"; asserts store_raw NOT called
    - test_no_geocoder_unchanged: builds a card whose name DOES match an IBGE município (e.g., "Uberlândia"); constructs TripAdvisorAtrativosIngest WITHOUT geocoder argument; asserts store_raw is called (matches via name fuzzy-match alone without geo-enrichment)
    - All new tests use @pytest.mark.asyncio and await ingest.produce(...)
    - Existing tests in the file remain unchanged and pass
  </behavior>

  <action>
    1. Read tests/unit/lanes/tripadvisor/test_atrativos.py fully to understand existing helper signatures (especially _make_card, _make_fake_client, _make_config, _IBGE_RECORDS, _DESTINO_RIO_MAP, _GEO_ID_MG constant, and the IbgeMunicipio constructor signature used in that file).

    2. Add import: from tests.fakes.fake_nominatim import FakeGeocoderClient at the top of the imports block.

    3. Add _make_coordless_card helper function after the existing _make_card helper: returns a dict with locationId=312332, name="Cachoeira do Tabuleiro", review_count=100, rating=4.0, category="Waterfalls" — NO lat/lng keys. This mirrors the real AttractionsFusion listing card shape.

    4. Add class TestAtrativosGeoEnrichment with three @pytest.mark.asyncio async test methods following 14-PATTERNS.md test_atrativos.py section exactly:
       - test_coordless_resolves_via_geo: constructs ibge_records with IbgeMunicipio for "Conceição do Mato Dentro" (ibge_code="3117900", uf="MG", lat=-19.047, lng=-43.426); builds destino_rio_map mapping "3117900" to (_PARENT_RIO_ID if defined, else any UUID, "tripadvisor:destination:303380"); FakeGeocoderClient fixture returning coords for "312332"; patches store_raw + process_nascente_record; assert store_raw called, payload municipio_id == "3117900", fake_geo.geocode_calls length == 1
       - test_quarantine_after_both_fail: ibge_records = [IbgeMunicipio for "São Paulo", "SP"]; FakeGeocoderClient(fixture_results={}); patches quarantine_poison; assert quarantine_poison called with matching task_name; assert store_raw NOT called
       - test_no_geocoder_unchanged: uses existing _IBGE_RECORDS + _make_card(name matching a record in _IBGE_RECORDS); constructs TripAdvisorAtrativosIngest without geocoder; assert store_raw called

    5. If any existing tests in test_atrativos.py call _ingest_one directly (not via produce), they must also be updated to await it. Check via grep and update if found.

    Node IDs MUST match 14-VALIDATION.md exactly: test_coordless_resolves_via_geo, test_quarantine_after_both_fail, test_no_geocoder_unchanged.
  </action>

  <verify>
    <automated>env -u RUN_REAL_EXTERNALS .venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_atrativos.py tests/unit/clients/test_nominatim.py -x -p no:warnings 2>&1 | tail -10</automated>
  </verify>

  <done>
    - test_coordless_resolves_via_geo passes: previously-quarantined coordless card resolves to Conceição do Mato Dentro
    - test_quarantine_after_both_fail passes: quarantine fires after both name-match and geo-enrichment fail
    - test_no_geocoder_unchanged passes: geocoder=None leaves existing behavior intact
    - All pre-existing test_atrativos.py tests still pass
    - Full quick-run passes: env -u RUN_REAL_EXTERNALS .venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_atrativos.py tests/unit/clients/test_nominatim.py -x
    - Full offline suite green: env -u RUN_REAL_EXTERNALS .venv/bin/python -m pytest -p no:warnings -q
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Level-3 — Real MG sweep confirms Nascente attraction count > 0 with municípios resolved</name>

  <what-built>
    Wave 2 Tasks 1–2 wired Nominatim geo-enrichment into TripAdvisorAtrativosIngest._ingest_one. Offline tests confirm the regression case (coordless card → resolves via FakeGeocoderClient). This checkpoint validates the full real path: live TripAdvisor session + real Nominatim calls + IBGE matching → Nascente attractions no longer mass-quarantined as ibge_unmatched.
  </what-built>

  <how-to-verify>
    Pre-conditions (from Phase-12/13 runbook):
    1. A valid TripAdvisor session must be injected: follow the RUNBOOK-NIVEL3 in data/tripadvisor/README (Phase-12 bootstrap) to capture a fresh session and POST it to /api/v1/tripadvisor/session. Confirm GET /api/v1/tripadvisor/session/status returns "ready".
    2. Start services: docker-compose up -d (or the project's equivalent local start command for FastAPI + Celery + Redis + Postgres).

    Run the sweep:
    3. Set RUN_REAL_EXTERNALS=1 and trigger a TripAdvisor atrativos sweep for MG — mirrors Phase-13 Level-3 runbook exactly. Use whichever CLI/endpoint was used in Phase 13 (POST /api/v1/sweep or the CLI sweep command documented in data/tripadvisor/README).

    Confirm success:
    4. Query Nascente: SELECT entity_type, count(*) FROM nascente WHERE source='tripadvisor' GROUP BY entity_type; — attraction count must be > 0 (target: matches the Phase-13 page's ~30 cards).
    5. Query quarantine table: SELECT task_name, count(*) FROM quarantine GROUP BY task_name ORDER BY count DESC LIMIT 10; — ibge_unmatched count must NOT dominate (should be 0 or only multi-município national parks like Lençóis Maranhenses).
    6. Spot-check: SELECT municipio_ibge, count(*) FROM nascente WHERE source='tripadvisor' AND entity_type='attraction' GROUP BY municipio_ibge ORDER BY count DESC LIMIT 5; — municípios should be non-null strings (not NULL).
    7. Second sweep (cache validation): re-run the same MG sweep; Nominatim call count in logs must be ≈0 (all served from cache). Look for "nominatim_cache_hit" in structlog output; should appear for every attraction.

    Success signals (all must hold):
    - Nascente entity_type='attraction' count > 0
    - ibge_unmatched quarantine is NOT the dominant outcome
    - municipio_ibge column is non-null on resolved records
    - Second sweep shows cache_hit logs, not fresh geocode calls
    - No RuntimeError or 429/IP-ban errors from Nominatim in logs
  </how-to-verify>

  <resume-signal>
    Type "approved" if all success signals hold.
    If Nascente attraction count is still 0 or ibge_unmatched dominates, describe: (a) the quarantine task_name appearing most, (b) any Nominatim errors in logs, (c) whether the session was "ready" before the sweep.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nominatim geo dict → atrativos._ingest_one | geocode result consumed as ibge_match inputs; lat/lon passed to haversine; municipio_name passed to resolve_municipio |
| atrativos._ingest_one → quarantine_poison | Decision to quarantine vs. ingest; geo-enrichment must not suppress a legitimate quarantine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure (LGPD) | atrativos._ingest_one — Nominatim result stored in Nascente payload | mitigate | Only lat/lon/osm_id are threaded from geo dict into the ingest payload (municipio_name used for matching only, not stored as a raw address string); CONTEXT decision #8 enforced |
| T-15-02 | Tampering | atrativos._ingest_one — geo.get("municipio_name") or name fallback | mitigate | fallback to `name` when municipio_name is None; resolve_municipio still validates against IBGE CSV (no arbitrary string injection into ibge_code); max_distance_km=50.0 is a fixed literal, not from the geo response |
| T-15-03 | Denial of Service | _ingest_one — geocoder.geocode per attraction card | mitigate | Rate limit enforced in NominatimGeocoderClient (wave 1); cache by locationId prevents re-calls on re-sweeps; NullGeocoderClient used in offline suite (zero network) |
| T-15-04 | Elevation of Privilege | quarantine bypass | mitigate | quarantine_poison fires when ibge_match is still None after BOTH attempts; geo-enrichment cannot skip the quarantine, only reduce legitimate misses; test_quarantine_after_both_fail verifies the gate |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Phase 14 installs no new packages; all dependencies pre-vetted |
</threat_model>

<verification>
After Tasks 1–2 (before Level-3):

1. Quick run passes: env -u RUN_REAL_EXTERNALS .venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_atrativos.py tests/unit/clients/test_nominatim.py -x -p no:warnings
2. Regression test passes: env -u RUN_REAL_EXTERNALS .venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_atrativos.py::test_coordless_resolves_via_geo -x
3. Both-fail quarantine test: env -u RUN_REAL_EXTERNALS .venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_atrativos.py::test_quarantine_after_both_fail -x
4. No-geocoder regression: env -u RUN_REAL_EXTERNALS .venv/bin/python -m pytest tests/unit/lanes/tripadvisor/test_atrativos.py::test_no_geocoder_unchanged -x
5. Full offline suite: env -u RUN_REAL_EXTERNALS .venv/bin/python -m pytest -p no:warnings -q — all green, zero real network

After Level-3 checkpoint approved:
6. Nascente SELECT confirms entity_type='attraction' count > 0
7. quarantine SELECT confirms ibge_unmatched is not dominant
</verification>

<success_criteria>
- atrativos.py: geocoder=None default, async _ingest_one, geo-enrichment before quarantine, 50.0 km radius at call site only
- ibge.py: default max_distance_km=15.0 unchanged
- Regression test passes: coordless card resolves to correct município instead of quarantining
- Both-fail test passes: quarantine fires when geocoder returns None
- No-geocoder test passes: existing behavior unchanged when geocoder omitted
- Full offline suite green
- Level-3 checkpoint approved: real MG sweep yields Nascente attraction > 0 with municípios resolved
</success_criteria>

<output>
Create `.planning/phases/14-coordless-attraction-geo-resolution-nominatim/14-02-SUMMARY.md` when done
</output>
