---
phase: 17.1-painel-brave-remaining-pages-real-backend-slice-2
plan: 05
type: execute
wave: 3
depends_on: ["17.1-02"]
files_modified:
  - dashboard/lib/runs-api.ts
  - dashboard/mocks/handlers/runs.ts
  - dashboard/components/painel/PainelVarreduras.tsx
  - dashboard/components/painel/__tests__/PainelVarreduras.test.tsx
autonomous: true
requirements: [UI-PAINEL-2]

must_haves:
  truths:
    - "Varreduras view renders a runs table (Início/UF/Fonte/Profundidade/Total/Sincr./Falhas/Status) filtered by uf/source/depth"
    - "The ↺ Falhas button fires the real reprocess PATCH and invalidates the runs query"
    - "7-day stat cards summarize recent runs"
    - "The MSW handler mirrors the backend RunsResponse shape field-for-field"
  artifacts:
    - path: "dashboard/lib/runs-api.ts"
      provides: "typed runs client (fetchRuns, reprocessRun, keys, window helpers)"
      contains: "fetchRuns"
    - path: "dashboard/mocks/handlers/runs.ts"
      provides: "MSW handlers at /api/api/v1/runs/..."
      contains: "/api/api/v1/runs"
    - path: "dashboard/components/painel/PainelVarreduras.tsx"
      provides: "Varreduras runs-history table view"
      contains: "PainelVarreduras"
  key_links:
    - from: "dashboard/components/painel/PainelVarreduras.tsx"
      to: "dashboard/lib/runs-api.ts"
      via: "useQuery(fetchRuns) + useMutation(reprocessRun)"
      pattern: "fetchRuns"
---

<objective>
Build the Varreduras frontend vertical slice: a typed runs client, its MSW handler mirroring the
backend RunsResponse, and the PainelVarreduras table view (runs by uf/source/depth with reprocess +
7-day stat cards).

Purpose: surfaces the new runs-history backend (plan 02) in the painel shell, mirroring the design's
runs table + reprocess.
Output: dashboard/lib/runs-api.ts, dashboard/mocks/handlers/runs.ts, PainelVarreduras.tsx + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-CONTEXT.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-RESEARCH.md
@.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: runs-api.ts client + runs.ts MSW handler</name>
  <files>dashboard/lib/runs-api.ts, dashboard/mocks/handlers/runs.ts</files>
  <read_first>
    - dashboard/lib/destinos-api.ts (client structure: docblock, interfaces, qs(), keys factory, apiFetch fns)
    - dashboard/lib/cost-api.ts:44-96 (COST_WINDOWS, costKeys.data(...), total/format helpers — the 7-day window idiom to copy)
    - dashboard/mocks/handlers/engine.ts (double-prefix rule :15, scenario factories)
    - brave/api/routers/runs.py (plan 02 output — RunItem/RunsResponse + reprocess shape to mirror)
  </read_first>
  <action>
    Create dashboard/lib/runs-api.ts mirroring destinos-api.ts + the cost-api window idiom: TS interfaces
    RunItem (id, started_at, ended_at, ufs, source, depth, total, synced, failed, status) + RunsResponse
    (items, total, offset, limit) mirroring the plan-02 Pydantic models; a runsKeys factory on one "runs"
    prefix; fetchRuns({ uf?, source?, depth? }) → apiFetch("api/v1/runs" + qs); reprocessRun(runId) →
    apiFetch PATCH "api/v1/runs/{id}/reprocess". Add small 7-day window helpers (mirror cost-api total/
    format) used by the view's stat cards. Relative api/v1/... form.
    Create dashboard/mocks/handlers/runs.ts mirroring engine.ts at the double-prefixed
    http://localhost:3000/api/api/v1/runs/...: factories runsListSuccess(), runsListEmpty(), and a
    reprocess handler; payloads typed against the lib interfaces (the contract mirror).
  </action>
  <acceptance_criteria>
    - `grep -c "fetchRuns\|reprocessRun" dashboard/lib/runs-api.ts` returns ≥2
    - `grep -c "/api/api/v1/runs" dashboard/mocks/handlers/runs.ts` returns ≥1
    - RunItem includes status + synced + failed fields
    - `cd dashboard && bunx tsc --noEmit` succeeds for the new client
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bun run test runs-api 2>/dev/null || cd dashboard && bunx tsc --noEmit</automated>
  </verify>
  <done>Typed runs client + MSW handler exist, mirror the backend RunsResponse field-for-field, at the double-prefixed path.</done>
</task>

<task type="auto">
  <name>Task 2: PainelVarreduras.tsx table view + test</name>
  <files>dashboard/components/painel/PainelVarreduras.tsx, dashboard/components/painel/__tests__/PainelVarreduras.test.tsx</files>
  <read_first>
    - dashboard/components/painel/PainelCusto.tsx (full file — table/grid scaffold, Seg filters :201-228, SummaryCard :230-245, useQuery, data-testid discipline)
    - dashboard/components/painel/PainelTopbar.tsx:84-99 (useMutation + toast + invalidate scaffold for reprocess)
    - dashboard/lib/runs-api.ts (Task 1 output)
    - dashboard/components/painel/__tests__/PainelCusto.test.tsx (renderWithClient, server.use, request assertion)
    - 17.1-CONTEXT.md "Specific Ideas" (Varreduras columns Início/UF/Fonte/Profundidade/Total/Sincr./Falhas/Status + reprocess; 7-day stat cards)
  </read_first>
  <action>
    Create dashboard/components/painel/PainelVarreduras.tsx ("use client") mirroring PainelCusto: a
    useQuery over fetchRuns with uf/source/depth Seg filters; render a runs table with columns Início
    (started_at), UF (ufs), Fonte (source), Profundidade (depth), Total, Sincr. (synced), Falhas
    (failed), Status — each row's Status mapped to a colored pill (concluido/parcial/falha/running). Add
    a ↺ Falhas action per row wired to a useMutation over reprocessRun with toast +
    invalidateQueries(["runs"]). Add 7-day SummaryCard stat cards (reuse the cost-api window helpers).
    Pure --painel-* token styling; data-testid on rows, status pills, reprocess button.
    Write __tests__/PainelVarreduras.test.tsx using renderWithClient + server.use(runsListSuccess()):
    assert the table renders rows with all columns + status pills; assert ↺ Falhas fires the real
    reprocess PATCH (assert outgoing request); assert runsListEmpty renders an empty state.
  </action>
  <acceptance_criteria>
    - `grep -c "fetchRuns\|reprocessRun" dashboard/components/painel/PainelVarreduras.tsx` returns ≥2
    - `grep -c "data-testid" dashboard/components/painel/PainelVarreduras.tsx` returns ≥3
    - `cd dashboard && bun run test PainelVarreduras` passes
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bun run test PainelVarreduras</automated>
  </verify>
  <done>PainelVarreduras renders the real runs table with status pills + 7-day cards and reprocesses failures against the real endpoint; component test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→BFF | operator lists/reprocesses runs here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-05-01 | Elevation | reprocess from the UI | mitigate | reprocessRun goes through the BFF Bearer + steward-guarded backend (plan 02) |
| T-17.1-05-02 | Tampering | handler path mismatch hides real shape | mitigate | handler registered at double-prefixed /api/api/v1/runs/... mirroring the typed contract |
| T-17.1-05-SC | Tampering | npm installs | accept | no installs |
</threat_model>

<verification>
- `cd dashboard && bun run test PainelVarreduras runs-api` green.
</verification>

<success_criteria>
Varreduras is a real view: runs load filtered by uf/source/depth, status pills + 7-day cards render, and ↺ Falhas hits the real reprocess endpoint; client+handler mirror the contract.
</success_criteria>

<output>
Create `.planning/phases/17.1-painel-brave-remaining-pages-real-backend-slice-2/17.1-05-SUMMARY.md` when done.
</output>
