"use client";

/**
 * PainelView — the Painel (Kanban) view body.
 *
 * STUB for slice 1: plan 17-05 replaces the body with the real Kanban (metric
 * cards, type filter, UF-scope dropdown, stage columns of record cards). This
 * slice only establishes the shell + topbar wiring around it.
 */
export function PainelView() {
  return (
    <div
      data-testid="painel-view"
      className="grid h-full place-items-center text-[var(--painel-muted)]"
    >
      Painel — carregando…
    </div>
  );
}
