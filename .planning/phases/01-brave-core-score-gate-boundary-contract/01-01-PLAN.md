---
phase: 01-brave-core-score-gate-boundary-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .env.example
  - .gitignore
  - Makefile
  - docker-compose.yml
  - alembic.ini
  - alembic/env.py
  - alembic/versions/0001_init_nascente_rio_mar.py
  - alembic/versions/0002_add_hnsw_index.py
  - brave/__init__.py
  - brave/config/__init__.py
  - brave/config/settings.py
  - brave/core/__init__.py
  - brave/core/models.py
  - brave/lanes/__init__.py
  - brave/lanes/base.py
  - brave/clients/__init__.py
  - brave/clients/base.py
  - brave/observability/__init__.py
  - brave/tasks/__init__.py
  - brave/api/__init__.py
  - dashboard/.gitkeep
  - tests/__init__.py
  - tests/conftest.py
  - tests/fakes/__init__.py
  - tests/unit/__init__.py
  - tests/integration/__init__.py
  - tests/contract/__init__.py
autonomous: false
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
  - CORE-06
  - CORE-07
  - CORE-08
  - CORE-11
  - TEST-01

must_haves:
  truths:
    - "docker-compose up starts Postgres 17 (pgvector 0.8.x confirmed) and Redis 7"
    - "uv venv + uv pip install installs all pinned deps without conflicts"
    - "alembic upgrade head creates nascente_records, rio_records, mar_records, llm_generations, audit_log, poison_quarantine with correct columns and constraints"
    - "HNSW index on rio_records.embedding is created by migration 0002"
    - "brave/ package tree exists with the three load-bearing boundaries: core/, lanes/, clients/ (per D-18)"
    - "ScoreConfig, LLMConfig, DBConfig in brave/config/settings.py load from env vars with BRAVE_SCORE_, BRAVE_LLM_, BRAVE_DB_ prefixes"
    - "All 8 client Protocol interfaces declared in brave/clients/base.py"
    - "Decisions covered by this plan: D-01, D-02, D-03, D-06, D-08, D-09, D-10, D-12, D-13, D-19 (medallion models, routing column, supersession, orchestration interface, HNSW migration, client Protocol boundary, pinned-slug config, ScoreConfig weights, score_version column, psycopg3)"
    - "pytest -q (with docker-compose services running) exits 0"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv-managed Python 3.12 project with all pinned deps"
      contains: "fastapi==0.136"
    - path: "docker-compose.yml"
      provides: "Postgres+pgvector + Redis for offline suite"
      contains: "pgvector/pgvector:pg17"
    - path: "alembic/versions/0001_init_nascente_rio_mar.py"
      provides: "All Phase 1 tables in one migration"
    - path: "alembic/versions/0002_add_hnsw_index.py"
      provides: "HNSW index on rio_records.embedding"
    - path: "brave/core/models.py"
      provides: "SQLAlchemy models: NascenteRecord, RioRecord, MarRecord, LLMGeneration, AuditLog, PoisonQuarantine"
    - path: "brave/config/settings.py"
      provides: "ScoreConfig, LLMConfig, DBConfig, AppConfig with pydantic-settings"
    - path: "brave/clients/base.py"
      provides: "Protocol definitions for all 8 external system clients"
  key_links:
    - from: "brave/core/models.py"
      to: "alembic/env.py"
      via: "Base.metadata referenced in target_metadata"
      pattern: "target_metadata"
    - from: "brave/config/settings.py"
      to: "brave/core/models.py"
      via: "DBConfig.url used in engine creation"
    - from: "brave/clients/base.py"
      to: "tests/fakes/"
      via: "Protocol structural typing (no isinstance checks)"

user_setup:
  - service: local-dev
    why: "Docker services must be running for integration tests"
    env_vars:
      - name: BRAVE_DB_URL
        source: "docker-compose.yml postgres service — default: postgresql+psycopg://brave:brave@localhost:5432/norteia_brave"
      - name: BRAVE_REDIS_URL
        source: "docker-compose.yml redis service — default: redis://localhost:6379/0"
      - name: BRAVE_LLM_OPENROUTER_API_KEY
        source: "Not needed in Phase 1 (fake LLM only); set to 'test' for offline runs"
      - name: BRAVE_NORTEIA_API_SERVICE_TOKEN
        source: "Not needed in Phase 1 (fake NorteiaApi only); set to 'test' for offline runs"
---

<objective>
Scaffold the norteia-brave Python project from zero: uv-managed package, docker-compose offline stack, SQLAlchemy models for all three medallion layers, Alembic migrations (including HNSW index), pydantic-settings config hierarchy, client Protocol boundary definitions, and the package skeleton that enforces the three load-bearing boundaries (core/ / lanes/ / clients/).

Purpose: Every subsequent plan depends on this scaffold existing — models, config, and client interfaces are the contracts all other code is written against. Getting this foundation right (correct column types, HNSW migration, Protocol interfaces, layer boundaries) prevents expensive rework.

Output: A fully-installable Python package with passing `pytest -q` (import smoke tests + schema validation against live DB) and a `make pipeline/up` that boots Postgres+pgvector+Redis correctly.
</objective>

<decisions_covered>
## Decisions Covered (from 01-CONTEXT.md)
This plan implements: D-01, D-02, D-03, D-04, D-06, D-08, D-09, D-10, D-12, D-13, D-18, D-19 — table-per-layer medallion models (D-01), routing/sub_state column not separate tables (D-02), supersession `superseded_by` versioning (D-03), Nascente row shape (D-04), orchestration-behind-interface scaffolding (D-06), HNSW pgvector index migration (D-08), client `clients/base.py` Protocol boundary (D-09), pinned-slug + fallback in pydantic-settings (D-10), ScoreConfig weights/thresholds in config (D-12), `score_version` column (D-13), core/lanes/clients package boundaries (D-18), psycopg 3 driver (D-19).
</decisions_covered>

<execution_context>
@/Users/leandro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leandro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-brave-core-score-gate-boundary-contract/01-CONTEXT.md
@.planning/phases/01-brave-core-score-gate-boundary-contract/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Verify package legitimacy before bulk install</name>
  <read_first>
    .planning/phases/01-brave-core-score-gate-boundary-contract/01-RESEARCH.md
    — Section "Package Legitimacy Audit" (all packages tagged [ASSUMED]; slopcheck unavailable at research time)
  </read_first>
  <what-built>
    The Package Legitimacy Audit in 01-RESEARCH.md lists 25 packages. All are tagged [ASSUMED] because slopcheck was unavailable during research. All are well-established projects (fastapi, celery, sqlalchemy, psycopg, pgvector, pact-python, etc.) confirmed on PyPI, but the [ASSUMED] tag means a final legitimacy check is required before the bulk install task.
  </what-built>
  <how-to-verify>
    Spot-check 3-5 packages from the audit table that you are least familiar with:
    1. celery-redbeat — visit https://pypi.org/project/celery-redbeat/ and confirm it maps to github.com/sibson/redbeat (legitimate scheduler, not a squatted package)
    2. pact-python — visit https://pypi.org/project/pact-python/ and confirm it maps to github.com/pact-foundation/pact-python (official Pact Foundation library)
    3. fakeredis — visit https://pypi.org/project/fakeredis/ and confirm it maps to github.com/cunla/fakeredis-py (established in-process Redis fake)
    4. pytest-socket — visit https://pypi.org/project/pytest-socket/ and confirm it maps to github.com/miketheman/pytest-socket
    5. celery-types — visit https://pypi.org/project/celery-types/ and confirm it maps to github.com/sbdchd/celery-types (Celery stubs for pyright/mypy)
    If any package looks wrong (squatted, no source repo, suspiciously new with no history), type "block: <package name>" and do not proceed.
  </how-to-verify>
  <acceptance_criteria>
    All spot-checked packages confirmed legitimate (correct source repo, established history, download counts matching well-known libraries).
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with scaffold, or "block: &lt;package&gt;" to halt.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold — pyproject.toml, docker-compose, Makefile, package tree</name>
  <read_first>
    .planning/phases/01-brave-core-score-gate-boundary-contract/01-RESEARCH.md
    — Sections "Standard Stack" (pinned versions), "Recommended Project Structure", "docker-compose.yml for Offline Test Suite", "§7.6 ScoreConfig via pydantic-settings", "SQLAlchemy Models (core/models.py sketch)"
    .planning/phases/01-brave-core-score-gate-boundary-contract/01-CONTEXT.md
    — Decisions D-01..D-04 (table-per-layer, routing column, supersession, Nascente columns), D-09 (instructor Mode.Tools), D-10 (DeepSeek slug + data_collection deny), D-12 (ScoreConfig), D-18 (package boundaries), D-19 (psycopg 3)
  </read_first>
  <files>
    pyproject.toml, uv.lock, .env.example, .gitignore, Makefile, docker-compose.yml,
    alembic.ini, alembic/env.py,
    alembic/versions/0001_init_nascente_rio_mar.py,
    alembic/versions/0002_add_hnsw_index.py,
    brave/__init__.py, brave/config/__init__.py, brave/config/settings.py,
    brave/core/__init__.py, brave/core/models.py,
    brave/lanes/__init__.py, brave/lanes/base.py,
    brave/clients/__init__.py, brave/clients/base.py,
    brave/observability/__init__.py,
    brave/tasks/__init__.py,
    brave/api/__init__.py,
    dashboard/.gitkeep,
    tests/__init__.py, tests/conftest.py,
    tests/fakes/__init__.py, tests/unit/__init__.py,
    tests/integration/__init__.py, tests/contract/__init__.py,
    tests/unit/test_scaffold_smoke.py
  </files>
  <behavior>
    - Test: importing brave.config.settings succeeds and ScoreConfig() yields weight_origem=30.0, weight_completude=20.0, weight_corroboracao=20.0, weight_atualidade=15.0, weight_validacao_humana=15.0, threshold_mar=85.0, threshold_dlq=51.0
    - Test: importing brave.core.models succeeds and NascenteRecord.__tablename__ == "nascente_records", RioRecord.__tablename__ == "rio_records", MarRecord.__tablename__ == "mar_records"
    - Test: importing brave.clients.base succeeds and LLMClientProtocol, NorteiaApiClientProtocol, PlacesClientProtocol, OTAClientProtocol, ApifyClientProtocol, WhatsAppClientProtocol, MturClientProtocol, NotebookLMClientProtocol are all defined as Protocol classes
    - Test: alembic upgrade head against live DB creates all expected tables (verified by querying information_schema.tables)
    - Test: SELECT extversion FROM pg_extension WHERE extname='vector' returns a value >= '0.8' (confirms pgvector image version)
  </behavior>
  <action>
    Create pyproject.toml with [project] name="norteia-brave", requires-python=">=3.12", version="0.1.0". Under [project.dependencies] pin all runtime deps exactly as specified in RESEARCH.md Standard Stack:
    fastapi[standard]==0.136.3, uvicorn[standard]==0.49.0, pydantic==2.13.4, pydantic-settings==2.14.1,
    celery==5.6.3, redis==8.0.0, celery-redbeat==2.3.3,
    sqlalchemy==2.0.50, psycopg[binary]==3.3.4, pgvector==0.4.2, alembic==1.18.4,
    openai==2.41.1, anthropic==0.109.1, instructor==1.15.1,
    httpx==0.28.1, tenacity==9.1.4, structlog==26.1.0.
    Under [project.optional-dependencies] dev: pytest==9.0.3, respx==0.23.1, vcrpy==8.1.1, fakeredis==2.36.1, pact-python==3.4.0, pytest-socket==0.8.0, ruff, pyright, celery-types==0.26.0, flower==2.0.1.
    Add [tool.ruff] and [tool.pyright] sections.

    Create docker-compose.yml with two services:
    - postgres: image pgvector/pgvector:pg17, env POSTGRES_DB=norteia_brave / POSTGRES_USER=brave / POSTGRES_PASSWORD=brave, port 5432:5432, volume postgres-data
    - redis: image redis:7-alpine, port 6379:6379, volume redis-data

    Create .env.example with BRAVE_DB_URL, BRAVE_REDIS_URL, BRAVE_LLM_OPENROUTER_API_KEY, BRAVE_LLM_DEEPSEEK_PRIMARY_SLUG, BRAVE_NORTEIA_API_SERVICE_TOKEN, BRAVE_NORTEIA_API_BASE_URL, BRAVE_SCORE_VERSION (default "v1.0"), RUN_REAL_EXTERNALS=0.

    Create Makefile targets: pipeline/up (docker-compose up -d), pipeline/down, pipeline/migrate (alembic upgrade head), pipeline/run (python -m brave.cli run-fixture), pipeline/test (pytest -q), pipeline/test-unit (pytest tests/unit -q).

    Create brave/config/settings.py with three pydantic-settings BaseSettings classes:
    - ScoreConfig: weight_origem=30.0, weight_completude=20.0, weight_corroboracao=20.0, weight_atualidade=15.0, weight_validacao_humana=15.0, threshold_mar=85.0, threshold_dlq=51.0, score_version="v1.0" — env_prefix="BRAVE_SCORE_"
    - LLMConfig: openrouter_base_url="https://openrouter.ai/api/v1", deepseek_primary_slug="deepseek/deepseek-chat", deepseek_fallback_slugs=["deepseek/deepseek-v3.2"], provider_data_collection="deny", usd_daily_budget=10.0 — env_prefix="BRAVE_LLM_"
    - DBConfig: url (required, no default), redis_url="redis://localhost:6379/0" — env_prefix="BRAVE_DB_"
    - AppConfig: aggregates ScoreConfig, LLMConfig, DBConfig as nested models; also exposes run_real_externals: bool = False

    Create brave/core/models.py with SQLAlchemy 2.0 DeclarativeBase and six mapped classes:
    - NascenteRecord: table nascente_records; columns id (UUID PK), source (String 64), source_ref (String 256, indexed), entity_type (String 64), uf (String 2), payload (JSON → JSONB), content_hash (String 64, indexed), version (int default 1), ingested_at (DateTime server_default func.now()), superseded_by_id (UUID FK nullable self-ref)
    - RioRecord: table rio_records; columns id (UUID PK), nascente_id (UUID FK), entity_type (String 64), uf (String 2), municipio_id (String 64 nullable indexed), routing (String 32 default "in_progress" indexed), dlq_reason (String 256 nullable), sub_state (String 64 nullable), normalized (JSON nullable), embedding (Vector(1536) nullable), score (Numeric(5,2) nullable), score_breakdown (JSON nullable), score_version (String 64 nullable), processed_at (DateTime nullable), canonical_key (String 256 nullable unique)
    - MarRecord: table mar_records; columns id (UUID PK), rio_id (UUID FK), entity_type (String 64), source_ref (String 256 unique indexed), canonical (JSON), provenance (JSON), reliability_score (Numeric(5,2)), score_version (String 64), parent_mar_id (UUID FK nullable self-ref), superseded_by_id (UUID FK nullable self-ref), published_at (DateTime server_default func.now())
    - LLMGeneration: table llm_generations; columns id (UUID PK), lane (String 64), model_slug (String 128), resolved_provider (String 128 nullable), prompt_tokens (int), completion_tokens (int), usd_cost (Numeric(10,6)), created_at (DateTime server_default func.now())
    - AuditLog: table audit_log; columns id (UUID PK), action (String 128), entity_type (String 64 nullable), record_id (UUID nullable), before_state (JSON nullable), after_state (JSON nullable), actor (String 128 nullable), created_at (DateTime server_default func.now())
    - PoisonQuarantine: table poison_quarantine; columns id (UUID PK), nascente_id (UUID nullable), task_name (String 256), error_message (Text), payload (JSON nullable), quarantined_at (DateTime server_default func.now())

    Create brave/clients/base.py with eight Protocol classes using typing.Protocol (runtime_checkable=False). Each Protocol defines the minimum interface:
    - LLMClientProtocol: async def extract(self, prompt: str, schema: type, mode: str = "tools") -> Any
    - NorteiaApiClientProtocol: async def push_destination(self, payload: dict) -> dict; async def push_attraction(self, payload: dict) -> dict
    - PlacesClientProtocol: async def text_search(self, query: str, uf: str) -> list[dict]; async def place_details(self, place_id: str) -> dict
    - OTAClientProtocol: async def price_check(self, place_id: str) -> dict | None
    - ApifyClientProtocol: async def scrape_ig(self, handle: str) -> dict
    - WhatsAppClientProtocol: async def send_template(self, to: str, template: str, params: dict) -> dict
    - MturClientProtocol: async def fetch_municipalities(self, uf: str) -> list[dict]
    - NotebookLMClientProtocol: async def fetch_report(self, municipio: str) -> dict

    Create brave/lanes/base.py with a Lane Protocol stub: async def produce(self, uf: str) -> None — no implementation; filled in Phase 2/3.

    Create alembic/env.py that imports brave.core.models.Base and sets target_metadata = Base.metadata. Reads DB URL from BRAVE_DB_URL env var.

    Create alembic/versions/0001_init_nascente_rio_mar.py: creates all six tables with exact columns/types from the models. Use op.create_table with explicit Column definitions. Set String lengths to match models. Use sa.JSON for JSONB columns (SQLAlchemy maps JSON → JSONB on PostgreSQL). Include __up__ and __down__.

    Create alembic/versions/0002_add_hnsw_index.py: op.execute("CREATE INDEX CONCURRENTLY IF NOT EXISTS rio_records_embedding_hnsw_idx ON rio_records USING hnsw (embedding vector_cosine_ops) WITH (m = 16, ef_construction = 64);") — requires pgvector extension. downgrade drops the index.

    Create tests/conftest.py with session-scoped fixtures: db_session (SQLAlchemy sync session against docker-compose DB), async_db_session (async session for FastAPI tests), fake_redis (fakeredis.FakeRedis for unit tests), app_config (AppConfig loaded from env).

    Create tests/unit/test_scaffold_smoke.py with parametrized import + attribute assertions verifying the behavior block above.
  </action>
  <verify>
    <automated>
      docker compose -f docker-compose.yml up -d postgres redis
      sleep 5
      BRAVE_DB_URL="postgresql+psycopg://brave:brave@localhost:5432/norteia_brave" alembic upgrade head
      BRAVE_DB_URL="postgresql+psycopg://brave:brave@localhost:5432/norteia_brave" pytest tests/unit/test_scaffold_smoke.py -q
    </automated>
  </verify>
  <acceptance_criteria>
    1. `uv pip install -e ".[dev]"` exits 0 with no dependency conflicts.
    2. `docker compose up -d` starts both postgres and redis healthy.
    3. `alembic upgrade head` exits 0 and creates tables: SELECT table_name FROM information_schema.tables WHERE table_schema='public' returns nascente_records, rio_records, mar_records, llm_generations, audit_log, poison_quarantine.
    4. `SELECT extversion FROM pg_extension WHERE extname='vector'` inside the postgres container returns a version string >= '0.8'.
    5. `pytest tests/unit/test_scaffold_smoke.py -q` exits 0 — all import + attribute assertions pass.
    6. `from brave.clients.base import LLMClientProtocol, NorteiaApiClientProtocol, PlacesClientProtocol, OTAClientProtocol, ApifyClientProtocol, WhatsAppClientProtocol, MturClientProtocol, NotebookLMClientProtocol` succeeds without ImportError.
    7. `from brave.config.settings import ScoreConfig; c = ScoreConfig(); assert c.weight_origem == 30.0 and c.threshold_mar == 85.0` succeeds.
    8. brave/core/models.py: NascenteRecord.routing attribute does NOT exist (no routing on Nascente per D-01); RioRecord.routing attribute EXISTS; MarRecord.source_ref is UNIQUE (per D-15).
  </acceptance_criteria>
  <done>Project installs cleanly; docker-compose boots Postgres (pgvector >= 0.8 confirmed) + Redis; Alembic migrations run to head; all six tables exist with correct schema; smoke tests pass; package boundaries brave/core/, brave/lanes/, brave/clients/ are importable and distinct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker-compose → developer machine | Postgres and Redis ports exposed only to localhost; no external network in dev |
| pyproject.toml → PyPI | Package install fetches from PyPI; [ASSUMED]-tagged packages require human legitimacy check (Task 0) |
| .env file → settings | Secrets (API keys, DB passwords) must never land in .env.example with real values; .gitignore must cover .env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | alembic migrations | mitigate | Alembic revision chain; never edit past migrations; CI runs upgrade head to verify |
| T-01-02 | Information Disclosure | .env secrets in git | mitigate | .gitignore includes .env, *.env, .env.*; .env.example has no real values; assert in CI |
| T-01-03 | Tampering | npm/pip package install | mitigate | Task 0 blocking human checkpoint for all [ASSUMED] packages; package versions pinned in pyproject.toml lock |
| T-01-SC | Tampering | pip/uv installs ([ASSUMED] packages) | mitigate | slopcheck + blocking human checkpoint (Task 0) for all [ASSUMED] packages before bulk install |
| T-01-04 | Denial of Service | pgvector HNSW index creation (CONCURRENTLY) | accept | Low risk in dev; CONCURRENTLY avoids table lock; migration is idempotent (IF NOT EXISTS) |
</threat_model>

<verification>
Run after both tasks complete:

```bash
# Services up
docker compose ps | grep -E "(postgres|redis)" | grep -c "running"
# Expect: 2

# Migrations applied
BRAVE_DB_URL="postgresql+psycopg://brave:brave@localhost:5432/norteia_brave" \
  python -c "
from sqlalchemy import create_engine, text, inspect
import os
engine = create_engine(os.environ['BRAVE_DB_URL'])
with engine.connect() as conn:
    tables = inspect(engine).get_table_names()
    expected = {'nascente_records','rio_records','mar_records','llm_generations','audit_log','poison_quarantine'}
    assert expected.issubset(set(tables)), f'Missing tables: {expected - set(tables)}'
    ext = conn.execute(text(\"SELECT extversion FROM pg_extension WHERE extname='vector'\")).scalar()
    assert ext and ext >= '0.8', f'pgvector version too old or missing: {ext}'
    print('OK: all tables + pgvector>=0.8')
"

# Smoke tests
pytest tests/unit/test_scaffold_smoke.py -v

# Boundary enforcement
python -c "
import brave.core.models as m
import brave.clients.base as c
assert hasattr(m.RioRecord, 'routing')
assert not hasattr(m.NascenteRecord, 'routing'), 'NascenteRecord must not have routing column'
assert all(hasattr(c, p) for p in ['LLMClientProtocol','NorteiaApiClientProtocol','PlacesClientProtocol'])
print('OK: boundaries correct')
"
```
</verification>

<success_criteria>
- pyproject.toml with all 25 pinned deps installs via `uv pip install -e ".[dev]"` with zero conflicts
- docker-compose up -d boots postgres (pgvector/pgvector:pg17) and redis healthy
- alembic upgrade head creates all 6 tables; pgvector extension >= 0.8 confirmed in DB
- HNSW index rio_records_embedding_hnsw_idx exists after migration 0002
- brave/core/, brave/lanes/, brave/clients/ are importable and structurally separate (no cross-imports at module level)
- 8 client Protocol interfaces declared in brave/clients/base.py
- ScoreConfig() yields exact §7.6 weights: origem=30.0, completude=20.0, corroboracao=20.0, atualidade=15.0, validacao_humana=15.0
- pytest tests/unit/test_scaffold_smoke.py -q exits 0
</success_criteria>

<output>
Create `.planning/phases/01-brave-core-score-gate-boundary-contract/01-01-SUMMARY.md` when done
</output>
