---
phase: "07"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - brave/core/dlq/__init__.py
  - brave/core/dlq/service.py
  - brave/api/routers/dlq.py
autonomous: true
requirements:
  - PLACE-06

must_haves:
  truths:
    - "D-06: brave/core/dlq/service.py exists with validate_and_promote_rio(session, rio, config=None) -> MarRecord | None — extracts the 4-step pattern (reassign normalized + flag_modified + flush, reprocess_record, refresh, promote_to_mar if routing=='mar') from dlq.py validate_dlq_record"
    - "D-06: brave/api/routers/dlq.py validate_dlq_record and validate_batch inner loop delegate to validate_and_promote_rio instead of inlining the 4-step pattern — behavior-preserving refactor"
    - "D-06: validate_and_promote_rio uses flag_modified (Pitfall 3) and reprocess_record (NOT process_nascente_record — Pitfall 4) — exact same steps as the current inlined code"

  artifacts:
    - path: brave/core/dlq/service.py
      provides: "validate_and_promote_rio helper (D-06)"
      contains: "validate_and_promote_rio"
    - path: brave/core/dlq/__init__.py
      provides: "Package init for brave.core.dlq"
    - path: brave/api/routers/dlq.py
      provides: "DLQ router delegating to service"
      contains: "validate_and_promote_rio"

  key_links:
    - from: brave/api/routers/dlq.py::validate_dlq_record
      to: brave/core/dlq/service.py::validate_and_promote_rio
      via: "direct call — replaces inline 4-step block"
      pattern: "validate_and_promote_rio"
    - from: brave/core/dlq/service.py::validate_and_promote_rio
      to: brave/core/rio/routing.py::reprocess_record
      via: "import + call"
      pattern: "reprocess_record"
---

<objective>
Extract the DLQ validate-and-promote 4-step pattern from brave/api/routers/dlq.py into a standalone brave/core/dlq/service.py function so the load-test harness (Plan 07-05) can call it directly without HTTP overhead, and so the router delegates to a single source of truth.

Purpose: The 4-step pattern (set validacao_humana=100 + flag_modified, reprocess_record, refresh, promote_to_mar if routing=='mar') appears verbatim in two places in dlq.py (lines ~155-182 in validate_dlq_record and lines ~231-265 in validate_batch inner loop). Extracting it eliminates duplication and provides the importable function the harness needs per D-06.

Output: brave/core/dlq/__init__.py (empty package init), brave/core/dlq/service.py (validate_and_promote_rio function), brave/api/routers/dlq.py (validate_dlq_record + validate_batch loop delegate to service function). Pure behavior-preserving refactor — no new logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-RESEARCH.md
@.planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-PATTERNS.md
@brave/api/routers/dlq.py
@brave/core/mar/service.py

<interfaces>
<!-- Key interfaces and exact lines to extract. Extracted from codebase. -->

From brave/api/routers/dlq.py — the 4-step pattern to extract (validate_dlq_record lines 155-182):
```python
from sqlalchemy.orm.attributes import flag_modified
normalized = dict(rio.normalized or {})
normalized["validacao_humana_value"] = 100.0
rio.normalized = normalized
flag_modified(rio, "normalized")
db.flush()

from brave.config.settings import ScoreConfig
from brave.core.rio.routing import reprocess_record
reprocess_record(db, rio_id, ScoreConfig())
db.refresh(rio)

if rio.routing == "mar":
    try:
        from brave.tasks.pipeline import push_destination_task
        push_destination_task.delay(str(rio_id))
    except Exception:
        from brave.core.mar.service import promote_to_mar
        promote_to_mar(db, rio)
```

Note: validate_and_promote_rio in service.py does NOT dispatch Celery tasks or write audit rows.
It only does: normalized+flag_modified+flush → reprocess_record → refresh → promote_to_mar.
The router (dlq.py) keeps its own audit write and Celery dispatch (push_destination_task) after calling validate_and_promote_rio.

From brave/core/mar/service.py (import pattern + module structure to follow):
- Top-level imports, then module functions
- `promote_to_mar(session: Session, rio: RioRecord) -> MarRecord`

From brave/core/rio/routing.py:
- `reprocess_record(session: Session, rio_id: uuid.UUID, config: ScoreConfig) -> None`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create brave/core/dlq/service.py with validate_and_promote_rio</name>
  <files>brave/core/dlq/__init__.py, brave/core/dlq/service.py</files>

  <read_first>
    - brave/api/routers/dlq.py (lines 127-193 validate_dlq_record + lines 196-267 validate_batch — already in context; extract the 4-step pattern precisely)
    - brave/core/mar/service.py (module structure + imports pattern)
    - .planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-PATTERNS.md §brave/core/dlq/service.py (exact extraction)
    - .planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-RESEARCH.md §RQ-4 (the 4-step pattern with Pitfall 3 and Pitfall 4 callouts)
  </read_first>

  <action>
    1. Create brave/core/dlq/__init__.py as an empty file (the directory does not exist yet; create both directory and file).

    2. Create brave/core/dlq/service.py with:

    Module docstring: "DLQ service — validate_and_promote_rio helper (D-06, Phase 7). Extracted from brave/api/routers/dlq.py validate_dlq_record inline logic. Called by both the DLQ router and the loadtest harness."

    Imports (top-level, not lazy — unlike dlq.py which uses lazy imports):
      from __future__ import annotations
      from sqlalchemy.orm import Session
      from sqlalchemy.orm.attributes import flag_modified
      from brave.config.settings import ScoreConfig
      from brave.core.models import MarRecord, RioRecord
      from brave.core.rio.routing import reprocess_record
      from brave.core.mar.service import promote_to_mar

    Function signature:
      def validate_and_promote_rio(
          session: Session,
          rio: RioRecord,
          config: ScoreConfig | None = None,
      ) -> MarRecord | None:

    Docstring: "Set validacao_humana=100 → re-score → promote_to_mar if routing=='mar'.\n\nExtracted from dlq.py validate_dlq_record. Pitfall 3: reassign+flag_modified required for SQLAlchemy JSON mutation tracking. Pitfall 4: reprocess_record (NOT process_nascente_record) re-scores an existing record.\n\nDoes NOT dispatch Celery tasks or write audit rows — caller is responsible.\n\nReturns:\n    MarRecord if promoted to Mar; None if routing != 'mar' after re-score."

    Body (exact 4-step pattern from dlq.py):
      config = config or ScoreConfig()

      # Step 1: CRITICAL — reassign + flag_modified (Pitfall 3)
      normalized = dict(rio.normalized or {})
      normalized["validacao_humana_value"] = 100.0
      rio.normalized = normalized
      flag_modified(rio, "normalized")
      session.flush()

      # Step 2: re-score — reprocess_record NOT process_nascente_record (Pitfall 4)
      reprocess_record(session, rio.id, config)
      session.refresh(rio)

      # Step 3: promote only when routing == 'mar'
      if rio.routing == "mar":
          return promote_to_mar(session, rio)
      return None
  </action>

  <verify>
    <automated>.venv/bin/python -c "from brave.core.dlq.service import validate_and_promote_rio; import inspect; sig = inspect.signature(validate_and_promote_rio); assert 'rio' in sig.parameters; print('dlq/service.py import OK')"</automated>
  </verify>

  <done>brave/core/dlq/__init__.py exists (empty); brave/core/dlq/service.py exports validate_and_promote_rio with the 4-step pattern; imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor dlq.py to delegate to validate_and_promote_rio</name>
  <files>brave/api/routers/dlq.py</files>

  <read_first>
    - brave/api/routers/dlq.py (full file — already in context; lines 127-193 validate_dlq_record, lines 196-267 validate_batch)
    - brave/core/dlq/service.py (just created in Task 1)
    - .planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-RESEARCH.md §RQ-4 (what the router keeps: audit write + Celery dispatch)
  </read_first>

  <action>
    This is a behavior-preserving refactor. The router keeps: 404 checks, before_state capture, Celery dispatch (push_destination_task), audit writes. It delegates the 4-step normalize+score+promote to validate_and_promote_rio.

    Changes to brave/api/routers/dlq.py:

    1. Add import at the top (after existing imports):
       from brave.core.dlq.service import validate_and_promote_rio

    2. In validate_dlq_record (lines 127-193):
       - Keep: rio = db.get(RioRecord, rio_id); 404 check; before_state capture
       - Replace the 4-step inline block (lines 153-182: flag_modified + flush + reprocess + refresh + push) with:
           validate_and_promote_rio(db, rio)
           db.refresh(rio)  # refresh after service call
       - Keep: the Celery push_destination_task dispatch (check if routing=='mar' and dispatch) — this stays in the router because it's a side-effect the service doesn't own
       - Keep: write_audit call
       Note: validate_and_promote_rio already calls promote_to_mar internally; the router's push_destination_task dispatch is additive (publishes to norteia-api). The existing Celery dispatch block is preserved unchanged.
       IMPORTANT: After calling validate_and_promote_rio, do NOT call promote_to_mar again in the router — the service already does it. The router's "if rio.routing == 'mar'" block should only dispatch push_destination_task (Celery), not call promote_to_mar.

    3. In validate_batch inner for-loop (lines 229-265):
       - Replace the inline 4-step block with:
           validate_and_promote_rio(db, rio)
           db.refresh(rio)
       - Keep: the push_destination_task dispatch (same pattern — only push, not promote)
       - Keep: write_audit per record

    4. Remove the now-unused lazy imports inside validate_dlq_record and validate_batch that were part of the inlined pattern:
       - `from sqlalchemy.orm.attributes import flag_modified` (or _flag_modified)
       - `from brave.config.settings import ScoreConfig` (or _ScoreConfig)
       - `from brave.core.rio.routing import reprocess_record` (or _reprocess_record)
       These are now handled in service.py.
       Preserve: `from brave.tasks.pipeline import push_destination_task` (the push dispatch + its inline push_destination fallback stay).
       REMOVE the router's own `promote_to_mar(db, rio)` call(s): `validate_and_promote_rio` already promotes, so the router must NOT call promote_to_mar again (double-promote risk in broker-less envs, T-07-06). The router's `if rio.routing == 'mar'` block dispatches push_destination_task only. Drop the now-unused `from brave.core.mar.service import promote_to_mar` import from the router if no call remains.

    Verify behavior is identical: same outcome for routing='mar' (promote via service + push) and routing!='mar' (no promote, no push). Audit writes unchanged.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import sys
with open('brave/api/routers/dlq.py') as f:
    src = f.read()
assert 'validate_and_promote_rio' in src, 'Missing validate_and_promote_rio call'
assert 'validacao_humana_value' not in src, 'Inline 4-step block not removed from router'
assert 'promote_to_mar(' not in src, 'Router must not call promote_to_mar after delegating to service (T-07-06 double-promote)'
print('dlq.py refactor OK')
"</automated>
  </verify>

  <done>brave/api/routers/dlq.py imports and calls validate_and_promote_rio; the inline normalized/flag_modified/reprocess pattern is no longer inlined; all audit writes and Celery dispatches preserved; existing router tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| steward/bearer → DLQ validate endpoint | Mutating endpoint; promotes records to Mar — trust boundary unchanged by this refactor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Elevation of Privilege | validate_and_promote_rio called from harness (no auth) | accept | harness is operator-local, not network-exposed; same function called via authenticated router in production; auth check is the router's responsibility (unchanged) |
| T-07-05 | Tampering | validate_and_promote_rio skips audit write | accept | audit write stays in the router (caller's responsibility); harness may omit audit — documented in service.py docstring; no audit regression in router path |
| T-07-06 | Tampering | double promote_to_mar — router still calls promote after service does | mitigate | Task 2 explicitly removes the router's promote_to_mar call from the Celery fallback block; only push_destination_task remains in the router's routing=='mar' block |
| T-07-SC | Tampering | No new package installs in this plan | accept | Pure code refactor; no new dependencies |
</threat_model>

<verification>
1. Import check: `python -c "from brave.core.dlq.service import validate_and_promote_rio"` exits 0
2. Inline pattern removed: `grep -c "validacao_humana_value" brave/api/routers/dlq.py` == 0
3. Delegation in place: `grep -c "validate_and_promote_rio" brave/api/routers/dlq.py` >= 2 (validate_dlq_record + validate_batch)
4. Existing DLQ tests pass: `.venv/bin/python -m pytest tests/ -k "dlq" -v` exits 0
</verification>

<success_criteria>
- brave/core/dlq/service.py has validate_and_promote_rio with the exact 4-step pattern (flag_modified + reprocess_record + promote_to_mar)
- brave/api/routers/dlq.py delegates to service function; inline duplication removed
- All existing DLQ tests (list, reprocess, descarte, validate, validate-batch) continue to pass
- validate_and_promote_rio importable from brave.core.dlq.service
</success_criteria>

<output>
Create `.planning/phases/07-real-places-hardening-targeted-atrativos-discovery-mtur-refr/07-02-SUMMARY.md` when done.
</output>
